# 🔇 PERCHE' `770202` E' MUTA — e la risposta **non** e' un guasto

**19/09/2026** · branch `lavoro` · 🚫 **SOLA LETTURA**: nessun EA, nessun preset, nessun
forward, nessun round lanciato. Il conto reale **10105439** non e' stato toccato in nessun modo.
Nessuna riga e' stata mandata a nessuno.

**La domanda:** perche' la sedia `770202` (`ABTG_Dow_Apertura_US`, U30USD, M5) non apre piu'
niente dal **28/08/2026**?

**La risposta in una riga:**

> ## 🟢 Perche' e' una sedia **SOLO LONG** su un Dow che da tre settimane **scende**, e il suo
> livello di rottura **non e' stato toccato**. La sedia e' **viva, in orario e corretta**: arma
> ogni seduta alle **15:05 server**. Il silenzio di **15 sedute** e' **DENTRO** la sua stessa
> distribuzione di backtest, dove il silenzio massimo misurato e' di **23 sedute**.

---

## ⓪ 🧪 IL CONTRO-ESEMPIO, COSTRUITO PRIMA — e non smentisce

**La misura che avrebbe fatto sbagliare questo referto**: se il motore, nel suo OOS, non avesse
**mai** avuto una siccita' lunga come quella in campo, allora *"e' il mercato"* sarebbe una scusa
e ci sarebbe un guasto da cercare.

L'ho costruita e girata su
`backtest_pipeline/risultati_prove/trades_portafoglio/abtg_trades_ABTG_Dow_Apertura_US_U30USD_770206.csv`
— il per-trade della **cella LONG-only** di R16c, la stessa che gira in campo (classe 465):

| misura | valore |
|---|---:|
| righe (deal) | 130 |
| `position_id` distinti | **96** |
| finestra | 2025-06-10 -> 2026-06-29 |
| sedute lun-ven nella finestra | **275** |
| frequenza | **0,349 op/seduta** |
| **siccita' PIU' LUNGA (sedute consecutive senza operazione)** | 🔴 **23** |
| siccita' >= 10 sedute | **4** volte |
| siccita' >= 14 sedute | **1** volta |

**Il silenzio in campo**: dal 28/08 al 18/09 ci sono **15 sedute lun-ven** (31/08, 01, 02, 03, 04,
07, 08, 09, 10, 11, 14, 15, 16, 17, 18/09).

> ✅ **15 < 23.** La siccita' di campo **non e' nemmeno la piu' lunga** che questo motore ha gia'
> prodotto nel suo stesso OOS. **Il contro-esempio non smentisce la causa: la conferma.**

⚠️ **E il limite, dichiarato**: e' raro, non banale. Sotto Poisson a 0,349/seduta,
`P(0 operazioni in 15 sedute) = e^-5,24 = 0,53%`; nell'OOS una siccita' >= 15 capita **1 volta su
95**. 👉 Quindi *"e' il mercato"* **da solo** non basterebbe. Serve il **meccanismo**, ed e' nel
paragrafo 2 — dove c'e' una **prova a prezzo esatto**.

---

## ① ✅ LA SEDIA E' VIVA, IN ORARIO E CORRETTA — quattro ipotesi cadono qui

**Fonte**: `backtest_pipeline/coda/referti/CODA_02_chi_ha_operato_2026*.log`, **undici** corse
notturne dal 09/09 al 19/09, **due terminali** (piccolo 50503392 e 100k 50504263).
**Fonte config**: `mql5/Presets/ABTG_Dow_Apertura_US_U30USD_M5_770202_100K.set` e
`mql5/Presets/sedie_piccolo/recupero2/sedia_ABTG_Dow_Apertura_US_770202.set`.

| ipotesi | verdetto | **la prova** |
|---|---|---|
| **2. EA staccato / in errore** | ❌ **FALSIFICATA** | scrive `RETEST armato` **ogni seduta**, su **tutti e due** i terminali, allo stesso secondo |
| **3a. ora di sessione sbagliata** | ❌ **FALSIFICATA** | logga alle **16:05:00 LOCALI = 15:05 SERVER** = `InpSessionHour 14:30` **+** `InpRangeMinutes 35`. **14, non 15**: l'ora e' quella del server, corretta |
| **3b. filtro spread** | ❌ **FALSIFICATA due volte** | `InpMaxSpread=0` (spento) **e** `ArmRetest()` stampa `RETEST armato` **solo DOPO** aver superato `SpreadOK()` (r.1278-1284). Se lo spread avesse bloccato, il log direbbe *"spread troppo alto"* |
| **3c. filtro notizie** | ❌ **FALSIFICATA tre volte** | `InpUseNewsFilter=false`; in blackout la fase `PH_BUILDING` fa `break` e **non armerebbe**; e non compare mai una riga di blackout |
| **1. interferenza fra sedie (hedging)** | ❌ **FALSIFICATA** | 👉 par. 3 |
| **4. binario vecchio** | ❌ **FALSIFICATA come causa** | par. 4 |
| **5. il mercato** | 🟢 **IN PIEDI, con prova** | par. 0 + par. 2 |

📌 **E il fatto che chiude il cerchio**: dopo `RETEST armato` **non compare mai** nessuna delle
righe di rifiuto che il codice sa stampare (`rottura con volumi insufficienti`, `saltato: stop
troppo stretto`, `BUY LIMIT (retest) fallito`, `su U30USD ci sono gia' N ...`). **L'EA non viene
respinto da niente: semplicemente non arriva mai al punto di mandare un ordine.**

---

## ② 🎯 LA PROVA A PREZZO ESATTO — il vicino con **l'asticella PIU' BASSA**

Questa e' la misura che rende il referto una **prova** e non una spiegazione plausibile.

**La configurazione che gira** (letta dal preset, non dedotta):
`InpEntryMode=2` (RETEST) · `InpRangeMinutes=35` · **`InpAllowLong=true`, `InpAllowShort=false`**
(🔴 **SOLO LONG**) · `InpBufferPoints=1000` · `InpUseEmaFilter=true` (EMA **1** vs **50** su **H4**)
· `InpRetestOffsetPts=400` · `InpPendingExpiryMin=120` · `InpMaxPosSimbolo=0`.

**Quanto vale il buffer in punti indice**, e non e' un'assunzione:
`EffectiveBuffer()` (r.1906-1911) = `max(InpBufferPoints, STOPS_LEVEL) * _Point`. Su U30USD
`_Digits=2` — **misurato** sulle righe di `ABTG_EMA200` nello stesso giornale, che stampano prezzi
a due decimali (`SELL LIMIT 2 @ 53357.10 SL 53441.20 TP 53188.90`). Quindi
**`1000 x 0,01 = 10,00 punti indice`**.
👉 **Il livello di rottura long di `770202` e' `massimo del range 14:30-15:05` + `10,00`.**

### Il vicino: `ABTG_ORB_Ottimizzato` (`770611`), stesso simbolo, **asticella piu' bassa**
`mql5/Presets/ABTG_ORB_Ottimizzato_U30USD_M5_770611_100K.set`: range **14:30-14:45** (15 minuti,
quindi **un sottoinsieme** del range di `770202`), `InpEntryPoints=10.0` (**gli stessi 10,00 punti
indice**), `InpAllowShort=false`, `InpPendingExpiryMin=600` (**dieci ore**).

- 📐 **Algebra, non opinione**: il massimo di 15 minuti e' **<=** il massimo di 35 minuti che lo
  contiene. Quindi **il BUY STOP dell'ORB scatta sempre a un prezzo <= al livello di rottura di
  `770202`**, e tutti e due si attivano sull'**ASK** (`MonitorRetest` r.1310 `ask >= buyTrig`;
  un BUY STOP scatta sull'ask). **Stessa variabile, stessa soglia o piu' bassa.**
- 🧾 **Il fatto, letto nel log dell'11/09**: `[ORB_OTT] BUY STOP @ 52733.50000 SL 52632.50000`,
  piazzato alle **15:45 locali = 14:45 server**. Lo stesso giorno `770202` arma con
  `range 52723.50000-52518.50000`. 👉 `52723,50 + 10,00 = ` **`52733,50`** = **lo stesso identico
  prezzo del BUY STOP dell'ORB**.
- 🔴 **Quel BUY STOP NON si e' mai eseguito**: l'ultima operazione di `770611` su U30USD e' del
  **03/09** su **tutti e due** i conti (`data/statements/trades_auto.csv` e `trades_100k.csv`).

> ## 🎯 **Su quella seduta e' PROVATO, non dedotto, che il livello di rottura di `770202` non e' stato toccato**: un ordine reale, dello stesso broker, a **quel prezzo**, con **dieci ore** di vita, non e' scattato.

### E il prezzo scende, seduta per seduta — dal log della sedia stessa
Il **range di apertura USA a 35 minuti** di U30USD, ricostruito dalle righe `RETEST armato`
(date corrette applicando la **classe 466**, par. 5):

| seduta | massimo range | minimo range | ampiezza | **livello di rottura long** (`max + 10,00`) |
|---|---:|---:|---:|---:|
| 2026-09-07 | 53172,50 | 53091,50 | 81,00 | 53182,50 |
| 2026-09-08 | 53033,50 | 52789,50 | 244,00 | 53043,50 |
| 2026-09-09 | 52552,50 | 52372,50 | 180,00 | 52562,50 |
| 2026-09-10 | 52269,50 | 52050,50 | 219,00 | 52279,50 |
| 2026-09-11 | 52723,50 | 52518,50 | 205,00 | **52733,50** ← la prova qui sopra |
| 2026-09-14 | 52478,50 | 52390,50 | 88,00 | 52488,50 |
| 2026-09-15 | 52320,50 | 52024,50 | 296,00 | 52330,50 |
| 2026-09-16 | 52185,50 | 51953,50 | 232,00 | 52195,50 |
| 2026-09-17 | 52034,50 | 51638,50 | 396,00 | 52044,50 |

**Il Dow ha perso 1.138 punti indice (-2,14%) in nove sedute** solo dentro questa finestra, e
**1.790 punti (-3,33%)** dal massimo di seduta del **28/08** (53.824,50, letto in `trades_auto.csv`).
👉 Una sedia **solo long** su una discesa monotona **e' progettata per stare zitta**.

### 🔎 Cosa resta aperto, ed e' UNA COSA SOLA
Dopo l'armamento restano **tre** strade, tutte di mercato o di disegno, **nessuna un guasto**:
**(A)** `bias = -1` (EMA H4 chiude il long) -> l'ordine non parte mai; **(B)** `bias` aperto,
rottura avvenuta, **BUY LIMIT piazzato e scaduto non eseguito** (offset 400 pt = **4,00 punti
indice** dentro il livello, scadenza 120 min — il codice lo chiama *"trade mancato = costo del
metodo"*, r.1290-1294); **(C)** rottura mai avvenuta.
🟢 Il par. 2 **prova (C) almeno per l'11/09**. Per le altre sedute il numero che decide (`bias`) sta
**dentro la stessa riga di log**, ma **oltre il carattere 110**, dove `CODA_02` taglia. 👉 Costo per
chiuderla: **una riga di sola lettura, secondi** (par. 6).

---

## ③ 🔴 NO, NON E' L'INTERFERENZA FRA SEDIE — e va detto forte, perche' e' la domanda giusta sul Nasdaq

L'interferenza `PositionClose(_Symbol)` su conto HEDGING
(`report/AUDIT_POSITIONSELECT_HEDGING_2026-09-03.md` r.39) **chiude posizioni di altri**. Qui non
c'e' niente da chiudere: **`770202` non ha mai mandato l'ordine.** Un vicino non puo' impedire un
invio — tranne per **una** via, e quella via e' **spenta e misurata**:

- 🚪 `InpMaxPosSimbolo` e' l'unico cancello dell'EA che conta le posizioni **di tutti gli EA** sul
  simbolo (`EsposizioneSimbolo()`, r.633-640). **Vale `0` in tutti e due i preset = SPENTO.**
- 🧾 E se avesse morso, **avrebbe lasciato una riga**: *"su U30USD ci sono gia' N fra posizioni e
  pendenti (tetto M, tutti gli EA): non piazzo"*. **Non compare mai.**
- 🧾 Le **4 posizioni storiche** di `770202` non mostrano nessuna impronta di chiusura estranea:
  `07/08 expert` (chiusura di fine sessione 17:30 server, **la sua**), `10/08 sl`, `13/08 sl`,
  `28/08 sl`; durate 9', 24', 32' e 2h05'. **Nessuna durata anomala, nessun `close_reason` strano.**

> 🟢 **Quindi il blocco che oggi ferma le due sedie Nasdaq NON e' quello che fa tacere `770202`.**
> La chiusura per ticket resta un prerequisito per avere due sedie sullo stesso simbolo — **ma
> questo caso non e' una prova a suo favore, e usarlo come tale sarebbe un numero attribuito alla
> sedia sbagliata.**

---

## ④ 🧬 IL BINARIO — due vintage diversi, **comportamento identico**

`backtest_pipeline/coda/referti/CODA_06_quale_codice_gira_20260919_033003.log`:

| dove | versione | righe | Guardian | compilato |
|---|---|---:|:--:|---|
| **piccolo 50503392** | 1.00 | 2065 | ❌ **no** | 2026-08-08 14:26 |
| **100k 50504263** | 1.01 | 2148 | ✅ SI | 2026-08-19 23:09 |
| banco `C:\MT5_Backtest` | 1.01 | 2148 | ✅ SI | 2026-09-18 17:25 |

🎯 **E' un esperimento naturale, gratis**: il binario del piccolo **non ha nemmeno la chiamata**
`ABTG_GuardiaIngresso`, quindi **il Guardian non puo' essere la causa li'**. I due binari
producono **la stessa riga allo stesso secondo con lo stesso range**. 👉 **Il vintage non spiega il
silenzio.**
⚠️ **Rilievo che resta aperto (non e' la causa di oggi, ma e' un rischio)**: il binario del piccolo
e' dell'**08/08** e **non legge il Guardian** — stessa famiglia del difetto gia' misurato su
`ABTG_EMA200` in `report/IL_GUARDIAN_CHE_SCHIEREREMO_2026-09-12.md`.

---

## ⑤ 🛠️ DIFETTO DELLO STRUMENTO TROVATO STRADA FACENDO — **classe 466**

`backtest_pipeline/righe/CODA_02_chi_ha_operato.ps1`:
- r.38 `Sort-Object LastWriteTime -Descending | Select-Object -First 3` → **dal piu' recente**;
- r.52 `$ultima[$k] = $riga.Trim()` dentro il ciclo → **sovrascrive**.

👉 **Il valore che sopravvive e' quello del file PIU' VECCHIO dei tre**: la colonna si chiama
*"ultima riga"* ed e' **l'ultima riga di due giorni fa**.

**Verificato empiricamente, non solo letto**: la corsa del **14/09 03:30** legge `[14, 13, 12]` e
mostra come "ultima riga" un `nuovo giorno` delle **01:00** — cioe' **sabato 12/09**, giorno senza
seduta USA. E le corse del **15/09 e del 16/09** mostrano **la stessa riga al millisecondo**
(`.265` / `.188`), che e' quella di **lunedi 14/09**. Con la regola corretta le nove sedute del
par. 2 si datano senza contraddizioni; con la vecchia se ne ottenevano due per lo stesso giorno.

📌 Classe **466** scritta in `backtest_pipeline/CHECKLIST_RIGA_DI_LANCIO.md` (ultime prese: 457-465).

---

## ⑥ 📏 LA MISURA PIU' CORTA PER CHIUDERE QUELLO CHE RESTA

🔴 **NON e' stata mandata a nessuno.** E' pronta e ha passato il cancello deterministico.

- **Script**: `backtest_pipeline/righe/RIGA_PERCHE_770202_MUTA.ps1`
  (`MARCATORE_RIGA_PERCHE_770202_MUTA_v1`, `RUNNER_SOLA_LETTURA`)
- **Da mandare**: `backtest_pipeline/righe/RIGA_PERCHE_770202_MUTA_DA_MANDARE.md`
- **Bersaglio**: 🖥️ **finestra PowerShell sul VPS**. Nessun MT5 aperto/chiuso/compilato, nessun
  grafico toccato, **conto reale 10105439 solo letto**.
- **Cosa fa**: stampa le righe **INTERE** (niente taglio a 110) di `ABTG_Dow_Apertura_US` e
  `ABTG_ORB_Ottimizzato` su U30USD degli ultimi 25 giorni, **con la data davanti**, leggendo i log
  in **ordine crescente** (riparazione della classe 466). Referto + zip sul Desktop del VPS.
- **Costo**: **secondi**. Legge file di testo, non tick. **Zero tempo macchina di backtest.**
- **Cancello**: `python3 backtest_pipeline/controlla_riga.py --ps1 ...` → **6 controlli passati,
  nessun difetto meccanico**.

**Cosa decide**: il campo `bias N` distingue (A) da (B)/(C), e la presenza di `BUY LIMIT (retest)`
distingue (B) da (C). 👉 Dice **su quale manopola** vale la pena spendere una corsa, e su quale no.

---

## ⑦ 🧭 LA BUSSOLA: **+0,22 op/giorno** — dove sta davvero, e dove NON sta

🔴 **Prima la cosa scomoda: la strada che sembra ovvia e' gia' misurata, ed e' CARA.**
`backtest_pipeline/risultati_prove/ABTG_Dow_Apertura_US/ABTG_Dow_Apertura_US_U30USD_OOS_ptc.csv`
(e il gemello `_IS_`), stessa griglia, stessa finestra:

| cella | lati | range | **deal OOS** | **PF OOS** | **DD OOS** | PF IS |
|---|---|---:|---:|---:|---:|---:|
| **Pass 1/7 — quella CHE GIRA** | 🔒 solo long | 35 | **130** | **1,270** | **4,39%** | 1,222 |
| Pass 4/10 — **stessa cella, short ACCESO** | 🔓 due lati | 35 | **203** | 🔴 **1,096** | 🔴 **8,68%** | 1,372 |
| Pass 2/8 — la MIGLIORE in OOS | solo long | 45 | 115 | 🟢 **1,639** | 🟢 **2,65%** | 0,843 |
| Pass 0/6 | solo long | 25 | 138 | 1,487 | 5,24% | 1,129 |
| Pass 3/9 | due lati | 25 | 218 | 1,205 | 9,92% | 1,132 |
| Pass 5/11 | due lati | 45 | 187 | 1,160 | 7,29% | 1,233 |

> 🔴 **Accendere il lato short compra +56% di deal e paga: PF da 1,270 a 1,096 e DD RADDOPPIATO,
> da 4,39% a 8,68%.** E il verso e' quello che deve far suonare l'allarme: **IS migliora
> (1,222 -> 1,372) mentre OOS peggiora.** Non e' un miglioramento: e' la firma di una scelta che
> piace al campione dentro e non a quello fuori.
> 🔴 **E in POSIZIONI il guadagno e' `[NON MISURATO]`**: il rapporto deal->posizioni 1,354 e'
> misurato sulla cella **long-only** e la **classe 465 vieta di trasportarlo** su una cella piu'
> grassa — sbaglierebbe **sempre nel verso ottimista**.

🟢 **Dove la frequenza si prende davvero, e costa meno**: la famiglia `ABTG_ApertureCore` ha gia'
una **terza** sedia accesa e nessuno la sta contando — **`770250`** (`ABTG_Nasdaq_Apertura_US`,
NASUSD M15, profilo ORO chart41, rischio 0,35): ha operato il **15/09 e il 18/09**, cioe' **dentro
la finestra in cui il Dow taceva**. 👉 **E' un simbolo decorrelato dal Dow, gia' in campo, a costo
zero di rischio nuovo.**
⚠️ **E un fatto che va guardato adesso, perche' morde sul conteggio della famiglia**: le **4**
giornate operative di `770202` sono **tutte e 4** giornate in cui ha operato anche `770101`
(32 giornate per `770101` da solo, **32** per l'unione). **Le due sedie sono accese negli stessi
giorni**: una famiglia fatta di sedie che sparano insieme alza il pavimento **e** la correlazione.

---

## ⑧ 🪦 CERTIFICATO — e qui **non si archivia niente**

`770202` **NON** viene archiviata, e non e' nemmeno un candidato da certificare: **e' una sedia
viva, in orario, con un contratto misurato** (PF OOS 1,270 · DD 4,39% · 130 deal / 96 posizioni su
275 sedute · 0,349 op/seduta). Il silenzio e' **nel contratto**, non contro il contratto.

🚦 **Criterio di uscita del 18/08, applicato alla lettera**:
- **RISCHIO**: nessun DD in corso — **zero posizioni**. Nessuna revisione dovuta.
- **MERITO** (20 operazioni di famiglia): `770202` ha **4** operazioni. **Non si giudica.**
- **TAGLIANDO** (6 mesi / frequenza molto sotto il promesso): 🟠 **qui il semaforo e' giallo e va
  scritto**: campo **0,129 op/seduta** contro promesso **0,349** = **37% del promesso**. 🔴 **Ma il
  tagliando NON scatta oggi**: la sedia e' viva dal **07/08**, cioe' **6 settimane**, non 6 mesi, e
  la siccita' e' dentro la distribuzione del motore (par. 0). 👉 **LA DATA IN CUI DIVENTA UNA
  DOMANDA NUOVA E' IL 30/09/2026**, calcolata e non scelta: **otto** sedute lun-ven dopo il 18/09
  portano il silenzio a **23**, cioe' al record che questo motore ha gia' prodotto nel suo OOS.
  Oltre quella soglia *"e' il mercato"* smette di essere sostenuto dai numeri di casa.
  🔴 **Ed e' il giorno prima della challenge**: se il 30/09 la sedia e' ancora muta, non ci si
  arriva con una sedia in piu', ci si arriva con una domanda aperta. Va segnato in agenda ORA.

---

## ⑨ 🚧 NON COPERTO — quello che **non** ho potuto verificare, e perche'

1. 🔴 **Il valore di `bias` seduta per seduta**: troncato dal referto notturno. E' l'unica cosa che
   separa (A) da (B)/(C). Chiuderla costa la riga del par. 6 (**secondi**).
2. 🔴 **Se in qualche seduta un `BUY LIMIT` sia stato piazzato e sia scaduto non eseguito**: un
   ordine **mai eseguito** non entra in `trades_auto.csv` (il TradeExporter esporta i **deal
   chiusi**). Stessa riga, stesso costo.
3. ⚠️ **`SYMBOL_TRADE_STOPS_LEVEL` di U30USD non misurato**: se fosse **> 1000 punti**, il livello
   di rottura sarebbe **piu' ALTO** di 10,00 punti indice. 🟢 **Errore a senso unico: rafforza la
   conclusione, non la indebolisce.**
4. ⚠️ **La prova a prezzo esatto del par. 2 vale per l'11/09**, la seduta in cui l'ORB ha piazzato
   e il suo BUY STOP coincide al centesimo con il livello di `770202`. Per le altre sedute l'ORB ha
   i **suoi** cancelli (`InpUseEma200Filter=true`, `InpMaxRangePct=0.8`, `InpOneTradePerDay`) e
   **potrebbe non aver piazzato**: li' il silenzio dell'ORB **non e' una prova**. Dichiarato.
5. ⚠️ **`_Digits=2` di U30USD e' dedotto** dai prezzi a due decimali stampati da `ABTG_EMA200`
   nello stesso giornale, **non letto da `SymbolInfoInteger`**. La riga del par. 6 non lo misura:
   se serve certezza, va aggiunta una lettura delle specifiche del simbolo.
6. ⚠️ **Nessun dato di prezzo U30USD indipendente in repo**: la discesa del par. 2 e' misurata
   **dai log della sedia stessa** e dai `session_high/low` di `trades_auto.csv`. Gli
   `data/snapshots/*.json` **non contengono il Dow** (solo DAX, S&P 500, Nasdaq 100, Nikkei,
   FTSE MIB) — verificato, non assunto.
7. ⚠️ **Il conteggio delle sedute usa lun-ven**, senza calendario festivi BCM. Un festivo USA
   dentro la finestra **accorcerebbe** le 15 sedute: 🟢 di nuovo errore a senso unico, rafforza.
