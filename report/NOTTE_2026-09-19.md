# 🌙 LA NOTTE DEL 18→19 SETTEMBRE 2026

---

## 🌙 COSA HA FATTO IL RUNNER STANOTTE

### 🔴 **Il referto del 19/09 NON C'È in repo.**
L'ultimo pubblicato è `REFERTO_RUNNER_20260918_033004.txt`. Storico delle pubblicazioni:
**16/09 · 17/09 · 18/09**, tutte alle 03:30 — **il 19/09 manca**.

⚠️ **E «non pubblicato» NON vuol dire «non girato»**, ed è una distinzione che va fatta:
i referti del **16, 17 e 18** sono stati spinti sul repo **tutti insieme il 18/09 alle 17:49**
(sei commit `Runner ABTG: referto automatico` con lo stesso timbro). **La pubblicazione è a
lotti, non in tempo reale.** Quindi il referto di stanotte può arrivare più tardi.
🔴 **Se il runner delle 03:30 di oggi abbia girato: [NON MISURATO].**

**Ultimo segno di vita del VPS**: commit `24e4a1f9`, *«Aggiornamento automatico trades»*,
**18/09 22:45**. Da lì in poi, silenzio — che a quell'ora è normale.

### ⚠️ I round che il promemoria chiede sono di SEI GIORNI FA
Il promemoria chiede l'esito di `R145a/b`, `R125a-f`, `R127a`, `R146a/b/c` *«armate ieri sera»* —
ma quella frase è del **13/09**. Stato reale, contato sui file:

| round | file di risultati |
|---|---:|
| `r127a` | **3** |
| `r145a` · `r145b` · `r125a` · `r146a` · `r146b` · `r146c` | 🔴 **0** |

👉 **Sei round su sette non hanno prodotto nulla, e sono fermi da sei giorni.** Non li ho
diagnosticati stanotte: entrano nella coda di oggi.

---

## 🔬 COSA HO DECISO E CORRETTO IO, in autonomia

### 1. 🟢 Riparata la pagella: si raggruppa per **(commento, MAGIC)**
Nata dalla scoperta di ieri sera: `770250` (viva) e `770201` (spenta) scrivono **la stessa
identica stringa** `"Nasdaq Apertura US SELL"`, e `analizza_trades.py` r.539 raggruppava per il
solo commento.
📐 **Misurato prima di scrivere la patch, e il numero mi ha ridimensionato**: i commenti
condivisi da più magic sono **tre**, ma i giorni in cui la pagella ha **davvero** fuso due sedie
sono **UNO SOLO** (27/07, `DAX M3 L` `770501`/`770502`). Le due Nasdaq non hanno mai operato lo
stesso giorno.
👉 La patch vale lo stesso, ma **per un motivo diverso da quello che avevo detto**: la collisione
scatta **il giorno in cui si accende una seconda sedia sul Nasdaq** — cioè quello che stiamo per
fare. Verificata nei due versi: il 27/07 separa (`+155,37` e `+8,28` dove prima era una riga da
`+163,65`), il 18/09 e il 15/09 restano **identici**.

### 2. 🔴 **CLASSE 438 — un difetto mio, e l'ha preso il hook, non io**
Per provare quella patch ho lanciato `analizza_trades.py` su tre date volendo solo **leggere**:
lo strumento **scrive anche** `report/giornata_*.md`. Risultato: la pagella del **15/09**
rigenerata contro il CSV di **oggi** (riga di freschezza falsata da `2026-09-15` a `2026-09-18`,
più una riga `GAP NIKKEI L` che nell'originale non c'era), e un `giornata_2026-07-27.md` **nato
dal nulla**.
✅ **Ripristinato tutto prima del commit.** 🔴 E la regola lo vietava per iscritto — l'avevo letta
la sera stessa.

### 3. 🟢 Diagnosticate le «46 righe fallite» del runner — **e metà non erano fallite**
`RIGA_SOTTILE_ROUND.ps1` rr.1642-1645 documenta i codici: **3 = «GIRATO CON RILIEVI… NON è un
fallimento»**. Ma `runner_abtg.ps1` r.778 conta come `$falliti` **tutto ciò che non è zero**.

| | righe | tempo macchina |
|---|---:|---:|
| uscita 0 | 81 | 637,4 min |
| uscita 3 — **i numeri ci sono** | 23 | 79,5 min |
| 🔴 uscita 2 — **non misurati** | 23 | **140,3 min** |

👉 **104 righe su 127 hanno prodotto numeri, non 81.** Il buco vero sono **23 round che bruciano
140 minuti a notte senza misurare** — i più cari: `R154a` **30,1 min**, `R155a` **23,1 min**,
`R166a` **16,3 min**.
⚠️ **[NON MISURATO]**: il codice `2` ha due cause dietro (*«CSV assenti»* **oppure** *«zero
operazioni»*) e quale sia non si sa ancora. Toppa scritta, **non applicata** (è VPS → cancello +
firma).

---

## 📊 LA LETTURA DEL BACKLOG
Il promemoria chiede dei «98 CSV del backlog notte 12/13-09». **Quella lettura è chiusa dal
13/09** (`LETTURA_BACKLOG_NOTTE_2026-09-13.md`) e i 48 round di `dal_vps/` sono stati
**indicizzati ieri** in `REGISTRO_TEST.md`. 🟢 **Niente di pendente qui.**

---

## ⚠️ COSA ASPETTA CLAUDIO
**Niente che tocchi il conto reale 10105439.** Quattro cose, tutte sue per firma:
1. 🔴 **Il Nasdaq**: `Pass 8`, `Pass 6`, o tutti e due. Il `.set` è pronto e ha passato il
   cancello dopo cinque correzioni. Dentro, **due righe sono sue**: `InpRiskPercent=1.0`
   (la sedia viva `770250` gira a **0,35%**) e **parziale/breakeven spenti**.
2. 🔴 **Ricompilare** `SuperWave` e `MaxMinNotte` sul **50503392** — ⚠️ non a HEAD:
   `872dba82` (08/09) per SuperWave.
3. 🟠 **La prop**: FTMO (1:15) o FundedNext (1:25). Blocca il pacchetto di schieramento.
4. ⚪ **La riga dell'ATR** in `MonitorCloseConfirm`, e **la toppa al conteggio del runner**.

---

## 🎯 LA CODA DI OGGI
1. 🥇 **Separare le due cause del codice `2`** sui 23 round: *CSV assente* (guasto) contro *zero
   operazioni* (file prova che non produce trade). **140 minuti a notte** in gioco.
2. **I sei round fermi da sei giorni** (`R145a/b`, `R125a`, `R146a/b/c`): zero file, nessuno li
   ha guardati.
3. 🥈 **`MaxMinNotte` sul Nasdaq** — 0 file su NASUSD contro 24 su D30EUR. La pista dalle immagini
   di Claudio, l'unica che cambia **il livello** invece dell'ingresso.
4. **R180** (duello Dow) e **R183** (croce delle slide): pronti, sotto cancello, in attesa di via.

---
*Fonti: `backtest_pipeline/coda/referti/` (elenco e timbri) · `git log --grep="Runner ABTG"` per
le date di pubblicazione · `backtest_pipeline/righe/RIGA_SOTTILE_ROUND.ps1` rr.1642-1645 ·
`backtest_pipeline/runner_abtg.ps1` rr.777-786 · `backtest_pipeline/analizza_trades.py` r.539 ·
conteggio dei file di risultati per i sette round, fatto con `find`.*
