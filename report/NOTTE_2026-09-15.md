# 🌙 Report turno di notte — 15/09/2026, ore 06:30 IT

> Nota: la Routine automatica delle 06:30 porta ancora il testo della notte
> 13→14/09 (R145a/b, R125a-f, R127a, R146/147 — gia' riportati in
> `report/NOTTE_2026-09-14.md`, lo stesso giorno). Questo file riporta la
> notte VERA appena passata (14→15/09), con numeri verificati oggi, non
> copiati dalla Routine.
>
> Claudio ha chiesto stamattina (dopo la domanda sul Guardian/Ramo A) di
> lavorare in background e fare il resoconto stasera: questo file e' la
> versione scritta, aggiornata mano a mano. Non e' stato mandato in chat
> per rispettare quella richiesta.

## 🌙 COSA HA FATTO IL RUNNER STANOTTE (14→15/09)

**NON ANCORA MISURATO.** Nessun `REFERTO_RUNNER_20260915_*` e' arrivato nel
repo (`backtest_pipeline/coda/referti/`, ultimo file presente e' ancora
`..._20260914_033004.txt`) al momento di scrivere. Serve che Claudio rilanci
`carica_risultati.ps1` sul VPS per portare i CSV nel repo — o che il runner
stesso li pubblichi, come nelle notti in cui ha impiegato fino alle 08-09 IT.

Cosa e' **certo**, verificato su `CODA.txt`: le righe armate ieri sera/stanotte
sono TUTTE ancora armate (0 `#DISARMATA#`), in ordine:
`r150a` (uscita `ABTG_Nasdaq_Live5m`, `InpTrailStartR`), `r148a/r148bl/r148bs`
(`ABTG_Cycle`, primo collaudo di compilazione MAI fatto — incrocio zero),
`r151a` (trailing `ABTG_MaxMinNotte` oro, 770402), `r152a` (export per-trade
`ABTG_Dow_Apertura_US`, 770202), `r153a` (`InpAtrExitPeriod` su `ABTG_PTE`
GBPUSD, 771332), `r154a` (`InpSLLookback` su `ABTG_SuperWave` U30USD, 770531).
Sei round nuovi, tutti passati dal doppio cancello prima di finire in coda.

## 🔬 COSA HO DECISO/CORRETTO IO IN AUTONOMIA

**Il pezzo che conta di piu': il Blocco 1 del runbook di ricompilazione e'
CHIUSO.** Due commit WIP fermi da giorni (`b45dd00`, 11/09, 10 EA + ~2.000
righe "solo logging" mai verificate; `b5d904a`, 29/08, cambio di meccanismo su
`ABTG_Nasdaq_Apertura_US`) sono stati passati dal doppio cancello — riga per
riga, nessuna tocca `OrderSend`/`PositionModify`/`PositionClose`/lotto/
rischio/stop/target/ingresso-uscita. **13 sedie tornano candidabili alla
ricompilazione**, inclusa `771531` (EMA200 Dow), l'unica delle 41 che passa
tutti i criteri di casa alla lettera. Claudio ha confermato stamattina il
Ramo A (nessun gesto sul Guardian del piccolo) e sta ricompilando lui stesso,
partendo da `771531` come consigliato (runbook aggiornato coi numeri veri di
versione/righe attesi dopo F7).

**Un buco di sicurezza vero, trovato e tappato**: verificando `b45dd00` e'
emerso che il cancello deterministico stesso (`controlla_riga.py`) aveva una
falla — la declassazione "raccolta innocua" (classe 234/235) guardava solo il
nome del cmdlet e il bersaglio, mai il CONTENUTO scritto da `Set-Content`/
`Add-Content`/`Out-File`. Riprodotto un payload-lanciatore che passava con
exit 0 (classe 339), riparato, riverificato che le 5 righe di raccolta oneste
del repo restano pulite e che 11/11 contro-esempi tornano verdi.

**Il Cycle** (`ABTG_Cycle.mq5`, dallo screenshot XAUUSD di Claudio): aggiunto
un secondo modo d'ingresso a zero parametri liberi (minimo/massimo locale,
`EstremoCiclo_Calc`), l'uscita a tempo che Claudio ha riportato dal suo
manuale (N barre, mai trailing), e la matrice completa degli assi (orario,
filtro range/trend — dichiarati la STESSA variabile vista da due proxy, non
due assi da moltiplicare). File prova `R148g` pronto, non armato: aspetta il
primo collaudo di compilazione di `R148a/bL/bS` stanotte.

**Il Monte Carlo del DD** (`mc_dd_cella.py`) era rotto — falliva il proprio
autotest. Diagnosticato: non un bug del motore, ma un test troppo debole
vicino a un salto discreto della distribuzione (misurato su 200 seed: 71/200
falsi FAIL a mc=2000, 0/50 a mc=100.000 — la convergenza prova il motore
corretto). Riparato senza toccare i criteri di produzione. Preparato un round
(`R152a`) per dargli finalmente un CSV per-trade vero sul Dow (770202).

**Otto sedie coperte con round di uscita/export oggi**: EMA200 Dow 771531,
ORB 770611, DAX_Apertura 770101, MaxMinNotte XAUUSD 770402, Dow_Apertura
770202 (preparatorio), PTE GBPUSD 771332, SuperWave U30USD 770531, e il Cycle
su NASUSD. Ogni consegna e' passata dal doppio cancello con almeno una
correzione reale trovata — riassunto per classe:
- **332**: la sentinella "posto occupato non morde" di R148a non si eredita
  al rilevatore minimo-locale (non alterna sempre di segno).
- **333**: falso FAIL dell'autotest di `mc_dd_cella.py` era rumore
  campionario, non un bug — corretto rafforzando SOLO il confronto interno.
- **334**: righe `DA_MANDARE` (spread flotta) pinnavano un commit PRE
  riparazione di sicurezza — la chiusura di `Stop-Process` senza filtro
  avrebbe potuto spegnere il conto REALE. Corretto in 3 file.
- **335/336/337/338**: disallineamenti fra censimenti/documenti superati e la
  realta' del repo (staleness di fonte, non di sostanza) su R148g/R151a.
- **340/341**: sintassi del cancello Python (`-PermettiCellaSingola` non
  esiste li'), citazione di documento superato (v1 invece di v2).
- **342**: `InpSLbufferPips=25` citato in prosa ma MANCANTE dal blocco
  eseguibile su R153a — avrebbe usato il default (5, 5x piu' piccolo),
  rompendo la sentinella su una corsa da 26 anni.
- **343/346**: bande sentinella (il sostituto di un'ancora assente) scritte
  in unita' sbagliate o con fonti mescolate — su R154a, con la banda
  originale (Trades 70-100) il round si sarebbe fermato da solo su un
  risultato CORRETTO (falso "banco sporco").
- **344**: `InpFirstFraction` classificato "uscita" dal censimento
  automatico ma nel sorgente e' un parametro d'INGRESSO — 21 celle fantasma
  tolte dalla mappatura.
- **345**: `controlla_prova.py` non legge `@FRAZIONEIS`, stampa sempre
  "celle x 2 passate" anche per una tranche sola — non bloccante, stima di
  tempo macchina doppia del vero per quei file.
- **347**: `R141e` (daxva) era fermo FUORI coda da 3 giorni — la diagnosi del
  12/09 era gia' giusta (il PF sale il 18% anche a edge zero, per pura
  geometria) ma la correzione non era mai arrivata alle sezioni che
  decidono il verdetto. Riparato con un test statistico (IC 95%
  sull'eccesso di PF, altopiano su celle adiacenti, mai un picco isolato).

**Due container riavviati a meta' lavoro** durante la notte: in entrambi i
casi il lavoro in corso e' stato recuperato da disco, verificato di persona
sul codice vero prima di fidarmene, e completato senza perdita.

## 📊 LA LETTURA DEL BACKLOG

Non pertinente a questo ciclo: nessun backlog di CSV in sospeso da leggere
stanotte (quello della notte 12/13-09 era gia' stato chiuso nei giorni
scorsi). Il backlog aperto adesso e' semmai l'opposto: il referto del runner
di STANOTTE non e' ancora rientrato (vedi sopra).

## ⚠️ COSA ASPETTA CLAUDIO

- 🔴 **In corso**: la ricompilazione del piccolo (50503392), Blocco 2 (22
  sedie) + Blocco 3 (13 sedie appena sbloccate), partendo da `771531`.
  Nessun'altra azione mia possibile qui: e' un gesto sul terminale vivo.
- 🔴 **Se non gia' fatto**: `r137c` (`InpTP1_ClosePct` 50→0, sedia 770101)
  risulta firmato e nel repo dal 14/09, ma se sia gia' caricato sul terminale
  REALE 10105439 resta **[NON VERIFICABILE da qui]**.
- 🟡 **Da guardare quando c'e' tempo, non urgente**: il preset committato di
  `sedia_MAXMIN_ORO_770402.set` mostra ancora `InpRiskPercent=1.0` — il
  taglio a 0,5% del 23/08 non e' mai stato ricommittato sul file (classe 338).
- 💶 Niente altro sul conto reale, sui parametri di rischio o sui soldi.

## 🎯 LA CODA DI OGGI

1. Aspettare il referto del runner di stanotte (6 round nuovi, incluso il
   primo collaudo di compilazione mai fatto del Cycle).
2. Continuare la mappatura delle manopole d'uscita mai provate sulle sedie
   ancora scoperte (ottava fatta stanotte, `R141e` appena riparato in coda
   dietro le altre).
3. Seguire la ricompilazione del piccolo quando Claudio la fa girare — la
   verifica post-gesto (§4 del runbook) e' pronta, non serve altro lavoro
   mio prima che lui agisca.
4. Resoconto completo stasera, come richiesto.
