# 🥇 `770402` MaxMin ORO — **COSA MANCA ESATTAMENTE PER RIAPRIRLA** (20/09/2026)

> **Sola lettura.** Nessun backtest eseguito, nessun EA modificato, nessun preset
> modificato, nessun parametro in forward toccato. Il conto reale `10105439` non è
> coinvolto. Due file prova NUOVI, già passati dal cancello deterministico.
>
> 🧭 **Bussola**: la challenge FTMO (`541452707`, 100k 2-Step) è comprata e schierata con
> **SEI** sedie. Questa è la settima, e vale **+1 sedia schierabile** — l'unità di misura
> del progetto. La domanda non è *«è buona?»*, è **«cosa manca, col numero accanto»**.

---

## 🎯 LA RISPOSTA IN TRE RIGHE

1. 🟢 **La ragione scritta nel `$SOSPESE` è per metà SBAGLIATA, e la metà sbagliata è
   quella che costa.** Il contratto di `770402` **non** è stato misurato col binario in
   campo: è stato misurato con un **TERZO** binario (`5fc0bc31`) che ha **già dentro** il
   fix del breakeven e quello del sizing. Fra quello e HEAD c'è **una sola guardia**, e il
   suo autore la dichiara **inerte nel tester** — cosa che ho riverificato dall'esterno
   seguendo la macchina a stati.
2. 🔴 **Il blocco VERO è un altro e nessuno l'aveva scritto: la TAGLIA.** Il preset FTMO
   in repo porta `InpRiskPercent=2.00`. Il DD misurato di questa sedia è **10,6% a 1%**
   (6,5 anni, n=693) e **19,72% a 1%** (22 anni). A **2,00%** fanno **~21%** e **~39%**,
   contro un muro FTMO **statico del 10%**. **Nessuna corsa di controllo cambia questo.**
3. 🟢 **E c'è una scorciatoia gratis**: due round di USCITA su questa identica sedia
   (`r151a` trailing, `r170b` flat di fine finestra) **hanno già girato** il 15-17/09 e i
   loro CSV sono **fermi sul banco**, mai caricati (trasporto bloccato dal 13/09, 47
   round). Recuperarli costa **ZERO tempo macchina**.

---

# 1. 🔬 IL DIFF VERO AI DUE PIN — e ce n'è un TERZO che cambia tutto

## 1.1 I tre binari, identificati per blob, non per nome

| pin | data | righe | blob `ABTG_MaxMinNotte.mq5` | chi è |
|---|---|---:|---|---|
| `08239510` | 28/07 | **539** | `5b6b37fb` | 🔴 **il binario IN CAMPO** sul piccolo `50503392` |
| **`5fc0bc31`** | 19/08 | **615** | `12afd16e` | 🟢 **il binario CHE HA MISURATO IL CONTRATTO** |
| `7d0da9f9` | 03/09 | **918** | `3ce0ccd6` | 🟠 **HEAD**, quello che si schiererebbe |

**Come ho identificato il binario del contratto** (è la cosa che nessuno aveva fatto):
`R100_REFERTO.md` r.3 dichiara il pin `adbc27c`; `R103_REFERTO_FINALE.md` r.5 dichiara il
pin `7e2fb0d`. `git rev-parse <pin>:mql5/Experts/ABTG_MaxMinNotte.mq5` restituisce per
**tutti e due** lo stesso blob `12afd16e`, cioè `5fc0bc31`.

> ## 🔴 **Il contratto della sedia NON è stato misurato né col binario in campo né con HEAD. È stato misurato con un terzo binario che sta in mezzo ai due.**
> E questo ribalta la lettura: il difetto del breakeven **non è una ragione per sospettare
> il contratto** (il fix è già dentro il binario che l'ha misurato). È una ragione per
> **non schierare il binario in campo**. Sono due frasi diverse.

## 1.2 🧨 Il breakeven «annegato nel parziale»: **in quali condizioni NON scatta**

Binario in campo, r.294-304 — lo stop in pari è **dentro** la condizione del parziale:

```
double cv=NormVol(vol*InpTP1Pct/100.0);
if(cv>0 && cv<vol && gTrade.PositionClosePartial(ticket,cv))
  { gPart1=true; if(InpBreakeven) gTrade.PositionModify(_Symbol,NormalizePrice(openP),tp); ... }
```

`NormVol(v)` fa `v=MathFloor(v/step)*step; return(v<min ? 0 : v);`

**La condizione esatta, risolta**: con `step = min = 0,01` e `InpTP1Pct=50`, `cv` vale 0 se
e solo se `vol < 0,02`, cioè **`vol == 0,01` esatto**. A 0,01 lotti il parziale non parte,
`gPart1` resta `false`, **il breakeven non viene nemmeno tentato** — e per di più il
**secondo** parziale (che richiede `gPart1`) non parte mai neanche lui.

📏 **Misurato in campo**: `9 operazioni su 9` a 0,01 lotti, `0` parziali eseguiti
(`MAXMINNOTTE_ORO_770402_DIAGNOSI_2026-09-13.md` §B.1). **Il difetto ha morso il 100% dei
trade della sedia viva.**

🟢 **E adesso il contro-esempio che assolve la FTMO, ed è algebrico — non un'opinione.**
A 100k e `InpRiskPercent=2.00` il rischio per trade è **2.000 USD**. Il lotto cade a 0,01
solo se `lossPerLot > 2.000/0,02 = 100.000 USD`. Su XAUUSD (contratto 100 oz) servirebbe
uno **SL di oltre 1.000 USD/oncia**. Gli SL veri di questa sedia, misurati su operazioni
reali, stanno fra **25 e 41 USD/oz**.

> 🟢 **A 2,00% su 100k il difetto del breakeven è MATEMATICAMENTE IRRAGGIUNGIBILE** — serve
> uno stop 25 volte più largo del più largo mai visto. **Sul conto piccolo mordeva perché
> il conto era piccolo, non perché l'oro sia speciale.**

## 1.3 ⏱️ `InpOneTradePerDay`: **quante operazioni al giorno permette l'uno e quante l'altro**

### Il binario in campo (`08239510`): l'input è **DICHIARATO E MAI LETTO**
`grep` sul sorgente: **1 sola occorrenza**, la riga `input bool InpOneTradePerDay = true;`
(r.52). **Zero letture.** È una **manopola inerte** nel senso letterale del censimento del
09/09.

**Ma il limite di 1/giorno c'è lo stesso, imposto da altri tre pezzi** (letti riga per riga):

| chiusura | dove | effetto |
|---|---|---|
| OCO | `HandleOCO(){ if(SelPos()) CancelPendings(); }` | appena una posizione vive, il pendente opposto **muore** → la straddle non fa due posizioni |
| macchina a stati | `gPhase` torna a `MMP_WAIT` **solo** in `ResetDay()`, cioè al cambio di giornata | `TryPlace()` non può girare due volte nello stesso giorno |
| scadenza + cutoff | `ORDER_TIME_SPECIFIED` a +90 min · cutoff 08:30 | i pendenti non eseguiti spariscono comunque |

> **Percorso normale: 1 operazione/giorno anche in campo.** E in forward lo conferma la
> misura: **9 operazioni, 9 giornate distinte, ZERO giorni con due ingressi** in 29 giorni.

### 🔴 Ma le PORTE esistono, e sono due — ed è lì che HEAD morde

| porta | quando si apre | quante op/giorno diventano possibili |
|---|---|---|
| **P1 — `SelPos()` accecato** | conto **HEDGING** con un'altra posizione sullo stesso simbolo e ticket **più basso**: `PositionSelect(_Symbol)` seleziona quella del vicino, `SelPos()` torna `false`, **l'OCO non cancella** | **2** (la straddle può riempire tutte e due le gambe) |
| **P2 — riavvio a giornata iniziata** | `gPhase` è una globale e riparte da `MMP_WAIT`; a trade già chiuso e pendenti già spariti la guardia anti-duplicato **non vede niente** → `TryPlace()` riparte | **1 + 1 per ogni riavvio** fra le 07:00 e le 08:30 |

**HEAD chiude P2 in modo definitivo** (`PuoArmare_Calc` interroga lo **storico del giorno**
per `DEAL_SYMBOL`+`DEAL_MAGIC`, che sopravvive a riavvio e ricompilazione) e **accorcia
P1** (`GiornataSpesa_Calc` cancella i pendenti superstiti appena il trade del giorno è
finito, invece di aspettare il cutoff). **Non chiude P1**, e il suo changelog lo dichiara
testualmente: *«NON tocca `SelPos()`»*.

| | binario in campo `08239510` | HEAD `7d0da9f9` |
|---|---|---|
| op/giorno, percorso normale | **1** | **1** |
| op/giorno con un vicino sull'oro (P1) | **fino a 2** | **fino a 2** (non risolto) |
| op/giorno dopo un riavvio (P2) | **1 + 1 per riavvio** | 🟢 **1, punto** |
| op/giorno **nel tester** | **1** | **1** |

> ### 🟢 **E questa è la riga che scioglie il nodo: nel TESTER i due si comportano IDENTICAMENTE, perché nel tester gira UN solo EA (P1 impossibile) e non ci sono riavvii (P2 impossibile).**

**Non l'ho preso dal changelog: l'ho rifatto.** Perché il ramo di HEAD morda, `gOperatoOggi`
dovrebbe accendersi **fra il cambio di giornata e le 07:00**. Le due sole vie sono (a) una
posizione nostra viva a cavallo della mezzanotte — impossibile, `InpCloseAtEnd=true` chiude
alle 17:30 e nel tester `SelPos()` vede sempre la nostra; (b) un nostro deal `DEAL_ENTRY_IN`
già registrato **oggi** — impossibile, il primo ingresso del giorno è alle 07:00 o dopo e il
box notturno non piazza niente. **Ed è una previsione falsificabile**: se il ramo mordesse,
l'EA stamperebbe una riga precisa. R193a la conta (§4).

## 1.4 📋 Il diff completo `5fc0bc31` → HEAD, per nome

Letto con `git diff ... | grep -v commenti`: **+303 righe, tutte in UN solo meccanismo.**
Nuovi: `gOperatoOggi`, `gUltimoCheckStorico`, `PosMia_Calc`, `TimbraGiornata_Calc`,
`GiornataSpesa_Calc`, `PuoArmare_Calc`, `ContaPosizioniMie`, `HaGiaOperatoOggi`,
`UnTradeAlGiorno`, `Autotest`, `input InpAutoTest`.

🟢 **NON toccati, verificati per ASSENZA dal diff**: `TryPlace`, `ManagePos`, `LotByRisk`,
`NormVol`, `SLforLong`/`SLforShort`, `ComputeBox`, `HandleOCO`, `CancelPendings`,
`EndOfDay`, `CorrBias`, il filtro news.

> **Fra il binario che ha scritto il contratto e quello che si schiererebbe non c'è nessuna
> differenza di INGRESSO, di STOP, di TAGLIA o di USCITA. C'è una guardia in più.**

---

# 2. 🏺 TUTTE LE MISURE IN ARCHIVIO SU `770402`, e con quale binario

| round | data | binario | finestra | modello | rischio | **n** | **PF** | **DD** |
|---|---|---|---|---|---:|---:|---:|---:|
| **R103** | 24/08 | **`5fc0bc31`** | 2020.01.01→2026.06.30 (6,5 a) | OHLC M1 | 0,5% | **693** | **1,31** | **10,6% @1%** (⇒ ~5,3% @0,5%) |
| **R100** | 23/08 | **`5fc0bc31`** | 2004.06.11→2026.06.30 (22 a) | OHLC M1 | 1,0% | *(non pubblicato)* | *(non pubblicato)* | **19,72% @1%** · pegg. giorno −1,07% |
| R17 | griglia | *(pre-`5fc0bc31`)* | da 2025.03.01, M5 | — | 1,0% | IS 40-103 · OOS 77-129 | IS 0,89-2,43 · OOS 1,46-2,27 | OOS 3,3-6,9% |
| R19/R19b | griglia | *(pre-`5fc0bc31`)* | da 2025.03.01, M5 | — | 1,0% | IS 59 · OOS 92 | IS 1,42 · OOS 2,45 | OOS 4,24% |
| **r151a** | 15/09 | 🟠 **banco (HEAD?)** | 2020.01.01→2026.06.30 | OHLC M1 | 0,5% | 🔴 **CSV MAI CARICATO** | | |
| **r170b** | 15-17/09 | 🟠 **banco (HEAD?)** | 2020.01.01→2026.06.30 | OHLC M1 | 0,5% | 🔴 **CSV MAI CARICATO** | | |

### 🔎 Le cose che questa tabella dice e che non erano scritte da nessuna parte

**a) ⚠️ R19 è una MANOPOLA INERTE conclamata**: nel censimento del 09/09 risulta
`2 passate → 1 esito unico`. Due giri, un risultato. *«L'abbiamo già provato»* qui voleva
dire *«l'abbiamo girato senza che cambiasse niente»*.

**b) 🔴 Le griglie R17/R19 hanno TUTTE `n < 150`.** Per la regola di casa il **MERITO** su
quelle celle è **SOSPESO**. L'unica misura di merito valida è R103 (`n=693`).

**c) 🟠 E il merito si SGONFIA allargando la finestra**: le celle R17 danno PF OOS 1,46-2,27
su ~1 anno; R103 sulla stessa sedia su 6,5 anni dà **1,31**. Non è una contraddizione, è la
lezione di sempre: **il PF bello sta nella finestra corta.** Il numero da usare è 1,31.

**d) 🔴 `InpMaxSpread`: il contratto è misurato su una cella DIVERSA da quella schierata.**
R103 ha girato con `InpMaxSpread=0` (nessun filtro); il preset vivo
(`sedie_piccolo/sedia_MAXMIN_ORO_770402.set` r.51) **e** il preset FTMO portano **150**. Il
filtro può solo TOGLIERE ingressi (`SpreadOK()` cancella `TryPlace`). In backtest OHLC è
verosimilmente inerte (spread modellato costante); **in campo no**. Lo spread vero di XAUUSD
su BCM e su FTMO è **`[NON MISURATO]`**.

**e) 🔴 E il numero 150 sfonda la frontiera del costo di casa.** `stop >= 40 × spread`: con
SL misurati 2.500-4.100 punti la frontiera ammette **spread ≤ 62-102 punti**. Il preset ne
ammette **150**, cioè **1,5-2,4×** oltre. 👉 **Proposta (non applicata): pinnare
`InpMaxSpread=60` nel preset FTMO.** È una modifica a un preset e quindi non la faccio.

**f) 🟠 Il preset in repo porta ancora `InpRiskPercent=1.0`**, mentre la taglia viva firmata
il 23/08 è **0,5%** e R103 ha girato a 0,5%. La firma del 23/08 non è mai arrivata nel file.
Non cambia il preset FTMO (che è 2.00 per firma del 20/09), ma è un disallineamento da
sapere.

**g) 🟢 E una BUONA notizia sulla frequenza, misurata due volte in modo indipendente.**
Il contratto scrive *«~3,7 posizioni/mese ⇒ ~0,17 op/g (dato R17)»* — ed è **sottostimato di
2,4×**. I due numeri veri concordano fra loro:
- **R103**: 693 op / 6,5 anni ≈ 1.638 giorni di borsa → **0,42 op/giorno di trading**
- **forward vero**: 9 op / 11-08→08-09 ≈ 21 giorni di borsa → **0,43 op/giorno di trading**

👉 **Backtest e campo dicono lo stesso numero al centesimo.** Con il pavimento di frequenza
per **FAMIGLIA** (firma 07/09) questa sedia non ha nessun problema di portata.

---

# 3. 🪦 IL CERTIFICATO DI MORTE **AL CONTRARIO** — cosa manca, per nome, col numero

| # | requisito | c'è? | il numero |
|---|---|:--:|---|
| **a** | **PF misurato col binario che si schiererebbe** | 🔴 **NO** | PF **1,31** c'è, ma su `5fc0bc31`. Su HEAD: **`[NON MISURATO]`** |
| **b1** | **n** | ✅ **SÌ** | **693** (R103, 6,5 anni) — ampiamente sopra il muro dei 150 |
| **b2** | **DD** | 🟠 **SÌ ma alla taglia SBAGLIATA** | **10,6% @1%** (6,5 a) e **19,72% @1%** (22 a). Alla taglia che si schiererebbe (**2,00%**): **`[NON MISURATO]`** |
| **c** | **gestione dell'uscita messa ad asse** | 🟠 **FATTA DUE VOLTE, MAI LETTA** | `r151a` (`InpTrailAtrMult` 0,8→3,2, **7 celle**, girata 15/09, uscita 3) · `r170b` (`InpCloseAtEnd`, **2 celle**, girata 15-17/09, uscita 3). **CSV fermi sul banco** |
| **d** | **simboli gemelli** | ✅ **SÌ, e con i certificati** | `D30EUR` **24 corse** (sedia viva `770411`) · `F40EUR` PF max **0,9985** DD **40,7%** · `E50EUR` **0,8398** / **35,2%** · `100GBP` **0,6717** / **48,3%** — *tre morti col certificato completo* · `EURUSD` 2 corse · `NASUSD` proposto in R187 |
| **e** | **TF cambiato** | ✅ **SÌ** | R17 ha messo **`InpMgmtTF`** ad asse su **5 valori** (M30/H1/H2/H3/H4), **20 passate → 20 esiti distinti** (la manopola ha morso). A buffer 350 il PF IS va da 2,16 a 2,43 sui cinque TF: **l'asse TF è un altopiano piatto**, la cella viva H2 non è un picco |

> ## 🪑 **VERDETTO: `770402` NON È MORTA E NON È NEANCHE «NON MISURATA». È SOSPESA PER DUE BUCHI SOLI, E TUTTI E DUE HANNO UN NUMERO E UN COSTO.**

### 🔴 I due buchi, e sono questi

> **BUCO 1 — il PF/n/DD su HEAD.** Costo stimato: **~1 minuto di macchina** (§5).
> **BUCO 2 — il DD alla taglia 2,00% che il preset porta.** Costo stimato: **~2 minuti.**

### ⚠️ E il TF: cosa NON serve rifare, perché il numero c'è già
Il **TF del grafico non è un parametro di segnale** per questo motore: `ComputeBox()` legge
sempre e solo `PERIOD_M1` (cablato), e la gestione legge `InpMgmtTF`. 👉 **Cambiare il TF
del grafico non cambia il motore**; l'unico vero asse-TF è `InpMgmtTF`, **già misurato in
R17**. Non lo rimetto in griglia: sarebbe allargare i parametri d'ingresso, che è
esattamente quello che il limite del 19/08 vieta.

---

# 4. 🛠️ LA VIA PIÙ CORTA AI NUMERI MANCANTI

## 4.0 🟢 PASSO ZERO — **GRATIS, e va fatto prima di tutto il resto**

`report/IL_TRASPORTO_E_FERMO_2026-09-17.md`: **i CSV non entrano nel repo dal 13/09**, e ci
sono **47 round con i numeri già fatti** che nessuno può leggere. Fra questi, **`r151a` e
`r170b` — i due round di USCITA di QUESTA sedia**.

Lo strumento esiste ed è già corretto: **`backtest_pipeline/carica_risultati.ps1`**
(`$GiorniIndietro = 30`, quindi il 15/09 è **dentro** finestra fino al 15/10).

> 🎯 **Bersaglio: 🖥️ una finestra PowerShell sul VPS.** Lo script **non tocca nessun
> terminale MT5** — né `50503392` (`BCM Markets MT5 Terminal`), né `50504263` (`...-V3`), né
> `10105439` (`C:\BCM_Reale`), né `50504400` (`C:\MT5_Backtest`): legge file dal disco e
> parla con l'API GitHub. È scritto nella sua stessa intestazione (r.4-9).
>
> 🚦 **La riga di lancio NON è in questo referto**: per la regola del cancello del 09/09
> deve passare da `controlla_riga.py` + `controllo-preventivo` prima di uscire, e quello è
> un lavoro che va chiesto, non improvvisato qui.

**Costo: ZERO tempo macchina, pochi minuti di rete.** Sblocca **9 celle di USCITA già
pagate** su questa sedia (7 di `r151a` + 2 di `r170b`) e altri 45 round.

## 4.1 📄 I DUE FILE PROVA, pronti e già passati dal cancello

```
backtest_pipeline/prove/R193a_binariocontrollo_MaxMinNotte_XAUUSD.txt   2 celle
backtest_pipeline/prove/R193b_taglia_MaxMinNotte_XAUUSD.txt             4 celle
```
```
$ python3 backtest_pipeline/controlla_prova.py <i due file>
  R193a_binariocontrollo_MaxMinNotte_XAUUSD.txt  pin=52  celle=2  OK
  R193b_taglia_MaxMinNotte_XAUUSD.txt            pin=52  celle=4  OK
  file: 2 | celle totali: 6 | passate: 12 | problemi: 0   ESITO: OK
```

**R193a — il controllo di binario.** Riproduce la cella di R103 **campo per campo**, su
HEAD, stessa finestra / modello / deposito. Asse **tecnico**: `InpMagic` su due gemelle
vergini `761700`/`761701` (cancello G1 di determinismo).

**R193b — la taglia contro il muro.** Asse: **`InpRiskPercent` 0,5 → 2,0 passo 0,5**.
🔥 **E questa manopola non è MAI stata messa ad asse in nessun file prova del progetto**
— verificato il 20/09: `grep -rl "^InpRiskPercent=.*||Y" backtest_pipeline/prove/` dà
**ZERO file su 863**. La taglia è sempre stata **pinnata e poi riscalata a mano**. È una
**casella libera**, non una casella provata.

## 4.2 💰 IL COSTO IN TEMPO MACCHINA, calibrato su una misura vera

**Àncora `[MISURATO]`**: `coda/referti/REFERTO_RUNNER_20260915_033005.txt` r.465-470 —
`r151a`, **stesso EA, stesso simbolo, stessa finestra, stesso modello, stesso deposito**:
`ESEGUITO in 143s` per **7 celle × 2 finestre = 14 passate** → **~10,2 s/passata**.

| round | celle | passate | tempo stimato |
|---|---:|---:|---:|
| R193a | 2 | 4 | **~45-90 s** |
| R193b | 4 | 8 | **~85-140 s** |
| **totale** | **6** | **12** | 🟢 **sotto i 4 MINUTI** |

> 🎯 **Bersaglio delle corse: 🪟 il banco di backtest `50504400`, cartella
> `C:\MT5_Backtest`** (`-TerminaleBacktest` non è opzionale, classe 204).
> **NON** `50503392` (`BCM Markets MT5 Terminal`) — che ha **questa stessa sedia viva** col
> magic `770402` e non si tocca; **NON** `50504263` (`...-V3`); **NON** `10105439`
> (`C:\BCM_Reale`, conto **REALE**).
> I magic dei due file (`761700`/`761701`/`761702`) sono **vergini in tutto il repo**,
> verificato il 20/09: nessun incrocio di etichette possibile con la sedia viva.

⚠️ **Vincolo delle ~100.000 barre**: non morde qui. Il round gira su **H2** su 6,5 anni
≈ **20.000 barre H2** — un quinto del tetto. Il box si calcola su M1 via `iBarShift`, che il
tester serve dallo storico e non dal buffer delle barre della corsa. 👉 **Nessuna tranche,
nessuno spezzettamento.** (Il tetto morderebbe su M15 e M5, che qui non servono.)

⚠️ **Modello 1 (OHLC M1) e NON 4, ed è l'inverso della regola di casa**: sull'oro i tick
reali BCM iniziano **2024.07.05** `[MISURATO, R100_REFERTO.md]` e la finestra parte dal
2020. È la stessa scelta di R100, R103 e R151a. **E non è un permesso**: il DD che esce è un
**LIMITE INFERIORE** del rischio.

---

# 5. 📐 L'ATTESA DICHIARATA **PRIMA** DEI NUMERI

*(Versione integrale, con i contro-esempi, dentro i due file prova. Qui il riassunto.)*

## 5.1 R193a — cosa mi aspetto

| # | attesa | banda |
|---|---|---|
| A1 | le gemelle `761700`/`761701` danno risultati **identici al centesimo** | cancello G1 |
| **A2** | 🎯 **la riga del gate compare ZERO volte nel log del tester** | **esattamente 0** |

🔴 **La stringa da cercare, COPIATA DAL SORGENTE e non riscritta a memoria** — `git show
7d0da9f9:mql5/Experts/ABTG_MaxMinNotte.mq5` **r.287**, e si cerca **così, carattere per
carattere** (accento tipografico `'` e non `à`, «h**o**» e non «h**a**»):

```
oggi ho gia' operato (storico simbolo+magic): non ripiazzo.
```

⚠️ E nel log vera compare **preceduta dal prefisso di `Log()`** (r.210,
`Print("[MaxMinNotte] ", m)`), cioè: `[MaxMinNotte] oggi ho gia' operato …`. Una ricerca
ancorata a inizio riga **non la trova**. 👉 Questa riga nasce da un mio errore trovato
**prima** della consegna: nella prima stesura avevo scritto *«oggi ha già operato»*, che nel
codice **non esiste** — e chi l'avesse cercata avrebbe trovato zero occorrenze e certificato
un PASS falso. Depositata come **classe 489**.
| A3 | `n_IS + n_OOS` | **[655 ; 700]**, centro 693 |
| A4 | coerenza del merito | `PF_OOS ≥ 1,10` · `PF_IS ≥ 1,00` |

**Perché A2 è il controllo decisivo e non il PF**: è l'unica osservabile che misura
*direttamente* se la guardia nuova ha morso. Non richiede di confrontarsi con un'altra
corsa, quindi non si porta dietro il confronto fra due driver diversi.

**Perché la banda A3 è asimmetrica**: il secondo ramo di HEAD può solo **togliere** ingressi
(cancella pendenti), **mai aggiungerne**. I +7 sopra 693 coprono solo la ripartizione al
confine IS/OOS del driver (`FrazioneIS 0,40`), che non è quella di R103.

### 🧪 Il contro-esempio, costruito prima
🔴 *«La riga esce zero volte perché l'ho guardata male.»* — Contro-prova gratis nella stessa
corsa: HEAD stampa in `OnInit` anche l'**autotest** dei predicati (`InpAutoTest`, pinnato a
`true`). Quell'autotest **non esiste prima di `7d0da9f9`**. 👉 **Il log deve contenere
l'autotest E NON contenere il gate. Una sola delle due non basta** — e insieme escludono
anche l'ipotesi alternativa *«il banco non ha ricompilato»*.

### 🛑 Cosa mi farebbe dire «NO, RESTA SOSPESA»
> **Se la riga del gate compare anche UNA SOLA VOLTA**: la frequenza su HEAD **non** è
> quella con cui il contratto è stato misurato, e `770402` resta sospesa finché il contratto
> non viene riscritto sui numeri di quella corsa.
> **Se `n` cade fuori da [655;700]**: la riproduzione non ha riprodotto. **Non si conclude
> né a favore né contro** — si cerca il perché.

## 5.2 R193b — cosa mi aspetto, e lo scrivo prima di vederlo

| taglia | DD atteso | fonte dell'attesa |
|---:|---|---|
| 0,5% | **4,0 - 7,0%** | centro 5,3% = 10,6% di R103 riportato alla taglia |
| 1,0% | 8 - 12% | R103 dà 10,6% sulla finestra piena |
| 1,5% | 14 - 17% | interpolazione fra moltiplicativo e lineare |
| **2,00%** | 🔴 **19 - 22%** | moltiplicativo `1−(1−0,053)⁴ = 19,6%` · lineare `21,2%` |

Più due controlli interni gratis: il DD deve essere **monotono crescente**, e `n` deve
essere **identico** sulle quattro celle (la taglia non tocca né l'ingresso né l'uscita).

### 🛑 LA SOGLIA CHE DECIDE, congelata prima
> 🔴 **Se il DD misurato a `InpRiskPercent=2.00` supera 8,0%, `770402` NON si schiera a
> 2,00%.** Otto e non dieci: il muro FTMO è **10% statico** (`docs/REGOLAMENTO_FTMO_2026-08.md`
> r.27) e il DD OHLC è un **limite inferiore**, quindi due punti sono il prezzo dichiarato
> del modello. **Non è negoziabile con un'altra griglia**: una griglia più fitta su un
> motore che sfonda il muro trova celle verdi per caso (regola del 19/08).

### 🧪 Il contro-esempio che conta di più
🔴 *«Se a 2,00% il DD esce SOTTO il 10%, la sedia passa.»* — **NO.** Un DD a 2,00% sotto il
10% **contraddirebbe tutte e due** le misure esistenti (R100: 19,72% a 1%; R103: 10,6% a
1%). Se esce, la conclusione non è *«buona notizia»* ma *«una delle tre misure mente, e
finché non so quale non schiero niente»*. Le quattro spiegazioni alternative da escludere
per nome sono elencate in `R193b` §C1.

🟢 **E l'assoluzione del deposito, fatta prima**: a 0,5% su 100.000 il rischio per trade è
500; con SL misurati 25-41 USD/oz (= 2.500-4.100 USD/lotto) il lotto esce **0,12-0,20**,
cioè **12-20 volte il minimo**. 👉 **Il pavimento `MathMax(mn,…)` non morde a nessuna delle
quattro taglie**, e la scala del rischio si legge pulita. A deposito 10.000 si ribalterebbe.

### 📏 E la regola di selezione, dichiarata insieme al numero
**Centro dell'altopiano, mai il picco.** In R193b non c'è nessuna cella da *scegliere*: si
consegna la **curva** DD(taglia) e la **taglia massima** sotto la soglia. In R193a non c'è
nessuna cella da scegliere per costruzione (asse tecnico). 👉 **Nessuno dei due round
seleziona niente**, ed è voluto: selezionare è esattamente il modo in cui si trovano i
picchi di rumore.

---

# 6. 🕳️ I BUCHI CHE DICHIARO, per nome

1. **`[NON MISURATO]` — lo spread vero di XAUUSD** su BCM e su FTMO. Senza quel numero la
   frontiera `stop ≥ 40×spread` si calcola sul *tetto* `InpMaxSpread`, non sul reale.
2. **`[NON MISURATO]` — PF e DD di `r151a` e `r170b`**: i round sono girati, i CSV sono sul
   banco. **Non li assumo in nessuna direzione**, né a favore né contro.
3. **`[NON MISURATO]` — `770402` alla taglia 2,00%** (è il punto di R193b).
4. **`[NON MISURATO]` — il segno NETTO del fix del breakeven** su questa sedia: il breakeven
   taglia anche i vincenti. Già dichiarato dal referto del 13/09, resta aperto.
5. 🔴 **NON RISOLTO DA NESSUNA RICOMPILAZIONE — il difetto C9 (`SelPos()` cieco).** L'08/09
   questa sedia è rimasta sul mercato **2h19m oltre la propria ora di flat** perché un
   vicino sull'oro aveva il ticket più basso. **C'è anche a HEAD.** 🟢 **Ma sulla FTMO la
   porta è CHIUSA di fatto**: delle sei sedie schierate **nessuna tocca XAUUSD** (D30EUR ×2,
   U30USD ×3, NASUSD ×1; i tre PostNews sono EURUSD/USDJPY). 👉 Se un giorno una seconda
   sedia oro entra in quel conto, **il difetto si riapre** — e va scritto ora, non allora.
6. **`[LIMITE DICHIARATO]`** — non ho letto nessun `.ex5`. Tutte le identificazioni di
   binario sono per **blob git** e per **conteggio righe** incrociato coi referti CODA.
7. **`[LIMITE DICHIARATO]`** — R103 ha pubblicato **un** PF di finestra piena, non due per
   sotto-finestra: un confronto PF-contro-PF cella per cella con R193a **non è possibile**,
   e R193a non finge di farlo.

---

# 7. ✍️ LE DUE COSE CHE RESTANO DI CLAUDIO

1. 🔴 **La TAGLIA di `770402` sulla FTMO.** Se R193b conferma l'attesa (DD ~20% a 2,00%), la
   scelta non è tecnica: o la sedia entra **sotto** la taglia uniforme, o **non entra**. E
   scendere sotto la taglia uniforme tocca il regolamento FTMO — il preset stesso scrive
   *«FTMO vieta le size erratiche»*. **Il numero glielo porto io; la firma è sua.**
   📊 Per il confronto (`FIRME_2026-09-19_SERA.md` r.66-68), a 2,00% le sedie già schierate
   stanno a **7,36% · 8,78% · 14,46%**. Se `770402` esce a ~20%, **è fuori scala rispetto
   alle sue compagne**, non «un po' più rischiosa».
2. 🟠 **Il `-Pin` con cui si compila**, se le due corse danno PASS: il bersaglio proposto è
   **HEAD `7d0da9f9`**, e vanno **insieme** il `.mq5` al pin **e** il preset
   `ABTG_MaxMinNotte_ORO_770402_FTMO.set` — come `SCHIERA_FTMO.ps1` r.218 dice già.
   ⚠️ **E un dettaglio da sapere prima**: quel preset **non pinna `InpAutoTest`**, che a HEAD
   esiste e ha default `true` → l'EA stamperà l'autotest all'avvio. È **innocuo** (stampa e
   basta), ma «benigno per default» non è «deciso».

---

## 📌 DICHIARAZIONE DI ONESTÀ

- **Questo referto è PONTEGGIO, non una sedia.** Non ha prodotto nessun PF nuovo. 🟢 **Ma ha
  prodotto tre fatti nuovi**: (1) il contratto è stato misurato con un **terzo** binario che
  nessuno aveva identificato, e questo **restringe** la sospensione invece di allargarla;
  (2) il blocco vero è la **taglia**, che nessun referto aveva collegato a questa sedia;
  (3) **due round di uscita su questa sedia sono già girati e i numeri sono sul banco**.
- **Niente è stato eseguito, modificato o promosso.** Due file prova scritti, due PASS del
  cancello deterministico, zero righe di lancio emesse.
- 🎯 **E la cosa che vale davvero**: `770402` era etichettata *«serve una corsa di controllo
  e una firma»*, senza che nessuno avesse scritto **quale** corsa e **quanto** costa. Ora la
  corsa è scritta, passata dal cancello, e costa **meno di quattro minuti di macchina**.

_Fonti: `git show`/`git diff`/`git rev-parse` sul branch `lavoro` ai pin `08239510`,
`5fc0bc31`, `7d0da9f9`, `adbc27c`, `7e2fb0d` · `backtest_pipeline/righe/SCHIERA_FTMO.ps1`
r.216-219 · `report/MAXMINNOTTE_ORO_770402_DIAGNOSI_2026-09-13.md` ·
`report/SCHIERAMENTO_FTMO_2026-09-20.md` §6.2 · `report/I_BINARI_DELLA_ROSA_2026-09-19.md` ·
`report/CONTRATTI_SEDIE.md` r.95 · `report/CENSIMENTO_CONTRATTI.md` r.220 ·
`backtest_pipeline/risultati_archivio/R100_REFERTO.md` · `R103_REFERTO_FINALE.md` r.16 ·
`R103_CRITERI.md` r.212 · `backtest_pipeline/risultati_prove/MaxMin_Oro_r17/*.csv` ·
`backtest_pipeline/risultati_archivio/CENSIMENTO_PF_TUTTI_2026-09-09.csv` r.19-20, 44 ·
`backtest_pipeline/REGISTRO_TEST.md` §R187, §gemelli europei ·
`report/IL_TRASPORTO_E_FERMO_2026-09-17.md` · `report/FIRME_2026-09-19_SERA.md` r.66-68 ·
`report/PRESET_FTMO_OROLOGIO_2026-09-20.md` · `docs/REGOLAMENTO_FTMO_2026-08.md` r.17/25/27 ·
`coda/referti/REFERTO_RUNNER_20260915_033005.txt` r.465-470._
