# 🎯 `EMA200` — L'**IS IN POSIZIONI**: la riga **NON** e' andata in coda, e il motivo e' **piu' utile della riga**

**12/09/2026** (sabato) · branch `lavoro` · runner sul VPS: **domenica 03:30**
**Mandato**: mettere `COLLAUDO_EMADOW_02_pertrade_IS.txt` in `backtest_pipeline/coda/CODA.txt` come round vero, per stanotte.

## 📄 AVVERTENZA SU QUESTO DOCUMENTO
🚫 **Qui dentro NON c'e' nessuna riga da mandare a Claudio, nessun terminale MT5 da aprire, nessuna azione a mano.** Tutti i comandi citati sono girati **da me su questa macchina Linux** (`pwsh 7.4.6`, `curl`, `git`, `python3`). 🪟 Nessuno dei quattro terminali MT5 (`50503392` · `50504263` · `10105439` · `50504400`) e' stato sfiorato, e **`CODA.txt` non e' stata toccata** (sha256 identico, dimostrato al §5).

---

# 1️⃣ LA RISPOSTA IN SEI RIGHE

🔴 **LA RIGA NON E' IN CODA, E NON DEVE ESSERCI.** Non per un difetto del file prova: per un **buco strutturale della corsia**. La catena ROUND **non ha nessun canale per il per-trade**, e il per-trade qui **e' il deliverable**, non un accessorio. Messo in coda cosi', il round sarebbe girato, avrebbe scritto il per-trade in `Common\Files` del banco, e **lo zip della mattina non lo avrebbe contenuto**: referto verde, due CSV, e la domanda ancora aperta.
🔴 **E sarebbe stato PEGGIO di niente.** L'unico conteggio leggibile nello zip e' la colonna `Trades`, che conta i **deal di uscita** (classe 226, fattore **2,0117** su questa sedia). Sul CSV `_IS` uscirebbe **circa 237** — un numero *plausibile*, vicino alla stima, che chiunque potrebbe leggere come "operazioni". 👉 **Una misura falsa con l'aria di una misura vera**, che e' esattamente il guasto per cui Claudio ha scritto la regola del 10/09.
🟢 **Il file prova e' stato CORRETTO su cinque difetti**, due dei quali gli avrebbero fatto **misurare la finestra sbagliata anche a canale aperto**. Gatato, `EXIT 0` su tutti e due gli strati deterministici.
🟢 **Il canale e' pronto, scritto e COLLAUDATO**: `righe/CODA_12_pertrade_posizioni.ps1`, corsia **SOLA LETTURA**, conta i `position_id` distinti e mette **deal / POSIZIONI / rapporto** nel referto. Passa i cancelli **VERI del runner** e **boccia cinque esche**. Costa **zero passate di tester**.
🟢 **Il tredicesimo giro di pin e' UN VALORE SOLO**, e l'ho misurato invece di stimarlo: `$SHA_WALK` e `$SHA_ROUND` **non cambiano** (verificati con `Get-FileHash` sui file **scaricati da raw** al pin nuovo). Cambia solo `$PIN`.
🔴 **E una trappola trovata per strada, che mordeva stanotte**: `COLLAUDO_EMADOW_GENERA.py` e' **vecchio**. Rilanciarlo riporta indietro **104 righe su cinque file prova** — fra cui **`COLLAUDO_EMADOW_05_tf_U30USD.txt`, che e' IN CODA STANOTTE** — e crea un file orfano col nome sbagliato. Banner di avviso messo, col numero.

---

# 2️⃣ 🔴 IL GIUDIZIO SUL FILE PROVA — e il difetto grosso **non e' nel file**

## 2.a — LA MISURA BLOCCANTE: la corsia ROUND non ha un canale per il per-trade

Il file prova dice di se stesso: *"si CONTANO i position_id distinti nel per-trade che l'EA esporta a fine test"*. Quel file l'EA lo scrive davvero — in `Common\Files` del banco. **Nessuno lo porta a casa.**

```console
$ occorrenze di 'abtg_trades' nella catena che gira DAVVERO
  backtest_pipeline/runner_abtg.ps1                 0
  backtest_pipeline/righe/RIGA_SOTTILE_ROUND.ps1    0
  backtest_pipeline/righe/RIGA_ROUND_VPS.ps1        0      <<< e "Common" la SALTA di proposito (r.548)
  backtest_pipeline/walkforward_generico.ps1        2      <<< r.1707-1708: due Write-Host di CONSIGLIO
```
E la raccolta e' scritta in chiaro, `RIGA_ROUND_VPS.ps1` r.622-624 e r.801: copia **solo** `$csvIS`, `$csvOOS`, il referto e il file prova. Nient'altro.

### 🔬 IL CONTRO-ESEMPIO, perche' un grep a zero da solo non dimostra niente
Se `abtg_trades` non esistesse nel repo, quei quattro zeri non direbbero niente (nome sbagliato, refuso, altro meccanismo).
```console
$ script in righe/RIGA_*.ps1 che NOMINANO abtg_trades e lo raccolgono : 36
$ script in righe/RIGA_*.ps1 in tutto                                 : 98
```
👉 **Il token esiste, e 36 corsie DEDICATE lo raccolgono** (`RIGA_BREAKIN`, `RIGA_CRT_EXT_S2`, `RIGA_NYRETEST`, `RIGA_DAXREENTRY`, `lancia_r81..r94`, ognuna con la sua `PerTradeNome()`). **E' la corsia ROUND che non ce l'ha.** Il grep discrimina: non e' un grep a vuoto.

### 🔎 E nemmeno le 11 righe di SOLA LETTURA lo coprono
```console
$ le 11 righe CODA_* PREESISTENTI: occorrenze di 'abtg_trades'  -> 0 su 11
  (contate ESCLUDENDO la CODA_12 che ho scritto io: se la si include il conto
   diventa 1 su 12, e sarebbe il mio stesso file a confermare il mio numero)
$ 'Common' compare in CODA_05 (3) e CODA_06 (1) -- e sono altro:
    CODA_05 r.296-304 ELENCA i file di Common\Files (nome, KB, data) e stampa
            il CONTENUTO solo di quelli che matchano "news|calend|event".
            'abtg_trades' non matcha. E CODA_05 gira PRIMA dei round.
    CODA_06 r.19  e' una frase in un commento.
```

### 🔴 E LA REGOLA DI CASA CHIEDEVA GIA' QUESTO, da prima di me
`backtest_pipeline/CHECKLIST_RIGA_DI_LANCIO.md`, **regola tripla**, punto **2**:
> *"Gli artefatti si **RICONTANO**, non si `Test-Path`. Per un per-trade il conto e' `righe - 1` = **operazioni**, ed e' esattamente il numero che il PASSO 0 sta misurando: **va NEL REFERTO**, non lasciato a chi apre il CSV. Tre esiti **diversi**: file assente / file a sola intestazione / N operazioni."*

👉 **La checklist lo chiede, la corsia ROUND non lo fa, e nessuno se n'era accorto perche' finora nessun round della coda aveva il per-trade come deliverable.** Questo e' il primo.

## 2.b — 🔴 I CINQUE DIFETTI DEL FILE PROVA, corretti

| # | difetto | come stava | perche' costava | adesso |
|---|---|---|---|---|
| **1** | 🔴 **`@FINOA` sbagliata di UN GIORNO** | `@FINOA 2025.06.10` | **2025.06.10 e' il PRIMO giorno dell'OOS di R112, non l'ultimo dell'IS.** Il file lo dichiarava *"DEDOTTO"* dal primo `close` dell'OOS e da un file del 09/09. **Non era da dedurre: e' aritmetica del driver** — `walkforward_generico.ps1` r.921: `$Meta = $Inizio.AddDays(floor(($FineDt-$Inizio).TotalDays * $FrazioneIS))`. Su `2024.09.26 -> 2026.06.30` sono **642 giorni**, `floor(642 x 0,40) = 256`, **Meta = 2025.06.09** | `@FINOA 2025.06.09` |
| **2** | 🔴 **il canale del taglio IS/OOS non c'era** | nessun `@FRAZIONEIS`, e in testa *"tranche unica (FrazioneIS 1.0 **dalla riga di lancio**)"* | **FALSO su questa corsia.** `RIGA_SOTTILE_ROUND.ps1` r.99-105 passa **sei** argomenti (`-Expert -Prova -Etichetta -Modello -Deposito -SoloControllo`) e `-FrazioneIS` **non c'e'** (lo dice il file stesso, nel blocco dell'undicesimo giro: *"su QUESTA corsia il driver NON riceve -Simbolo, -Periodo, -DaQuando, -Fino, -FrazioneIS"*). Il driver avrebbe usato il **default 0,40**, spezzato la finestra in due, e **la gamba che gira per SECONDA avrebbe troncato il per-trade buono** | `@FRAZIONEIS 0.002`, e il §2.c spiega perche' **non 1.0** |
| **3** | 🟠 **il costo dichiarato era meta'** | *"2 celle x 1 corsa x 1 gamba = 2 passate"*, `T = 0,75 min` | Il driver gira **sempre** due gambe (il `foreach($w in $WF)` e' cablato). E lo dice il cancello da solo: `controlla_prova.py` stampa **`passate (celle x 2 finestre): 4`** | **4 passate nominali**, `T = 0,6 + 0,077 x 4 =` **0,908 min** |
| **4** | 🔴 **l'attesa dichiarata rendeva l'esito SCONTATO** | banda **102-118**, cioe' **150 FUORI** dalla banda | Un file prova che conosce la risposta **non misura: conferma**. E la banda veniva dai rapporti di **tre CELLE diverse** (`r1`/`r2`/`r3`), non di **finestre** diverse | banda **102-165**, con **150 DENTRO** |
| **5** | 🔴 **il contro-esempio del file era ARITMETICAMENTE FALSO** | *"per arrivare a 150 posizioni il rapporto dovrebbe scendere a **1.58, FUORI dalla banda misurata**"* | `237 / 150 = 1,58`. Il **pavimento** del rapporto secondo la decomposizione di casa e' **1,4397** (se il parziale TP1 non scattasse mai). 🔴 **1,58 sta DENTRO [1,4397 ; 2,3143]**: verificato, `True`. Il contro-esempio **non falsificava niente**, e sotto sembrava che lo facesse | riscritto con la decomposizione vera |

### 📐 La decomposizione che smonta il difetto 4-5 (fonte: `report/EMA200_I_DUE_REQUISITI_2026-09-12.md` §3.2)
```
2,0117  =  1
        +  0,5720   <- parziale TP1 (147 posizioni a 2 deal)     -> lo governa InpTP1Pct
        +  0,4397   <- CHIUSURE SPEZZATE (20 posizioni, 113 deal in piu')
                       riempimento frazionato in mercato veloce  -> NESSUN parametro
```
👉 Il **43,97%** del rapporto non lo controlla nessuna manopola. Una banda costruita su tre celle dello stesso motore **non e' una banda sulle finestre**.

## 2.c — 🔴 PERCHE' **NON** `@FRAZIONEIS 1.0`, che era la scelta "ovvia"

`@FRAZIONEIS 1.0` e' il modo di casa per dire "una tranche sola", e il driver lo accetta (r.731-733, l'avviso a r.732: *"FrazioneIS 1.0 = UNA SOLA TRANCHE. La gamba OOS e' DEGENERE"*). **Ma il ciclo delle due gambe e' cablato**, quindi la seconda gamba parte con `FromDate 2025.06.10 > ToDate 2025.06.09`. E qui la casa ha gia' scritto la risposta, e non e' rassicurante — `CHECKLIST_RIGA_DI_LANCIO.md`:
> *"**Cosa fa MT5 con `FromDate > ToDate` NON E' MISURATO.** ... Se MT5 esegue anche una sola passata a zero operazioni su quella finestra, il per-trade della IS — che in un PASSO 0 **e' il deliverable** — resta con la **sola intestazione**."*

🔴 **Con 1.0 esiste lo scenario peggiore di tutti: la sentinella passa (il CSV `_IS` e' stato scritto PRIMA e resta giusto) e il conteggio delle posizioni e' spazzatura.** Un numero verde su una misura morta.
🟢 **Con `0.002` quello scenario non esiste**, e la garanzia e' **l'ORDINE**, che e' un fatto e non un'ipotesi:
```
finestra 2024.09.26 -> 2025.06.09    giorni 256    floor(256 x 0,002) = 0
  gamba "IS"  = 2024.09.26 -> 2024.09.26   gira PRIMA  -> si butta
  gamba "OOS" = 2024.09.27 -> 2025.06.09   gira DOPO   -> E' LA MIA
```
👉 **La gamba che si vuole misurare gira per ULTIMA: qualunque cosa faccia la prima, e' il suo per-trade che viene sovrascritto, non il contrario.** E resta valido anche il giorno in cui qualcuno applichera' la toppa *"mai troncare"* all'EA (regola tripla, punto 1): allora la prima gamba non creerebbe nemmeno il file.

### 🧊 IL PREZZO DELL'ORDINE, dichiarato e con il numero
La finestra misurata e' l'IS di R112 **meno il solo 2024.09.26**.
🟢 **Costo in operazioni: ZERO, e si dimostra dal codice.** `ABTG_EMA200.mq5` r.305 (`EmaVal` torna **0** se `CopyBuffer` non riempie) + la guardia di `OnNewBar` (`if(ema<=0 || atr<=0){ cF_ind++; return; }`): **finche' l'EMA200 su H1 non ha 200 barre l'EA non puo' piazzare niente**. Il feed BCM sugli indici parte il **2024.09.26**: 200 barre H1 sono **circa 9 giorni di borsa**, quindi la prima operazione possibile e' intorno al **2024.10.08**. Il 2024.09.26 sta **dentro il riscaldamento dell'indicatore**.
⚠️ **Margine residuo, dichiarato**: il tester carica pre-storico prima di `FromDate` quando ce n'e', e con `FromDate 2024.09.27` ha **24 barre in piu'** da spendere nel riscaldamento. Effetto atteso: **0-1 posizione** in piu' o in meno. Su una banda 102-165 con la soglia a 150 e' **sotto la risoluzione della domanda**, e **la sentinella lo intercetta comunque**.

## 2.d — ✅ CLASSE 273 (`-Modello`): il file e la riga dicono la stessa cosa che fanno
- il file prova, in testa: **`BANCO: Modello 4 (TICK REALI)`**;
- la riga proposta al §4: **`-Modello 4`**;
- `RIGA_SOTTILE_ROUND.ps1` r.493: `4 = tick reali, la verita'; 1 = OHLC M1, solo screening` → **coerente, e il commento non e' invertito**;
- `walkforward_generico.ps1` r.1676: `$comeGira = if($Modello -eq 4){ "tick reali" }` → **il driver stampa "tick reali"**, quindi il referto della mattina non puo' mentire su questo.
- 🔑 E il **`-Deposito`**: R112 girava a **100000** (`righe/RIGA_R112_EMADOW_CONTRATTO.ps1` r.204, letto). Il default della sottile e' **10000**: la riga **deve** portare `-Deposito 100000`, altrimenti la sentinella non ha speranza. I quattro round `r136a..d` gia' in coda portano `100000`: coerente.

## 2.e — ✅ CLASSE 226: il file misura le POSIZIONI, non i deal — e adesso c'e' chi le conta
Il file chiede i **`position_id` distinti**, non `Trades`. ✅ Giusto. 🔴 **Ma finche' nessuno li conta, la domanda giusta resta senza risposta**: e' per questo che il §3 esiste.

## 2.f — 🟠 UN DIFETTO EREDITATO che NON ho introdotto e che va detto
R112 passava **`-Spread 0`** (`Spread=0` nell'`.ini`, *"spread corrente, dichiarato"*). **La corsia ROUND non passa `-Spread`**, quindi il driver usa il default **`-1`** e la riga `Spread` **non viene scritta**: MT5 usa il valore che ha in memoria (`walkforward_generico.ps1` r.192 e r.958). 👉 La sentinella potrebbe fallire **per questo** e non per la finestra. 🟢 **Il rilevatore esiste gia' e gira stanotte**: `r136a` ha la **stessa** ancora (IS 237 deal / PF 1,20110 / DD 5,7325%). Se `r136a` riproduce, il banco e' quello.

---

# 3️⃣ 🟢 IL CANALE — `CODA_12_pertrade_posizioni.ps1`, scritto, gatato, **collaudato**

📄 `backtest_pipeline/righe/CODA_12_pertrade_posizioni.ps1` · **12.870 byte** · **ASCII puro (byte >127 = 0)** · corsia **SOLA LETTURA** · **zero passate di tester**
🔑 sha256 = `3948DAF97FC4A342E51C6B8CD5584CFF3D9F19B1F7DF4BCE0A7BD86C9B3623B4`

**Cosa fa**: apre i `abtg_trades_*.csv` di `Common\Files` e stampa, per ognuno, **deal di uscita** · **POSIZIONI** (`position_id` distinti) · **rapporto** · primo e ultimo `close_time` · data e dimensione del file; poi **confronta i GEMELLI** dello stesso EA+simbolo. Legge in **streaming** (`StreamReader`, `FileShare::ReadWrite` come `CODA_05`), con **tetto dichiarato** a 40 MB / 400.000 righe — perche' la catena del runner **non ha timeout**.
👉 E' la **regola tripla della checklist, punti 2 e 3**, fatta. Il punto 1 e' una toppa all'EA e non la fa nessuno script.

## 3.a — 🔬 IL COLLAUDO, con le attese calcolate a parte in `python3`
Cartella finta, cinque file, quattro esiti diversi. **Le attese le ho calcolate con `python3`+`csv`, cioe' con un altro programma**, non col mio stesso codice:
```console
                                                    atteso (python3)      uscito (CODA_12)
abtg_trades_ABTG_EMA200_U30USD_771521.csv   deal 517  posizioni 257   ->   517 / 257 / 2,0117   OK
abtg_trades_ABTG_EMA200_U30USD_771522.csv   deal 517  posizioni 257   ->   517 / 257 / 2,0117   OK
abtg_trades_ABTG_Esca_NASUSD_900011.csv     deal  20  posizioni  13   ->    20 /  13 / 1,5385   OK
abtg_trades_ABTG_Esca_NASUSD_900012.csv     deal  15  posizioni  10   ->    15 /  10 / 1,5      OK
abtg_trades_ABTG_Finto_D30EUR_900001.csv    deal   0  posizioni   0   ->   "SOLA INTESTAZIONE"  OK
```
🟢 **E il 517/257/2,0117 e' il numero che un'altra sessione aveva gia' scritto** (`report/EMA200_I_DUE_REQUISITI_2026-09-12.md` §3.1): verificato contro i numeri veri di qualcun altro, non contro me stesso.

## 3.b — 🧪 I CONTRO-ESEMPI: il confronto dei gemelli **sa bocciare**?
Un confronto che non puo' fallire non e' un confronto. Ho costruito **due gemelli che DEVONO divergere** (stesso EA+simbolo, uno con 20 deal e uno con 15):
```console
=== IL CONFRONTO DEI GEMELLI (regola tripla, punto 3) ===
  abtg_trades_ABTG_EMA200_U30USD : 2 celle, TUTTE a 517 deal / 257 posizioni -- GEMELLI COERENTI
  abtg_trades_ABTG_Esca_NASUSD   : 2 celle, GEMELLI DIVERGENTI -- deal 15/20 e posizioni 10/13
  abtg_trades_ABTG_Finto_D30EUR  : una sola cella, nessun gemello da confrontare.
per-trade contati: 5 su 5   gruppi di gemelli divergenti: 1
```
✅ **Li prende.** E gli altri tre rami, provati uno per uno:
```console
cartella VUOTA         -> "NESSUN per-trade in questa cartella" + le tre cause da guardare
cartella INESISTENTE   -> "la cartella NON ESISTE ... e' un fatto, e va letto come tale"
intestazione senza position_id -> "NON CONTATO -- intestazione senza colonna position_id: [...]"
```
🟢 **I tre esiti che la checklist chiede di NON confondere (assente / sola intestazione / N operazioni) escono come tre stringhe diverse.**

## 3.c — 🔑 I CANCELLI **VERI DEL RUNNER**, eseguiti sul suo codice
Non una mia reimplementazione: ho estratto le **definizioni** del runner (`r.1-446`) e chiamato le **sue** funzioni.
```console
VagliaScript ok     : True
VagliaScript corsia : LETTURA
VagliaScript motivo : G1 e G2 passati
VagliaArgomenti ok  : True   G4: nessun argomento

--- CONTRO-ESEMPI: i cancelli VERI sanno bocciare? ---
  esca marcatore via       ok=False   G1: manca il marcatore 'RUNNER_SOLA_LETTURA' ...
  esca due marcatori       ok=False   G1: lo script ha TUTTI E DUE i marcatori ...
  esca riga che duplica    ok=False   G2: riga 256 contiene 'Copy-Item' ...
  esca riga che scrive     ok=False   G2: riga 256 contiene 'Set-Content' ...
  esca avvia un terminale  ok=False   G2: riga 256 contiene 'terminal64.exe' ...
```
✅ **G1, G2, G4 passati dal codice vero, e cinque esche bocciate.** 📌 Nota di onesta': nelle `r.1-446` un grep per `Start-Process`/`Set-Content` da' dei colpi — sono la **tabella dei DIVIETI** (r.141-145, r.189-190) e la regex del cancello (r.366), cioe' **dati e pattern**, non effetti.

## 3.d — ✅ I CANCELLI DETERMINISTICI, da soli, senza pipe (classe 254)
```console
$ python3 backtest_pipeline/controlla_riga.py --oggetto ps1 backtest_pipeline/righe/CODA_12_pertrade_posizioni.ps1
  PASSATI (6): ASCII puro | compila: 0 errori dal parser PowerShell vero |
               param block RICONOSCIUTO (Cartella) | nessun costrutto pwsh-7-only |
               formati .NET | nessun Parse decimale senza cultura invariante
  ESITO: nessun difetto meccanico.                                        EXIT = 0

$ python3 backtest_pipeline/controlla_prova.py backtest_pipeline/prove/COLLAUDO_EMADOW_02_pertrade_IS.txt
  COLLAUDO_EMADOW_02_pertrade_IS.txt  ABTG_EMA200.mq5  pin=42 celle= 2  OK
  file: 1 | celle totali: 2 | passate (celle x 2 finestre): 4 | problemi: 0
  ESITO: OK                                                               EXIT = 0

$ python3 backtest_pipeline/controlla_riga.py --oggetto prova backtest_pipeline/prove/COLLAUDO_EMADOW_02_pertrade_IS.txt
  OK   file prova ASCII puro                                              EXIT = 0
```
📌 Il parametro `-Cartella` di `CODA_12` esiste **solo** per poterla collaudare fuori dal VPS (e' quello che ha prodotto il §3.a). In coda **non si passa nessun argomento** e vale il default `%APPDATA%\MetaQuotes\Terminal\Common\Files`. `VagliaArgomenti` su stringa vuota: **`G4: nessun argomento`**.
🔤 **ASCII contato con `python3`, mai con `grep '[^\x00-\x7F]'`** (ricetta rotta senza `-P`):
```console
CODA_12_pertrade_posizioni.ps1        12870 byte, byte >127 = 0
COLLAUDO_EMADOW_02_pertrade_IS.txt    14551 byte, byte >127 = 0
COLLAUDO_EMADOW_GENERA.py                     byte >127 = 0
CODA.txt                              byte >127 = 30, DELTA MIO = 0 (non l'ho toccata)
```

---

# 4️⃣ 📋 LE DUE RIGHE, **pronte e NON messe in coda** — e cosa serve prima

## 4.a — 🔴 IL TREDICESIMO GIRO DI PIN: **UN VALORE SOLO**, e l'ho misurato
Il file prova corretto vive a **`a3143e98eebcf2860be213bc21cf9f0433b20466`**. Perche' la catena lo prenda, `$PIN` dentro `RIGA_SOTTILE_ROUND.ps1` deve puntare **li'**. Le altre due impronte **non cambiano**, e non lo dico: l'ho **misurato con `Get-FileHash` sui file scaricati da `raw` al pin nuovo**.

| valore | adesso (al pin di coda del canarino `8499939`) | al tredicesimo giro | misurato |
|---|---|---|---|
| `$PIN` | `b7979d82ec9d35c2ef4c9cb94ebcd1126aba2ce8` | 🔴 **`a3143e98eebcf2860be213bc21cf9f0433b20466`** | il file prova corretto sta li' (§5.b) |
| `$SHA_WALK` | `15DE7D5F...6828F1C6` | 🟢 **INVARIATA** | `Get-FileHash` del driver da raw @`a3143e9` = **`15DE7D5F...6828F1C6`** |
| `$SHA_ROUND` | `348ED533...9D0A315B` | 🟢 **INVARIATA** | `Get-FileHash` di `RIGA_ROUND_VPS.ps1` da raw @`a3143e9` = **`348ED533...9D0A315B`** |

🟢 **E non ho toccato nessuno dei tre.** Il mio commit, misurato: `git diff --stat 8b7e1cf a3143e9 -- walkforward_generico.ps1 RIGA_SOTTILE_ROUND.ps1 RIGA_ROUND_VPS.ps1 runner_abtg.ps1 mql5/ coda/CODA.txt` → **vuoto**.

## 4.b — LE DUE RIGHE, nell'ordine (il pin di coda della prima e' il commit del giro di pin, ancora da fare)
```text
<pin del 13o giro> | backtest_pipeline/righe/RIGA_SOTTILE_ROUND.ps1 | -Expert ABTG_EMA200 -Prova COLLAUDO_EMADOW_02_pertrade_IS.txt -Etichetta cemad02 -Modello 4 -Deposito 100000
a3143e98eebcf2860be213bc21cf9f0433b20466 | backtest_pipeline/righe/CODA_12_pertrade_posizioni.ps1
```
🔴 **IN FONDO, dopo i 19 round e dopo il canarino**, e in **quest'ordine** (prima il round, poi il conteggio: i per-trade li scrive il tester).
🔴 **E IL BENEFICIO DELL'ORDINE E' ZERO PER LA LETTURA — classe 274.** `runner_abtg.ps1` r.780 chiude il `foreach`, r.793-794 scrivono il referto, `W()` accumula in memoria: **riga 11 e riga 32 si leggono nello stesso istante**. Quindi "in fondo" **non** lo motivo con *"cosi' si legge subito"*. Lo motivo con due cose misurate: **(1) ordine di CAUSA** — il conteggio deve venire dopo il round che produce i file; **(2) raggio d'azione** — nella catena non c'e' **nessun timeout** (`Start-Process -Wait` senza `-TimeoutSec`: runner r.774, `RIGA_SOTTILE_ROUND` r.636, `RIGA_ROUND_VPS` r.655), e `continue` copre una riga che **muore**, non una che **si pianta**. Il codice **nuovo** si mette dove, se si piantasse, ha gia' alle spalle tutto il resto.

## 4.c — 🔴 PERCHE' NON LE HO MESSE IO
1. **La regola del 09/09 e' bloccante e non e' una formalita'**: *"NIENTE esce dalla sessione verso Claudio o verso il VPS senza un PASS"*, **due strati**, e *"se il controllo non e' ancora tornato, SI ASPETTA"*. Il secondo strato (`controllo-preventivo`) **non posso invocarlo**. Il canarino ha potuto rischiare perche' era **una riga con `-SoloControllo`**: zero tester, zero CSV. **Qui c'e' codice NUOVO che gira sul VPS** e un round che apre MT5. Non e' lo stesso peso.
2. **Il mandato diceva anche l'altra meta'**: *"Se una verifica non torna, FERMATI e scrivilo. Meglio nessuna riga che una riga che domattina racconta una cosa falsa."* **Una verifica non e' tornata**, ed e' quella che decide se la misura arriva.
3. **Il costo di aspettare e' UNA NOTTE, non la challenge.** Il costo di sbagliare e' un numero falso su **l'unica delle 41 sedie vive** che passa i cancelli alla lettera — e un numero falso su quella sedia costa molto piu' di una notte.
4. 🟢 **E dopo il PASS, metterle e' UN COMANDO**: giro di pin (un valore), poi due righe appese in fondo a `CODA.txt`. Tutto il resto e' fatto e misurato.

---

# 5️⃣ 🔒 LE 31 RIGHE PREESISTENTI SONO INTATTE — e `CODA.txt` non l'ho aperta

## 5.a — LA PROVA AL BYTE, in `python3` (non a occhio)
```console
$ CODA.txt  HEAD: 28061 byte  sha256 d1fdadabb1d4ba8b...
$ CODA.txt  ora : 28061 byte  sha256 d1fdadabb1d4ba8b...
$ IDENTICA AL BYTE: True
$ righe eseguibili: 31
     19  1445abf80666883ea2f6a4ecb983e91a2eb26834      <<< i 19 round
      4  d22c61fc...   1 5d4ab50d...   1 d6362cbb...   1 269de579...
      1  32718b9f...   1 3a648eb6...   1 4833b9dc...   1 bbd2aa02...   <<< le 11 di sola lettura
      1  84999392d0a8726fb3f03d8ffb9b0133657e0055      <<< il canarino
$ round a 1445abf8 : 19
$ canarino ultimo  : True
$ byte >127        : 30  (come prima: delta mio ZERO)
```
✅ **19/19 · 11/11 · canarino ancora ultimo · totale 31 · sha256 identico.** Non "non l'ho toccata": **dimostrato**.
📌 **`--oggetto coda` NON ESISTE (classe 275)**, quindi **non l'ho lanciato**: `controlla_riga.py --oggetto riga` su `CODA.txt` da' un FAIL a 3 bloccanti **identico sulla coda non toccata**, ed e' lo strumento puntato su un oggetto che non e'. Il mio delta su quel file e' **zero byte**, quindi non c'e' nemmeno un delta da misurare.

## 5.b — 🔬 LE VERIFICHE `raw`, con HTTP e impronte, **piu' il controllo negativo**
```console
(a) CODA_12 @a3143e9                                       HTTP 200
    sha256 raw   = 3948DAF9...9B3623B4
    sha256 local = 3948DAF9...9B3623B4        IDENTICO: True   byte>127: 0
(b) file prova CORRETTO @a3143e9                           HTTP 200
    sha256 = B49B1EB5...667163DB   14551 byte   IDENTICO alla copia locale: True
    contiene '@FRAZIONEIS 0.002'       -> True
    contiene '@FINOA      2025.06.09'  -> True
(c) lo STESSO file prova @b7979d8 (il pin della catena COLLAUDATA)  HTTP 200
    5958 byte   contiene '@FRAZIONEIS' -> False
                contiene '@FINOA    2025.06.10' (il difetto) -> True
    >>> ECCO PERCHE' IL GIRO DI PIN E' OBBLIGATORIO: la catena collaudata
        scaricherebbe la versione DIFETTOSA, con 200 e senza un avviso.
(d) la sottile @84999392 (pin di coda del canarino)        HTTP 200
    $PIN = b7979d82...  $SHA_WALK = 15DE7D5F...  $SHA_ROUND = 348ED533...
(e) il driver @b7979d8    Get-FileHash = 15DE7D5F...6828F1C6   = $SHA_WALK  OK
(f) il driver @a3143e9    Get-FileHash = 15DE7D5F...6828F1C6   INVARIATO   OK
(g) RIGA_ROUND_VPS @a3143e9 Get-FileHash = 348ED533...9D0A315B INVARIATO   OK
CONTROLLI NEGATIVI, altrimenti i 200 non dicono niente:
(h) righe/CODA_99_non_esiste.ps1 @a3143e9                   HTTP 404
(i) righe/CODA_12_pertrade_posizioni.ps1 @84999392          HTTP 404
```
🟢 **(h) e (i) sono la meta' che conta**: un 200 vale solo se qualcosa, sulla stessa URL e col solito percorso, sa dare 404. E la **(i)** e' anche la prova che il pin di `CODA_12` non puo' essere quello del canarino.

## 5.c — 🔴 LA TRAPPOLA TROVATA PER STRADA, e mordeva **stanotte**
Ho aggiornato `COLLAUDO_EMADOW_GENERA.py` perche' riproducesse il file prova corretto. **Rilanciandolo, ha riscritto CINQUE file prova che non erano miei** — e uno e' **in coda stanotte**. Ho rimesso tutto a posto (`git checkout`, e `git status` lo dimostra) e poi ho **misurato** il danno, girando il generatore **a HEAD** in una cartella di prova:
```console
COLLAUDO_EMADOW_01_spread_scala_ini.txt    50 righe diverse
COLLAUDO_EMADOW_03_uscita_TP1PCT.txt       30 righe diverse
COLLAUDO_EMADOW_04_uscita_TRAILING.txt     18 righe diverse
COLLAUDO_EMADOW_06_latenza.txt              4 righe diverse
COLLAUDO_EMADOW_05_tf_U30USD.txt            2 righe diverse   <<< E' IN CODA (etichetta cemad05)
COLLAUDO_EMADOW_00_*                        NOME DIVERSO: il generatore scrive
    '00_canarino_spread.txt', in repo c'e' '00_manopola_maxspread.txt'
    -> lanciarlo crea un OTTAVO file orfano e lascia in piedi quello vero
```
E le 2 righe del **05** sono **la riga del COSTO**: il generatore dice ancora *"14 passate PER GAMBA"* invece di *"14 passate"* in tutto. 👉 **Chi rigenerasse rimetterebbe in circolo un costo sbagliato su un file che gira.**
**Causa**: il commit `92c9621` (*"secondo giro: applicati 5 bloccanti + 4 gravi del cancello di giudizio"*) ha corretto i `.txt` **a mano** e non ha toccato il generatore. 🔑 **I `.txt` sono la verita', il generatore no.**
🟢 **Riparazione fatta, e non e' un commento generico**: banner in testa a `GENERA.py` con i cinque numeri, il nome del file orfano, la ragione, la procedura per usarlo senza danni (`git status --porcelain backtest_pipeline/prove/` **deve nominare solo il file voluto**) e la direzione giusta della riparazione futura.
🟢 **E la mia modifica al generatore non ha sfiorato gli altri sei**: ho aggiunto **due parametri OPZIONALI** (`testa`, `dirs`, default `None`) invece di parametrizzare `TESTA`, che e' condivisa da sette file. Verificato: il generatore riproduce il file prova corretto **al byte** (`14551 == 14551`, `True`), e `git status` nomina **solo** `02` e `GENERA.py`.

---

# 6️⃣ 📖 L'ATTESA DICHIARATA **PRIMA** — cosa si legge, e cosa vuol dire

🔴 **Vale per la notte in cui il round girera' (NON stanotte).** La scrivo adesso perche' un'attesa scritta dopo i numeri non e' un'attesa.

## Prima di tutto: **il controllo di appoggio, gratis e obbligatorio**
Nel log del round devono comparire **tutte e tre**:
```
taglio IS/OOS preso da '@FRAZIONEIS' nel file prova: 0.002
IS  2024.09.26 - 2024.09.26   (la gamba che si butta)
OOS 2024.09.27 - 2025.06.09   (qui si misura)
```
Se manca la prima riga, **nessuna delle tre uscite qui sotto vale**: la direttiva e' stata ignorata e il round ha girato a **0,40**.

## E poi la sentinella, sul CSV **`_OOS`** (si legge al contrario del solito)
`ABTG_EMA200_U30USD_OOS_cemad02.csv` deve dare **237 deal (232-242) · PF 1,20110 (+-0,05) · DD 5,7325%**.
Il CSV `_IS_cemad02` e' atteso **vuoto o assente**, e il referto stampera' **`CSV di QUESTA corsa copiati: 1 (attesi 2)`**: quel **1** e' la spia che la gamba morta e' morta davvero.

## 🔔 LE TRE USCITE, e la terza **non e' una risposta**

| | cosa si legge in `CODA_12` | verdetto | cosa si fa |
|---|---|---|---|
| 🟢 **A — SOPRA 150** | `abtg_trades_ABTG_EMA200_U30USD_766620.csv` → **POSIZIONI >= 150**, e `766621` **identico** (`GEMELLI COERENTI`) | **L'Emendamento A e' soddisfatto su ENTRAMBE le parti**: OOS 257 ✅ + IS >= 150 ✅. Il **requisito 2 del CERTIFICATO si chiude** e `EMA200` va a **5 su 5** (con `R136a/b/c/d` per il requisito 3) | Si scrive nel `REGISTRO_TEST.md` e si porta il pacchetto a Claudio per la firma dello schieramento |
| 🟠 **B — SOTTO 150** | **POSIZIONI < 150**, gemelli coerenti | Il requisito 2 e' **MISURATO** (non piu' `[NON MISURATO]`), e dice che **questo split** non regge l'Emendamento A. 🔴 **E la strada NON e' ammorbidire**: l'Emendamento A del 16/08 dice **esplicitamente** che *"DOVE collocarla NON e' deciso"*. Si **sposta lo split** (60/40 invece di 40/60, con l'IS che diventa la finestra lunga) e si rimisura — adesso c'e' il canale per farlo | Una riga in coda con `@FRAZIONEIS 0.60`, 4 passate |
| ⚠️ **C — CATENA ROTTA** | manca la riga `preso da '@FRAZIONEIS'` · oppure la sentinella e' fuori tolleranza · oppure **`GEMELLI DIVERGENTI`** · oppure `SOLA INTESTAZIONE` · oppure `NESSUN per-trade in questa cartella` | 🔴 **NE' A NE' B. Non e' una risposta**: la domanda resta aperta e si ripara il guasto, che `CODA_12` nomina da solo (quale dei cinque). `GEMELLI DIVERGENTI` in particolare vuol dire **banco non deterministico**, e allora **non si legge nessun numero di quella notte** | Si ripara e si rilancia. **Non si arrotonda** |

🔑 **E LA MISURA DISCRIMINA DAVVERO, che e' la condizione perche' sia una misura.** Banda **102-165**, soglia **150 DENTRO**. Il rapporto che servirebbe per 150 e' `237/150 = 1,58`, e **1,58 sta DENTRO** l'intervallo ammesso dalla decomposizione (pavimento **1,4397**, tetto misurato **2,3143**): verificato, `True`. 👉 **L'esito NON e' ovvio in partenza.** La prima stesura del file prova, con la banda 102-118, lo rendeva ovvio — ed e' il difetto 4-5 del §2.b.

---

# 7️⃣ 🔴 COSA RESTA `[NON MISURATO]` — detto per intero

1. 🔴 **`[NON MISURATO]` IL NUMERO.** Le posizioni dell'IS restano `[NON MISURATO]`, e **stanotte non si misurano**. Il requisito 2 del CERTIFICATO resta **PARZIALE**, ed `EMA200` resta a **4 su 5 + 1 parziale** (il requisito 3 lo chiudono `R136a/b/c/d`, che girano stanotte e **non li ho toccati**).
2. 🔴 **`[NON MISURATO]` IL SECONDO STRATO DEL CANCELLO.** La regola del 09/09 vuole **due** strati. Il deterministico e' passato su tutto (`EXIT 0`, lanciati **da soli, senza pipe**, output **letto**, commit in un comando **separato**). L'agente **`controllo-preventivo`** 🔴 **non e' stato invocato: in questa sessione non ho uno strumento per lanciare un sottoagente.** Lo lancia chi coordina, su questa consegna. 🟢 **E stavolta NIENTE E' USCITO verso il VPS**: `CODA.txt` e' intatta, quindi il PASS del secondo strato e' la condizione per **mettere** le righe, non per **togliere** qualcosa.
3. 🔴 **`[NON MISURATO]` `CODA_12` sotto Windows PowerShell 5.1.** Qui gira `pwsh 7.4.6`; il VPS ha la **5.1**. E' **ASCII puro verificato** (il difetto del 17/08 non la tocca) e il cancello dice *"nessun costrutto pwsh-7-only"* + *"formati .NET"* + *"compila: 0 errori dal parser PowerShell vero"*, ma **girata sulla 5.1 non e' stata**. I costrutti usati (`StreamReader`, `HashSet[string]`, `Get-ChildItem`, `Split`, `PadRight`) sono tutti di .NET Framework 4, e la 5.1 li ha — **ma "ce li ha" non e' "l'ho vista girare"**.
4. 🔴 **`[NON MISURATO]` cosa fa MT5 con la gamba da UN GIORNO** (`2024.09.26 -> 2024.09.26`). Gira due passate a vuoto, oppure nessuna? **Non lo so, e non serve saperlo**: qualunque cosa faccia, gira **PRIMA**, quindi e' il suo per-trade che viene sovrascritto. 🟢 Questo e' il motivo per cui ho scelto `0.002` e **non** `1.0`.
5. 🔴 **`[NON MISURATO]` la riga `Spread`** (§2.f). Ereditato da tutti i 19 round in coda, non introdotto da me. Il rilevatore e' `r136a`, che gira stanotte.
6. ⚠️ **`[NON MISURATO]` che `@FRAZIONEIS` sia onorata sulla catena vera.** Risponde **il canarino, stanotte** (`-Etichetta canfrz`). 🟢 **E questa volta la dipendenza e' un REGALO, non un rischio**: siccome il mio round **non puo' girare stanotte**, girera' **dopo** che il canarino ha risposto. Se l'uscita e' **B** (direttiva ignorata), la mia riga **non si mette affatto**.
7. ⚠️ **Il margine di +-1 posizione** dal riscaldamento dell'EMA200 sul giorno 2024.09.26 (§2.c). Dichiarato, quantificato, e sotto la risoluzione della domanda.
8. ⚠️ **DUE CLASSI NUOVE per `CHECKLIST_RIGA_DI_LANCIO.md`** (non le scrivo io: quel file e' in mano a un altro agente). Il testo e' pronto:
   > **276 — «La corsia ROUND del runner non ha nessun canale per il per-trade, e un file prova che lo chiede come deliverable muore in silenzio.»** Misurato il 12/09/2026: `abtg_trades` compare **0** volte in `runner_abtg.ps1`, `RIGA_SOTTILE_ROUND.ps1` e `RIGA_ROUND_VPS.ps1` (che **salta** `Common` di proposito, r.548) e **2** volte in `walkforward_generico.ps1`, dove sono **due `Write-Host` di consiglio**. `RIGA_ROUND_VPS` raccoglie **solo** `$csvIS`, `$csvOOS`, il referto e il file prova. Contro-esempio: **36 script su 98** in `righe/RIGA_*.ps1` il per-trade lo raccolgono, quindi il token esiste e il grep discrimina. 🔴 **Il guasto e' invisibile**: il round esce verde, lo zip ha due CSV, e l'unico conteggio dentro e' `Trades` = **deal di uscita** (classe 226) — un numero *plausibile* letto come "operazioni". Rimedio: una riga di **SOLA LETTURA** in coda dopo il round (`righe/CODA_12_pertrade_posizioni.ps1`), che non tocca nessuno script della catena pinnata.
   >
   > **277 — «`prove/COLLAUDO_EMADOW_GENERA.py` e' vecchio: rilanciarlo riporta indietro cinque file prova, uno dei quali e' in coda.»** Misurato il 12/09/2026 girandolo in una cartella di prova: **104 righe** diverse su `01`/`03`/`04`/`05`/`06` e un **file orfano** (`00_canarino_spread.txt` contro il `00_manopola_maxspread.txt` che sta in repo). Il **05** e' in `CODA.txt` (`cemad05`) e le sue 2 righe diverse sono **la riga del costo**. Causa: il commit `92c9621` ha corretto i `.txt` **a mano** senza toccare il generatore. 🔑 **Regola: quando un generatore e la sua uscita divergono, la verita' e' l'USCITA** — e chi tocca l'uno deve misurare l'altro, subito, con `git status --porcelain` sulla cartella.

---

# 8️⃣ 🎯 DOVE SIAMO, DA SOCIO

🟢 **Buona notizia prima, che e' anche la piu' importante: stanotte i 19 round e il canarino partono INTATTI.** `CODA.txt` ha lo **stesso sha256** di stamattina, 19/19 a `1445abf8`, canarino ancora ultimo. Nessun round e' stato messo a rischio per fare posto al mio — e visto che `R136a/b/c/d` chiudono il **requisito 3** di `EMA200`, domattina la sedia guadagna comunque un requisito.
🟢 **Seconda buona notizia: il buco che ha fermato la mia riga era li' da prima, e adesso ha un nome, un numero e una toppa collaudata.** Un round della coda che chiede il per-trade come deliverable sarebbe morto in silenzio — **oggi, e ogni volta**. Ora c'e' `CODA_12`, che passa i cancelli veri del runner e sa dire *"gemelli divergenti"* invece di dire *"verde"*.
🟢 **Terza: il file prova adesso misura quello che dice di misurare.** Aveva **un giorno di finestra sbagliato**, **nessun canale per il taglio**, **meta' del costo**, e un **contro-esempio aritmeticamente falso** che dichiarava l'esito prima di misurarlo. Quattro numeri su cinque erano sbagliati **a favore della conclusione che ci aspettavamo** — e quella e' la firma della conferma, non della verifica.
🔴 **E la notizia scomoda, detta senza addolcirla: oggi il numero non arriva.** L'IS in posizioni resta `[NON MISURATO]` e `EMA200` resta a **4 su 5 + 1 parziale**. Non e' una giornata di misura: e' **ponteggio**, e va dichiarato come tale. Ma e' ponteggio che costa **una notte** e che impedisce a un numero falso di entrare nel fascicolo dell'**unica delle 41 sedie** che passa i cancelli alla lettera. A tre settimane da ottobre, un numero falso su quella sedia costa molto piu' di una notte.

**Il prossimo passo e' corto e tutto misurato**: PASS del secondo strato → **un** valore di `$PIN` → **due righe** appese in fondo a `CODA.txt`. E la notte dopo, la domanda che l'Emendamento A fa da agosto ha un numero accanto. 💪
