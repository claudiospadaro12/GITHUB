# 🌙 REPORT DELLA NOTTE 19→20/09/2026 — la notte prima della challenge

> **23 commit · 7 agenti rientrati · 10 classi di difetto nuove (470-480) · zero numeri di mercato.**
>
> 🧭 **BUSSOLA, dichiarata subito**: stanotte **non è nata nessuna sedia nuova**. È stato
> costruito il **ponteggio per schierare quelle che abbiamo** su un conto che ancora non
> esiste. Con la challenge che si compra oggi, è la cosa che valeva di più — ma resta
> ponteggio, e va detto così.

---

## 🌙 COSA HA FATTO IL RUNNER

📌 **Il referto delle 03:30 del 20/09 non è ancora pubblicato, e NON è un guasto.**
Quelli del 19/09 (stessa corsa delle 03:30) sono stati committati alle **14:42** — undici ore
dopo. La pubblicazione è differita per costruzione.

🔎 **Cosa dovrà dire quando arriva** (è la verifica della toppa per ticket applicata ieri):
`CODA_06` deve stampare **2091 / 2191 / 2123** al posto di 2033 / 2133 / 2065.
⚠️ Col righello di `CODA_06`, che conta **uno in più** di `wc -l` (classe 456). Chi cercasse
2090/2190/2122 concluderebbe che la toppa non è entrata.

---

## 🔬 COSA È STATO DECISO E CORRETTO, con il numero accanto

### 🩹 1. La toppa per ticket è entrata a HEAD — era **il** prerequisito delle sette sedie
La correzione applicata a mano sul piccolo il 19/09 **esisteva solo in una cartella di
appoggio** e non era mai entrata nei sorgenti principali. Misurato: a HEAD i tre Apertura
avevano ancora **2 `PositionClose(_Symbol)` ciascuno**.
- Morde perché la rosa mette **due sedie su D30EUR e tre su U30USD**: su HEDGING quella
  chiamata chiude **la posizione più vecchia del simbolo di chiunque**.
- 🔴 E la guardia che c'era **peggiorava le cose**: `SelectMyPosition()` è già hedge-safe,
  quindi l'EA **trovava la propria posizione, decideva di chiuderla e chiudeva quella del
  vicino**. Al tick dopo, senza guardia, chiudeva anche la sua. **Due morti, la prima di
  un'altra sedia.**
- ✅ Verificato prima del commit: **0 occorrenze vive** nei tre file, graffe 236/236 ·
  230/230 · 264/264. 🟢 Effetto collaterale: **il Nasdaq si è sbloccato.**

### 🕐 2. L'orologio FTMO è diverso di DUE ORE — e ogni preset era sbagliato
`docs/REGOLAMENTO_FTMO_2026-08.md` r.130: *«FTMO server = ora italiana +1 … **da RIMAPPARE
in tutti gli .ini!**»*. E BCM = ora italiana −1 → **FTMO = BCM + 2**.
- **10 preset rimappati**, 27 valori spostati, **diff automatico 10/10 OK**, zero differenze
  fuori elenco, nessun originale toccato.
- 🎯 **Controllo incrociato**: due agenti, due strade indipendenti, **stesso numero**
  (`770101`: BCM 8/17 → FTMO 10/19).
- 🛑 Due casi in cui l'agente **si è fermato invece di sommare**: il box notturno che
  scavalla la mezzanotte, e `770511` dove `0-24` **non è un orario** (+2 darebbe «26»).

### 📄 3. Il `.set` di `770260` non esisteva — buco bloccante, chiuso
Claudio l'ha firmata dentro ieri sera; `grep` su tutti i `.set` = **zero file**.
🪦 **Non era sparito: non è MAI stato committato** (classe 475). Ricostruito dal CSV grezzo
del round (`Pass=8`: PF OOS **1,10936** · n 94 · DD **3,6753%**).
🔴 E una scoperta di contorno: il binario del round aveva **78 input**, HEAD ne ha **98**.
Un `.set` vecchio avrebbe lasciato **18 parametri al default senza che nessuno l'avesse
deciso**. Ora 98 su 98, e i 18 nuovi verificati **inerti** leggendo il gating riga per riga.

### 🧨 4. La collisione ECB/FOMC: la FOMC non avrebbe operato MAI
`ABTG_PostNews_ECB_EURUSD.set` portava `InpMagic=771202`, **che è della FOMC** — stesso
simbolo, stesso EA. Meccanismo letto nel sorgente, non ipotizzato: la scadenza **non è un
istante, è una finestra** — la ECB chiama `CloseAllMine()` **a ogni tick dalle 17:15 a
mezzanotte**, e la FOMC piazza alle **19:40**, dentro quella finestra.
👉 **I suoi pendenti cancellati entro un tick, per sempre**, e nel log solo una riga innocua.
Magic nuovo **771204** (libero, verificato). 🔴 **IN ATTESA DI FIRMA**: `DA_FIRMARE.md`
r.152-153 riserva la modifica a Claudio. File inerte, nessuna sedia viva toccata.

### 🙈 5. La sedia NFP è cieca per costruzione
`ABTG_PostNews_NFP_USDJPY.set` legge `abtg_news_postnews_2010_2025_UTC.csv`:
**600 righe, ultimo evento 2025.07.03, eventi 2026 = ZERO**. E `aggiorna_news.ps1` aggiorna
**solo** `abtg_news.csv`: quel file non lo tocca nessuno. → **zero ordini, per sempre.**
🔴 E il canarino d'allarme **non scatta**: conta gli eventi *utili* (186, tutti passati), non
quelli *futuri* — all'avvio stamperebbe una riga tranquillizzante su una sedia morta.
🚫 Non corretto: **cambiare quale calendario legge una sedia è una decisione, non un refuso.**

### 📉 6. Il cancello di costo dei PostNews NON passa
`771202` **28,9×** · `771203` **26,8×** · `771201` **22,0×**, contro i **40×** di casa. E sono
numeri su spread di **calma** (0,400 pip), mentre queste sedie operano 10-45 minuti dopo un
annuncio ad alto impatto, con `InpMaxSpread=0` = **filtro spread disattivato**.

### 🔢 7. TRE TAGLIE DIVERSE nella flotta — ed è la firma che blocca tutto
Verificato preset per preset:
`0,65%` ×4 (DAX, Dow, EMA200, MaxMin DAX) · `1,00%` ×3 (SuperWave, Nasdaq, ORO) ·
`1,30%` ×3 (PostNews).
Nasce da un'**asimmetria**: le quattro sedie che avevano una variante `_100K` sono passate a
0,65%, le altre no.

| flotta delle sei sedie, leva 1:15, conto 100.000 $ | margine | % conto |
|---|---:|---:|
| tutte a **0,65%** | 116.210 $ | 116,2% |
| 🔴 **com'è adesso (mista)** | **140.576 $** | 🔴 **140,6%** |

🔴 **`770511` da sola a 1,00% mangia il 46% del conto.** A 0,65% ne mangerebbe il 30.
⚠️ Cifre `[INFERITO]`: **nessuna specifica di contratto FTMO è mai stata letta.**

### 🕵️ 8. Due correzioni che gli agenti hanno fatto a ME
- **Lo stop-out per margine non è il rischio n.1.** Scatta a ≈ −54% di conto: il muro statico
  del 10% breccia molto prima. 🔴 **Il danno vero è l'ORDINE RIFIUTATO** (`not enough money`),
  **silenzioso**, e colpisce sempre le americane perché le europee il margine se lo sono già
  preso alle 07:00. *Si crede di correre con sette sedie e si corre con tre.*
- **La mia lettura della collisione Nasdaq non reggeva** (citavo la sezione sbagliata del
  referto). 🟢 La conclusione tiene per un'altra ragione: `770250` **non va sul conto prop**.

### 🔬 9. Il piano di misura che avevo dato io **non era eseguibile** → classe 470
Avevo detto: *«confronta la colonna `volume` del file per-trade fra vecchio e nuovo»*.
```
ABTG_EMA200 @ 344a11b9 (il binario IN CAMPO, 04/08)  ->  ExportTrades = 0
ExportTrades nasce il 08/08, QUATTRO GIORNI DOPO
```
🔴 **Due gambe su quattro non potevano produrre il dato**, e l'avremmo scoperto dopo aver
acceso il banco. 👉 *La diagnostica è quasi sempre più giovane del codice che deve diagnosticare.*
✅ Ridisegnata: si prende il ramo **nuovo** (che la diagnostica ce l'ha) e gli si mette un
interruttore che riproduce il calcolo **vecchio**. Il `.ex5` è **lo stesso** nelle due gambe →
*«a parità di segnale»* diventa un **fatto strutturale**, non una speranza.

### 🗓️ 10. La trappola della domenica → classe 479
La colonna **«Ora»** di Market Watch **non è un orologio**: a mercato chiuso è il **timestamp
dell'ultimo tick**, cioè venerdì sera — e le 23:00 sembrano plausibili.
🔴 **Su quel numero stavamo per rigenerare dieci preset.**
✅ Chiuso con `TimeTradeServer()`, che il terminale **calcola** e vale a mercato fermo.
Verificato nel sorgente: r.329 `TimeTradeServer()`, r.330 `TimeCurrent()` scritto ma
**etichettato come ultimo tick**.

---

## 🛠️ GLI STRUMENTI PRONTI PER STASERA

| strumento | cosa fa | stato |
|---|---|---|
| `SCHIERA_FTMO.ps1` (v2) | **scopre** il terminale FTMO e installa 9 EA + 10 preset | ✅ **13/13 contro-esempi**, e in **ogni** rifiuto **zero file nel terminale** |
| `PREVOLO_FTMO.ps1` | sonda di pre-volo, sola lettura | ✅ 10 contro-esempi |
| `ABTG_PrevoloFTMO_Specifiche.mq5` | 1 F7 → chiude **4 caselle e mezza** su 6 | 🔴 mai compilato |
| `MISURA_LOTTI_U30USD.ps1` | il delta lotti di `771531`/`770511` | ✅ 11 contro-esempi, 20-60 min |
| `analisi_griglia_h4.py` | il bias H4 BCM contro FTMO | ✅ **7/7** su dati sintetici |
| `mql5/Presets/FTMO/` | 10 preset rimappati | ✅ diff 10/10 |

⏱️ **Catena di accensione: 62-105 minuti.** Ci sta in una sera.
🔴 **Domenica si schierano SEI sedie + Guardian, non sette**: `770402` ORO resta fuori
(serve una corsa di controllo). *Un preset senza il suo EA è una trappola.*

### 🧩 E un F7 che sarebbe morto al primo colpo
Il pacchetto prescriveva `ABTG_Guardian.mq5` a HEAD con l'include **v1.20**. Verificato:
```
Guardian HEAD chiama          definita in v1.20
  ABTG_ClusterParse_Calc              0
  ABTG_ClusterGVRadice                0
  ABTG_SimboloNelCluster_Calc         0
```
✅ Guardian pinnato a **`d884f7e1` (v1.12)**: compila con la v1.20 provata **e** porta il fix
del 06/09 — baseline giornaliera dall'**EQUITÀ**, non dal bilancio. 🔴 Su FTMO è *quel* numero
che boccia, e la versione in campo (v1.10) **non ce l'ha**.

---

## 🔴 IL RISCHIO CHE RESTA APERTO SU UNA SEDIA CHE VA IN CAMPO STASERA

`770202` Dow decide il bias con una **EMA su H4** (`InpUseEmaFilter=true`, `InpFilterTF=16388`).
Verificato nel sorgente: `CopyBuffer(..., 1, 1, ...)` legge **l'ultima barra CHIUSA**.
🔴 Le barre H4 dei due broker **non cadono negli stessi istanti**: all'ingresso,
**BCM guarda la chiusura delle 11:00 UTC, FTMO quella delle 13:00 UTC**.
👉 E su una sedia **solo-long** un bias opposto non peggiora l'ingresso: **lo CANCELLA.**
Il Dow potrebbe semplicemente **non sparare mai** su FTMO.

🔑 **Si chiude con tre minuti di Claudio**: una barra H4 è l'unione di 4 barre H1, quindi due
griglie sfalsate sono **due raggruppamenti della stessa serie H1**. Serve **una esportazione
H1 del Dow dal banco 50504400**, e il resto è offline.
📐 Soglie già scritte: **< 3%** il problema non esiste · **3-25%** si schiera dichiarando lo
scarto · **> 25%** su FTMO **non è la stessa sedia**.

---

## ⚠️ COSA ASPETTA CLAUDIO

### 🟢 Sul conto reale 10105439: **NIENTE.** Non è stato toccato e non serve che faccia nulla.

**Solo suo — rischio e soldi:**
1. 🔴 **LA TAGLIA.** O tutto a **0,65%** (e il cap C1 = 5 × 0,65 torna coerente), o la flotta
   mista **sapendo** che costa **140,6%** di margine a 1:15. **È la firma che sblocca l'AutoTrading.**
2. 📸 **Standard o Swing, e la leva vera.** Standard = **1:100**, Swing = **indici 1:15**.
   Con 1:15 entrano **tre** sedie su sette a 0,65%; con 1:100 entrano tutte col 36% del conto.
   🟢 E in **valutazione non si applica nessuna restrizione**, per nessun tipo di conto.
3. ✍️ **Il magic `771204`** della ECB (il file è pronto, la modifica è riservata a lui).
4. ✍️ **`InpMaxSpread=0` su `770411`**: filtro spread **spento** su un broker di cui non
   conosciamo gli spread. Firmare o misurare.
5. ✍️ **La misura del delta lotti** (già firmata ieri): la riga è pronta, 20-60 minuti.

---

## 🎯 LA CODA DI OGGI

1. 🥇 **La sonda di pre-volo + il lettore delle specifiche**, appena il terminale FTMO è
   installato. **Prima di qualunque grafico.** Chiude orologio, simboli, margine, Digits,
   spread, leva.
2. 🥈 **L'export H1 del Dow** (3 minuti) → il numero del bias H4 prima di accendere `770202`.
3. 🥉 **La misura del delta lotti** sul banco → sblocca l'F7 di `771531` e `770511`.
4. 📋 Poi la checklist dello schieramento, in ordine, col Guardian **per primo** — così un
   errore d'include si scopre al primo F7 e non al nono.

---

## 🙋 GLI ERRORI DELLA NOTTE, per nome

- 🔴 **Il mio piano di misura non era eseguibile** (classe 470): confronto su una colonna che
  il binario vecchio non ha mai scritto.
- 🔴 **Ho dato il file sbagliato in un mandato**: `ABTG_SuperWave.mq5` (= sedia 770531) invece
  di `ABTG_SuperWave_DOW_H1_Ottimizzato.mq5` (= 770511). Preso dal controllo.
- 🔴 **Ho autorizzato un agente a cambiare un magic** che `DA_FIRMARE.md` riservava a Claudio.
  Il file è inerte e la modifica è dichiarata in attesa di firma, ma **ha ragione il repo**.
- 🔴 **Due volte** due agenti hanno grepato lo **stesso numero di classe nello stesso minuto**
  (470/471 e 474). Risolto da entrambe le parti, **nessun doppione in checklist** —
  ma *«cerca il numero col grep»* **non basta** in parallelo: va riletto **prima del push**.
- 🔴 Un agente ha trovato un difetto **nel proprio collaudo** (classe 480): i numeri di testa
  erano giusti, ma calcolati su **427 sedute invece di 60** — giudicava anche il riscaldamento,
  dove `1000·k + 1000·(1−k)` fa `1000.0000000000001` e il bias usciva **−1 sistematico**.
- 🔴 Un agente ha trovato che **la sua stessa riga avrebbe PRESCRITTO l'errore di fuso** che
  era nata per prevenire (classe 477): coi preset già rimappati, la tabella avrebbe stampato
  `BCM 10 → FTMO 12`. 👉 *I contro-esempi verificavano la COPIA, non il REFERTO.*
