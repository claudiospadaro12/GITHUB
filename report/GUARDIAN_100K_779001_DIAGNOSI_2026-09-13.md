# 🛡️ DIAGNOSI — `ABTG_Guardian` `779001` sul 100k `50504263`

> **13/09/2026 · SOLA LETTURA.** Nessun EA toccato, nessun preset scritto, nessuna
> ricompilazione, nessun `CODA.txt`, nessun backtest. La macchina di backtest è
> ferma (`report/LA_CODA_NON_HA_PUBBLICATO_2026-09-13.md`) e **questo lavoro non
> la usa**.
>
> 🔴 **Zero saldi, zero equità, zero P/L** del 100k `50504263` e del reale
> `10105439`. Qui ci sono solo **parametri** (già pubblici in `mql5/Presets/`),
> **orari**, **conteggi di operazioni** e **righe di codice**.
>
> 🚫 **Questa NON è una proposta di schieramento.** È la diagnosi che Claudio
> chiede di vedere prima. Il pacchetto, se servirà, è un secondo passo con le
> sue quattro passate di cancello.

---

## 🎯 IN UNA RIGA

Il Guardian del conto della challenge gira il codice del **19/08** e gli manca il
**fix critico della baseline** del 06/09. 🟢 **Ma metà del difetto è spenta da un
input del preset, e l'altra metà è misurata mordere su 1 giornata su 25** — quindi
l'urgenza **scende**, e non di poco. 🔴 **Resta però un buco che da qui non si può
chiudere**, ed è l'unico motivo per cui questa sedia non è archiviabile stasera.

---

# A. 📌 DATAZIONE ESATTA DEL BINARIO IN CAMPO

**Il pin è `1f4c92b` del 2026-08-19, ed è UNIVOCO** `[MISURATO]`.

| fonte | dato |
|---|---|
| cartella | `C:\Program Files\BCM Markets MT5 Terminal -V3` (**100k `50504263`**) |
| sorgente in cartella | `ABTG_Guardian.mq5` **v1.11**, **468 righe**, `.ex5` compilato **2026-08-19 23:10** — `CODA_06_quale_codice_gira_20260910_033002.log` **r.257** |
| grafico | `chart07.chr`, AUDNZD, profilo `SQUADRA 100K`, `.chr` del 2026-09-06 08:01 — `CODA_08_preset_dai_chr_20260912_033002.log` **r.3696** |

Il runner conta `$t -split "\r?\n"` = `wc -l` **+1**
(`backtest_pipeline/righe/CODA_06_quale_codice_gira.ps1` r.110). Applicando il `+1`:

```
1f4c92b  19/08  wc=467  runner=468  v1.11   <-- IL BINARIO IN CAMPO  ✅ unico a combaciare
d884f7e  06/09  wc=498  runner=499  v1.12
1b6a095  06/09  wc=512  runner=513  v1.12
cdb2037  07/09  wc=705  runner=706  v1.13
a21d0c0  08/09  wc=899  runner=900  v1.14   <-- HEAD
a53820e  18/08  wc=413  runner=414  v1.10   (è la copia FERMA sul piccolo, non una sedia)
```

🟢 **Doppia conferma, indipendente**: `1f4c92b` è datato **2026-08-19 07:45 UTC** =
09:45 ora VPS, e l'`.ex5` è delle **23:10** dello stesso giorno. La compilazione
**segue** il commit di 13 ore. Nessuna altra versione ha 468 righe.

**Commit mancanti: 4.**

---

# B. 🗂️ CLASSIFICAZIONE DEI 4 COMMIT MANCANTI — letti dal diff, non dal messaggio

| # | commit | data | classe | 🔴 morde al **default**? | 🔴 morde su **QUESTA sedia**, col SUO preset? |
|---|---|---|---|---|---|
| 1 | `d884f7e` | 06/09 | 🔴 **SEGNALE** | 🔴 **SÌ** | 🟠 **A METÀ** — vedi §C.1 |
| 2 | `1b6a095` | 06/09 | 🟡 **DIAGNOSTICA** | no | ❌ no |
| 3 | `cdb2037` | 07/09 | 🔴 SEGNALE (opt-in) | no — `=0` / `""` | ❌ no |
| 4 | `a21d0c0` | 08/09 | 🔴 SEGNALE (opt-in) | no — `=0` | ❌ no |

## 1️⃣ `d884f7e` — «baseline dall'EQUITÀ, non dal bilancio». **Verificato riga per riga**

Il commit tocca **esattamente tre punti di calcolo** e un blocco di nomi. Li elenco
tutti, con la riga nel **binario in campo** (`git show 1f4c92b:mql5/Experts/ABTG_Guardian.mq5`):

| # | riga in campo | prima (v1.11, **quello che gira**) | dopo (v1.12) |
|---|---|---|---|
| **P1** | **r.264** | `gStart = (...check? get : bal)` | `... : eq` |
| **P2** | **r.276** (`OnInit`) | `GlobalVariableSet(GV_DAYSTART, bal)` | `..., eq` |
| **P3** | **r.347** (`OnTimer`) | `GlobalVariableSet(GV_DAYSTART, bal)` | `..., eq` |
| **P4** | r.242-246 | `ABTG_GUARD_<acc>_START` … | stessi nomi **+ suffisso `_V2`** (forza una ricattura pulita) |

**E l'asimmetria che è il bug**, sempre nel binario in campo:
```
r.357   double dayStart = GlobalVariableGet(GV_DAYSTART);   // scritto col BILANCIO
r.366   double dailyLoss = dayStart - eq;                   // confrontato con l'EQUITÀ
r.367   double totalDD   = (InpDDMode==1)? (gPeak-eq) : (gStart-eq);
```
👉 **`dailyLoss` mette a confronto due grandezze diverse.** L'errore è, esattamente:

> **errore_baseline = (equità − bilancio) all'istante del reset = CREDITO + flottante aperto**

## 2️⃣ `1b6a095` — 🟡 DIAGNOSTICA, e nient'altro

`git diff d884f7e 1b6a095 -- mql5/Experts/ABTG_Guardian.mq5`: **una sola `PrintFormat`**
inserita dopo `gStart` (+14 righe, di cui 11 di commento). Nessuna condizione, nessuna
soglia, nessun `GlobalVariableSet`. Il commento dichiara anche il secondo scopo: tiene
**viva** la variabile `bal`, che dopo il fix non avrebbe più lettori in `OnInit` — e una
variabile non usata è un warning che la riga di deploy tratta come bloccante.
🔎 **Ironia da segnare**: quella riga stampa `equity`, `bilancio` e la **differenza**, cioè
**esattamente il numero che serve per sapere se il difetto morde**. È l'unica versione che
sa dircelo, ed è quella che non è in campo.

## 3️⃣ `cdb2037` — cap C2 per cluster. **Inerte**

`InpMaxClusterRiskPct = 0` (`mql5/Experts/ABTG_Guardian.mq5:165`) e `InpClusterMappa = ""`
(r.166). Il parser gira **solo** dentro `if(InpMaxClusterRiskPct>0 && StringLen(InpClusterMappa)>0)`.
Col preset di questa sedia (§C) i due valori **non esistono nemmeno come manopola**.
📌 E resta valido l'emendamento del 12/09 in `CLAUDE.md`: **il C2 non serve adesso** (il
`C1` a 3,25% è già acceso e più stringente del cluster `AZIONARIO` proposto a 3,5%).

## 4️⃣ `a21d0c0` — modo dichiarato della baseline. **Inerte**

`InpDailyBaseline = 0` (`mql5/Experts/ABTG_Guardian.mq5:147`) e
`BaselineGiorno_Calc(bal,eq,modo)` (r.235) restituisce `eq` per `modo==0` **e per
qualunque valore fuori scala**. A default è la v1.13 esatta.

---

# C. 🧪 IL CONTRO-ESEMPIO — costruito PRIMA del verdetto, e **ha retto due volte su tre**

> Regola di casa del 10/09: *"devo costruire IO il contro-esempio che lo farebbe
> sbagliare, e far vedere che non sbaglia"*. Qui la tesi da rompere è
> **«il Guardian del 100k va aggiornato SUBITO»**.

## 🟢 C.1 — PRIMO CONTRO-ESEMPIO: **`InpStartBalance` spegne metà del fix.** ✅ REGGE

Il preset in campo porta `InpStartBalance=100000` **esplicito**
`[MISURATO]` — `CODA_08_preset_dai_chr_20260912_033002.log` r.3702, ricopiato in
`mql5/Presets/ABTG_Guardian_50504263_779001_VIVO.set`.

E il codice in campo, r.263-264:
```
if(InpStartBalance>0) gStart=InpStartBalance;
else                  gStart=(GlobalVariableCheck(GV_START)? GlobalVariableGet(GV_START) : bal);
```
👉 **Il ramo `else` non viene MAI eseguito su questa sedia.** Il punto **P1** del fix
è **codice morto** qui: `gStart` vale 100000 e basta.

### 🟢 E la conseguenza è più grossa di quanto sembri: **il fermo totale è INTATTO**
```
r.367   totalDD = (InpDDMode==1)? (gPeak-eq) : (gStart-eq)    // InpDDMode=0 -> statico
r.363   totalLimit = InpTotalDDPct/100 * gStart               // 9,9% di 100000
```
`gStart` è corretto per input, `eq` è l'equità vera: **il DD totale al 9,9% — la rete
che ferma la challenge — misura esattamente quello che deve misurare, oggi, in campo.**
🎉 Il paracadute grosso non è bucato. Questa è la notizia buona della serata.

**Cosa NON copre il contro-esempio**: i punti **P2** e **P3** (`GV_DAYSTART`) **non**
sono protetti da nessun input. La baseline **giornaliera** è ancora presa dal bilancio.
Quindi restano degradate **due sole** cose: la **pausa morbida B1 a 4,0%** e
l'**emergenza giornaliera a 4,9%**.

## 🟢 C.2 — SECONDO CONTRO-ESEMPIO: **la finestra in cui il flottante può sporcare la baseline è quasi sempre vuota.** ✅ REGGE, con un numero

La baseline si scrive **una volta al giorno**, allo scoccare di `InpDailyResetHour=23`
(ora **server**) — `PropDayKey()` r.85-91, scrittura r.347. Su un riavvio **dentro** la
stessa giornata prop la riga `if((int)GlobalVariableGet(GV_DAYKEY)!=pk)` è falsa e
**la baseline NON viene riscritta**. Quindi conta **solo** cosa è aperto alle **23:00 server**.

🟢 **E il reset è all'ora giusta**: server BCM = ora italiana − 1, quindi
**23:00 server = 00:00 CE(S)T**, che è l'istante di FTMO. L'**ORA** non è in discussione:
è in discussione solo la **GRANDEZZA**.

**Misura, su operazioni vere del 100k** (`data/statements/trades_100k.csv`, finestra
**10/08 → 11/09/2026**, 29 operazioni, 18 giornate con almeno un'apertura):

| | |
|---|---:|
| operazioni totali | **29** |
| **posizioni aperte a cavallo delle 23:00 server** | 🟢 **1** |
| durata mediana di una posizione | **0h 36m** |
| durata massima | **12h 53m** |

L'unica:
```
pid 3260596  225JPY  magic 770901   apre 27/08 18:00:00  ->  chiude 28/08 06:53:03
```
👉 **1 giornata su 25 coperte.** In tutte le altre, alle 23:00 server **non c'è nessuna
posizione aperta** e — *in assenza di credito* — bilancio ed equità **coincidono**:
il difetto vale **0,00**.

### 📏 E quando morde, di quanto morde? **Si può LIMITARE, e il limite è piccolo**
`dailyPct = 100 · dailyLoss / gStart` (r.369) con `gStart` = 100000 **fisso**. L'errore in
punti percentuali è quindi `errore_baseline / 1000`. Il flottante di quell'unica posizione
è limitato dal suo **SL**, e quella sedia è a contratto **0,65%** (`770901`, censimento
13/09 §5). Quindi:

> **limite superiore dell'errore introdotto dal flottante: ~0,65 punti percentuali,
> su 1 giornata su 25.** Contro un cancello a 4,9: **≤13% della distanza**, un giorno su
> venticinque.

⚠️ Il limite vale perché **la posizione ha uno SL**. `InpWarnNoSL=true` è acceso e il
giornale del 11-12/09 non porta warning di posizioni senza SL
(`CODA_09_giornale_operativo_20260912_033002.log` r.153-154).

## 🔴 C.3 — TERZO CONTRO-ESEMPIO: **«e se sul 100k ci fosse un CREDITO?»** ❌ NON REGGE — ed è l'unico buco vero

Il credito è **costante**: non morde 1 giorno su 25, morde **TUTTI I GIORNI**, e sempre
nel verso **fail-open** (baseline troppo bassa → `dailyLoss` sottostimata → la pausa e
l'emergenza scattano **TARDI**). È il caso per cui `d884f7e` è stato scritto.

🔴 **Da qui NON è misurabile: `[NON MISURATO]`.**
- il giornale stampa **solo** `eq=` (r.461-462 del binario in campo: `PrintFormat` con
  `eq, dailyPct, totalPct, riskPct`) — **bilancio e credito non ci sono**;
- la riga che li stamperebbe affiancati è **`1b6a095`**, cioè **uno dei commit mancanti** (§B.2);
- `data/statements/trades_100k.csv` contiene operazioni, **non lo stato del conto**;
- e la conclusione di `report/DISALLINEAMENTO_CAMPO_v2_2026-09-12.md` r.117-118 resta in
  piedi: *"non posso dire se quel conto abbia un credito… non posso certificare che valga
  zero, e quindi non lo certifico."*

### 🟢 MA SI PUÒ MISURARE IN 30 SECONDI, SENZA TOCCARE NIENTE — e questo è NUOVO
Il binario **in campo** (v1.11) disegna già il pannello sul grafico, r.442:
```
"Saldo iniziale: %.2f\nEquity: %.2f   Balance: %.2f\n--- GIORNO ---\nInizio giorno: %.2f\n"
```
e `InpShowPanel=true` è nel preset (`CODA_08…log` r.3708).

👉 **Sul grafico AUDNZD H1 del terminale `50504263` (`C:\Program Files\BCM Markets MT5
Terminal -V3`), il pannello del Guardian mostra GIÀ `Equity`, `Balance` e `Inizio
giorno`, uno sotto l'altro.** A mercato chiuso (nessuna posizione aperta):

- `Equity − Balance = 0` → **nessun credito** → 🟢 il difetto vale ~zero e questa sedia
  scende a manutenzione ordinaria;
- `Equity − Balance ≠ 0` → **quello è il credito** → 🔴 e allora il difetto morde **ogni
  giorno**, e l'urgenza torna massima.

### 🎯 BERSAGLIO DELL'AZIONE — dichiarato per esteso, come vuole la regola dei terminali
✋ **AZIONE A MANO DENTRO MT5**, sul terminale **`50504263`** (il **100k**), cartella
programma **`C:\Program Files\BCM Markets MT5 Terminal -V3`**, grafico **AUDNZD H1**.
🔴 **NON si tocca**: il piccolo `50503392`, il **REALE `10105439`** (`C:\BCM_Reale`), il
banco `50504400` (`C:\MT5_Backtest`), Pepperstone, Tickmill.
🔒 Zero rischio: **si GUARDA**, non si tocca. Nessun input, nessun riattacco, nessuna
ricompilazione, nessun ordine.

E perché la finestra si riconosca da una **stampa** e non a occhio dal titolo (regola del
06/09), prima si esegue questa riga — 🖥️ **finestra PowerShell sul VPS**, sola lettura,
non pilota nessun terminale:
```powershell
Get-Process terminal64 | Select-Object Id, MainWindowTitle, Path | Format-Table -AutoSize
```
La finestra giusta è quella con `Path` che contiene **`MT5 Terminal -V3`**.

Il valore in euro **non va scritto nel repo**: basta che Claudio dica
**«la differenza è zero»** oppure **«non è zero»**.

---

# D. ⚖️ VERDETTO SU QUESTA SEDIA

| gate | stato col binario in campo |
|---|---|
| 🟢 **DD totale 9,9%** (ferma la challenge, `FlattenAll` r.310-328 con `InpCloseAllMagics=true`) | ✅ **CORRETTO** — `gStart` da input, confronto sull'equità |
| 🟠 **Emergenza giornaliera 4,9%** | ⚠️ baseline dal **bilancio**. Errore ≤ ~0,65 punti da flottante, 1 giorno su 25 · **+ credito `[NON MISURATO]`** |
| 🟠 **Pausa morbida B1 4,0%** | ⚠️ idem |
| 🟢 **Cap C1 3,25%** (`InpMaxOpenRiskPct`) | ✅ non tocca la baseline: `OpenRiskPct(eq,…)` r.410 |
| 🟢 **Canale verso gli EA** | ✅ i nomi `ABTG_PAUSA_GIORNO` / `ABTG_CAP_RISCHIO` / `ABTG_GUARDIAN_BATTITO` sono **identici** fra l'include del 19/08 e HEAD `[MISURATO]`: la flotta a versioni miste **si parla lo stesso**. `d884f7e` rinomina solo le GV **interne** (`_START`, `_DAYSTART`, …), che nessun EA legge |
| 🟢 **Il Guardian è VIVO** | ✅ `CODA_09…log` r.153-154: riga `[GUARDIAN]` presente sul terminale `-V3`. (Sul piccolo `50503392`, r.73 e r.79: *"nessuna riga"*) |

> ## 🟠 **Urgenza: MEDIA — e diventa ALTA o BASSA in base a UNA lettura da 30 secondi.**
> Il paracadute grosso (9,9% totale) **funziona**. Quello degradato è il cancello
> giornaliero, e di quanto lo sia dipende **solo** dal credito. **Non archiviabile,
> non emergenza.**

---

# E. 🥇 PRIORITÀ FRA LE DUE SEDIE — **con un numero, non un'impressione**

> Referto gemello: `report/MAXMINNOTTE_ORO_770402_DIAGNOSI_2026-09-13.md`

| | 🛡️ **`779001` Guardian** | 🌙 **`770402` MaxMinNotte oro** |
|---|---|---|
| conto | **100k `50504263`** — quello su cui si prova la challenge | **piccolo `50503392`** — DEMO |
| commit mancanti | **4** | **5** |
| 🔴 **quanti MORDONO davvero** | **1 su 4** (`d884f7e`, e **solo a metà**) | **1 su 5** (`d4da7d7`) |
| frequenza misurata del morso | **1 giornata su 25** (flottante) · credito **`[NON MISURATO]`** | **9 trade su 9 = 100%** |
| ampiezza misurata del morso | **≤ 0,65 punti** contro un cancello a **4,9** = **≤13% della distanza** | **≤ 2 perdite piene su 9** (≈ −2R), segno netto `[NON MISURATO]` |
| costo se morde davvero | 🔴 **la challenge** (una violazione giornaliera = fuori) | 🟠 **decine di euro su un DEMO** (0,01 lotti sull'oro) |
| l'aggiornamento RISOLVE il problema peggiore della sedia? | 🟢 **sì** | 🔴 **NO** — il difetto che è **misurato aver morso** (C9, `SelPos` cieco, 08/09) **non è fra i commit mancanti e c'è anche a HEAD** |

## 🥇 **PRIMA IL GUARDIAN `779001`.** E il numero che decide è questo:

> **su 5 cancelli del Guardian, 3 sono corretti e 2 sono degradati — e i 2 degradati
> stanno sul conto che DEFINISCE l'obiettivo del progetto.** Sulla MaxMinNotte, invece,
> **0 dei 5 commit mancanti** tocca il difetto che ha prodotto l'unico danno misurato.

Detto in modo brutale: **aggiornare il Guardian ripara la cosa peggiore che ha;
aggiornare la MaxMinNotte no.** A parità di rischio della manovra, si fa prima quella
che paga.

## 🎯 MA LA MOSSA PIÙ REDDITIZIA DI STANOTTE NON È UNA RICOMPILAZIONE

È una **lettura da 30 secondi** (§C.3): il pannello del Guardian **già disegnato** sul
grafico AUDNZD del terminale `50504263` mostra `Equity`, `Balance` e `Inizio giorno`.
Quella differenza decide **da sola** se il Guardian è a urgenza **BASSA** (nessun credito
→ difetto ~zero, 1 giorno su 25) o **MASSIMA** (credito → fail-open **tutti i giorni**).

🔴 **Finché quel numero manca, l'ordine qui sopra è la stima migliore disponibile,
non un fatto.** Chiedere la lettura costa zero e non tocca nessun EA: è esattamente il
_"se manca poco alla convalida, si CHIEDE a Claudio"_ del motto del 09/09.

## 🗓️ E la terza sedia del censimento, per chiudere il cerchio
`770611` ORB sul 100k è già stata misurata e **degradata a non-emergenza**
(`report/ORB_DOW_100K_COMPRESENZA_2026-09-13.md`: 1 compresenza su 25 giorni, zero danni).
👉 **Le "3 sedie peggiori" del censimento, guardate una per una, sono diventate
1 media + 2 basse.** Il censimento non ha sbagliato a segnalarle: ha fatto il suo mestiere,
che è portarle sul tavolo. 🟢 **Misurarle le ha ridimensionate tutte e tre — e questa è una
buona notizia, non un anticlimax.**

---

## 📌 DICHIARAZIONE DI ONESTÀ

- **Ponteggio, non sedia.** Questo referto non produce nessun PF nuovo e non avvicina di
  un metro una sedia schierabile il 1° ottobre. Serve perché il Guardian è la rete che
  tiene in piedi tutte le altre. Va contato per quello che è.
- 🔴 **`[NON MISURATO]`, elencati per nome, non "tutto il resto"**: (1) il **credito** del
  100k; (2) il **flottante** della posizione del 27/08 alle 23:00 esatte — so che c'era,
  non so di che segno; (3) quante volte il terminale `-V3` **si riavvia** — un riavvio con
  giornata prop nuova cattura la baseline all'ora del riavvio, non alle 23:00, e questo
  vale **su tutte e due** le versioni.
- **Non ho letto nessun `.ex5`.** Il pin è sorgente-in-cartella + data di compilazione. Se
  qualcuno avesse aggiornato il `.mq5` senza ricompilare, il pin sarebbe del sorgente.
  `[LIMITE DICHIARATO]`, come nel censimento del 13/09.
- **Campione sottile, dichiarato**: 29 operazioni in 25 giornate coperte. Basta a dire
  *"attraversare le 23:00 è raro"*, **non** basta a dire *"succede il 4% dei giorni"* con
  precisione.
- **Zero saldi, zero equità, zero P/L.** Verificato prima di committare.

_Fonti: `backtest_pipeline/coda/referti/CODA_06_quale_codice_gira_20260910_033002.log` ·
`CODA_08_preset_dai_chr_20260912_033002.log` · `CODA_09_giornale_operativo_20260912_033002.log` ·
`data/statements/trades_100k.csv` · `git log`/`git show` del branch `lavoro` a HEAD ·
`mql5/Presets/ABTG_Guardian_50504263_779001_VIVO.set`._
