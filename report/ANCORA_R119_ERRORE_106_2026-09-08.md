# 🔍 PERCHÉ L'ANCORA È MORTA — **error 106**, e non era la macchina

Passo 7 lanciato l'08/09/2026 alle **17:43**. Fallito in **meno di un minuto**.

---

## 🟢 PRIMA LE COSE ANDATE BENE, che non sono poche
| | |
|---|---|
| tetto barre | `MaxBars=10000000 -- sufficiente` ✅ la correzione delle 17:41 è entrata |
| terminale scelto | `C:\MT5_Backtest`, via **parametro esplicito** ✅ |
| PID non bersaglio **prima** | `7824, 8664, 9780` |
| PID non bersaglio **dopo** | `7824, 8664, 9780` ✅ **il conto reale non è stato sfiorato** |
| tempo macchina perso | **zero**: è morto subito, non dopo ore di tick |

## 🔴 LA CAUSA, letta nel log di MetaEditor
`<dati>\MQL5\Experts\ABTG_ORB_Ottimizzato.log` e `..._DAX_Apertura_EU.log`:

```
ABTG_ORB_Ottimizzato.mq5(106,10) : error 106: file 'Include\ABTG_PausaGuardian.mqh' not found
Result: 1 errors, 0 warnings
```
Identico su tutte e due le sedie. E il censimento della cartella:

| | |
|---|---|
| `.mq5` copiati dal driver | ✅ 2, freschi delle 17:43 |
| **`.ex5` prodotti** | 🔴 **0** |
| `.mqh` totali nel terminale | 237 (tutti **di sistema**) |
| **`.mqh` NOSTRI** (`ABTG*`, `OptFrame*`) | 🔴 **0** |

## 🧩 IL MECCANISMO, in tre righe di codice
`backtest_pipeline/walkforward_generico.ps1`:
- r. **245**: scarica **solo** `mql5/Experts/<EA>.mq5`;
- r. **852**: `Copy-Item $srcFile -Destination $MqlExperts` — **solo il `.mq5`**;
- r. **854**: nessun `.ex5` → `Muori "compilazione fallita"` → **codice 1**.

Ma gli EA hanno `#include <ABTG_PausaGuardian.mqh>`, e nel repo gli include
stanno in `mql5/Include/`. 👉 **Il driver non li ha mai portati.**

> ### 🎯 Sul vecchio PC di backtest quel file c'era da mesi, messo a mano una
> volta e mai più pensato. `C:\MT5_Backtest` è un'installazione **nuova e
> vuota**: lì l'assunzione è saltata.
>
> **Non è la macchina che non riproduce. È un file da 12 KB che non è mai arrivato.**

---

## 📏 LA LEZIONE, ed è più grande del bug
Il passo 7 esiste per rispondere a *"la macchina nuova è la stessa macchina?"*.
La risposta di oggi è: **non ancora — e non per i dati, per l'ambiente**.

Un terminale nuovo non è solo un `terminal64.exe` e uno storico: è anche
**tutto ciò che qualcuno, mesi fa, ci ha messo dentro a mano e non ha
scritto da nessuna parte**. Ogni pezzo del genere è un'assunzione che vive
solo su quella macchina, e si scopre **solo** quando si prova a ricostruirla.

👉 Per questo il passo 7 **va fatto**, e va fatto **prima** di fidarsi dei
numeri: non è burocrazia, è l'unico modo di stanare le assunzioni invisibili.

## ✅ COSA È STATO FATTO
1. **Subito**: i tre include nostri (`ABTG_PausaGuardian.mqh`, `OptFrame.mqh`,
   `ABTG/ABTG_ApertureCore.mqh`) messi a mano in `MQL5\Include` con una riga
   che **verifica** l'esito (`dei NOSTRI: 3`), così l'ancora riparte oggi.
2. **Perché non succeda più**: il driver impara a portarsi dietro gli include
   da solo, su qualunque terminale, e a stampare **le righe di errore di
   MetaEditor** invece del solo *"compilazione fallita"* — oggi Claudio non
   aveva modo di sapere perché.
