# ✍️ FIRME DEL 12/09/2026

---

## 1. 🟢 CONFERMATO: `InpRiskPercent = 0,65%` su `770101` — **VOLUTO**

**Claudio, 12/09 sera, testuale:** _"Sì, 0,65 è voluto, LO ABBIAMO DECISO INSIEME"_

### Il fatto che aveva bisogno della conferma
| quando | dove | valore |
|---|---|---|
| 02/09 | `VERBALE_CHIUSURA_770101_2026-09-02.md` §C2 | `InpRiskPercent = **1.0**` |
| **12/09 sera** | pannello del terminale **50504263**, grafico `D30EUR,M5` | `InpRiskPercent = **0.65**` |

Il valore è **sceso fra il 02/09 e oggi**, e io l'ho letto da una fotografia, non da un
verbale. 🔴 **Un parametro di rischio non si deduce da uno screenshot**: per questo ho
chiesto, invece di scriverlo come fatto. Ora è un fatto.

### 🟢 E combacia con la misura indipendente di oggi
Il collaudo prop (`INDURIMENTO_PROP_DUE_SEDIE_2026-09-12.md`), Monte Carlo sui giorni
veri e **appaiati** (una riga per giorno, righe mescolate intere → la correlazione
same-day è conservata):

| taglia | p95 | p99 | verdetto contro i muri 4,9% / 9,9% |
|---|---:|---:|---|
| **1,00%** | 9,90% | 🔴 **12,17%** | **sfonda** |
| **0,65%** | 6,76% | 🟢 **8,24%** | tiene, con **mezzo punto** di margine |

👉 **Lo 0,65% non è una preferenza: è la condizione di sopravvivenza del portafoglio a
due sedie.** La decisione di Claudio e il numero misurato oggi **coincidono**, e sono
arrivati per due strade indipendenti.

### 🔴 Conseguenza operativa da non perdere
Il margine residuo sul muro del **9,9%** è **mezzo punto**. Quindi:
- 🚫 **non c'è spazio per una TERZA sedia** senza rimisurare il DD combinato;
- 🚫 nessuna taglia si alza senza rifare il Monte Carlo appaiato;
- ⚠️ e il tetto per **cluster C2 al 3,0%** — firmato il 07/09 — **manca proprio lì**:
  in campo il Guardian è quello di agosto, **15 input contro 19**, e la manopola
  **non c'è nemmeno**. È in misura adesso.

---

## 2. 🔴 NON FIRMATO, E IL MOTIVO È UN DIFETTO NELL'EA: `InpMaxSpread` resta **0**

Stavo per chiedere a Claudio di firmare un tetto di spread a **272 punti MT5**.
🔴 **Il cancello ha letto l'EA e la firma si è rivelata una trappola — CLASSE 294.**

```
ABTG_DAX_Apertura_EU.mq5
  r.1441  if(!SpreadOK()) { ABTGLog(...); return(true); }   <-- TRUE, esce PRIMA di:
  r.1443  gBuffer = EffectiveBuffer();                       <-- mai eseguita
  r.1444  gBias   = TrendBias();                             <-- mai eseguita
  r.718   if(ArmRetest()) gPhase = PH_ARMED;                 <-- true = ARMATO
  r.751   else MonitorRetest();                              <-- gira lo stesso
  r.948   gBuffer = 0;                                       <-- ResetDay
  r.1471  buyTrig = NormalizePrice(gRangeHigh + gBuffer);    <-- quindi + 0,00
  EffectiveBuffer() = max(InpBufferPoints, stopsLvl) * _Point   ->  SEMPRE > 0
```

👉 Nella giornata bloccata per spread la sedia **non salta il trade**: lo prende con la
soglia di rottura **abbassata di 5,00 punti indice** e **senza filtro di tendenza**.
🔴 **Accendere `InpMaxSpread` la rende PIÙ AGGRESSIVA proprio quando lo spread è alto.**

**È dormiente solo perché in campo `InpMaxSpread=0`** (e `InpMinRangePts`/`InpMaxRangePts`
anch'essi a 0): quel ramo non è **mai** stato percorso. 🔴 **La firma lo avrebbe svegliato,
su una sedia che gira sul conto REALE 10105439.**

**La toppa**: sul rifiuto di `SpreadOK()` in `ArmRetest()` (e in `ArmOpenConfirm()`) la
giornata va **chiusa** — `gPhase = PH_DONE` — non armata. È **dentro l'EA**, quindi
**[FIRMA DI CLAUDIO]**: segnalata, non toccata.

⚠️ E una correzione a un numero che avevo dato io: il **MAX 12,00 punti indice** all'ora
08 è **un tick su 1.847.049**, un solo istante in 21 mesi, e **non c'è prova che sia
caduto alle 08:35** — l'unico momento in cui la sedia decide. Il numero difendibile è il
**p95 = 2,70**. Il valore 272 era tarato bene; **la motivazione era gonfiata**.

---

## 3. 📉 LE ALTRE DUE, cadute misurando
- **`A1` tetto posizioni+pendenti**: 🔴 **BOCCIATO.** Conta le posizioni sul **simbolo**,
  da **tutti** gli EA: sul demo ci sono **163 posizioni D30EUR su 14 magic distinti**, e un
  tetto a 1 perderebbe **28 giornate su 50 = 56,0%**. Dimezzerebbe la frequenza, che è il
  requisito principale. 🟢 E il difetto che doveva curare è **già chiuso da solo**: dal
  07/08 sono **16 giornate, 16 posizioni, una al giorno**.
- **`InpMinStopPts`**: 🔴 **segno invertito.** Il 19,7% di posizioni "sotto il pavimento
  duro" ha **PF 1,358**, e il gruppo che un floor taglierebbe ha **PF 4,888**.
  **Non va difeso: porta il motore.**

---

## 🧾 STATO DELLE QUATTRO
| manopola | esito |
|---|---|
| `InpRiskPercent 0,65` | 🟢 **CONFERMATO DA CLAUDIO** |
| `InpTP1_ClosePct 50→0` | 🟡 raccomandato, **in misura stanotte** (`q770be`) |
| `InpMaxSpread` | 🔴 **NON FIRMABILE** finché la classe 294 non è tappata |
| `A1` e `InpMinStopPts` | 🔴 **bocciate misurando**, restano a 0 |
