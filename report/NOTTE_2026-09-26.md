# 🌙 NOTTE 25/26 SETTEMBRE 2026 — report del turno di notte (scritto alle 03:50, chiuso alle 06:30)

## 🎯 La domanda di Claudio prima di dormire: «controlla nella challenge se il lato short del DAX retest l'ho fatto correttamente»
**Risposta onesta: [NON VERIFICATO stanotte], con un indizio a favore e nessuno contro.**
- La sonda `CODA_01` v3 (colonna LATI aggiunta ieri sera, PASS del cancello, pin `e81da508`) e' passata dal runner alle **03:30:03** (corsia LETTURA, G1/G2 passati, 7 s, uscita 0) e ha letto i grafici salvati di `C:\FTMO` (profilo `Default`): **9 sedie su 11 file `.chr`**, tutti modificati il **2026-09-25 alle 20:47** — cioe' MT5 ha salvato il profilo **nell'istante in cui Claudio ha aperto il grafico nuovo** per la 770105 (Journal: `loaded successfully` alle 20:47:27).
- In quel salvataggio la 770105 **non c'e'**: i due `.chr` in piu' sono `'Main'/senza nome` (grafici senza `<expert>`), cioe' il grafico e' stato salvato **prima** che l'EA finisse di caricarsi. Il limite era dichiarato (classe 822): il `.chr` si aggiorna solo quando MT5 salva il profilo, e **stanotte non c'era riserva automatica** (`CODA_09` scarta le righe `loaded`/`CONFIG`, e la sedia — attaccata alle 20:47 di venerdi' — non ha ancora operato). Zero righe con `770105` in tutte le 12 sonde.
- Quello che la foto salvata **conferma** della gemella: `CLAU12_DAX_Apertura_EU GER40.cash M5 magic 770101 rischio 2.0 lati SOLO LONG` (preset ricostruito da `chart01.chr`: 82 parametri, `InpAllowLong=true`, `InpAllowShort=false`, `InpMagic=770101`, `InpRiskPercent=2.0`, `InpTrailTF=5`): la 770101 e' rimasta **solo long**, come deve.
- 👉 **Come si chiude**: (a) una foto della scheda **Esperti** o del **Navigatore** del terminale `541452707` (`C:\FTMO`) con i due grafici GER40.cash M5; oppure (b) **lunedi' alle 10:00 FTMO** la 770105 arma i suoi ordini e `CODA_09` li legge nel giornale; oppure (c) un salvataggio del profilo (File → Profili → Salva) e la sonda della notte dopo la vede con `lati SOLO SHORT`.

## 🤖 COSA HA FATTO IL RUNNER STANOTTE (`REFERTO_RUNNER_20260926_033003.txt`)
- 12 righe di coda, **12 eseguite, 0 in corsia ROUND**, tutte in corsia LETTURA con G1/G2 passati; ~2 minuti. Il banco `50504400` resta spento. Nessuno Strategy Tester sul VPS.
- `CODA_03`: il terminale del **piccolo `50503392` sul VPS** (`C:\Program Files\BCM Markets MT5 Terminal`, cartella `215D85D7…`) ha come ultimo login nel giornale il **20/09 04:57**; la pagella di ieri (`giornata_2026-09-25.md`, 4 passate del cancello) lo da' **fermo dal 23/09 19:35** — quindi anche le sedie **non-indice** del piccolo sono ferme, e le chiusure viste in pagella sul conto 50503392 vengono dall'altro terminale loggato sullo stesso conto (il PC di backtest). Decisione di Claudio: riaccenderlo o no (le sedie indice sui demo sono sospese dal 25/09 13:07 per l'hedging).
- `CODA_08` su `C:\FTMO`: i 9 preset ricostruiti dai `.chr` (770101 SOLO LONG · 770202 SOLO LONG · 770260 L+S · 771531 L+S · 770511 L+S · Guardian 80000/4,5/9,3/3,5/cap 4,00 · 770411 SOLO SHORT rischio 2,00 · TradeExporter · SpreadLogger): coerenti con ieri.

## 🔬 COSA HO FATTO IO STANOTTE (tutto su `lavoro`, tutto col PASS del cancello)
1. **R253** (gap-down short DAX, ricerca) — referto `REFERTO_R253_2026-09-25.md`, **3 passate**: catena verde; curva in fase **29 posizioni**; costo AMMESSO (stop mediano 116 pti = 68× spread; valore del punto **misurato** dal per-trade eliminando l'ingresso: 1,000 EUR/pt/lotto in 46/46); R1 5,63% / R2 −0,98% / R3 3 **NON VIOLATI su n 29** (P che un motore senza edge passi R1: 0,56); merito SOSPESO, EP −0,04 R, segno discorde da CE9 ma dentro l'attesa di toro; O1 vs 770411 17%; O2 vs 770105 NON MISURABILE (zip R252 mai arrivato). **NON ANCORA MISURATO.** Ricerca, non sedia.
2. **TrailFix, il pacchetto per le 4 sedie vive su FTMO** (`mql5/Experts/trailfix_9fca63d9/`, 2 passate): i tre sorgenti = pin in campo `9fca63d9` + la sola Parte A (guardia del lato dello stop prima della `modify`, con la tolleranza di mezzo punto trovata dal cancello — classe 820: senza, con StopsLevel > 0 avrebbe bloccato il 40-71% delle modify buone). Diff riproducibili byte per byte, LEGGIMI onesto. **Non e' in campo.**
3. **La prova di neutralita' R254** che sta fra il pacchetto e la firma: 8 file prova (PIN contro PIN+A per 770101/770105/770202/770260, corsa singola, cancelli C0 a-d / N0-N3 / C1 / C2 congelati, 2 passate), lo script `PASSATA_TRAILFIX.ps1` (882 righe, **3 passate**: ex5 vecchio = classe 270, TLS, C2 fail-closed sui log illeggibili da tutti e due i lati = classi 828/829) e la riga di lancio `RIGA_PASSATA_TRAILFIX_PC_BACKTEST.txt` (**PASS**, pin `7769a5d6`). Scoperta del collaudatore, verificata alla fonte: il driver di casa **non puo'** fare questa prova (`Optimization=1` fisso r.2013, EA sempre da HEAD, niente sottocartelle) e **in ottimizzazione il giornale del tester non scrive i rifiuti** (R251: 24 "avviato", 0 deal) — per questo serve la corsa singola.
4. **La riga di SOLA LETTURA per la 770105** (`righe/RIGA_VERIFICA_770105_FTMO_VPS.txt`, 3 passate: la prima bocciata dallo strato 1 per la classe 173, la terza corretta dal cancello con il Giornale del terminale per `loaded/removed`): legge il giornale Esperti di `C:\FTMO` del 25-26/09 e stampa le righe `CONFIG IN USO` delle sedie DAX Apertura — la 770105 si riconosce da `lati=SOLO SHORT` alle 20:47 (l'EA non stampa il magic; la riga dice solo che la sedia e' **partita** con quei parametri, non che e' ancora attaccata: per quello guarda le righe `removed`). Un minuto sul VPS, nessuna foto.
5. **Buco chiuso nel cancello deterministico** (`controlla_riga.py`, classe 830, trovato dal controllo-preventivo): le chiamate statiche .NET che scrivono (`[IO.File]::WriteAllText/Delete`, `[IO.StreamWriter]::new`, `Open` in scrittura) passavano per "sola lettura" in una riga inline. Ora sono FAIL; il lettore condiviso in lettura (classe 163) resta ammesso; regressione su 34 righe invariata.
6. Classi nuove nella checklist: **822-830** (otto in una notte; la 824 e' andata in collisione con la pagella e la mia e' diventata 825 — classe 194, di nuovo).

## ⚠️ COSA ASPETTA CLAUDIO (solo le sue cose)
- 🖥️ **La riga di lettura sul VPS per la 770105** (`RIGA_VERIFICA_770105_FTMO_VPS`): un minuto, chiude il [NON VERIFICATO] o lo conferma.
- 🖊️ **La prova R254 sul PC di backtest**: una riga, ~10-20 min (tetto 60): ⏸ ancora da inviare, arriva col report delle 06:30 con il bersaglio dichiarato. Se esce **NEUTRA** su tutte e quattro le sedie, resta la **firma** per ricompilare le CLAU12 su FTMO (senza posizioni aperte). Se no, il pacchetto non si schiera.
- La **770105**: una foto di Esperti/Navigatore chiude il [NON VERIFICATO] in un minuto.
- Il **piccolo sul VPS** fermo dal 23/09: riaccendere o no.
- Aperti da giorni: zip **R252**, CSV dello **scalper** (1.07), R246 INVERNO, R249, ORB EURAUD sul reale, taglia/cap.

## 🎯 LA CODA DI OGGI (sabato, mercati chiusi)
1. Riga R254 → Claudio → zip → referto della neutralita'.
2. Se NEUTRA: pacchetto della ricompilazione CLAU12 (riga per `C:\FTMO`, regola dei terminali, senza posizioni aperte: e' sabato, non ce ne sono) → firma.
3. CSV dello scalper, se arriva → `analizza_scalper.py`.
4. Bussola, detta onesta: la notte ha prodotto **sicurezza per quattro sedie vive** (TrailFix) e **un referto di ricerca**; nessuna sedia nuova si e' avvicinata al 1° ottobre.
