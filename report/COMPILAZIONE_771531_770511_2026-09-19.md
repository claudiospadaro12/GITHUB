# 🔨 IL PACCHETTO DI COMPILAZIONE DELLE DUE SEDIE — `771531` e `770511`

**19/09/2026** · branch `lavoro` · firma di Claudio: *«SI FIRMO ENTRAMBE, COMPILA PURE»*
Censimento di partenza: `report/I_BINARI_DELLA_ROSA_2026-09-19.md`
Script: `backtest_pipeline/righe/COMPILA_771531_770511.ps1` · **PIN `9d39c873`**
Modello (ha funzionato oggi): `backtest_pipeline/righe/TOPPA_TICKET_50503392.ps1`

---

## 🛑 LA COSA DA LEGGERE PER PRIMA: **SÌ, LA COMPILAZIONE CAMBIA LE TAGLIE**

Su **tutte e due** le sedie, e non come effetto collaterale: è **il contenuto** dei
commit che stiamo portando in campo. La firma di Claudio dice *«compila»*. **Non dice
*«cambia le taglie»*.** Quindi il pacchetto è pronto e verificato, ma 🔴 **l'F7 va
chiesto con questa riga davanti**, non dopo.

### `770511` SuperWave DOW H1 Ott — il **pavimento del lotto**, `872dba82`
Il commit che stiamo portando **è** il fix del sizing. Tre righe riordinate in `Enter()`:

| | vintage in campo `344a11b9` | pin `872dba82` |
|---|---|---|
| ordine | `lotPend=NormVol(totLot-lotMkt)` **poi** `if(lotMkt<=0) lotMkt=volMin` | `if(lotMkt<=0) lotMkt=volMin` **poi** `lotPend=NormVol(totLot-lotMkt)` |
| volume totale quando `NormVol(totLot*0.3333)` → 0 | **`totLot + volMin`** | `volMin` (+ il resto, se supera il minimo) |

- Il preset della sedia porta `InpFirstFraction=0.3333`, `InpUsePending=true`,
  `InpRiskPercent=1.0` (`mql5/Presets/sedie_piccolo/recupero2/sedia_ABTG_SuperWave_DOW_H1_Ottimizzato_770511.set`).
- Il difetto è **verificato nel binario che gira**, non dedotto (censimento di oggi).
- 📏 **Direzione del cambiamento: la taglia SCENDE**, e scende verso il contratto
  dichiarato (1,0%). Non è un aumento di rischio: è la fine di un eccesso.
- 🔴 **Ma il numero per QUESTA sedia è [NON MISURATO].** Il **1,42% su contratto 1,0%**
  del `DIARIO.md` del 20/08 è della **gemella `770531`** (`ABTG_SuperWave` base, H4):
  stesso difetto, stesso vintage, **misura di un'altra sedia**. Su `770511` sappiamo
  che il difetto c'è; **di quanto abbia gonfiato le taglie non lo sappiamo**.
- 👉 Conseguenza che pesa più del resto: **il forward di `770511` è stato prodotto da un
  binario che piazzava più lotti del dichiarato.** Ricompilare non «sistema» il forward:
  lo rende **non confrontabile** con quello di prima.

### `771531` EMA200 Dow — il sizing **`OrderCalcProfit`**, `3af47ed9` (dentro `26a18566`)
`LotByRisk()` smette di calcolare la perdita per lotto dal tick value nudo e la chiede a
`OrderCalcProfit`, che converte in valuta conto. Il commit stesso dichiara la regola:
*«sui simboli sani i due calcoli coincidono: il comportamento cambia SOLO dove il tick
value mente»*.
- 🟠 **Su `U30USD` con conto in EUR non è mai stato misurato se coincidono.** Il
  censimento di oggi lo segna `[NON MISURATO su U30USD]` ed è ancora vero.
- 👉 Quindi: **potrebbe non cambiare niente, e potrebbe cambiare il lotto.** Finché non
  si misura, «non cambia» è un'ipotesi, non un fatto.

📌 **Come si chiude il buco in dieci minuti, senza toccare il conto:** attaccare i due
`.ex5` nuovi nel **Tester** (`C:\MT5_Backtest`, **50504400**) sullo stesso simbolo/TF e
confrontare i lotti con quelli del binario vecchio. È una misura, non un'opinione. ✋ Non
l'ho fatta io: non lancio round.

---

## 🔴 E CAMBIA ANCHE IL COMPORTAMENTO IN FORWARD: LE DUE SEDIE COMINCIANO A LEGGERE IL GUARDIAN

Oggi **nessuna delle due** legge il Guardian: in campo girano `344a11b9` del **04/08**,
e `grep InpUsaGuardian` sul sorgente in campo dà **zero**. Dopo l'F7:

| sedia | pin | cosa entra |
|---|---|---|
| `771531` | `26a18566` (19/08, *Migrazione Guardian pezzo 5*) | `input bool InpUsaGuardian = true` + `ABTG_GuardiaIngresso()` davanti all'ordine |
| `770511` | `872dba82` (che contiene `f8ebc321`, 19/08, *pezzo 4*) | idem |

🟢 **La buona notizia, ed è misurata:** sul piccolo **50503392 non gira nessun Guardian**
(giornale 11 e 12/09: *«GUARDIAN: nessuna riga»*). La guardia è **fail-open per
costruzione** — `ABTG_CanaleEsiste()` cerca tre variabili globali e, se non ci sono,
ritorna `true` e si passa. 👉 **Da lunedì il comportamento operativo non cambia.**

🔴 **La notizia che va detta lo stesso:** `InpUsaGuardian` ha **default `true`** e **non
è in nessuno dei due `.set`**. Il giorno in cui un Guardian si accende su quel terminale
— o il giorno in cui queste sedie si spostano su un conto prop — **le due sedie
cominciano a obbedirgli senza che nessuno abbia toccato niente.** È quello che vogliamo,
ma dev'essere una decisione, non una sorpresa.

🟠 **Stesso discorso per due input nuovi di SuperWave** (`InpPendingAtr`, `InpSLBufferAtr`
da `7f80a87a`): default `0` = inerti, **ma non sono nel `.set`**. Alla riattacca vanno
**guardati**, non scrollati (classe 25).

---

## ✅ I DUE BERSAGLI — confermati con il diff in mano, e uno **corretto**

### 1. `ABTG_EMA200.mq5` → **`26a18566`** ✅ confermato
### 2. 🔴 `ABTG_SuperWave_DOW_H1_Ottimizzato.mq5` → **`872dba82`** — **NON** `ABTG_SuperWave.mq5`

La richiesta nominava `mql5/Experts/ABTG_SuperWave.mq5`. **È il file sbagliato**, ed è la
**classe 462** della checklist, ripagata: `ABTG_SuperWave.mq5` è la sedia **`770531`**
(H4), un'altra. Verificato **per nome** in `CODA_01` del 19/09 — non dedotto dal nome
della famiglia:

```
ABTG_EMA200                       U30USD  H1  magic 771531  rischio 1.0  [ORO\chart33.chr]
ABTG_SuperWave_DOW_H1_Ottimizzato U30USD  H1  magic 770511  rischio 1.0  [ORO\chart37.chr]
```

🟢 **Il PIN però era giusto**: `872dba82` ha toccato **15 sorgenti**, la variante
`_Ottimizzato` compresa, e anche per lei HEAD è `b45dd009`. Cambia il **file**, non il
commit.

### 🔬 Perché non HEAD — il diff di `b45dd009`, guardato e non creduto sulla parola

`b45dd009` ↔ suo padre `e2a10a7c`, sui due file: **+301 / −34 righe**. Letto per intero:

| cosa | quante volte |
|---|---|
| `input bool InpLogImbuto = true` (nuovo, governa **solo** il log) | 2 (uno per EA) |
| contatori `long c*_*=0` | 18 + 18 |
| `ImbutoRaccogli` / `ImbutoStampa` / `ImbutoGiro` (leggono contatori, fanno `Print`) | 3 + 3 |
| `return;` diventati `{ cXxx++; return; }` | ~14 + ~16 |
| **condizioni di trading cambiate** | **ZERO** |
| **soglie, lotti, SL, TP, ordini cambiati** | **ZERO** |

🟢 **Verdetto onesto: il WIP è, su questi due file, innocuo per il trading.** Nessun
contatore entra in una condizione. **Non è un bug nascosto: è diagnostica.**

🔴 **E però il bersaglio resta il pin, per due ragioni che non si annullano:**
1. quel codice **non è mai passato dal cancello e non è mai stato compilato** (il
   messaggio di commit lo scrive in maiuscolo). **Un F7 a due giorni dalla challenge non
   è il posto dove si scopre un errore di compilazione.** `26a18566` e `872dba82` sono
   stati compilati e girano (o sono girati) davvero;
2. `b45dd009` tocca **10 EA e 2 strumenti** insieme: prenderlo per due EA vuol dire
   dichiarare buono un commit che parla anche di altri otto.

### 🧨 COSA CI PERDIAMO, detto per nome (non nascosto in una nota)
**L'imbuto di mortalità su queste due sedie.** Cioè: quando una candidata d'ingresso
viene scartata, il Giornale **continuerà a non dire da quale condizione**. Le due sedie
restano **CIECHE sul rifiuto** (`report/PERCHE_ENTRANO_POCO_2026-09-11.md`). 👉 È un
costo di **diagnosi sulla frequenza**, non di soldi — e si ripaga in un secondo giro,
quando l'imbuto avrà un PASS.

---

## 📦 IL PACCHETTO — ogni file con la sua revisione dichiarata

| file | destinazione nel terminale | revisione | `wc -l` | `CODA_06` | sha256 scheletro (16) |
|---|---|---|---:|---:|---|
| `ABTG_EMA200.mq5` | `MQL5\Experts` | **`26a18566`** | **552** | 553 | `5CA99D90A5F34E96` |
| `ABTG_SuperWave_DOW_H1_Ottimizzato.mq5` | `MQL5\Experts` | **`872dba82`** | **645** | 646 | `3C487F289023CEDC` |
| `ABTG_PausaGuardian.mqh` | `MQL5\Include` | **`26a18566`** (v1.20, blob `cc90fb73`) | **398** | 399 | `D179846B407FDACC` |

E le impronte di **partenza**, quelle che lo script pretende di trovare (vintage in campo
`344a11b9`, 04/08):

| file | `wc -l` | `CODA_06` | sha256 scheletro (16) |
|---|---:|---:|---|
| `ABTG_EMA200.mq5` | **486** | 487 | `C60EC2B47DEF781E` |
| `ABTG_SuperWave_DOW_H1_Ottimizzato.mq5` | **563** | 564 | `D1BEBB233564EB6C` |

📏 **Due righelli, sempre** (classe 456): `wc -l` è quello di MetaEditor e dello script;
`CODA_06` conta **uno in più**. Chi confronta col log notturno usa il secondo.

### 🔴 IL PEZZO CHE MANCAVA A TUTTI: **`ABTG_PausaGuardian.mqh`**
I due `.mq5` nuovi fanno `#include <ABTG_PausaGuardian.mqh>`. **Senza quel file l'F7
muore su `cannot open include file`** — ed è la **classe 27** della checklist.

🔴 **E nessuno sa se c'è.** `CODA_06` apre **solo** `MQL5\Experts`
(`CODA_06_quale_codice_gira.ps1` r.72): **`MQL5\Include` non è mai stato censito da
nessuno**. Sul piccolo tutti gli EA in campo sono senza Guardian, quindi l'include
potrebbe non essere mai stato installato lì.

**Cosa fa lo script, e perché così:**
- **assente** → lo installa alla **v1.20** (blob `cc90fb73`), che è **la stessa già
  compilata e viva sul 100k** dal 19/08 (`5fc0bc31`): provata, non nuova;
- **presente e uguale alla v1.20 o alla versione di HEAD** → **non lo tocca**, e lo dice;
- **presente e sconosciuto** → **non lo tocca**, stampa lo sha e avvisa.

📌 **Perché non lo sovrascrive mai:** quell'include è condiviso da **una ventina** di EA
su quel terminale. Riscriverlo cambierebbe il prossimo F7 di chiunque, compresi gli altri
agenti che stanno lavorando adesso. *Si verifica, non si riscrive* — la stessa disciplina
di `RIGA_DEPLOY_GUARDIAN_CONTOREALE.ps1`.

⚠️ **Deviazione dichiarata, perché una deviazione taciuta è un difetto.** Al pin di
SuperWave (`872dba82`) l'include vale `08be2815` (**2461 righe**, v1.51 + tetto cluster
C2), non `cc90fb73`. Ho scelto la **v1.20** e il motivo è verificato: **i due EA usano
UNA SOLA funzione dell'include**, `ABTG_GuardiaIngresso(bool,string)` — una occorrenza
ciascuno, `grep` fatto ai pin — e la **firma è identica** nelle due versioni. La v1.51+C2
arriva dal commit `cdb2037a`, che è a sua volta un **«LAVORO IN CORSO»**. 👉 Fra un
include *provato* e uno *WIP*, a due giorni dalla challenge si prende quello provato.

---

## 🖥️ DOVE SI MANDA QUESTA STRINGA

👉 **In una finestra PowerShell sul VPS.**

🎯 **BERSAGLIO, per esteso:** la **cartella DATI** del terminale MT5 **50503392**
(programma `C:\Program Files\BCM Markets MT5 Terminal`, cartella dati
`%APPDATA%\MetaQuotes\Terminal\215D85D767A1C39E22D242C8114BF9F5`).

🔴 **CHE COSA NON VIENE TOCCATO.** Sul VPS ci sono **SETTE** cartelle dati. Questa
stringa ne guarda **UNA** e ignora le altre **SEI**:

| conto | programma | toccata? |
|---|---|---|
| **50503392** | `C:\Program Files\BCM Markets MT5 Terminal` | ✅ **SI, solo questa** |
| **50504263** | `...BCM Markets MT5 Terminal -V3` | ❌ NO |
| **10105439** | `C:\BCM_Reale` (**conto REALE**) | ❌ NO |
| **50504400** | `C:\MT5_Backtest` | ❌ NO |
| — | `C:\MT5_MANUALE` | ❌ NO |
| — | `C:\Program Files\Pepperstone MetaTrader 5` | ❌ NO |
| — | `C:\Program Files\Tickmill Europe MT5 Terminal` | ❌ NO |

🚫 **NON compila, NON apre, NON chiude nessun MT5.** Nello script **non esiste una sola
chiamata** capace di terminare un processo: niente `Stop-Process`, niente `Get-Process`,
niente `Start-Process`. **Copia file, e basta.**

---

## ▶️ LA STRINGA (blocco intero, un comando solo)

```powershell
& { $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12;
    $pin='9d39c873f8bf18a426e96df8141da2c900ca0372'; $p="$env:USERPROFILE\COMPILA_771531_770511.ps1"; Remove-Item $p -EA SilentlyContinue;
    irm "https://raw.githubusercontent.com/claudiospadaro12/GITHUB/$pin/backtest_pipeline/righe/COMPILA_771531_770511.ps1" -OutFile $p;
    if(-not (Select-String -Path $p -SimpleMatch -Pattern 'MARCATORE_COMPILA_771531_770511_v1' -Quiet)){ throw 'SCRIPT VECCHIO: manca il marcatore MARCATORE_COMPILA_771531_770511_v1.' };
    $global:LASTEXITCODE = 0; & $p -Pin $pin;
    if($LASTEXITCODE -ne 0){ Write-Host ('FERMATO (uscita ' + $LASTEXITCODE + '): NON premere F7. Manda tutto l output qui sopra.') -ForegroundColor Red }
    else { Write-Host 'FATTO (uscita 0). La tabella qui sopra dice se i file sono ENTRATI adesso o se erano GIA AGGIORNATI: nel primo caso sul Desktop trovi la cartella COMPILA_771531_770511_<data> e lo zip degli ORIGINALI.' -ForegroundColor Green } }
```

🕐 **Dura pochi secondi.** Se ci mette più di mezzo minuto, è la rete.

📌 **Le ultime tre righe non sono decorazione, e me le ha insegnate un
contro-esempio eseguito** (classe **469**, nuova): la prima stesura finiva con
`& $p -Pin $pin; Write-Host 'Fine. Sul Desktop trovi ... lo zip'`. Girata contro un
bersaglio inesistente, lo script ha **rifiutato con uscita 1** e la riga ha comunque
stampato *«sul Desktop trovi lo zip»* — **falso** — e ha restituito **0**. È la
classe **13** della checklist, ed è entrata qui **copiando il modello che oggi ha
funzionato**. Ora il `$LASTEXITCODE` viene azzerato prima e controllato `-ne 0` dopo.

---

## 📋 COSA VEDI IN CONSOLE, e cosa deve dire

```
[3/8] misura delle impronte attuali (nessuna scrittura ancora):
      ABTG_EMA200.mq5                        righe 486 (CODA_06: 487)   sha C60EC2B47DEF781E  -> DA AGGIORNARE
      ABTG_SuperWave_DOW_H1_Ottimizzato.mq5  righe 563 (CODA_06: 564)   sha D1BEBB233564EB6C  -> DA AGGIORNARE
...
  ABTG_EMA200.mq5
      prima 486 (CODA_06: 487)   dopo 552 (CODA_06: 553)   -> ENTRATA
  ABTG_SuperWave_DOW_H1_Ottimizzato.mq5
      prima 563 (CODA_06: 564)   dopo 645 (CODA_06: 646)   -> ENTRATA
```

🔴 **Guarda la riga `ora :` in cima all'output** (formato `AAAA-MM-GG HH:MM:SS`, ora
locale del VPS): **deve essere adesso.** Se è vecchia, stai leggendo uno scroll di prima
e non l'esecuzione appena fatta. *(Il 17/08 un referto stantio è stato rimandato due
volte in buona fede.)*

**Codici d'uscita:** `0` = fatto, **oppure** era già tutto a posto · `1` = fermato
**prima** di toccare qualunque cosa · `2` = almeno una copia non è andata a segno (gli
originali sono nello zip).

---

## 👉 E POI L'F7 — a mano, e **solo dopo la risposta sulle taglie**

1. apri **MetaEditor** del terminale **50503392** (`C:\Program Files\BCM Markets MT5
   Terminal`). Se non sei sicuro di quale finestra sia, incolla **questa riga di sola
   lettura** (stampa PID, titolo e cartella — **non tocca niente**):
   ```powershell
   Get-Process terminal64,metaeditor64 -ErrorAction SilentlyContinue | Select-Object Id, MainWindowTitle, Path | Format-Table -AutoSize
   ```
2. 🔴 **se uno dei due `.mq5` è già aperto in una scheda, chiudi la scheda senza salvare
   e riaprilo.** Altrimenti al primo salvataggio riscrivi sopra il sorgente appena messo.
3. **F7** su `ABTG_EMA200.mq5` e su `ABTG_SuperWave_DOW_H1_Ottimizzato.mq5`.
   ⚠️ **Detto prima, non dopo:** **nessuna di queste due revisioni è mai stata compilata
   su questo terminale.** Se l'F7 dà errore non è una sorpresa: è il collaudo che
   funziona.
4. rimuovi e riattacca i due EA (magic **771531** e **770511**) perché prendano il nuovo
   `.ex5`, e **ricarica i preset** guardando i tre input nuovi (§ Guardian qui sopra).

### 🔍 Come si vede che l'F7 è andato
- `ABTG_SuperWave_DOW_H1_Ottimizzato` passa da **versione 1.00 a 1.01** (visibile nel
  titolo della finestra MT5);
- 🔴 **`ABTG_EMA200` NON cambia versione** (resta `1.00`): `26a18566` non alza il
  `#property version`. **Si riconosce solo dal conteggio righe.** Chi cerca il numero di
  versione conclude, sbagliando, che l'F7 non è andato.
- ✅ Verifica indipendente che arriva da sola: il runner delle **03:30** (`CODA_06`) deve
  stampare **553** e **646** al posto di **487** e **564**.

---

## 🧪 I CONTRO-ESEMPI — **eseguiti**, non raccontati

`pwsh` disponibile in ambiente: parse reale dello script (`Parser::ParseFile`) → **0
errori, 2850 token**. Poi **sette** scenari girati davvero su un VPS simulato (`APPDATA`
e `USERPROFILE` finti, struttura `MetaQuotes/Terminal/215D85D7.../MQL5/Experts`,
scaricamento **vero** da `raw.githubusercontent.com`):

| # | scenario | atteso | esito |
|---|---|---|---|
| 1 | `origin.txt` dice `C:\BCM_Reale` (**conto REALE**) | rifiuto, niente toccato | ✅ uscita **1**, *«VIETATO … contiene BCM_Reale»* |
| 2 | giro buono, vintage in campo → pin | entrambi entrano | ✅ uscita **0**, 486→552 e 563→645, include installato, zip sul Desktop |
| 3 | **secondo lancio** sugli stessi file | niente backup a vuoto | ✅ uscita **0**, *«GIA AGGIORNATO»*, Desktop resta a **2** voci |
| 4 | file manomesso: **stesse 486 righe, sha diverso** | si ferma, e **salva lo stesso** gli originali | ✅ uscita **1**, SuperWave **intatto a 563**, zip con tutti e due i file |
| 5 | pin messo su `b45dd009` (il WIP) | lo scaricato non combacia | ✅ uscita **1**, *«righe 690 contro 552»*, terminale **intatto** |
| 6 | `.mq5` **mancante** sul terminale | rifiuto prima di ogni scrittura | ✅ uscita **1**, Desktop **vuoto** |
| 7 | pin **inesistente** → `404` | messaggio leggibile, niente scritto | ✅ uscita **1**, terminale **intatto**, zip già pronto |
| 8 | 🆕 **la stringa intera incollata com'è**, contro un bersaglio che non esiste | la coda **non** deve dire che è andata bene | 🔴 **FALLITO alla prima stesura** → corretto (classe **469**), ora ✅ *«FERMATO (uscita 1): NON premere F7»* |
| 9 | 🆕 la stringa intera, ramo buono e **ramo «erano già a posto»** | coda coerente in tutti e due | ✅ *«FATTO (uscita 0)»*, 486→552 e 563→645, secondo lancio senza backup nuovo |

📌 Il n° **4** è quello che conta per i file: **stesso conteggio righe, contenuto
diverso.** Un controllo basato sulle sole righe lo avrebbe lasciato passare. Lo sha
dello scheletro no.
📌 Il n° **8** è quello che conta per il metodo: era l'unico difetto vero del pacchetto,
**ed è uscito solo perché la stringa è stata ESEGUITA.** Riletta a mente sembrava a
posto — «ho copiato il modello che ha funzionato» è esattamente il ragionamento che il
10/09 ci è costato una giornata.

⚠️ **Non coperto, e lo dico:** i contro-esempi girano su **pwsh 7 in Linux**, con i
separatori `\` sostituiti da `/` (unica differenza fra lo script vero e la copia di
prova, verificata con `diff`). Il VPS ha **Windows PowerShell 5.1**: costrutti usati sono
tutti 5.1-compatibili (nessun ternario, nessun `&&`/`||` fra comandi, nessun
`-AsHashtable`), ma **l'esecuzione su 5.1 vero non è stata fatta**.

---

## 🚦 ESITO DEL CANCELLO

Riportato per intero nel messaggio di consegna: `python3
backtest_pipeline/controlla_riga.py --ps1` sullo script e `--riga` sulla stringa, più
l'audit **a mano** dei rilievi `[457]` (il cancello non vede i percorsi fra apici e non
cerca `Stop-Process`: verificati a mano **tutte** le scritture del file).

**Le 8 scritture dello script, elencate una per una:**
- 6 fuori dal terminale: 3 `New-Item` di cartelle e 1 `Copy-Item` + 1 `Compress-Archive`
  + 1 `WriteAllBytes` sotto **`$env:USERPROFILE\Desktop`**;
- **2 dentro il terminale**, e tutte e due partono dalla **costante** `$dati` certificata
  con `origin.txt`: `Copy-Item` in `MQL5\Experts` (percorso ottenuto da `Get-ChildItem`
  **dentro** `$dati`) e `WriteAllBytes` dell'include in `MQL5\Include`, **solo** nel ramo
  «assente».
- `grep -E 'Stop-Process|Kill\(|taskkill|CloseMainWindow|Get-Process|Start-Process'` sul
  codice → **zero occorrenze** (una sola riga di **commento** che dice che non ci sono).

---

## ❓ LE DUE DOMANDE APERTE, che non sono mie da chiudere

1. 🛑 **Le taglie.** L'F7 le cambia su tutte e due (sopra, con i numeri). La firma di
   Claudio copre *«compila»*. **Serve il suo sì esplicito su «e la taglia cambia».**
2. 🟠 **Quanto cambia su `U30USD`.** `[NON MISURATO]` su tutte e due le sedie. Si chiude
   con una corsa nel Tester (**50504400**, `C:\MT5_Backtest`) che confronta i lotti
   vecchio/nuovo. **Costo: minuti.** Vale la pena farla **prima** di lunedì.
