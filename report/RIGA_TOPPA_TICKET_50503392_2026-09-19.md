# 🩹 LA RIGA CHE PORTA LA TOPPA IN CAMPO — chiusura per TICKET

**19/09/2026** · firma: `report/FIRMA_2026-09-19_CHIUSURA_PER_TICKET.md`
Referti: `report/DUE_SEDIE_NASDAQ_IL_BLOCCO_2026-09-19.md` ·
`report/TOPPA_CHIUSURA_PER_TICKET_2026-09-19.md`
Sorgenti patchati: `backtest_pipeline/toppe_da_applicare/2026-09-19/`
Script: `backtest_pipeline/righe/TOPPA_TICKET_50503392.ps1` · **PIN `dae79f16`**

---

## 🖥️ DOVE SI MANDA QUESTA STRINGA

👉 **In una finestra PowerShell sul VPS.**

🎯 **BERSAGLIO, per esteso:** la **cartella DATI** del terminale MT5
**50503392** (programma `C:\Program Files\BCM Markets MT5 Terminal`, cartella
dati `%APPDATA%\MetaQuotes\Terminal\215D85D767A1C39E22D242C8114BF9F5`,
sottocartella `MQL5\Experts`).

🔴 **CHE COSA NON VIENE TOCCATO.** Sul VPS ci sono **SETTE** cartelle dati
(censite stanotte da `CODA_06`). Questa stringa ne guarda **UNA** e ignora le
altre **SEI**, che restano intatte:

| conto | programma | cartella dati | toccata? |
|---|---|---|---|
| **50503392** | `C:\Program Files\BCM Markets MT5 Terminal` | `215D85D7...` | ✅ **SI, solo questa** |
| **50504263** | `...BCM Markets MT5 Terminal -V3` | `BCA8AD18...` | ❌ NO |
| **10105439** | `C:\BCM_Reale` (**conto REALE**) | `E23E1504...` | ❌ NO |
| **50504400** | `C:\MT5_Backtest` | `04C7A32B...` | ❌ NO |
| — | `C:\MT5_MANUALE` | `CF6C240A...` | ❌ NO |
| — | `C:\Program Files\Pepperstone MetaTrader 5` | `73B7A242...` | ❌ NO |
| — | `C:\Program Files\Tickmill Europe MT5 Terminal` | `857385E4...` | ❌ NO |

📌 **La settima era una sorpresa**: `C:\MT5_MANUALE`. Non era nell'elenco di
partenza, ed è nel log del censimento. È in lista fra le **vietate**.

🚫 **Questa stringa NON compila, NON apre e NON chiude nessun MT5.** Dentro lo
script **non esiste una sola chiamata** capace di terminare un processo: niente
`Stop-Process`, niente `Get-Process`, niente `Start-Process`. **Copia file, e
basta.** La compilazione è **F7 a mano**, ed è tua (passo 5 della firma).

---

## ▶️ LA STRINGA (blocco intero, un comando solo)

```powershell
& { $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12;
    $pin='dae79f160bef13bf09ed035d028453d37e09aef9'; $p="$env:USERPROFILE\TOPPA_TICKET_50503392.ps1"; Remove-Item $p -EA SilentlyContinue;
    irm "https://raw.githubusercontent.com/claudiospadaro12/GITHUB/$pin/backtest_pipeline/righe/TOPPA_TICKET_50503392.ps1" -OutFile $p;
    if(-not (Select-String -Path $p -SimpleMatch -Pattern 'MARCATORE_TOPPA_TICKET_50503392_v1' -Quiet)){ throw 'SCRIPT VECCHIO: manca il marcatore MARCATORE_TOPPA_TICKET_50503392_v1.' };
    $global:LASTEXITCODE = 0; & $p -Pin $pin;
    if($LASTEXITCODE -ne 0){ Write-Host ('FERMATO (uscita ' + $LASTEXITCODE + '): NON premere F7, non e stato sostituito niente. Manda tutto l output qui sopra.') -ForegroundColor Red }
    else { Write-Host 'FATTO (uscita 0). Sul Desktop trovi la cartella TOPPA_TICKET_50503392_<data> e lo zip degli ORIGINALI.' -ForegroundColor Green } }
```

📌 **Coda corretta il 19/09/2026 (classe 469).** Qui c'era
`& $p -Pin $pin; Write-Host 'Fine. Se e arrivata in fondo, sul Desktop trovi ...'`:
una mitigazione **in prosa** (*«se è arrivata in fondo»*), non un controllo. Se lo
script rifiutava con **uscita 1**, la riga stampava lo stesso la frase sui file
prodotti e il blocco restituiva **0**. È la classe **13**, e ricopiando questo
modello è finita dentro la riga di compilazione delle sedie `771531`/`770511`, dove
è stata trovata **eseguendo** la stringa contro un bersaglio inesistente. Ora il
`$LASTEXITCODE` si azzera prima e si controlla `-ne 0` dopo.

🕐 **Dura pochi secondi.** Se ci mette più di mezzo minuto, è la rete.

---

## 🛡️ LE TRE RETI, in ordine di scatto

1. **CERTIFICATO DEL BERSAGLIO.** Lo script non *cerca* la cartella: il
   percorso è una **costante**. Poi la **certifica** leggendo `origin.txt`
   dentro la cartella dati e pretendendo l'uguaglianza **esatta** con
   `C:\Program Files\BCM Markets MT5 Terminal`, dopo aver rifiutato per nome i
   sei percorsi vietati. Se `origin.txt` manca, **muore**.
2. **IMPRONTA PRIMA DI SOVRASCRIVERE.** I tre file presenti devono avere
   **2032 / 2132 / 2064** righe di **contenuto** *e* lo sha256 dello
   **scheletro ASCII** del vintage `3af47ed9`. **Una sola che non combacia e
   non viene sostituito NIENTE** (classe **449**).
3. **IMPRONTA ANCHE SUI FILE SCARICATI**, prima di scrivere: **2090 / 2190 /
   2122** righe e sha atteso. Pin sbagliato o cache di GitHub → muore **prima**
   di toccare il terminale.

📦 **Copia di sicurezza**: i tre originali finiscono sul **Desktop del VPS** in
`TOPPA_TICKET_50503392_<data>_<ora>\ORIGINALI\` **+ lo zip**
`TOPPA_TICKET_50503392_<data>_<ora>_ORIGINALI.zip`. **Viene fatta PRIMA del
controllo delle impronte**, di proposito: se l'impronta non torna, la base
giusta per rifare la toppa è già pronta da mandare, e non serve un secondo
giro sul VPS.

---

## 📋 COSA VEDI IN CONSOLE, e cosa deve dire

Alla fine esce questa tabella, **riletta dal disco del terminale**, non dedotta:

```
  FILE                            PRIMA    DOPO   ESITO
  ABTG_Nasdaq_Apertura_US.mq5      2032    2090   ENTRATA
  ABTG_DAX_Apertura_EU.mq5         2132    2190   ENTRATA
  ABTG_Dow_Apertura_US.mq5         2064    2122   ENTRATA
```

🔴 **Guarda la riga `ora:` in cima all'output** (formato `AAAA-MM-GG HH:MM:SS`,
ora locale del VPS): deve essere **adesso**. Se è vecchia, stai guardando uno
scroll di prima e non l'esecuzione appena fatta.

**Codici d'uscita:** `0` = tutte e tre entrate · `1` = fermato **prima** di
toccare qualunque cosa (bersaglio non certificato, impronta non combaciante,
download non verificato) · `2` = almeno una copia non è andata a segno (gli
originali sono nello zip).

---

## 👉 E POI TOCCA A TE — a mano, in MetaEditor

1. apri **MetaEditor** del terminale **50503392**
   (`C:\Program Files\BCM Markets MT5 Terminal`). Se non sei sicuro di quale
   finestra sia, incolla **questa riga di sola lettura** (stampa PID, titolo e
   cartella — **non tocca niente**):
   ```powershell
   Get-Process terminal64,metaeditor64 -ErrorAction SilentlyContinue | Select-Object Id, MainWindowTitle, Path | Format-Table -AutoSize
   ```
2. 🔴 **se uno dei tre `.mq5` è già aperto in una scheda di MetaEditor,
   chiudi la scheda senza salvare e riaprilo.** Altrimenti al primo salvataggio
   riscrivi sopra la toppa appena messa (e non te ne accorgi).
3. **F7** su `ABTG_Nasdaq_Apertura_US.mq5`,
   `ABTG_DAX_Apertura_EU.mq5`, `ABTG_Dow_Apertura_US.mq5` — **0 errori attesi**.
   ⚠️ **Detto prima, non dopo:** questi tre file **non sono mai stati
   compilati** (qui non c'è MetaEditor). **Il tuo primo F7 è anche il primo
   vero collaudo.** Se dà errore non è una sorpresa: è il collaudo che
   funziona — mandami il messaggio e lo sistemo.
4. in **MT5 50503392**: rimuovi e riattacca i tre EA, perché prendano il nuovo
   `.ex5`.

✅ **La verifica indipendente arriva da sola**: il runner delle **03:30**
(`CODA_06`) rilegge i sorgenti. La notte dopo deve stampare **2091 / 2191 /
2123** al posto di 2033 / 2133 / 2065.
📐 *Perché +1:* `CODA_06` conta le righe con uno `-split` su un file che finisce
con un a-capo, quindi stampa **sempre uno in più** del contenuto vero. Le righe
di contenuto sono **2090 / 2190 / 2122**. *(Il `LEGGIMI.md` della toppa dice
"2090 / 2190 / 2122" nel referto notturno: è la cifra giusta, letta con il
righello sbagliato. Qui è corretta.)*

---

## 🚦 ESITO DEL CANCELLO

Riportato per intero nel messaggio di consegna alla sessione principale:
strato 1 `backtest_pipeline/controlla_riga.py` su **lo script** e su **questo
documento**, più la checklist dei difetti già pagati e il **contro-esempio del
terminale sbagliato**.
