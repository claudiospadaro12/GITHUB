# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Segreti / ambiente locale
.env

# Output generati
report_output.html

# token sveglia report (mai committare)
.gh_report_token.txt

piano_trading/node_modules/
piano_trading/*.pdf
piano_trading/package*.json

# output generati dai report (dry-run/test)
weekly_output.html
report_output.html
weekly_output.html
weekly_report.pdf

# artefatti del banco di prova (rigenerabili)
backtest_pipeline/src_prove/
backtest_pipeline/src_pte/
backtest_pipeline/anteprima_*.ini
backtest_pipeline/gen_*.ini

# Copie di sicurezza delle pagelle prima di ogni rigenerazione (09/09/2026):
# servono a recuperare una Lettura cancellata per sbaglio, non a essere
# versionate -- sarebbero un duplicato di ogni giornata_*.md a ogni corsa.
report/.backup/

# worktree isolate degli agenti in background (16/09/2026): copie di lavoro
# temporanee create dal tool Agent con isolation:"worktree" -- non sono
# consegne, sono lo spazio di lavoro dell'agente mentre gira.
.claude/worktrees/

# Cache di PowerShell (pwsh) creata dai contro-esempi degli agenti: 19/09/2026.
# Non e roba di progetto e non va in repo.
.cache/

# Alberi finti dei contro-esempi degli agenti (falsi %APPDATA% con terminali MT5
# ricostruiti). Sono banchi di prova usa-e-getta: non vanno in repo. 20/09/2026.
casi/
