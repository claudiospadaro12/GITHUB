# Copia questo file in `.env` e compila i valori (NON committare `.env`).

# --- Claude / Anthropic ---
ANTHROPIC_API_KEY=sk-ant-...

# --- ChatGPT / OpenAI (opzionale: solo per il tavolo di confronto automatico) ---
# Senza questa chiave il confronto funziona lo stesso, in modalita' manuale
# (copia/incolla del dossier dentro ChatGPT). Vedi confronto_ia/README.md.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5

# --- Email (Gmail SMTP) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=tuoindirizzo@gmail.com
# App Password di Gmail (16 caratteri, NON la password normale). Vedi README.
SMTP_PASSWORD=xxxxxxxxxxxxxxxx
EMAIL_TO=claudiospadaro12@gmail.com

# --- Test ---
# FORCE_RUN=1   # esegue anche fuori dalle 07:00
# DRY_RUN=1     # non invia email, salva report_output.html
