# =====================================================================
#  PROVA v2 — ABTG_AltaVelocita, COMBO A: ciclo H4 -> ingresso M5
#  Lancio: ABTG_AltaVelocita -Modello 1 -Etichetta v2h4
#  (tesi v2 congelata l'11/08 sera in ALTA_VELOCITA_TESI.md)
# =====================================================================
# IPOTESI (dichiarata prima dei numeri): il pezzo che guadagna e' la
#   lettura delle PUNTE dell'RSI (una per ciclo, due decrescenti =
#   direzione, divergenza = via libera, doppio massimo = attesa), che
#   la v1 surrogava con un lookback. La v2 la implementa davvero.
# LO SWEEP (4 celle) — A/B a una variabile + severita' divergenza:
#   InpUseRsiPunte 0/1: 0 = v1.1 esatta (baseline, x2 = controllo
#     gemelli integrato), 1 = motore punte.
#   InpRequireDivergence 0/1: con punte accese, 0 = divergenza O
#     convergenza, 1 = SOLO divergenza (la piu' severa del manuale).
# LETTURA (ordine dichiarato dal costruttore della v2):
#   1. numero trade: col motore acceso DEVE calare (se no, bug);
#   2. PF; 3. peggior giornata %.
# CRITERI DI ACCETTAZIONE: gli stessi 4 congelati (positiva in entrambe
#   le finestre; PF OOS >= 1,10; vicini positivi; pegg. giornata > -2,5%
#   all'1%). OHLC = solo collaudo; verdetto ai tick.
# PATTO: v2 rossa su ENTRAMBE le combo ai tick = capitolo chiuso, no v3.
# =====================================================================
@SIMBOLO  GBPUSD
@PERIODO  M5
@DAQUANDO 2024.09.26

InpCicloTF=16388
InpStMult=3.0

InpUseRsiPunte=0||0||1||1||Y
InpRequireDivergence=0||0||1||1||Y

InpMagic=771402
