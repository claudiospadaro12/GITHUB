& { $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; $PIN='223dc6548b5cb2031e17988107b520544d839970'; $T0=Get-Date; if($env:COMPUTERNAME -ne 'DESKTOP-H4D7CAJ'){ throw ('QUESTA RIGA GIRA SOLO SUL PC DI BACKTEST DESKTOP-H4D7CAJ. Qui la macchina si chiama: ' + $env:COMPUTERNAME + '. Sul VPS VMI3047753 non si lancia: la challenge FTMO 541452707 sta operando.') }; if((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0){ throw 'MT5 risulta APERTO su questo PC. PRIMA di chiuderlo GUARDA I GRAFICI: questo terminale e C:\Program Files\BCM Markets MT5 Terminal ed e loggato sul DEMO 50503392, lo stesso conto del piccolo sul VPS. Il 14/08/2026 da QUESTA macchina sono partiti ordini VERI (#3160534/#3160535, -104,60). Se ci sono SEDIE attaccate ai grafici, staccale. Poi chiudi il terminale A MANO e rilancia.' }; $W=Join-Path $env:USERPROFILE 'abtg_round'; New-Item -ItemType Directory -Force -Path $W | Out-Null; $S=Join-Path $W 'RIGA_ROUND_VPS.ps1'; Remove-Item -LiteralPath $S -Force -ErrorAction SilentlyContinue; irm ('https://raw.githubusercontent.com/claudiospadaro12/GITHUB/' + $PIN + '/backtest_pipeline/righe/RIGA_ROUND_VPS.ps1?cb=' + [Guid]::NewGuid().ToString('N')) -OutFile $S; if(-not (Select-String -LiteralPath $S -SimpleMatch -Pattern 'MARCATORE_RIGA_ROUND_VPS_v2' -Quiet)){ throw 'RIGA_ROUND_VPS.ps1 scaricata SENZA il marcatore v2: copia vecchia o cache di GitHub. Non si parte.' }; Write-Host ('pc  : ' + $env:COMPUTERNAME) -ForegroundColor Green; Write-Host ('pin : ' + $PIN) -ForegroundColor Green; Write-Host ('data: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss')) -ForegroundColor Green; Write-Host 'R244 -- il TASSO DI RIEMPIMENTO di ABTG_MaxMinNotte su D30EUR M15 TICK REALI, due lati separati. Asse InpEntryCutoffHour 9..17 = 9 celle per file. 2 file, 18 celle, 18 passate VERE (+18 sulla gamba OOS degenere, costo ~0). E la famiglia della sedia viva FTMO 770411: quella sedia NON viene toccata (qui magic 762500/762510, nessun preset, nessun terminale del VPS).' -ForegroundColor Cyan; Write-Host 'TEMPO ATTESO: 4-6 minuti. Misura di QUESTO EA su QUESTA macchina, un agente solo: R242 del 24/09 (ABTG_MaxMinNotte D30EUR M15 tick reali, stessa finestra, stesso deposito) = 14 passate vere in ~3 minuti (07:18-07:21), cioe 0,214 min/passata con compilazione e avvii inclusi; 18 passate x 0,214 = 3,9 minuti. TETTO 12 minuti (il doppio della banda alta: la base e UNA corsa sola, letta al minuto, e qui la coda destra ha piu operazioni). NON fermarla prima di 20 minuti: oltre i 20 non e lenta, e bloccata.' -ForegroundColor Yellow; Write-Host 'ATTESO E NORMALE, NON E UN GUASTO: @FRAZIONEIS 1.0 = UNA SOLA TRANCHE, la gamba OOS e DEGENERE e tutti e due i round usciranno con rc 2 = NON MISURATO (CSV OOS mancanti o vuoti). IL NUMERO DA LEGGERE E IL CSV _IS. Il guasto vero sarebbe rc 1 (non e partito).' -ForegroundColor Yellow; Write-Host 'BERSAGLIO: SOLO il terminale del PC di backtest (C:\Program Files\BCM Markets MT5 Terminal, demo 50503392). NON TOCCATI, per nome: VPS VMI3047753 e TUTTE le sue cartelle dati -- FTMO 541452707 (C:\FTMO, sei sedie vive), REALE 10105439 (C:\BCM_Reale), 100k 50504263, piccolo 50503392, manuale 50503635 (C:\MT5_MANUALE), banco 50504400 (C:\MT5_Backtest), Pepperstone, Tickmill. Questa riga NON chiude nessun processo: se trova MT5 aperto si FERMA.' -ForegroundColor Yellow; Write-Host 'PROMEMORIA CHE VALE ANCHE A TERMINALE GIA CHIUSO (li la guardia qui sopra non parla): il driver APRE quel terminale per far girare il tester e poi lo spegne da solo, e quel terminale e loggato sul DEMO 50503392. Se ai grafici sono rimaste SEDIE attaccate con Algo Trading acceso, quelle OPERANO DAVVERO mentre il tester gira -- il 14/08/2026 da questa macchina e successo. Se non sei sicuro, fermati ADESSO con Ctrl+C, apri il terminale, guarda i grafici, stacca le sedie, chiudilo e rilancia.' -ForegroundColor Yellow; Write-Host 'Deposito 100000 PASSATO ESPLICITAMENTE (par. 13 dei due file prova): il default del driver e 10000, e il CONTROLLO 1 (la cella C=12 deve riprodurre alla cifra l ancora di R242, girato a 100000) fallirebbe per un motivo finto. -Spread NON passato, come in R242: deve restare lo stesso.' -ForegroundColor Yellow; $ErrorActionPreference='Continue'; $J=@(@{e='ABTG_MaxMinNotte';p='R244a_riempimento_MaxMinNotte_D30EUR_short.txt';t='R244a';m=4},@{e='ABTG_MaxMinNotte';p='R244b_riempimento_MaxMinNotte_D30EUR_long.txt';t='R244b';m=4}); $sha166=@{}; $sha166[(Join-Path $W 'src_prove\ABTG_MaxMinNotte.mq5')]='9346F16A4CAAD4CF18EE9B772CAF06A497B3C6727E0D0B65250E377E75BFAE80'; $sha166[(Join-Path $W 'src_include\ABTG_PausaGuardian.mqh')]='3EC971152E85E0082488CC4243FF45AE09948C191D52AB96050B48F94641A737'; $nDiv=0; $esiti=New-Object System.Collections.ArrayList; $dsk=[Environment]::GetFolderPath('Desktop'); foreach($x in $J){ $vec=Join-Path $dsk ('ROUND_' + $x.t); if(Test-Path -LiteralPath $vec){ Remove-Item -LiteralPath $vec -Recurse -Force -ErrorAction SilentlyContinue; Write-Host ('  tolta la cartella di una corsa PRECEDENTE: ROUND_' + $x.t) -ForegroundColor DarkYellow }; $vez=Join-Path $dsk ('ROUND_' + $x.t + '.zip'); if(Test-Path -LiteralPath $vez){ Remove-Item -LiteralPath $vez -Force -ErrorAction SilentlyContinue } }; foreach($x in $J){ $E=$x.e; $P=$x.p; $L=$x.t; $M=$x.m; $nAtt=0; while((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0 -and $nAtt -lt 36){ Start-Sleep -Seconds 5; $nAtt=$nAtt+1 }; if((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0){ Write-Host ('ATTENZIONE: dopo 180 secondi MT5 e ancora VIVO. Il round ' + $L + ' parte lo stesso, ma il pre-volo del driver lo fermera con rc 1: NON sara misurato.') -ForegroundColor Red }; Write-Host ''; Write-Host ('=== ' + $L + '   ' + $E + '   ' + $P + '   modello ' + $M + '   deposito 100000 ===') -ForegroundColor Cyan; & powershell.exe -NoProfile -ExecutionPolicy Bypass -File $S -Expert $E -Prova $P -Etichetta $L -Pin $PIN -Modello $M -Deposito 100000; $rc=$LASTEXITCODE; $div=@(); if($rc -ne 1){ foreach($k in @($sha166.Keys)){ if(-not (Test-Path -LiteralPath $k)){ $div += ((Split-Path -Leaf $k) + ' ASSENTE') } elseif((Get-FileHash -LiteralPath $k -Algorithm SHA256).Hash -ne $sha166[$k]){ $div += ((Split-Path -Leaf $k) + ' SHA256 DIVERSO DAL PIN') } } }; if($rc -eq 1){ $m166='motore NON VERIFICATO (non e partito)' } elseif($div.Count -eq 0){ $m166='motore = pin (SHA256)' } else { $m166=('MOTORE DIVERSO DAL PIN: ' + ($div -join ', ')); $nDiv=$nDiv+1 }; [void]$esiti.Add(($L.PadRight(6) + ' modello ' + $M + '   rc ' + $rc + '   ' + $m166)) }; $out=Join-Path $dsk 'ROUND_R244_2026-09-24'; if(Test-Path -LiteralPath $out){ Remove-Item -LiteralPath $out -Recurse -Force -ErrorAction SilentlyContinue }; New-Item -ItemType Directory -Force -Path $out | Out-Null; $nCart=0; $manc=New-Object System.Collections.ArrayList; foreach($x in $J){ $src=Join-Path $dsk ('ROUND_' + $x.t); if(Test-Path -LiteralPath $src){ Copy-Item -LiteralPath $src -Destination $out -Recurse -Force -ErrorAction SilentlyContinue; $nCart=$nCart+1 } else { [void]$manc.Add($x.t) } }; $lg=Join-Path $out 'LOG_TESTER'; New-Item -ItemType Directory -Force -Path $lg | Out-Null; $nl=0; foreach($f in @(Get-ChildItem -Path (Join-Path $env:APPDATA 'MetaQuotes') -Recurse -Filter '*.log' -ErrorAction SilentlyContinue | Where-Object { $_.LastWriteTime -ge $T0 } | Sort-Object LastWriteTime -Descending | Select-Object -First 200)){ Copy-Item -LiteralPath $f.FullName -Destination (Join-Path $lg (([string]$nl).PadLeft(4,'0') + '_' + $(if($f.Directory.Parent){$f.Directory.Parent.Name}else{'ignota'}) + '_' + $f.Directory.Name + '_' + $f.Name)) -Force -ErrorAction SilentlyContinue; $nl=$nl+1 }; $ptd=Join-Path $out 'PERTRADE'; New-Item -ItemType Directory -Force -Path $ptd | Out-Null; $npt=0; foreach($mg in @('762510','762500')){ $ptf=Join-Path $env:APPDATA ('MetaQuotes\Terminal\Common\Files\abtg_trades_ABTG_MaxMinNotte_D30EUR_' + $mg + '.csv'); if((Test-Path -LiteralPath $ptf) -and ((Get-Item -LiteralPath $ptf).LastWriteTime -ge $T0)){ Copy-Item -LiteralPath $ptf -Destination $ptd -Force; $npt=$npt+1 } }; $ri=New-Object System.Collections.ArrayList; [void]$ri.Add('RIEPILOGO R244 -- tasso di riempimento, InpEntryCutoffHour 9..17 (ABTG_MaxMinNotte, D30EUR M15, lati separati)'); [void]$ri.Add('data: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss')); [void]$ri.Add('fine: ' + (Get-Date).ToString('yyyy-MM-dd HH:mm:ss') + '   durata minuti: ' + [int](((Get-Date)-$T0).TotalMinutes) + '   (stima 4-6; tetto 12; oltre 20 e BLOCCATA)'); [void]$ri.Add('pc  : ' + $env:COMPUTERNAME + '   (PC di backtest; VPS VMI3047753 NON toccato)'); [void]$ri.Add('pin : ' + $PIN); [void]$ri.Add('deposito 100000 passato (come R242); -Spread NON passato = INCOGNITA DICHIARATA, identica a R242'); [void]$ri.Add('FRAZIONEIS 1.0 = una tranche sola: la gamba OOS e DEGENERE, i suoi CSV non si leggono'); [void]$ri.Add(''); foreach($z in $esiti){ [void]$ri.Add($z) }; [void]$ri.Add(''); [void]$ri.Add('rc 0 = ROUND GIRATO | 2 = NON MISURATO | 3 = GIRATO CON RILIEVI | 1 = non e partito'); [void]$ri.Add('QUI rc 2 E ATTESO SU TUTTI E DUE E NON E UN GUASTO (FRAZIONEIS 1.0: la gamba OOS non produce CSV). Si legge il CSV _IS. Guasto vero = rc 1.'); [void]$ri.Add('CLASSE 166 (EA e include arrivano dal RAMO lavoro, non dal pin): ' + $(if($nDiv -eq 0){'MOTORE = PIN in ogni round partito'}else{'MOTORE DIVERSO DAL PIN in ' + $nDiv + ' round: il CONTROLLO 1 (ancora C=12) NON si legge, si rifa il pin sul ramo e si rilancia'})); [void]$ri.Add(''); [void]$ri.Add('CARTELLE ATTESE: 2   TROVATE: ' + $nCart + $(if($nCart -eq 2){'   -> CATENA COMPLETA'}else{'   -> CATENA ROTTA, MANCANO: ' + ($manc -join ', ')})); [void]$ri.Add(''); [void]$ri.Add('PRIMA DI QUALUNQUE NUMERO, il par. 5 dei file prova. CONTROLLO 1 -- la cella C=12 riproduce R242 alla cifra: R244a SHORT n ESATTAMENTE 101, PF entro 0,00002 da 1,40859 (DD 2,0209, Profit +2799,28); R244b LONG n ESATTAMENTE 157, PF entro 0,00002 da 0,74387 (DD 5,6463, Profit -3321,63). Se non torna, NESSUN PF di quel file si legge.'); [void]$ri.Add('CONTROLLO 2 -- n NON DECRESCENTE lungo tutto l asse 9..17, senza una sola eccezione (teorema sul codice, non attesa statistica).'); [void]$ri.Add('UNITA: ogni n del CSV e in DEAL (InpTP1Pct=50, uscita frazionata), NON in posizioni. k = deal/posizioni NON esce dai CSV di ottimizzazione: lo puo dare SOLO il PERTRADE qui sotto (UNA cella, l ultima eseguita). Se non si identifica, serve la passata SINGOLA a C=12 (par. 6-bis), che deve dare esattamente 101 deal sullo short. Finche k manca, il verdetto sul pavimento dei 150 e NON ANCORA MISURATO, mai n>=150 RAGGIUNTO. Il DD si legge lo stesso (Emendamento B).'); [void]$ri.Add('PERTRADE (Common Files, magic 762510 e 762500, scritti DOPO l avvio): ' + $npt + ' su 2. E la ULTIMA passata eseguita di ciascun lato: righe = deal di uscita, si identifica la cella confrontandole con la colonna Trades del CSV _IS; posizioni = position_id distinti; k = righe / posizioni. Se la cella non si identifica in modo univoco o il file ha zero righe, k resta NON MISURATO.'); [void]$ri.Add('Le 9 celle sono NIDIFICATE: si leggono col PF MARGINALE fra celle contigue, non come 9 prove indipendenti (par. 8-bis).'); ($ri -join "`r`n") | Set-Content -LiteralPath (Join-Path $out 'RIEPILOGO_R244.txt') -Encoding ASCII; $zip=Join-Path $dsk 'ROUND_R244_2026-09-24.zip'; if(Test-Path -LiteralPath $zip){ Remove-Item -LiteralPath $zip -Force -ErrorAction SilentlyContinue }; Compress-Archive -Path (Join-Path $out '*') -DestinationPath $zip -Force; Write-Host ''; Write-Host ('RACCOLTA: ' + $out); Write-Host ('ZIP PRONTO DA MANDARE: ' + $zip) -ForegroundColor Green; if($nCart -eq 2){ Write-Host 'CATENA COMPLETA: 2 cartelle su 2.' -ForegroundColor Green } else { Write-Host ('CATENA ROTTA: attese 2, TROVATE ' + $nCart + '. MANCANO: ' + ($manc -join ', ')) -ForegroundColor Red }; if($nDiv -eq 0){ Write-Host 'CLASSE 166: il motore compilato e quello del pin in ogni round partito (SHA256).' -ForegroundColor Green } else { Write-Host ('CLASSE 166: MOTORE DIVERSO DAL PIN in ' + $nDiv + ' round. il CONTROLLO 1 (ancora C=12) NON si legge: si rifa il pin sul ramo e si rilancia.') -ForegroundColor Red }; Write-Host ('   LOG_TESTER = ' + $nl + ' file .log (servono se un round esce NON MISURATO). NON contengono i contatori dell imbuto: le passate girano in Optimization=1, cioe sugli AGENTI, e li le Print non si leggono.') -ForegroundColor Gray; Get-ChildItem -LiteralPath $out | Select-Object Name,LastWriteTime | Format-Table -AutoSize | Out-Host; Write-Host 'ESITI:' -ForegroundColor Cyan; foreach($z in $esiti){ Write-Host ('   ' + $z) }; Write-Host ('durata totale minuti: ' + [int](((Get-Date)-$T0).TotalMinutes)) -ForegroundColor Cyan }