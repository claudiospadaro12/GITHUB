& { if($env:COMPUTERNAME -ne 'VMI3047753'){ throw ('VIETATO: questa riga si incolla SOLO nella finestra PowerShell del VPS VMI3047753. Macchina attuale: ' + $env:COMPUTERNAME + '. Qui non si esegue niente.') }; $ErrorActionPreference='Continue'; Write-Host 'BERSAGLIO: finestra PowerShell sul VPS VMI3047753. SOLA LETTURA: legge il giornale Esperti di ieri (25/09) e di oggi della cartella dati di C:\FTMO (conto 541452707) e stampa le righe di avvio e CONFIG IN USO delle sedie DAX Apertura (770101 e 770105). Nessun terminale toccato, nessun file scritto, nessun processo aperto o chiuso.' -ForegroundColor Yellow; Write-Host 'NON TOCCATI, per nome: FTMO 541452707 (solo LETTO il suo giornale), REALE 10105439 (C:\BCM_Reale), 100k 50504263 (BCM Markets MT5 Terminal -V3), piccolo 50503392 (BCM Markets MT5 Terminal), manuale 50503635 (C:\MT5_MANUALE), banco 50504400 (C:\MT5_Backtest), Pepperstone, Tickmill.' -ForegroundColor Yellow; Write-Host '--- terminali vivi (PID, titolo, cartella) ---' -ForegroundColor Cyan; Get-Process terminal64 -ErrorAction SilentlyContinue | Select-Object Id, MainWindowTitle, Path | Format-List; $D=Join-Path $env:APPDATA 'MetaQuotes\Terminal\46C9F8E9FF0C747B2B5E09BCC13D5237\MQL5\Logs'; if(-not (Test-Path -LiteralPath $D)){ Write-Host ('Cartella non trovata: ' + $D + ' -> la cartella dati di C:\FTMO non e questa: mandami una foto di questa finestra.') -ForegroundColor Red; return }; $tot=0; foreach($g in @('20260925','20260926')){ $f=Join-Path $D ($g + '.log'); Write-Host ('=== ' + $f) -ForegroundColor Cyan; if(-not (Test-Path -LiteralPath $f)){ Write-Host '    (file assente)'; continue }; $t=''; try { $t = Get-Content -LiteralPath $f -Raw -Encoding Unicode -ErrorAction Stop } catch { Write-Host ('    file non leggibile adesso: ' + $_.Exception.Message) -ForegroundColor Red; continue }; $righe = @($t -split "`r?`n" | Where-Object { $_ -match 'DAX Apertura EU' -and $_ -match 'avviato su|CONFIG IN USO|lati=' }); $tot += $righe.Count; foreach($r in $righe){ if($r -match 'SOLO SHORT'){ Write-Host ('    ' + $r) -ForegroundColor Green } elseif($r -match 'SOLO LONG'){ Write-Host ('    ' + $r) -ForegroundColor White } else { Write-Host ('    ' + $r) -ForegroundColor Gray } } }; Write-Host ('--- righe trovate: ' + $tot + ' ---') -ForegroundColor Cyan; Write-Host 'COME SI LEGGE: la 770105 e attaccata bene se il 25/09 intorno alle 20:47 (ora del PC) c e una riga CONFIG IN USO con lati=SOLO SHORT e rischio=2.00% (verde), e la 770101 resta lati=SOLO LONG (bianca; se la 770101 non e stata riavviata il 25/09 la sua riga puo mancare: NON e un problema, il grafico salvato la conferma SOLO LONG). Se le righe verdi sono ZERO: NON VERIFICATO, mandami una foto della scheda Esperti del terminale C:\FTMO. Mandami comunque una foto di questa finestra.' -ForegroundColor Yellow }
