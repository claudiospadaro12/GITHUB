& { if($env:COMPUTERNAME -ne 'VMI3047753'){ throw ('VIETATO: questa riga si incolla SOLO nella finestra PowerShell del VPS VMI3047753. Macchina attuale: ' + $env:COMPUTERNAME + '. Qui non si esegue niente.') }; $ErrorActionPreference='Continue'; Write-Host 'BERSAGLIO: finestra PowerShell sul VPS VMI3047753. SOLA LETTURA: legge i giornali di ieri (25/09) e di oggi della cartella dati di C:\FTMO (conto 541452707): scheda Esperti (righe di avvio e CONFIG IN USO delle sedie DAX Apertura 770101 e 770105) e Giornale del terminale (loaded/removed di DAX_Apertura_EU). Nessun terminale toccato, nessun file scritto, nessun processo aperto o chiuso.' -ForegroundColor Yellow; Write-Host 'NON TOCCATI, per nome: FTMO 541452707 (solo LETTI i suoi giornali), REALE 10105439 (C:\BCM_Reale), 100k 50504263 (BCM Markets MT5 Terminal -V3), piccolo 50503392 (BCM Markets MT5 Terminal), manuale 50503635 (C:\MT5_MANUALE), banco 50504400 (C:\MT5_Backtest), Pepperstone, Tickmill.' -ForegroundColor Yellow; Write-Host '--- terminali vivi (PID, titolo, cartella) ---' -ForegroundColor Cyan; Get-Process terminal64 -ErrorAction SilentlyContinue | Select-Object Id, MainWindowTitle, Path | Format-List; $B=Join-Path $env:APPDATA 'MetaQuotes\Terminal\46C9F8E9FF0C747B2B5E09BCC13D5237'; if(-not (Test-Path -LiteralPath (Join-Path $B 'MQL5\Logs'))){ Write-Host ('Cartella non trovata: ' + $B + '\MQL5\Logs -> la cartella dati di C:\FTMO non e questa: mandami una foto di questa finestra.') -ForegroundColor Red; return }; $tot=0; $ko=0; foreach($k in @('MQL5\Logs','Logs')){ foreach($g in @('20260925','20260926')){ $f=Join-Path $B ($k + '\' + $g + '.log'); Write-Host ('=== ' + $k + '\' + $g + '.log') -ForegroundColor Cyan; if(-not (Test-Path -LiteralPath $f)){ Write-Host '    (file assente)'; continue }; $t=''; try { $t = Get-Content -LiteralPath $f -Raw -Encoding Unicode -ErrorAction Stop } catch { $ko++; Write-Host ('    FILE NON LETTO (non e uno zero): ' + $_.Exception.Message) -ForegroundColor Red; continue }; $righe = @($t -split "`r?`n" | Where-Object { ($_ -match '\[DAX Apertura EU\] (avviato su|CONFIG IN USO)') -or ($_ -match 'expert \S*DAX_Apertura_EU .*(loaded successfully|removed)') }); $tot += $righe.Count; foreach($r in $righe){ if($r -match 'lati=SOLO SHORT'){ Write-Host ('    ' + $r) -ForegroundColor Green } elseif($r -match 'lati=SOLO LONG'){ Write-Host ('    ' + $r) -ForegroundColor White } elseif($r -match 'removed'){ Write-Host ('    ' + $r) -ForegroundColor Red } else { Write-Host ('    ' + $r) -ForegroundColor Gray } } } }; Write-Host ('--- righe trovate: ' + $tot + '   file NON letti: ' + $ko + ' ---') -ForegroundColor Cyan; if($ko -gt 0){ Write-Host 'ATTENZIONE: almeno un file non si e potuto leggere: il conteggio qui sopra NON e completo.' -ForegroundColor Red }; Write-Host 'COME SI LEGGE (tutte le ore sono ora del PC, non ora server): la prova e una riga VERDE in MQL5\Logs\20260925.log intorno alle 20:47 con CONFIG IN USO ... lati=SOLO SHORT | rischio=2.00%. Quella riga NON stampa il magic: la prova e il lato, perche su C:\FTMO la sola sedia DAX Apertura con lati=SOLO SHORT e la 770105 (la 770101 e SOLO LONG; se non e stata riavviata il 25/09 la sua riga bianca puo mancare e NON e un problema). La riga verde dice che la sedia e PARTITA con quei parametri, non che e ancora attaccata: per quello guarda in Logs le righe rosse removed di DAX_Apertura_EU DOPO le 20:47 (se ce ne sono, mandamele). Non dice nemmeno se la faccina e sorridente. Se le righe verdi sono ZERO: NON VERIFICATO, mandami una foto della scheda Esperti del terminale C:\FTMO. Mandami comunque una foto di questa finestra.' -ForegroundColor Yellow }
