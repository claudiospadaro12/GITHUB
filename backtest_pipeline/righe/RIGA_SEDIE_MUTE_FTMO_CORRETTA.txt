& { if($env:COMPUTERNAME -ne 'VMI3047753'){ throw ('VIETATO: questa riga si incolla SOLO nella finestra PowerShell del VPS VMI3047753. Macchina attuale: ' + $env:COMPUTERNAME + '. Qui non si esegue niente.') }; $ErrorActionPreference='Continue'; $INV=[Globalization.CultureInfo]::InvariantCulture; $t0=Get-Date; $BERS='C:\FTMO'; $CONTO='541452707'; $BUDG=75; $MAXF=44; $MAXB=4194304; $NLOG=8; $MAXCHR=400; $SCAD=0; $oggiF=$t0.ToString('yyyyMMdd',$INV); $ieriF=$t0.AddDays(-1).ToString('yyyyMMdd',$INV); $FOK=0; $FKO=0; $BYT=0; $TRONCO=0; $PARZ=0; Write-Host ''; Write-Host ('=== SEDIE FTMO: ARMATE O MUTE? -- SOLA LETTURA -- ' + $t0.ToString('yyyy-MM-dd HH:mm:ss',$INV) + ' (ora Windows del VPS = ora italiana) ===') -ForegroundColor Cyan; Write-Host ('    macchina ' + $env:COMPUTERNAME + '   utente ' + $env:USERNAME + '   -- le cartelle dati si vedono SOLO dal profilo che fa girare i terminali: se questo utente non e quello, le tabelle escono corte, e NON vuol dire che le sedie non ci sono.'); Write-Host ('    BERSAGLIO: il terminale ' + $BERS + ', conto ' + $CONTO + '. NIENTE ALTRO.') -ForegroundColor Green; Write-Host ('    NON SCRIVE NIENTE, da nessuna parte, su nessuno dei sette terminali di questa macchina: 50503392 (BCM Markets MT5 Terminal), 50504263 (-V3), 10105439 (C:\BCM_Reale), 50504400 (C:\MT5_Backtest), 50503635, Pepperstone, Tickmill.') -ForegroundColor Green; Write-Host ('    E DI QUEI SETTE APRE UN FILE SOLO, e te lo dico invece di lasciartelo dedurre: origin.txt -- una riga di testo, dentro la CARTELLA DATI di ognuno, che contiene il percorso di installazione. Serve a riconoscere PER NOME quale cartella dati e quella di ' + $BERS + ' invece di indovinarla (regola dei terminali multipli). Di quelle sette cartelle NON apre altro: ne log, ne preset, ne .chr, ne storico, ne saldo. SI, questo vuol dire che apre un file di testo anche nella cartella dati del conto reale 10105439: e sola lettura e non blocca niente, ma la decisione e tua. Se non la vuoi, si toglie -- e allora la cartella dati del bersaglio va indicata a mano.') -ForegroundColor Yellow; Write-Host '    QUESTA RIGA LEGGE E BASTA: nessun tester avviato (il 21/09 un tester sul VPS ha inchiodato la macchina mentre le sedie operavano), nessun processo avviato o chiuso, nessun file scritto o cancellato, nessun EA attaccato o staccato, nessun .chr e nessun preset modificati, nessuna GlobalVariable toccata, nessun MT5 aperto.' -ForegroundColor Green; Write-Host ''; Write-Host '    GLI OROLOGI, e sono TRE (senza questa legenda i numeri qui sotto non vogliono dire niente):' -ForegroundColor Yellow; Write-Host '      [L] ora LOCALE del PC = ora italiana: e quella dei NOMI dei file di log (AAAAMMGG.log), degli orari stampati DENTRO i log da MT5, e di ogni data di file.'; Write-Host '      [S] ora SERVER FTMO = ora italiana +1 (FTMO = BCM+2, BCM = italiana -1): e quella del GRAFICO, di TimeCurrent(), degli InpSessionHour dei preset e della colonna open_time del CSV del TradeExporter.'; Write-Host '      [!] E DENTRO IL TESTO di una riga un EA puo CITARE un orario di SERVER (esempio: avviato su GER40.cash. Apertura server 10:00): il timbro a inizio riga e [L], il numero citato nella frase e [S]. Il 06/08 un EA fu dichiarato in ritardo di un ora perche i due orari furono letti come se fossero lo stesso.'; Write-Host '      [B] ora SERVER BCM = ora italiana -1: NON compare qui dentro, e citata solo per non confonderla con [S]. Un orario senza etichetta in questo stampato e un difetto, non un dettaglio.'; Write-Host ('    TETTI DI COSTO (qui operano sei sedie vere, e il 21/09 un tester ha inchiodato questa macchina): ' + $BUDG + ' secondi, e il cronometro e controllato PRIMA di ogni file in TUTTE le sezioni che aprono file (1 cartelle dati, 2 grafici, 3 giornali); max ' + ($NLOG*2) + ' file di log (gli ultimi ' + $NLOG + ' per cartella, con tetto di sicurezza ' + $MAXF + '); max ' + $MAXCHR + ' file .chr; max ' + [math]::Round($MAXB/1MB,0) + ' MB letti per file (oltre si legge la CODA del file e si marca PARZIALE). Nessun processo avviato e nessun tester: e un ciclo di lettura su un core solo, i file si aprono in FileShare.ReadWrite cosi MT5 continua a scriverci sopra senza accorgersene. Cio che non fa in tempo esce marcato TRONCO: MAI con uno zero.'); function LC($p){ $r=[pscustomobject]@{T='';B=0;Tot=0;E='ILLEGGIBILE';P=0}; $fs=$null; try{ $fs=[IO.File]::Open($p,[IO.FileMode]::Open,[IO.FileAccess]::Read,[IO.FileShare]::ReadWrite) }catch{ return $r }; try{ $len=$fs.Length; $r.Tot=$len; if($len -eq 0){ $r.E='VUOTO'; $fs.Dispose(); return $r }; $off=0; $tak=$len; if($len -gt $MAXB){ $off=$len-$MAXB; $off=$off-($off % 2); $tak=$len-$off; $r.P=1 }; [void]$fs.Seek($off,[IO.SeekOrigin]::Begin); $b=[byte[]]::new($tak); $got=0; while($got -lt $tak){ $k=$fs.Read($b,$got,$tak-$got); if($k -le 0){ break }; $got+=$k }; $fs.Dispose(); $r.B=$got; if($got -eq 0){ $r.E='ZERO BYTE LETTI'; return $r }; $z=0; $n=[math]::Min(400,$got); for($i=1;$i -lt $n;$i+=2){ if($b[$i] -eq 0){ $z++ } }; if($got -ge 2 -and $b[0] -eq 255 -and $b[1] -eq 254){ $r.T=[Text.Encoding]::Unicode.GetString($b,2,$got-2); $r.E='UTF16-BOM' } elseif($z -gt ($n/4)){ $r.T=[Text.Encoding]::Unicode.GetString($b,0,$got); $r.E='UTF16-noBOM' } else { $r.T=[Text.Encoding]::UTF8.GetString($b,0,$got); $r.E='UTF8/ANSI' }; return $r }catch{ try{ $fs.Dispose() }catch{ }; return $r } }; function BEX($t){ $m=[regex]::Match($t,'(?s)<expert>(.*?)</expert>'); if($m.Success){ return $m.Groups[1].Value }; return '' }; function BIN($b){ if(-not $b){ return '' }; $m=[regex]::Match($b,'(?s)<inputs>(.*?)</inputs>'); if($m.Success){ return $m.Groups[1].Value }; return '' }; function CAMPO($t,$nome){ if(-not $t){ return '-' }; $m=[regex]::Match($t,'(?im)^[ \t]*' + [regex]::Escape($nome) + '[ \t]*=[ \t]*(.*?)[ \t]*$'); if($m.Success -and $m.Groups[1].Value.Trim().Length -gt 0){ return $m.Groups[1].Value.Trim() }; return '-' }; function TAGLIA($s,$n){ if($null -eq $s){ return '' }; $s=($s -replace '[^\u0020-\u007E]',' ').Trim(); if($s.Length -gt $n){ return $s.Substring(0,$n-3) + '...' }; return $s }; $SEDIE=@(); $SEDIE+=[pscustomobject]@{M='770101';F='DAX_Apertura_EU';E='DAX Apertura EU   (banco D30EUR M5)'}; $SEDIE+=[pscustomobject]@{M='770411';F='MaxMinNotte_DAX_Short_Ottimizzato';E='MaxMin DAX Short  (banco D30EUR M15)'}; $SEDIE+=[pscustomobject]@{M='770202';F='Dow_Apertura_US';E='Dow Apertura US   (banco U30USD M5)'}; $SEDIE+=[pscustomobject]@{M='771531';F='EMA200';E='EMA200 Dow        (banco U30USD H1)'}; $SEDIE+=[pscustomobject]@{M='770511';F='SuperWave_DOW_H1_Ottimizzato';E='SuperWave DOW     (banco U30USD H1)'}; $SEDIE+=[pscustomobject]@{M='770260';F='Nasdaq_Apertura_US';E='Nasdaq RETEST     (banco NASUSD M5)'}; $SEDIE+=[pscustomobject]@{M='779001';F='Guardian';E='Guardian v1.12    (NON trada: sorveglia)'}; $ST=@{}; foreach($s in $SEDIE){ $ST[$s.M]=[pscustomobject]@{Sedia=$s; Chr='';Nome='';Sym='';Per='';MagicChr='';Verb='';Guard='';Prof=''; Oggi=0;Fin=0;Arm=0;NoGo=0;Fail=0;Avv=0; Ult='';UltF=''; ArmEs='';NoGoEs='';FailEs=''; Csv=0; CsvUlt=''} }; Write-Host ''; Write-Host '    LA ROSA CERCATA, per nome e per magic (classe 605: sul terminale FTMO gli EA sono rinominati ABTG_* -> CLAU12_*, quindi ogni ricerca qui accetta TUTTI E DUE i prefissi; una regex col solo ABTG_ troverebbe zero e sembrerebbe un guasto):' -ForegroundColor Yellow; foreach($s in $SEDIE){ Write-Host ('      ' + $s.M + '  (ABTG_|CLAU12_)' + $s.F.PadRight(38) + $s.E) }; Write-Host ''; Write-Host '--- 0. IL TERMINALE E ACCESO? (Get-Process: lettura pura, nessun processo toccato) ---' -ForegroundColor Cyan; $pr=@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue); $prB=@(); $prAltri=0; foreach($p in $pr){ $pp=''; try{ $pp=$p.Path }catch{ $pp='' }; if($pp -and $pp.ToUpperInvariant().StartsWith($BERS.ToUpperInvariant())){ $prB+=$p } else { $prAltri++ } }; Write-Host ('    processi terminal64 vivi in tutto: ' + $pr.Count + '   di cui dentro ' + $BERS + ': ' + $prB.Count + '   altri (NON sono il bersaglio: li conto e basta, non li nomino e non li tocco): ' + $prAltri); foreach($p in $prB){ $tt=''; try{ $tt=$p.MainWindowTitle }catch{ $tt='' }; $st='(ora di avvio non leggibile da questo utente)'; try{ $st=$p.StartTime.ToString('yyyy-MM-dd HH:mm',$INV) }catch{ }; Write-Host ('    BERSAGLIO VIVO: PID ' + $p.Id + '   avviato ' + $st + ' [L]   titolo: ' + (TAGLIA $tt 70)) -ForegroundColor Green }; if($prB.Count -eq 0){ Write-Host ('    NESSUN processo terminal64 con percorso dentro ' + $BERS + '. Puo voler dire terminale SPENTO, oppure che questo utente non vede il percorso degli altri processi (serve lo stesso utente). Le sedie non possono operare con il terminale spento: se qui esce 0, tutto cio che segue e STORIA, non stato vivo.') -ForegroundColor Yellow }; $root=''; if($env:APPDATA){ $root=Join-Path $env:APPDATA 'MetaQuotes\Terminal' }; $cand=@(); Write-Host ''; Write-Host '--- 1. LA CARTELLA DATI DEL BERSAGLIO (certificata da origin.txt, non indovinata) ---' -ForegroundColor Cyan; if($root -eq '' -or -not (Test-Path -LiteralPath $root)){ Write-Host ('    NON ESISTE la radice delle cartelle dati [' + $root + ']: questa riga gira come utente ' + $env:USERNAME + '. Se i terminali girano sotto un ALTRO utente, riaprire PowerShell con quello.') -ForegroundColor Yellow } else { foreach($d in @(Get-ChildItem -LiteralPath $root -Directory -ErrorAction SilentlyContinue | Sort-Object Name)){ if(((Get-Date)-$t0).TotalSeconds -ge $BUDG){ $SCAD=1; Write-Host '    *** TEMPO SCADUTO nella sezione 1: le cartelle dati restanti NON sono state guardate. ***' -ForegroundColor Red; break }; $o=Join-Path $d.FullName 'origin.txt'; $t='(nessun origin.txt)'; if(Test-Path -LiteralPath $o){ $rr=LC $o; $t=($rr.T -replace '[^\u0020-\u007E]','').Trim() }; $seg=''; if($t -eq $BERS){ $seg='   <== BERSAGLIO'; $cand+=$d.FullName }; Write-Host ('    ' + $d.Name + '   ' + $t + $seg) } }; $portab=$BERS + '\MQL5'; if($cand.Count -eq 0 -and (Test-Path -LiteralPath $portab)){ Write-Host ('    PORTABLE: nessuna cartella dati punta a ' + $BERS + ', ma ' + $portab + ' esiste: il terminale puo essere in modalita PORTABLE e allora i dati stanno li. Uso quel percorso, e lo DICHIARO.') -ForegroundColor Yellow; $cand+=$BERS }; if($cand.Count -eq 0){ Write-Host ''; Write-Host ('NESSUNA cartella dati certificata per ' + $BERS + ' e nessun albero MQL5 nella cartella programma. NON PROSEGUO: senza certificato non so di chi sono i log, e rispondere sulle sedie sbagliate e peggio che non rispondere.') -ForegroundColor Red } else { if($cand.Count -gt 1){ Write-Host ('    ATTENZIONE: ' + $cand.Count + ' cartelle candidate. Le elenco tutte e leggo la PRIMA.') -ForegroundColor Yellow; foreach($x in $cand){ Write-Host ('       candidata: ' + $x) } }; $D=$cand[0]; Write-Host ('    CARTELLA DATI LETTA: ' + $D) -ForegroundColor Green; Write-Host ''; Write-Host '--- 2. E ATTACCATA? (dai .chr) -- NB: classe 545/546: un .chr e la FOTO SALVATA del grafico, non la verita viva. MT5 lo riscrive alla chiusura o al cambio profilo: se un EA e stato attaccato o staccato DOPO l ultimo salvataggio, qui esce il dato vecchio. La verita viva e la faccina sul grafico. ---' -ForegroundColor Cyan; $chartsRoot=Join-Path $D 'MQL5\Profiles\Charts'; $attivo=''; $fonte=''; $certo=0; if(-not (Test-Path -LiteralPath $chartsRoot)){ Write-Host ('    NON ESISTE: ' + $chartsRoot + '   (ESITO A: non ho guardato, non e uno zero misurato)') -ForegroundColor Yellow } else { $profili=@(Get-ChildItem -LiteralPath $chartsRoot -Directory -ErrorAction SilentlyContinue); $conChr=@(); foreach($p in $profili){ $c=@(Get-ChildItem -LiteralPath $p.FullName -Filter 'chart*.chr' -ErrorAction SilentlyContinue); if($c.Count -gt 0){ $conChr+=[pscustomobject]@{N=$p.Name;Q=$c.Count;U=($c | Sort-Object LastWriteTime -Descending | Select-Object -First 1).LastWriteTime} } }; $cfg=Join-Path $D 'config'; $ign=@(); if(Test-Path -LiteralPath $cfg){ foreach($fi in @(Get-ChildItem -LiteralPath $cfg -Filter '*.ini' -ErrorAction SilentlyContinue | Sort-Object Name)){ $tt=(LC $fi.FullName).T; if(-not $tt){ continue }; foreach($m in [regex]::Matches($tt,'(?im)^[ \t]*(ProfileLast|LastProfile|CurrentProfile|ProfileName|Profile)[ \t]*=[ \t]*(.+?)[ \t]*$')){ $v=$m.Groups[2].Value.Trim(); if($v.Length -eq 0){ continue }; if($attivo -eq ''){ foreach($c in $conChr){ if($c.N -ieq $v){ $attivo=$c.N; $certo=1; $fonte='[CONFIG] config\' + $fi.Name + ' -> ' + $m.Groups[1].Value + '=' + $v } } }; if($attivo -eq ''){ $ign+=('config\' + $fi.Name + ' -> ' + $m.Groups[1].Value + '=' + $v) } } } }; if($attivo -eq '' -and $conChr.Count -eq 1){ $attivo=$conChr[0].N; $certo=1; $fonte='[UNICO] e l unico profilo con grafici salvati' }; if($attivo -eq '' -and $conChr.Count -gt 1){ $pp=@($conChr | Sort-Object U -Descending)[0]; $attivo=$pp.N; $certo=0; $fonte='[ASSUNTO] nessuna chiave di profilo utile nei config' + $(if($ign.Count -gt 0){' (chiavi lette ma non corrispondenti: ' + ($ign -join '; ') + ')'}else{''}) + ': preso il profilo col .chr piu recente (' + $pp.U.ToString('yyyy-MM-dd HH:mm',$INV) + ' [L])' }; Write-Host ('    profili con grafici salvati: ' + $conChr.Count + '   ATTIVO: ' + $(if($attivo -eq ''){'NON DETERMINATO'}else{$attivo}) + '   ' + $fonte); foreach($c in $conChr){ $seg=''; if($c.N -eq $attivo){ $seg='   <== attivo' }; Write-Host ('       profilo ' + $c.N.PadRight(24) + ' chart*.chr: ' + ([string]$c.Q).PadLeft(3) + '   piu recente ' + $c.U.ToString('yyyy-MM-dd HH:mm',$INV) + ' [L]' + $seg) }; if($certo -eq 0 -and $attivo -ne ''){ Write-Host '    ATTENZIONE: il profilo attivo e un RIPIEGO, non un fatto letto dai config. Le sedie che risultano NON attaccate potrebbero stare in un altro profilo: guardare la colonna profilo qui sotto.' -ForegroundColor Yellow }; $nchr=0; $senzaExp=0; $illChr=0; $fuori=@(); $chrLetti=0; foreach($p in $profili){ if($SCAD -eq 1){ break }; foreach($x in @(Get-ChildItem -LiteralPath $p.FullName -Filter 'chart*.chr' -ErrorAction SilentlyContinue | Sort-Object Name)){ if($chrLetti -ge $MAXCHR -or ((Get-Date)-$t0).TotalSeconds -ge $BUDG){ $SCAD=1; break }; $chrLetti++; $rr=LC $x.FullName; if($rr.T -eq ''){ $illChr++; continue }; $nchr++; $be=BEX $rr.T; if($be -eq ''){ $senzaExp++; continue }; $nome=CAMPO $be 'name'; if($nome -eq '-'){ $mp=[regex]::Match($be,'(?i)path\s*=\s*Experts\\(.+?)\.ex5'); if($mp.Success){ $nome=[IO.Path]::GetFileNameWithoutExtension($mp.Groups[1].Value) } }; $ins=BIN $be; $mg=CAMPO $ins 'InpMagic'; if($mg -eq '-'){ $mg=CAMPO $ins 'InpMagicNumber' }; $sym=CAMPO $rr.T 'symbol'; $per=CAMPO $rr.T 'period'; $vb=CAMPO $ins 'InpVerbose'; $gu=CAMPO $ins 'InpUsaGuardian'; $key=''; foreach($s in $SEDIE){ if($nome -match ('(?i)^(ABTG_|CLAU12_)' + [regex]::Escape($s.F) + '$')){ $key=$s.M } }; if($key -eq ''){ foreach($s in $SEDIE){ if($mg -eq $s.M){ $key=$s.M } } }; if($key -eq ''){ foreach($s in $SEDIE){ if($nome -match ('(?i)(ABTG_|CLAU12_)' + [regex]::Escape($s.F))){ $key=$s.M } } }; if($key -eq ''){ $fuori+=($p.Name + '\' + $x.Name + '  EA=' + $nome + '  magic=' + $mg + '  ' + $sym + ' per=' + $per); continue }; $e=$ST[$key]; if($e.Chr -eq '' -or $p.Name -eq $attivo){ $e.Chr=$x.Name; $e.Nome=$nome; $e.Sym=$sym; $e.Per=$per; $e.MagicChr=$mg; $e.Verb=$vb; $e.Guard=$gu; $e.Prof=$p.Name } } }; Write-Host ('    file .chr letti: ' + $nchr + '   senza blocco <expert> (grafico senza EA): ' + $senzaExp + '   illeggibili: ' + $illChr + $(if($SCAD -eq 1){'   *** FERMATO dal tetto (' + $MAXCHR + ' file .chr oppure ' + $BUDG + ' secondi): NON ho guardato tutti i grafici, quindi qui sotto NESSUNA sedia si puo chiamare NON ATTACCATA ***'}else{''})) -ForegroundColor $(if($SCAD -eq 1){'Yellow'}else{'Gray'}); if($fuori.Count -gt 0){ Write-Host ('    EA attaccati che NON sono della rosa (' + $fuori.Count + ') -- elencati per nome, mai come tutto il resto:') -ForegroundColor Yellow; foreach($x in $fuori){ Write-Host ('       ' + $x) } } else { Write-Host '    EA attaccati fuori rosa: nessuno' } }; Write-Host ''; Write-Host ('  {0,-7} {1,-30} {2,-12} {3,-5} {4,-8} {5,-8} {6,-9} {7,-12} {8}' -f 'magic','EA nel .chr (nome VERO in campo)','simbolo','per','InpMagic','Verbose','Guardian','profilo','file .chr'); foreach($s in $SEDIE){ $e=$ST[$s.M]; $nm=$e.Nome; if($nm -eq ''){ $nm='-- NESSUN .chr LO NOMINA --' }; Write-Host ('  {0,-7} {1,-30} {2,-12} {3,-5} {4,-8} {5,-8} {6,-9} {7,-12} {8}' -f $s.M,(TAGLIA $nm 30),(TAGLIA $e.Sym 12),$e.Per,$e.MagicChr,$e.Verb,$e.Guard,(TAGLIA $e.Prof 12),$e.Chr) -ForegroundColor $(if($e.Nome -eq ''){'Yellow'}else{'Gray'}) }; Write-Host '  NB: InpVerbose e la manopola che decide se l EA SCRIVE nella scheda Esperti. Se e false, zero righe NON vuol dire sedia muta: vuol dire sedia SILENZIATA DA NOI, e la domanda va girata al giornale del terminale (sezione 4). Nei preset FTMO in repo e true su tutte e sei.' -ForegroundColor Yellow; Write-Host '  NB2: la colonna Guardian e InpUsaGuardian preso dallo stesso .chr, e dice che cosa CHIEDE il preset -- NON che cosa fa il binario. Il 12/09 e stato misurato che il .ex5 in campo di ABTG_EMA200 (344a11b, 486 righe) non contiene NESSUNA occorrenza di InpUsaGuardian: con quel binario la manopola a true e fail-open lo stesso. Un true qui va confermato sul binario, non creduto.' -ForegroundColor Yellow; $FOK=0; $FKO=0; $BYT=0; $TRONCO=0; $PARZ=0; $INVENT=@(); $JTRADE=@(); $JALGO=@(); $JCONN=@(); $GAVV=''; $GFILO=''; $GBAT=''; $GBATOGGI=''; $nGuard=0; $P_FAIL='(?i)fallito|failed|rejected|invalid|prohibit|disabled|non consentito|not enough money|no money|autotrading|retcode|errore|error '; $P_ARM='(?i)STOP @|LIMIT @|placed for execution|piazzat|ordine inviato'; $P_NOGO='(?i)niente trade|non confermata|spread troppo alto|saltato|non piazzo|nessuna rottura|non ancora calcolabili|doji|non riarmo|whipsaw|troppo largo|troppo stretto'; $P_AVV='(?i)avviato|initialized|loaded successfully|CONFIG IN USO'; $P_TRADE='(?i)buy stop|sell stop|buy limit|sell limit|instant buy|instant sell|market buy|market sell|deal #|order #|position #|placed for execution|expired|cancell|canceled|removed|triggered|activated|stop loss|take profit'; $P_ALGO='(?i)automated trading|autotrading|auto trading|algotrading|algo trading|trading automatico|allowlivetrading|expert advisors are|experts are'; $P_CONN='(?i)authorized on|login|connect|disconnect|previous'; Write-Host ''; Write-Host '--- 3. I GIORNALI CHE HO APERTO -- e questa e la PROVA che uno zero qui dentro sia un FATTO e non una lettura fallita (classe 601: MT5 scrive in UTF-16, e i log stanno in DUE alberi diversi) ---' -ForegroundColor Cyan; Write-Host ('  {0,-22} {1,-12} {2,-12} {3,-12} {4,-6} {5,-11} {6}' -f 'cartella','file','byte totali','byte letti','righe','codifica','esito'); foreach($lg in @(@('GIORNALE terminale',(Join-Path $D 'logs')),@('scheda ESPERTI',(Join-Path $D 'MQL5\Logs')))){ $eti=$lg[0]; $dir=$lg[1]; if(-not (Test-Path -LiteralPath $dir)){ Write-Host ('  {0,-22} {1,-12} {2,-12} {3,-12} {4,-6} {5,-11} {6}' -f $eti,'-','-','-','-','-','CARTELLA NON ESISTENTE: ' + $dir) -ForegroundColor Yellow; continue }; $lf=@(Get-ChildItem -LiteralPath $dir -Filter '*.log' -ErrorAction SilentlyContinue | Sort-Object Name); if($lf.Count -eq 0){ Write-Host ('  {0,-22} {1,-12} {2,-12} {3,-12} {4,-6} {5,-11} {6}' -f $eti,'0','-','-','-','-','NESSUN .log in ' + $dir) -ForegroundColor Yellow; continue }; $ult=@($lf | Select-Object -Last $NLOG); Write-Host ('  ' + $eti + ': ' + $lf.Count + ' file .log in cartella, ne leggo gli ULTIMI ' + $ult.Count + ' PER INTERO (il nome del file E la data [L]; un evento di ieri o di cinque giorni fa NON sta nel file di oggi -- classe 546).'); foreach($f in $ult){ if(((Get-Date)-$t0).TotalSeconds -ge $BUDG){ $TRONCO=1; Write-Host '  *** TEMPO SCADUTO: da qui in poi i file NON sono stati letti. TRONCO. ***' -ForegroundColor Red; break }; if(($FOK+$FKO) -ge $MAXF){ $TRONCO=1; Write-Host '  *** TETTO FILE RAGGIUNTO: da qui in poi i file NON sono stati letti. TRONCO. ***' -ForegroundColor Red; break }; $r=LC $f.FullName; $san=$false; if($r.T -ne ''){ $san=($r.T -match '[0-9]{2}:[0-9]{2}:[0-9]{2}') }; if($r.T -eq '' -or -not $san){ $FKO++; $mot=$r.E; if($r.T -ne '' -and -not $san){ $mot='LETTO MA SENZA ORARI: codifica sbagliata o file non di log' }; Write-Host ('  {0,-22} {1,-12} {2,-12} {3,-12} {4,-6} {5,-11} {6}' -f '',$f.Name,$r.Tot.ToString($INV),$r.B.ToString($INV),'-',$r.E,('ILLEGGIBILE (' + $mot + '): su questo file uno ZERO non dimostra niente')) -ForegroundColor Yellow; continue }; $FOK++; $BYT+=$r.B; if($r.P -eq 1){ $PARZ++ }; $righe=@($r.T -split "`r`n|`n"); $isOggi=($f.BaseName -eq $oggiF); $isIeri=($f.BaseName -eq $ieriF); $esito='letto'; if($r.P -eq 1){ $esito='PARZIALE: letta solo la CODA (' + [math]::Round($r.B/1MB,1) + ' MB su ' + [math]::Round($r.Tot/1MB,1) + '): le righe di INIZIO giornata non ci sono' }; $esito=$esito + '   ultima scrittura ' + $f.LastWriteTime.ToString('yyyy-MM-dd HH:mm:ss',$INV) + ' [L]'; if($isOggi){ $esito=$esito + '   <== OGGI ' + $oggiF + ' [L]: se questa ultima scrittura e di POCHI MINUTI fa, il terminale sta scrivendo ADESSO ed e la prova piu economica che e vivo; se e di ORE fa, e la prima cosa da spiegare' }; Write-Host ('  {0,-22} {1,-12} {2,-12} {3,-12} {4,-6} {5,-11} {6}' -f '',$f.Name,$r.Tot.ToString($INV),$r.B.ToString($INV),$righe.Count.ToString($INV),$r.E,$esito) -ForegroundColor $(if($r.P -eq 1){'Yellow'}else{'Gray'}); foreach($L in $righe){ if($L.Length -lt 8){ continue }; if($L -match '(?i)guardian|pausa|cap rischio|cluster'){ $nGuard++; $LT=TAGLIA $L 150; if($L -match '(?i)avviato'){ $GAVV=$f.Name + ' | ' + $LT }; if($L -match '(?i)filo'){ $GFILO=$f.Name + ' | ' + $LT }; if($L -match 'eq='){ $GBAT=$f.Name + ' | ' + $LT; if($isOggi){ $GBATOGGI=$f.Name + ' | ' + $LT } } }; if($L -match '(?i)(ABTG_|CLAU12_)'){ foreach($s in $SEDIE){ if($L -match ('(?i)(ABTG_|CLAU12_)' + [regex]::Escape($s.F) + '([^A-Za-z0-9_]|$)')){ $e=$ST[$s.M]; $e.Fin++; if($isOggi){ $e.Oggi++ }; $e.Ult=TAGLIA $L 130; $e.UltF=$f.Name; if($L -match $P_FAIL){ $e.Fail++; if($e.FailEs -eq ''){ $e.FailEs=$f.Name + ' | ' + (TAGLIA $L 120) } } elseif($L -match $P_ARM){ $e.Arm++; if($e.ArmEs -eq ''){ $e.ArmEs=$f.Name + ' | ' + (TAGLIA $L 120) } } elseif($L -match $P_NOGO){ $e.NoGo++; if($e.NoGoEs -eq ''){ $e.NoGoEs=$f.Name + ' | ' + (TAGLIA $L 120) } } elseif($L -match $P_AVV){ $e.Avv++ } } } }; if(($isOggi -or $isIeri) -and $L -match $P_TRADE){ if($JTRADE.Count -lt 40){ $JTRADE+=($eti + ' ' + $f.Name + ' | ' + (TAGLIA $L 140)) } }; if($L -match $P_ALGO){ if($JALGO.Count -lt 12){ $JALGO+=($eti + ' ' + $f.Name + ' | ' + (TAGLIA $L 140)) } }; if(($isOggi -or $isIeri) -and $L -match $P_CONN){ if($JCONN.Count -lt 10){ $JCONN+=($eti + ' ' + $f.Name + ' | ' + (TAGLIA $L 140)) } } } } }; Write-Host ('  TOTALE LETTURE: file letti BENE ' + $FOK + '   file ILLEGGIBILI ' + $FKO + '   file letti solo in CODA (PARZIALI) ' + $PARZ + '   byte letti ' + $BYT.ToString($INV) + $(if($TRONCO -eq 1){'   *** TRONCO: i conteggi qui sotto sono un MINIMO, non la verita ***'}else{''}) + $(if($PARZ -gt 0){'   *** ' + $PARZ + ' file letti solo in CODA: una sedia con ZERO righe puo avere parlato nella parte NON letta. Nessuno zero qui si puo chiamare MUTO ***'}else{''})) -ForegroundColor $(if($TRONCO -eq 1 -or $FKO -gt 0 -or $PARZ -gt 0){'Yellow'}else{'Green'}); if($FOK -eq 0){ Write-Host '  >>> ZERO FILE LETTI: tutto cio che segue e un NON MISURATO, non un NO. Non si conclude niente sulle sedie da questo stampato.' -ForegroundColor Red }; Write-Host ''; Write-Host '--- 4. HA PARLATO? HA ARMATO? -- conteggi presi dalle righe che portano il NOME dell EA (ABTG_ o CLAU12_), quindi ATTRIBUITI, non dedotti ---' -ForegroundColor Cyan; Write-Host ('  {0,-7} {1,-34} {2,-7} {3,-7} {4,-6} {5,-6} {6,-6} {7,-6} {8}' -f 'magic','sedia','righe','di cui','ARMI','NO-GO','ERRORI','avvii','ultima riga sua (orari nel testo: [L] se li stampa MT5)'); Write-Host ('  {0,-7} {1,-34} {2,-7} {3,-7} {4,-6} {5,-6} {6,-6} {7,-6} {8}' -f '','','finestra','OGGI','','','','',''); foreach($s in $SEDIE){ $e=$ST[$s.M]; Write-Host ('  {0,-7} {1,-34} {2,-7} {3,-7} {4,-6} {5,-6} {6,-6} {7,-6} {8}' -f $s.M,(TAGLIA $s.E 34),$e.Fin,$e.Oggi,$e.Arm,$e.NoGo,$e.Fail,$e.Avv,$(if($e.Ult -eq ''){'(nessuna riga a suo nome nei file letti)'}else{$e.UltF + ' | ' + (TAGLIA $e.Ult 90)})) }; Write-Host ''; Write-Host '  ESEMPI VERBATIM (la prima riga di ogni tipo, cosi si vede COSA ho contato e non solo QUANTO):' -ForegroundColor Cyan; foreach($s in $SEDIE){ $e=$ST[$s.M]; if($e.ArmEs -ne '' -or $e.NoGoEs -ne '' -or $e.FailEs -ne ''){ Write-Host ('   ' + $s.M + ' ' + $s.E); if($e.ArmEs -ne ''){ Write-Host ('      ARMA  : ' + $e.ArmEs) -ForegroundColor Green }; if($e.NoGoEs -ne ''){ Write-Host ('      NO-GO : ' + $e.NoGoEs) }; if($e.FailEs -ne ''){ Write-Host ('      ERRORE: ' + $e.FailEs) -ForegroundColor Red } } }; Write-Host ''; Write-Host '--- 5. IL GIORNALE DEL TERMINALE: collegamento, Algo Trading, eventi di ordine ---' -ForegroundColor Cyan; Write-Host ('  5a) COLLEGAMENTO E CONTO (atteso ' + $CONTO + '):'); if($JCONN.Count -eq 0){ Write-Host '      nessuna riga di collegamento nei file di oggi e di ieri. Non e una prova di disconnessione: MT5 la scrive solo quando lo stato CAMBIA.' -ForegroundColor Yellow } else { foreach($x in $JCONN){ $cc=''; if($x -match $CONTO){ $cc='   <== e il conto atteso' }; Write-Host ('      ' + $x + $cc) } }; Write-Host ''; Write-Host ('  5b) ALGO TRADING (il bottone verde). Su disco lo stato VIVO non esiste. Qui si leggono due cose diverse: (i) le righe che MT5 scrive QUANDO lo stato cambia, e (ii) la chiave [Experts] AllowLiveTrading dei config -- che e lo stato di AVVIO, o dell ultimo salvataggio del terminale, NON quello di adesso. Se non esce niente la risposta e NON MISURATO, mai SPENTO. La verifica certa e guardare il bottone Algo Trading DENTRO il terminale ' + $BERS + ' (conto ' + $CONTO + '), e in nessuno degli altri sei.') -ForegroundColor Yellow; if($JALGO.Count -eq 0){ Write-Host '      nessuna riga di abilitazione/disabilitazione nei file letti.' } else { foreach($x in $JALGO){ Write-Host ('      ' + $x) } }; $kalgo=0; $cfg2=Join-Path $D 'config'; if(Test-Path -LiteralPath $cfg2){ foreach($fi in @(Get-ChildItem -LiteralPath $cfg2 -Filter '*.ini' -ErrorAction SilentlyContinue | Sort-Object Name)){ $tt=(LC $fi.FullName).T; if(-not $tt){ continue }; foreach($m in [regex]::Matches($tt,'(?im)^[ \t]*(AllowLiveTrading|AllowDllImport|AutoTrading|AlgoTrading|ExpertsEnable[A-Za-z]*|EnableExpert[A-Za-z]*|ExpertsDllImport|ExpertsTrading|Enabled|Account|Login)[ \t]*=[ \t]*(.+?)[ \t]*$')){ $kalgo++; Write-Host ('      config\' + $fi.Name + ' -> ' + $m.Groups[1].Value + '=' + $m.Groups[2].Value.Trim()) } } }; if($kalgo -eq 0){ Write-Host '      nessuna delle chiavi cercate (AllowLiveTrading, AllowDllImport, AutoTrading, AlgoTrading, ExpertsEnable*, EnableExpert*, ExpertsDllImport, ExpertsTrading, Enabled, Account, Login) compare nei config letti: da qui NON si legge, ed e un NON MISURATO -- non uno SPENTO. Il segnale indiretto sono gli ERRORI della colonna ERRORI qui sopra (con Algo Trading spento ogni invio fallisce e l EA lo scrive).' -ForegroundColor Yellow }; Write-Host ''; Write-Host ('  5c) EVENTI DI ORDINE nei file di OGGI (' + $oggiF + ') e di IERI (' + $ieriF + ') [L]. NB: NON sono attribuibili per sedia: il giornale non stampa il magic. Su U30USD/US30 stanno TRE sedie (770202 M5, 771531 H1, 770511 H1): il simbolo da solo non basta, e lo dichiaro invece di indovinare.'); if($JTRADE.Count -eq 0){ Write-Host ('      NESSUN evento di ordine nei file di oggi e di ieri. Con ' + $FOK + ' file letti bene e ' + $FKO + ' illeggibili, questo zero vale quanto vale: se gli illeggibili sono 0, e un fatto.') -ForegroundColor Yellow } else { foreach($x in $JTRADE){ Write-Host ('      ' + $x) } }; Write-Host ''; Write-Host '--- 6. IL GUARDIAN GIRA? ---' -ForegroundColor Cyan; Write-Host ('   righe guardian/pausa/cap rischio/cluster trovate in tutto: ' + $nGuard); Write-Host ('   [GUARDIAN] avviato ....... ' + $(if($GAVV -eq ''){'NON TROVATO negli ultimi ' + $NLOG + ' giornali per cartella'}else{$GAVV})); Write-Host ('   [GUARDIAN] filo / GV ..... ' + $(if($GFILO -eq ''){'NON TROVATO (e la riga che dice se gli EA leggono le STESSE GlobalVariable del guardiano)'}else{$GFILO})); Write-Host ('   [GUARDIAN] battito eq= ... ' + $(if($GBAT -eq ''){'NON TROVATO (esce ogni 5 minuti SOLO con InpVerbose=true e col tempo server che avanza)'}else{$GBAT})); Write-Host ('   battito nel file di OGGI: ' + $(if($GBATOGGI -eq ''){'NO'}else{'SI -- ' + $GBATOGGI})); Write-Host ('   NB: LE GLOBALVARIABLE NON SI LEGGONO DA DISCO: MT5 le tiene nel terminale e le scarica in un file binario non documentato. Questa riga NON le legge e NON prova a indovinarle: quello che vedi sopra sono le righe che il Guardian STAMPA. Il valore vivo di ABTG_PAUSA_GIORNO_' + $CONTO + ' e compagne si vede solo da dentro MT5 (Strumenti > Variabili globali), ed e una AZIONE A MANO, non questa riga.') -ForegroundColor Yellow; Write-Host ''; Write-Host '--- 7. HA OPERATO DAVVERO? il CSV del TradeExporter (attribuzione CERTA: la colonna magic viene dallo storico dei deal) ---' -ForegroundColor Cyan; $csv=''; if($env:APPDATA){ $csv=Join-Path $env:APPDATA 'MetaQuotes\Terminal\Common\Files\ABTG_Trades_FTMO.csv' }; Write-Host ('   file atteso: ' + $csv + '   (Common\Files e condivisa fra i terminali della macchina: di tutto quel cestino leggo SOLO il file che si chiama ABTG_Trades_FTMO.csv, che e quello scritto dal preset di ' + $CONTO + ')'); if($csv -eq '' -or -not (Test-Path -LiteralPath $csv)){ Write-Host '   NON ESISTE. Vuol dire che il TradeExporter non ha ancora scritto (non e schierato, o nessun deal da esportare): NON vuol dire che le sedie non hanno operato. E un NON MISURATO.' -ForegroundColor Yellow } else { $rc=LC $csv; $rig=@($rc.T -split "`r`n|`n" | Where-Object { $_.Trim().Length -gt 0 }); Write-Host ('   letto: ' + $rc.B.ToString($INV) + ' byte su ' + $rc.Tot.ToString($INV) + '   codifica ' + $rc.E + '   righe ' + $rig.Count + '   ultima scrittura ' + (Get-Item -LiteralPath $csv).LastWriteTime.ToString('yyyy-MM-dd HH:mm',$INV) + ' [L]'); if($rig.Count -lt 2){ Write-Host '   INTESTAZIONE SOLTANTO: zero deal esportati.' -ForegroundColor Yellow } else { $hd=@($rig[0] -split ';'); $iM=-1; $iT=-1; $iS=-1; for($i=0;$i -lt $hd.Count;$i++){ $h=$hd[$i].Trim().Trim('"'); if($h -eq 'magic'){ $iM=$i }; if($h -eq 'open_time'){ $iT=$i }; if($h -eq 'symbol'){ $iS=$i } }; if($iM -lt 0){ Write-Host ('   la colonna magic NON c e in questa PRIMA RIGA: ' + (TAGLIA $rig[0] 140) + ' -- non attribuisco niente per non sbagliare.' + $(if($rc.P -eq 1){' CAUSA QUASI CERTA: il file supera ' + [math]::Round($MAXB/1MB,0) + ' MB e ne ho letta solo la CODA, quindi la prima riga NON e l intestazione. Non e un difetto del CSV: e il mio tetto.'}else{''})) -ForegroundColor Yellow } else { for($i=1;$i -lt $rig.Count;$i++){ $cc=@($rig[$i] -split ';'); if($cc.Count -le $iM){ continue }; $mg=$cc[$iM].Trim().Trim('"'); if($ST.ContainsKey($mg)){ $e=$ST[$mg]; $e.Csv++; if($iT -ge 0 -and $cc.Count -gt $iT){ $e.CsvUlt=$cc[$iT].Trim().Trim('"') + $(if($iS -ge 0 -and $cc.Count -gt $iS){' ' + $cc[$iS].Trim().Trim('"')}else{''}) } } }; Write-Host ('  {0,-7} {1,-34} {2,-10} {3}' -f 'magic','sedia','posizioni','ultima open_time [S] = ora server FTMO = italiana +1'); foreach($s in $SEDIE){ $e=$ST[$s.M]; Write-Host ('  {0,-7} {1,-34} {2,-10} {3}' -f $s.M,(TAGLIA $s.E 34),$e.Csv,$(if($e.CsvUlt -eq ''){'(nessuna riga con questo magic)'}else{$e.CsvUlt})) } } } }; }; Write-Host ''; Write-Host '=== IL VERDETTO, SEDIA PER SEDIA -- e la REGOLA e scritta prima dei numeri, non dopo ===' -ForegroundColor Cyan; Write-Host '   SI LEGGE DALL ALTO, e vince la prova piu FORTE: le righe di log a suo nome e il CSV col suo magic BATTONO il .chr, perche il .chr e una foto salvata (classe 545) e il log e un fatto accaduto.' -ForegroundColor Yellow; Write-Host '   ARMATA E DISCIPLINATA = ha righe a suo nome, di cui almeno una OGGI, piu almeno un NO-GO o un ARMA. Il motore parla e decide: e quello che vogliamo.'; Write-Host '   HA PARLATO, ma OGGI NO = ha righe sue nella finestra letta e zero nel file di OGGI. Prima di chiamarlo guasto: il mercato e aperto? che ora e? il file di oggi cresce durante la giornata.'; Write-Host '   HA OPERATO (prova dal CSV) = zero righe nei giornali letti, MA posizioni chiuse col SUO magic nel CSV del TradeExporter. Il magic viene dallo storico dei deal: attribuzione certa. Ha operato, punto -- il silenzio nei log e un altro problema.'; Write-Host '   PARLA ma non ho visto decisioni = ha righe sue, ma nessuna che io sappia classificare come ARMA o come NO-GO. Si guardano i verbatim.'; Write-Host '   SILENZIATA DA NOI = InpVerbose LETTO nel .chr e diverso da true: per costruzione non scrive nella scheda Esperti. Non e un guasto, e una manopola.'; Write-Host '   NON ATTACCATA NEL PROFILO SALVATO = nessun .chr la nomina E zero righe sue nei log. Classe 545: e la foto salvata, non la verita viva.'; Write-Host '   NON MISURABILE DA QUI = zero file letti bene, oppure file illeggibili, oppure lettura TRONCA o solo in CODA, oppure la lettura dei .chr e stata fermata dal tetto, oppure InpVerbose NON C E nel .chr (e allora non so nemmeno se quell EA ha la bocca). NON e un NO: e un non misurato.' -ForegroundColor Yellow; Write-Host '   MUTA -- SOSPETTO GUASTO = attaccata, InpVerbose LETTO e uguale a true, e ZERO righe sue, con ZERO file illeggibili, ZERO tronchi, ZERO code e ZERO tetti scattati. E l unico verdetto che ACCUSA, e si merita tutte e sei le condizioni insieme: qui si va a guardare la faccina sul grafico.' -ForegroundColor Red; Write-Host ''; Write-Host ('  {0,-7} {1,-34} {2,-36} {3}' -f 'magic','sedia','verdetto','su cosa si regge'); foreach($s in $SEDIE){ $e=$ST[$s.M]; $v=''; $perche=''; $col='Gray'; if($FOK -eq 0){ $v='NON MISURABILE DA QUI'; $perche='zero giornali letti bene (' + [string]$FKO + ' illeggibili): non ho guardato, quindi non concludo'; $col='Yellow' } elseif($e.Fin -gt 0 -and ($e.NoGo -gt 0 -or $e.Arm -gt 0)){ $v=$(if($s.M -eq '779001'){'VIVO (e non deve armare: non trada)'}elseif($e.Oggi -gt 0){'ARMATA E DISCIPLINATA'}else{'HA PARLATO, ma OGGI NO'}); $perche=[string]$e.Fin + ' righe sue (' + [string]$e.Oggi + ' oggi), ARMI ' + [string]$e.Arm + ', NO-GO ' + [string]$e.NoGo + ', ERRORI ' + [string]$e.Fail + $(if($e.Oggi -eq 0){'. OGGI zero: se il mercato e aperto e il file di oggi e stato letto, e una domanda aperta; se e presto o e festivo, no'}else{''}); $col=$(if($e.Oggi -gt 0){'Green'}else{'Yellow'}); if($e.Nome -eq ''){ $perche=$perche + '. NB: nessun .chr la nomina, ma le righe a suo nome nei log BATTONO la foto salvata (classe 545): e attaccata, e il .chr e vecchio' } } elseif($e.Csv -gt 0){ $v='HA OPERATO (prova dal CSV)'; $perche=[string]$e.Csv + ' posizioni chiuse col suo magic nel CSV del TradeExporter (attribuzione CERTA dallo storico dei deal), ma ZERO righe sue nei giornali letti: ha operato di sicuro, ed e il SILENZIO nei log da spiegare (InpVerbose? finestra dei giornali troppo corta?)'; $col='Green' } elseif($e.Fin -gt 0){ $v=$(if($s.M -eq '779001'){'VIVO (e non deve armare: non trada)'}else{'PARLA ma non ho visto decisioni'}); $perche=[string]$e.Fin + ' righe sue (' + [string]$e.Oggi + ' oggi), zero ARMI e zero NO-GO: guarda gli esempi verbatim e la sezione 5'; $col=$(if($s.M -eq '779001'){'Green'}else{'Yellow'}) } elseif($e.Nome -eq '' -and $SCAD -eq 1){ $v='NON MISURABILE DA QUI'; $perche='nessun .chr la nomina, MA la lettura dei .chr e stata fermata dal tetto (' + $MAXCHR + ' file o ' + $BUDG + ' secondi): non ho guardato tutti i grafici, quindi NON dico che non e attaccata'; $col='Yellow' } elseif($e.Nome -eq ''){ $v='NON ATTACCATA (profilo salvato)'; $perche='nessun .chr la nomina, ne come ABTG_ ne come CLAU12_, e zero righe sue nei giornali letti. Classe 545: il .chr e la foto salvata, non la verita viva -- la conferma e la faccina sul grafico'; $col='Yellow' } elseif($e.Verb -ne '' -and $e.Verb -ne '-' -and $e.Verb -notmatch '(?i)^(true|1)$'){ $v='SILENZIATA DA NOI'; $perche='InpVerbose=' + $e.Verb + ': per costruzione non scrive. Zero righe NON e un guasto'; $col='Yellow' } elseif($e.Verb -eq '' -or $e.Verb -eq '-'){ $v='NON MISURABILE DA QUI'; $perche='attaccata (' + $e.Chr + ') e ZERO righe sue, MA nel blocco <inputs> del .chr NON C E nessuna chiave InpVerbose: non so se questo EA scrive nella scheda Esperti. Uno zero, senza sapere se il motore ha la bocca, NON si chiama MUTA -- si guarda la faccina sul grafico e si legge InpVerbose dalla finestra delle proprieta dell EA'; $col='Yellow' } elseif($FKO -gt 0 -or $TRONCO -eq 1 -or $PARZ -gt 0 -or $SCAD -eq 1){ $v='NON MISURABILE DA QUI'; $perche='zero righe sue, MA file illeggibili=' + [string]$FKO + ', letti solo in coda=' + [string]$PARZ + ', tronco=' + [string]$TRONCO + ', tetto scattato=' + [string]$SCAD + ': lo zero NON e un fatto e non si chiama MUTA'; $col='Yellow' } else { $v='MUTA -- SOSPETTO GUASTO'; $perche='attaccata (' + $e.Chr + ', InpVerbose=' + $e.Verb + ') e ZERO righe a suo nome in ' + $FOK + ' giornali letti bene, 0 illeggibili, 0 tronchi, 0 code, 0 tetti scattati'; $col='Red' }; if($e.Csv -gt 0){ $perche=$perche + '   [CSV: ' + $e.Csv + ' posizioni chiuse esportate]' }; Write-Host ('  {0,-7} {1,-34} {2,-36} {3}' -f $s.M,(TAGLIA $s.E 34),$v,$perche) -ForegroundColor $col }; Write-Host ''; Write-Host ('IMPIEGATI ' + [math]::Round(((Get-Date)-$t0).TotalSeconds,1).ToString('0.0',$INV) + ' secondi su un tetto di ' + $BUDG + '. File letti bene ' + $FOK + ', illeggibili ' + $FKO + ', byte ' + $BYT.ToString($INV) + $(if($TRONCO -eq 1 -or $PARZ -gt 0 -or $SCAD -eq 1){'. *** LETTURA INCOMPLETA (tronca=' + [string]$TRONCO + ', file letti solo in coda=' + [string]$PARZ + ', tetto scattato in sez.1/2=' + [string]$SCAD + '): ogni conteggio e un MINIMO ***'}else{'. Lettura COMPLETA entro i tetti.'})) -ForegroundColor $(if($TRONCO -eq 1 -or $FKO -gt 0 -or $SCAD -eq 1){'Yellow'}else{'Green'}); Write-Host ('CHE COSA QUESTA RIGA NON HA GUARDATO, per nome: la faccina sul grafico (sola verita viva su attaccato/staccato) e lo stato VIVO del bottone Algo Trading -- tutti e due si guardano DENTRO il terminale ' + $BERS + ', conto ' + $CONTO + ', e in nessuno degli altri sei; il valore vivo delle GlobalVariable del Guardian; se il BINARIO in campo legga davvero il Guardian (la colonna Guardian dice che cosa chiede il preset, non che cosa fa il .ex5); gli ordini pendenti APERTI in questo momento (il giornale racconta gli eventi, non lo stato); il saldo e l equita; e gli altri sei terminali, conto reale 10105439 compreso, di cui ho aperto il solo origin.txt. Per queste servono occhi dentro MT5, non una riga di sola lettura.') -ForegroundColor Yellow; Write-Host 'FINE. Niente aperto, niente chiuso, niente attaccato, niente staccato, niente scritto, nessun tester avviato, nessun altro terminale toccato.' -ForegroundColor Cyan }