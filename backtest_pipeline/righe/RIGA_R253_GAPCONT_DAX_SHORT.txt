& { $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; Write-Host 'BERSAGLIO: SOLO una finestra PowerShell sul PC di backtest DESKTOP-H4D7CAJ, che apre e chiude da sola il SUO terminale C:\Program Files\BCM Markets MT5 Terminal (loggato sul demo 50503392). NON TOCCATI, per nome: il VPS VMI3047753 e TUTTE le sue cartelle dati -- FTMO 541452707 (C:\FTMO, sedie vive della challenge), REALE 10105439 (C:\BCM_Reale), 100k 50504263 (BCM Markets MT5 Terminal -V3), piccolo 50503392 sul VPS (BCM Markets MT5 Terminal), manuale 50503635 (C:\MT5_MANUALE), banco 50504400 (C:\MT5_Backtest, SPENTO finche la challenge opera), Pepperstone, Tickmill. Questa riga NON chiude nessun processo: se trova MT5 aperto si FERMA.' -ForegroundColor Yellow; $PIN='59cb4f9e76505a8bb2f742b7574c8919ade0d494'; $T0=Get-Date; if($env:COMPUTERNAME -ne 'DESKTOP-H4D7CAJ'){ throw ('QUESTA RIGA GIRA SOLO SUL PC DI BACKTEST DESKTOP-H4D7CAJ. Qui la macchina si chiama: ' + $env:COMPUTERNAME + '. Sul VPS VMI3047753 non si lancia: la challenge FTMO 541452707 sta operando (firma del 21/09/2026).') }; if((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0){ throw 'MT5 risulta APERTO su questo PC. PRIMA di chiuderlo GUARDA I GRAFICI: questo terminale e C:\Program Files\BCM Markets MT5 Terminal ed e loggato sul DEMO 50503392, lo stesso conto del piccolo sul VPS. Il 14/08/2026 da QUESTA macchina sono partiti ordini VERI (#3160534/#3160535, -104,60). Se ci sono SEDIE attaccate ai grafici, staccale. Poi chiudi il terminale A MANO e rilancia.' }; $W=Join-Path $env:USERPROFILE 'abtg_round'; New-Item -ItemType Directory -Force -Path $W | Out-Null; $S=Join-Path $W 'RIGA_ROUND_VPS.ps1'; Remove-Item -LiteralPath $S -Force -ErrorAction SilentlyContinue; irm ('https://raw.githubusercontent.com/claudiospadaro12/GITHUB/' + $PIN + '/backtest_pipeline/righe/RIGA_ROUND_VPS.ps1?cb=' + [Guid]::NewGuid().ToString('N')) -OutFile $S; if(-not (Select-String -LiteralPath $S -SimpleMatch -Pattern 'MARCATORE_RIGA_ROUND_VPS_v2' -Quiet)){ throw 'RIGA_ROUND_VPS.ps1 scaricata SENZA il marcatore v2: copia vecchia o cache di GitHub. Non si parte.' }; if((Get-FileHash -LiteralPath $S -Algorithm SHA256).Hash -ne '3341756FB37889DBD6E827A87BAC26893E01C85B6C18343918083B01B4DD3425'){ throw 'RIGA_ROUND_VPS.ps1 scaricata con SHA256 DIVERSO da quello del pin 59cb4f9e. Non si parte.' }; $J=@(@{e='ABTG_GapContinuation';s='D30EUR';p='R253a_gapcont_DAX_short_ora8.txt';t='R253a';m=4;g1='792701';g2='792751';lo='08:15:00';hi='16:26:59';np=33;ax='InpMagicNumber';hp='4ED6BAAFC6ECC90BB9DE243FD94D1E8FC0916DCAFCB47C5C1DCFDA286E3FC0CF';cf=@('2025.03.31','2026.03.30');d='gap-down SHORT, ora 8 / 16:30 = in fase d ESTATE (file di testa)'},@{e='ABTG_GapContinuation';s='D30EUR';p='R253b_gapcont_DAX_short_ora9.txt';t='R253b';m=4;g1='792702';g2='792752';lo='09:15:00';hi='17:26:59';np=33;ax='InpMagicNumber';hp='C79D05527082181C64B392DBF7BE32DDA25E720441081CD10CF4E4E70A037EBB';cf=@('2024.10.28','2025.10.27');d='gap-down SHORT, ora 9 / 17:30 = in fase d INVERNO'}); $shaEa166=@{}; $shaEa166['ABTG_GapContinuation']='69A8A45D574535286CBE35F70A37DA4D110F7EB08889D513BB9830617DBAAA11'; $shaInc166=@{}; $shaInc166[(Join-Path $W 'src_include\ABTG_PausaGuardian.mqh')]='3EC971152E85E0082488CC4243FF45AE09948C191D52AB96050B48F94641A737'; if(@($J).Count -ne 2 -or @($shaEa166.Keys).Count -ne 1 -or @($shaInc166.Keys).Count -ne 1 -or ((@($J | ForEach-Object { $_.t })) -join ',') -ne 'R253a,R253b'){ throw 'RIGA R253 INCOERENTE: attesi 2 job nell ordine R253a,R253b, 1 impronta EA e 1 impronta include. Non si parte.' }; Write-Host ('pc  : ' + $env:COMPUTERNAME) -ForegroundColor Green; Write-Host ('pin : ' + $PIN) -ForegroundColor Green; Write-Host ('data: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss')) -ForegroundColor Green; Write-Host 'R253 -- RICERCA: CONTINUAZIONE DEL GAP-DOWN ALL APERTURA XETRA, D30EUR, SOLO SHORT. E RICERCA (file di testa, riquadro iniziale): NON avvicina nessuna sedia al 1 ottobre, NON promuove niente in campo, NON tocca preset, EA, sedie, conti, taglie. 2 file, EA ABTG_GapContinuation (adozione minima del Nikkei 225 Gap Continuation EA, Code Base 75301), D30EUR, TICK REALI (modello 4), deposito 10000, rischio di BANCO 1,0 (non una taglia). Il TF del grafico M5 e INERTE per questo EA (tutte le CopyRates sono PERIOD_M1 fisso, testa par. 5). R253a ora 8 / chiusura 16:30 (in fase con la cash Xetra SOLO d ESTATE), R253b ora 9 / 17:30 (in fase SOLO d INVERNO). Asse tecnico = InpMagicNumber gemello (792701/792751 e 792702/792752, passo 50): 4 celle x 2 gambe = 8 PASSATE, di cui 4 MONCONI da UN giorno (@FRAZIONEIS 0.001: gamba IS = solo il 2024.09.26) e 4 GAMBE CONTINUE 2024.09.27 -> 2026.06.30 (641 giorni). Ordine: R253a, R253b. Il round giudica la CATENA, il COSTO, il RISCHIO solo in negativo e la SOVRAPPOSIZIONE (testa par. 3); il MERITO e SOSPESO per aritmetica qualunque cosa esca (n << 150). La curva IN FASE, K1, R1-R3, O1 e O2 NON sono in questa riga: li fa il referto dai per-trade (testa par. 4 e 6). Non tocca 770411, 770105, 770101 (qui magic 7927xx, nessun preset, nessun terminale del VPS).' -ForegroundColor Cyan; Write-Host 'TEMPO ATTESO: 5-12 minuti, COME DICHIARATO nel file di testa par. 7 [STIMA, non misurata su questo EA]: 4 passate lunghe da 641 giorni di D30EUR a tick, 1-3 minuti ciascuna (la VWAP rilegge le M1 della seduta a ogni tick SOLO nelle giornate armate, ~14%), piu i 4 monconi da un giorno; UN SOLO agente su questa macchina (classe 751) quindi in serie. TETTO 40 minuti: NON fermarla prima di 40; oltre i 40 non e lenta, ci si ferma e si guarda. Il tempo VERO lo stampa il RIEPILOGO (durata minuti) e va riportato nel referto: e la prima misura di tempo di questo EA su questa macchina. Ogni gamba continua e UNA SOLA passata da 641 giorni (~1,8 anni, oltre il tetto storico di ~1,3 anni per corsa a M5 con 100.000 barre): il tetto barre NON lo tocca questa riga, lo VERIFICA il pre-volo del driver (config\common.ini, riga tetto barre del REFERTO_ROUND: atteso MaxBars=100000000 come in R246/R251) e la riga lo ristampa job per job.' -ForegroundColor Yellow; Write-Host 'PROMEMORIA CHE VALE ANCHE A TERMINALE GIA CHIUSO (li la guardia qui sopra non parla): il driver APRE quel terminale per far girare il tester e poi lo spegne da solo, e quel terminale e loggato sul DEMO 50503392. Se ai grafici sono rimaste SEDIE attaccate con Algo Trading acceso, quelle OPERANO DAVVERO mentre il tester gira -- il 14/08/2026 da questa macchina e successo. Se non sei sicuro, fermati ADESSO con Ctrl+C, apri il terminale, guarda i grafici, stacca le sedie, chiudilo e rilancia.' -ForegroundColor Yellow; Write-Host 'BANCO (file di testa, riquadro VINCOLI DI BANCO, classe 692): -Deposito 10000 PASSATO ESPLICITAMENTE (= R251/R252: i tetti R1/R2 del par. 6 sono in % di QUESTO deposito), -Modello 4. NIENTE -Spread, NIENTE -Ritardo, NIENTE -FrazioneIS: il taglio lo da @FRAZIONEIS 0.001 nei file (Meta = Inizio + floor(642 x 0,001) = Inizio), e un -FrazioneIS diverso fa morire il driver. Etichetta DIVERSA per ogni file (R253a, R253b): i CSV hanno per nome Expert_Simbolo_Tag_Etichetta.' -ForegroundColor Yellow; Write-Host 'CLASSE 166: il driver compila l EA e l include dal RAMO lavoro, NON dal pin. Il motore misurato e quello che sta sul ramo AL MOMENTO DEL LANCIO. Al pin 59cb4f9e ABTG_GapContinuation.mq5 ha SHA256 69A8A45D... e ABTG_PausaGuardian.mqh 3EC97115... (unico include NOSTRO dell EA, r.101; l altro e Trade/Trade.mqh di MT5). La riga ricontrolla le due impronte DOPO OGNI JOB e l impronta del file prova scaricato dal pin: se qualcuno ha spinto sul ramo un EA o un include diversi, quel file esce MOTORE DIVERSO DAL PIN e NON si legge (si rifa il pin sul ramo e si rilancia).' -ForegroundColor Yellow; Write-Host 'ATTESO E NORMALE, NON E UN GUASTO (modello RIGA_R246_OROLOGIO finestre A e RIGA_R252, classe 766): la gamba IS di OGNI file dura UN giorno e il suo CSV _IS esce VUOTO o da 0 byte, quindi il driver esce rc 2 = NON MISURATO su tutti e due i file. La riga ASSOLVE rc 2 SOLO con E0 (testa par. 6): CSV _OOS di quel file fresco con 2 righe con Trades>0 (la negazione dell ipotesi alternativa, non la sola esclusione). rc 0 = anche il moncone ha Trades>0, cioe il 2024.09.26 ha operato: ammesso, si scrive (quel giorno esce comunque dalla curva). rc 3 = girato con RILIEVI del driver: si leggono nel REFERTO_ROUND, non e un guasto da solo. GUASTO VERO = rc 1 (non e partito), oppure _OOS mancante, vecchio o senza 2 righe con Trades>0, con QUALUNQUE rc. Se E0 fallisce (Trades = 0) il file e NULLO e si fa UNA corsa singola per leggere il funnel di PrintFunnel (in ottimizzazione i Print sono spenti): NON la fa questa riga, non si cambia nessun parametro. I PIN DAL CSV (P0, classe 780) si leggono SOLO dal _OOS: 33 pin fissi (32 numerici, bool 0/1, InpSessionTimeMode = 1, piu InpComment confrontato come STRINGA esatta: GCDAXS8 / GCDAXS9) + InpMagicNumber = 34 confronti per riga, 68 per file. Il per-trade di ogni gemella e la gamba continua: righe > 0, prima chiusura >= 2024.09.27, ultima >= 2026.03.30 e <= 2026.06.30, somma net_profit = Profit del _OOS entro 0,05, Trades del _OOS = righe (una riga per DEAL DI USCITA: la parziale al 40% e la chiusura finale sono DUE righe della stessa position_id, quindi righe > posizioni; verificato su R251b: Trades 243 = 243 righe / 181 posizioni). Un per-trade del moncone, con la sola intestazione, qui esce NON BUONO.' -ForegroundColor Yellow; $ErrorActionPreference='Continue'; $numF={ param($v) $d=[double]::NaN; if(-not [double]::TryParse(('' + $v).Trim(), [Globalization.NumberStyles]::Float, [Globalization.CultureInfo]::InvariantCulture, [ref]$d)){ $d=[double]::NaN }; $d }; $rowAx={ param($rws,$axn,$val) @($rws | Where-Object { [Math]::Abs((& $numF $_.$axn) - [double]$val) -le 0.000001 }) }; $ptRead={ param($pf,$mg,$hh,$cc) $o=@{n=0;sum=0.0;nNet=0;nMg=0;nDate=0;dMin='nessuna';dMax='nessuna';s2=0;s2first='';l0=0;l0first='';bad=$true;r=(New-Object System.Collections.ArrayList)}; $righe=@(Get-Content -LiteralPath $pf | Where-Object { ('' + $_).Trim() -ne '' } | Select-Object -Skip 1); $o.n=$righe.Count; $dd=New-Object System.Collections.ArrayList; foreach($rg in $righe){ $f=('' + $rg).Split(';'); if($f.Count -lt 8){ continue }; $dS=''; $tS=''; if($f[0] -match '^([0-9]{4}\.[0-9]{2}\.[0-9]{2}) ([0-9]{2}:[0-9]{2}:[0-9]{2})'){ $dS=$Matches[1]; $tS=$Matches[2]; $o.nDate=$o.nDate+1; [void]$dd.Add($dS); if(([string]::CompareOrdinal($tS, $hh) -lt 0) -or ([string]::CompareOrdinal($tS, $cc) -gt 0)){ $o.s2=$o.s2+1; if($o.s2first -eq ''){ $o.s2first=$f[0] } } }; if($f[2] -eq $mg){ $o.nMg=$o.nMg+1 }; if(('' + $f[4]).Trim() -ne '0'){ $o.l0=$o.l0+1; if($o.l0first -eq ''){ $o.l0first=($f[0] + ' deal_type ' + $f[4]) } }; $vN=& $numF $f[7]; if(-not [double]::IsNaN($vN)){ $o.sum=$o.sum+$vN; $o.nNet=$o.nNet+1 }; [void]$o.r.Add(@{d=$dS;t=$tS;pid=$f[3];k=($f[0] + ';' + $f[1] + ';' + $f[3] + ';' + $f[4] + ';' + $f[5] + ';' + $f[6] + ';' + $f[7]);net=$vN}) }; if($dd.Count -gt 0){ $sd=[string[]]@($dd); [Array]::Sort($sd,[StringComparer]::Ordinal); $o.dMin=$sd[0]; $o.dMax=$sd[$sd.Count-1] }; $o.bad=($o.n -eq 0 -or $o.nDate -ne $o.n -or $o.nMg -ne $o.n -or $o.nNet -ne $o.n -or $o.r.Count -ne $o.n -or [string]::CompareOrdinal($o.dMin,'2024.09.27') -lt 0 -or [string]::CompareOrdinal($o.dMax,'2026.03.30') -lt 0 -or [string]::CompareOrdinal($o.dMax,'2026.06.30') -gt 0); $o }; $nPos={ param($rr) @(@($rr | ForEach-Object { $_.pid }) | Sort-Object -Unique).Count }; $fmt={ param($v) ([Math]::Round([double]$v,2)).ToString('0.00',[Globalization.CultureInfo]::InvariantCulture) }; $nDiv=0; $nPart=0; $tIni=@{}; $rcMap=@{}; $nulMap=@{}; $rowsMap=@{}; $ptMap=@{}; $tettoMap=@{}; $preMap=@{}; $esiti=New-Object System.Collections.ArrayList; $dsk=[Environment]::GetFolderPath('Desktop'); foreach($x in $J){ $vec=Join-Path $dsk ('ROUND_' + $x.t); if(Test-Path -LiteralPath $vec){ Remove-Item -LiteralPath $vec -Recurse -Force -ErrorAction SilentlyContinue; Write-Host ('  tolta la cartella di una corsa PRECEDENTE: ROUND_' + $x.t) -ForegroundColor DarkYellow }; $vez=Join-Path $dsk ('ROUND_' + $x.t + '.zip'); if(Test-Path -LiteralPath $vez){ Remove-Item -LiteralPath $vez -Force -ErrorAction SilentlyContinue } }; foreach($x in $J){ $E=$x.e; $P=$x.p; $L=$x.t; $M=$x.m; $SIMB=$x.s; $nAtt=0; while((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0 -and $nAtt -lt 36){ Start-Sleep -Seconds 5; $nAtt=$nAtt+1 }; if((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0){ Write-Host ('ATTENZIONE: dopo 180 secondi MT5 e ancora VIVO. Il round ' + $L + ' parte lo stesso, ma il pre-volo del driver lo fermera con rc 1: NON sara misurato.') -ForegroundColor Red }; Write-Host ''; Write-Host ('=== ' + $L + '   ' + $E + '   ' + $SIMB + '   ' + $P + '   ' + $x.d + '   magic ' + $x.g1 + '/' + $x.g2 + '   modello ' + $M + '   deposito 10000 ===') -ForegroundColor Cyan; $tJ=Get-Date; $tIni[$L]=$tJ; & powershell.exe -NoProfile -ExecutionPolicy Bypass -File $S -Expert $E -Prova $P -Etichetta $L -Pin $PIN -Modello $M -Deposito 10000; $rc=$LASTEXITCODE; $rcMap[$L]=$rc; $div=@(); $nFile166=0; if($rc -ne 1){ $nPart=$nPart+1; $eaLoc=Join-Path $W ('src_prove\' + $E + '.mq5'); $nFile166=$nFile166+1; if(-not (Test-Path -LiteralPath $eaLoc)){ $div += ($E + '.mq5 ASSENTE') } elseif((Get-FileHash -LiteralPath $eaLoc -Algorithm SHA256).Hash -ne $shaEa166[$E]){ $div += ($E + '.mq5 SHA256 DIVERSO DAL PIN') }; foreach($k in @($shaInc166.Keys)){ $nFile166=$nFile166+1; if(-not (Test-Path -LiteralPath $k)){ $div += ((Split-Path -Leaf $k) + ' ASSENTE') } elseif((Get-FileHash -LiteralPath $k -Algorithm SHA256).Hash -ne $shaInc166[$k]){ $div += ((Split-Path -Leaf $k) + ' SHA256 DIVERSO DAL PIN') } } }; if($rc -eq 1){ $m166='motore NON VERIFICATO (non e partito)' } elseif($div.Count -eq 0){ $m166=('motore = pin (SHA256, ' + $nFile166 + ' file)') } else { $m166=('MOTORE DIVERSO DAL PIN: ' + ($div -join ', ')); $nDiv=$nDiv+1 }; $pvLoc=Join-Path $W ('prove\' + $P); $pvOk=$false; if(-not (Test-Path -LiteralPath $pvLoc)){ $pvTxt='prova ASSENTE' } elseif((Get-FileHash -LiteralPath $pvLoc -Algorithm SHA256).Hash -ne $x.hp){ $pvTxt='prova SHA256 DIVERSO DAL PIN' } else { $pvOk=$true; $pvTxt='prova = pin (SHA256)' }; $pinA=@{}; $pinS=@{}; $axF=@(); if($pvOk){ foreach($lnP in @(Get-Content -LiteralPath $pvLoc)){ if(('' + $lnP) -match '^(Inp[A-Za-z0-9_]+)=([^|]*)\|\|.*\|\|([YN])\s*$'){ $pNm=$Matches[1]; $pVs=$Matches[2].Trim(); $pFl=$Matches[3]; if($pVs -eq 'true'){ $pVd=1.0 } elseif($pVs -eq 'false'){ $pVd=0.0 } else { $pVd=& $numF $pVs }; if($pFl -eq 'N'){ $pinA[$pNm]=$pVd } else { $axF += $pNm } } elseif(('' + $lnP) -match '^(Inp[A-Za-z0-9_]+)=([^|=]*)$'){ $pinS[$Matches[1]]=$Matches[2].Trim() } } }; $pinParse=($pvOk -and ((@($pinA.Keys).Count + @($pinS.Keys).Count) -eq $x.np) -and (@($pinS.Keys).Count -eq 1) -and $pinS.ContainsKey('InpComment') -and (@($axF).Count -eq 1) -and ($axF[0] -eq $x.ax) -and (-not $pinA.ContainsKey($x.ax)) -and (-not $pinS.ContainsKey($x.ax)) -and (@($pinA.Values | Where-Object { [double]::IsNaN($_) }).Count -eq 0) -and $pinA.ContainsKey('InpSessionTimeMode') -and ([Math]::Abs([double]$pinA['InpSessionTimeMode'] - 1.0) -le 0.000001)); $pinAtt=($x.np + 1) * 2; $tetto='tetto barre NON LETTO dal REFERTO'; $refP=Join-Path $dsk ('ROUND_' + $L + '\REFERTO_ROUND_' + $L + '.txt'); if(Test-Path -LiteralPath $refP){ $tl=@(Select-String -LiteralPath $refP -Pattern '^tetto barre' -SimpleMatch:$false | Select-Object -First 1); if($tl.Count -gt 0){ $tetto=('' + $tl[0].Line).Trim() -replace '\s+',' ' } }; $tettoMap[$L]=$tetto; $csvI=Join-Path $W ('risultati_prove\' + $E + '\' + $E + '_' + $SIMB + '_IS_' + $L + '.csv'); if((Test-Path -LiteralPath $csvI) -and ((Get-Item -LiteralPath $csvI).LastWriteTime -ge $tJ)){ $isB=(Get-Item -LiteralPath $csvI).Length; $isN=@(Get-Content -LiteralPath $csvI -ErrorAction SilentlyContinue | Where-Object { ('' + $_).Trim() -ne '' }).Count; $isTxt=('_IS fresco ' + $isB + ' byte, ' + $isN + ' righe con intestazione' + $(if($isB -eq 0 -or $isN -le 1){' (moncone di UN giorno: vuoto ATTESO, non conta)'}else{' (il moncone ha SCRITTO righe: il 2024.09.26 ha operato, si legge)'})) } else { $isTxt='_IS assente o vecchio (moncone di UN giorno: ATTESO, non conta)' }; $csvO=Join-Path $W ('risultati_prove\' + $E + '\' + $E + '_' + $SIMB + '_OOS_' + $L + '.csv'); $rO=@(); if((Test-Path -LiteralPath $csvO) -and ((Get-Item -LiteralPath $csvO).LastWriteTime -ge $tJ)){ try { $rO=@(Import-Csv -LiteralPath $csvO) } catch { $rO=@() } }; $rowsMap[$L]=$rO; $nO=$rO.Count; $nOnum=@($rO | Where-Object { ('' + $_.Trades).Trim() -match '^[0-9]+$' }).Count; $nOpos=@($rO | Where-Object { ('' + $_.Trades).Trim() -match '^[1-9][0-9]*$' }).Count; $oosOk=($nO -eq 2 -and $nOnum -eq 2 -and $nOpos -eq 2); $axOk=$false; $pinN=0; $pinKoN=0; $pinD=''; if($oosOk){ $axV=@($rO | ForEach-Object { & $numF $_.($x.ax) } | Sort-Object); $axE=@([double]$x.g1,[double]$x.g2) | Sort-Object; $axOk=($axV.Count -eq 2); if($axOk){ for($ia=0;$ia -lt 2;$ia++){ if(-not ([Math]::Abs($axV[$ia] - $axE[$ia]) -le 0.000001)){ $axOk=$false } } }; if($pinParse){ foreach($pcR in $rO){ foreach($pcK in @($pinA.Keys)){ $pinN=$pinN+1; $pcV=& $numF $pcR.$pcK; if(-not ([Math]::Abs($pcV - [double]$pinA[$pcK]) -le 0.000001)){ $pinKoN=$pinKoN+1; if($pinKoN -le 6){ $pinD=($pinD + ' ' + $pcK + '=[' + $pcR.$pcK + '] atteso ' + $pinA[$pcK]) } } }; foreach($pcK in @($pinS.Keys)){ $pinN=$pinN+1; if(('' + $pcR.$pcK).Trim() -cne $pinS[$pcK]){ $pinKoN=$pinKoN+1; if($pinKoN -le 6){ $pinD=($pinD + ' ' + $pcK + '=[' + $pcR.$pcK + '] atteso ' + $pinS[$pcK]) } } }; $pinN=$pinN+1; $pcA=& $numF $pcR.($x.ax); if(-not (([Math]::Abs($pcA - [double]$x.g1) -le 0.000001) -or ([Math]::Abs($pcA - [double]$x.g2) -le 0.000001))){ $pinKoN=$pinKoN+1; if($pinKoN -le 6){ $pinD=($pinD + ' ' + $x.ax + '=[' + $pcR.($x.ax) + '] atteso ' + $x.g1 + '/' + $x.g2) } } } }; $oosTxt=('_OOS fresco 2 righe (Trades>0 su 2) = E0 ok, asse ' + $(if($axOk){'ok'}else{'DIVERSO [' + ($axV -join '/') + ']'})) } else { $oosTxt=('_OOS NON BUONO (righe ' + $nO + ', con Trades>0 ' + $nOpos + ', attese 2 e 2) = E0 FALLITO = GUASTO VERO') }; $ptTxt=''; $ptKo=0; foreach($mg in @($x.g1,$x.g2)){ $ptf=Join-Path $env:APPDATA ('MetaQuotes\Terminal\Common\Files\abtg_trades_' + $E + '_' + $SIMB + '_' + $mg + '.csv'); $o=$null; if((Test-Path -LiteralPath $ptf) -and ((Get-Item -LiteralPath $ptf).LastWriteTime -ge $tJ)){ $o=& $ptRead $ptf $mg $x.lo $x.hi; $o.sumOk=$false; $o.prOos='n.d.'; $o.trOos='n.d.'; if($oosOk){ $rr=@(& $rowAx $rO $x.ax $mg); if($rr.Count -eq 1){ $o.prOos=('' + $rr[0].Profit); $o.trOos=('' + $rr[0].Trades); $o.sumOk=(([Math]::Abs((& $numF $rr[0].Profit) - $o.sum) -le 0.05) -and ((& $numF $rr[0].Trades) -eq $o.n)) } }; $t1=($mg + ': ' + $o.n + ' righe (' + (& $nPos $o.r) + ' posizioni), somma ' + (& $fmt $o.sum) + ', chiusure ' + $o.dMin + ' -> ' + $o.dMax + ', _OOS Trades ' + $o.trOos + ' Profit ' + $o.prOos + ' -> ' + $(if($o.bad){'PER-TRADE NON BUONO (righe ' + $o.n + ', date valide ' + $o.nDate + ', magic ' + $o.nMg + ', net leggibili ' + $o.nNet + '; attese righe > 0, prima chiusura >= 2024.09.27, ultima >= 2026.03.30 e <= 2026.06.30)'}elseif(-not $o.sumOk){'SOMMA O RIGHE DIVERSE DAL CSV _OOS (entro 0,05)'}else{'ok'}) + ', L0 deal con deal_type diverso da 0: ' + $o.l0 + $(if($o.l0 -gt 0){' (primo: ' + $o.l0first + ')'}else{''}) + ', S2 uscite fuori ' + $x.lo + '-' + $x.hi + ': ' + $o.s2 + $(if($o.s2 -gt 0){' (prima: ' + $o.s2first + ')'}else{''})); if($o.bad -or -not $o.sumOk){ $ptKo=$ptKo+1 } } else { $t1=($mg + ': per-trade MANCANTE o vecchio'); $ptKo=$ptKo+1 }; $ptMap[$L + '|' + $mg]=$o; $ptTxt=($ptTxt + ' [' + $t1 + ']') }; $g1='NON VERIFICABILE'; $oA=$ptMap[$L + '|' + $x.g1]; $oB=$ptMap[$L + '|' + $x.g2]; if($oosOk -and $ptKo -eq 0 -and $null -ne $oA -and $null -ne $oB){ $g1='ok'; $rA=@(& $rowAx $rO $x.ax $x.g1); $rB=@(& $rowAx $rO $x.ax $x.g2); if($rA.Count -ne 1 -or $rB.Count -ne 1){ $g1='NON VERIFICABILE (righe gemelle non trovate)' } else { foreach($cn in @('Trades','Profit','Profit Factor','Equity DD %','Peggior Giornata %')){ if(-not ([Math]::Abs((& $numF $rA[0].$cn) - (& $numF $rB[0].$cn)) -le 0.005)){ $g1=('KO: CSV _OOS gemelle diverse su ' + $cn + ' (' + $rA[0].$cn + ' contro ' + $rB[0].$cn + ')') } } }; if($g1 -eq 'ok'){ if($oA.n -ne $oB.n){ $g1=('KO: per-trade gemelli con righe diverse (' + $oA.n + ' contro ' + $oB.n + ')') } else { for($i=0;$i -lt $oA.n;$i++){ if($g1 -eq 'ok' -and $oA.r[$i].k -ne $oB.r[$i].k){ $g1=('KO: per-trade gemelli diversi alla riga ' + ($i+1) + ' (' + $oA.r[$i].k + ' contro ' + $oB.r[$i].k + ')') } } } } }; $ptBuoni=($null -ne $oA -and $null -ne $oB -and -not $oA.bad -and -not $oB.bad); $l0=$(if($ptBuoni){$(if(($oA.l0 + $oB.l0) -eq 0){'ok'}else{'KO: ' + ($oA.l0 + $oB.l0) + ' deal di uscita con deal_type diverso da 0 (un BUY che chiude uno short e 0: qui c e un LONG, pin InpEnableBuyGaps non arrivato?)'})}else{'NON VERIFICABILE'}); $s2=$(if($ptBuoni){$(if(($oA.s2 + $oB.s2) -eq 0){'ok'}else{'KO: ' + ($oA.s2 + $oB.s2) + ' uscite fuori ' + $x.lo + '-' + $x.hi})}else{'NON VERIFICABILE'}); $mot=New-Object System.Collections.ArrayList; if($rc -eq 1){ [void]$mot.Add('rc 1 (non e partito)') }; if($div.Count -gt 0){ [void]$mot.Add('MOTORE DIVERSO DAL PIN') }; if(-not $pvOk){ [void]$mot.Add($pvTxt) } elseif(-not $pinParse){ [void]$mot.Add('PIN DEL FILE PROVA NON LEGGIBILI (' + @($pinA.Keys).Count + ' numerici + ' + @($pinS.Keys).Count + ' stringa su ' + $x.np + ', asse ' + ($axF -join '/') + ', InpSessionTimeMode atteso 1, InpComment atteso)') }; if(-not $oosOk){ [void]$mot.Add('E0 FALLITO: CSV _OOS NON BUONO') } elseif(-not $axOk){ [void]$mot.Add('ASSE DIVERSO nel _OOS') }; if($pinKoN -gt 0){ [void]$mot.Add('P0 PIN DAL CSV _OOS DIVERSO (' + $pinKoN + ' valori:' + $pinD + ')') } elseif($oosOk -and $pinParse -and $pinN -ne $pinAtt){ [void]$mot.Add('P0 PIN DAL CSV _OOS NON LEGGIBILE (' + $pinN + ' confronti su ' + $pinAtt + ')') } elseif($oosOk -and $pinParse){ $oosTxt=($oosTxt + ', P0 PIN DAL CSV ok (' + $pinN + ' confronti su ' + $pinAtt + ')') }; if($ptKo -gt 0){ [void]$mot.Add('C0 PER-TRADE NON BUONI ' + $ptKo + ' su 2') }; if($g1 -ne 'ok'){ [void]$mot.Add('G1 ' + $g1) }; if($l0 -ne 'ok'){ [void]$mot.Add('L0 ' + $l0) }; if($s2 -ne 'ok'){ [void]$mot.Add('S2 ' + $s2) }; $rcTxt=$(if($rc -eq 2 -and $oosOk){'rc 2 (moncone _IS di un giorno = FALSO ALLARME ATTESO, assolto da E0 sul _OOS)'}elseif($rc -eq 0){'rc 0 (anche il moncone ha operato: il 2024.09.26 ha scritto Trades>0)'}elseif($rc -eq 3){'rc 3 (girato CON RILIEVI: leggere il REFERTO_ROUND)'}else{'rc ' + $rc}); $nulMap[$L]=($mot -join '; '); $pre='NON LEGGIBILE (file NULLO)'; if($mot.Count -eq 0){ $rA=@(& $rowAx $rO $x.ax $x.g1); $cfT=New-Object System.Collections.ArrayList; foreach($cd in $x.cf){ $rc1=@($oA.r | Where-Object { $_.d -eq $cd }); $sc1=0.0; foreach($q in $rc1){ $sc1=$sc1+$q.net }; [void]$cfT.Add($cd + ': ' + $(if($rc1.Count -gt 0){'POSIZIONE PRESENTE (' + (& $nPos $rc1) + ' pos., ' + $rc1.Count + ' deal, net ' + (& $fmt $sc1) + ') -> fuori dalla curva in fase, si scrive a parte'}else{'nessuna posizione'})) }; $pre=('deal ' + $oA.n + ', POSIZIONI ' + (& $nPos $oA.r) + ' (banda attesa del par. 3: 10-45 in fase, qui e il conteggio dell INTERA gamba, estate + inverno), Profit ' + $rA[0].Profit + ', PF ' + $rA[0].'Profit Factor' + ', Equity DD % ' + $rA[0].'Equity DD %' + ', Peggior Giornata % ' + $rA[0].'Peggior Giornata %' + ' (numeri GREZZI dell intera gamba, NON in fase, NON un verdetto) | SEDUTE DI CONFINE (classe 805) ' + ($cfT -join '; ')) }; $preMap[$L]=$pre; [void]$esiti.Add(($L.PadRight(6) + ' ' + $rcTxt + '   ' + $m166 + '   ' + $pvTxt + '   ' + $tetto + '   ' + $isTxt + ' | ' + $oosTxt + ' | G1 ' + $g1 + ' | L0 ' + $l0 + ' | S2 ' + $s2 + ' | per-trade' + $ptTxt + '   ' + $(if($mot.Count -gt 0){'FILE NULLO: ' + ($mot -join '; ')}else{'file NON nullo'}) + ' | PRE-LETTURA ' + $pre)) }; $ok1={ param($tt) (-not $nulMap.ContainsKey($tt)) -or ('' + $nulMap[$tt]) -eq '' }; $out=Join-Path $dsk ('ROUND_R253_GAPCONT_DAX_SHORT_' + $T0.ToString('yyyy-MM-dd')); if(Test-Path -LiteralPath $out){ Remove-Item -LiteralPath $out -Recurse -Force -ErrorAction SilentlyContinue }; New-Item -ItemType Directory -Force -Path $out | Out-Null; $nCart=0; $manc=New-Object System.Collections.ArrayList; foreach($x in $J){ $src=Join-Path $dsk ('ROUND_' + $x.t); if(Test-Path -LiteralPath $src){ Copy-Item -LiteralPath $src -Destination $out -Recurse -Force -ErrorAction SilentlyContinue; $nCart=$nCart+1 } else { [void]$manc.Add($x.t) } }; $lg=Join-Path $out 'LOG_TESTER'; New-Item -ItemType Directory -Force -Path $lg | Out-Null; $nl=0; foreach($f in @(Get-ChildItem -Path (Join-Path $env:APPDATA 'MetaQuotes') -Recurse -Filter '*.log' -ErrorAction SilentlyContinue | Where-Object { $_.LastWriteTime -ge $T0 } | Sort-Object LastWriteTime -Descending | Select-Object -First 200)){ Copy-Item -LiteralPath $f.FullName -Destination (Join-Path $lg (([string]$nl).PadLeft(4,'0') + '_' + $(if($f.Directory.Parent){$f.Directory.Parent.Name}else{'ignota'}) + '_' + $f.Directory.Name + '_' + $f.Name)) -Force -ErrorAction SilentlyContinue; $nl=$nl+1 }; $ptd=Join-Path $out 'PERTRADE'; New-Item -ItemType Directory -Force -Path $ptd | Out-Null; $npt=0; $ptAtt=2*@($J).Count; $ptManc=New-Object System.Collections.ArrayList; foreach($x in $J){ foreach($mg in @($x.g1,$x.g2)){ $ptf=Join-Path $env:APPDATA ('MetaQuotes\Terminal\Common\Files\abtg_trades_' + $x.e + '_' + $x.s + '_' + $mg + '.csv'); if((Test-Path -LiteralPath $ptf) -and $tIni.ContainsKey($x.t) -and ((Get-Item -LiteralPath $ptf).LastWriteTime -ge $tIni[$x.t])){ Copy-Item -LiteralPath $ptf -Destination $ptd -Force; $npt=$npt+1 } else { [void]$ptManc.Add($x.t + '/' + $mg) } } }; $nulli=New-Object System.Collections.ArrayList; $nonNulli=New-Object System.Collections.ArrayList; foreach($x in $J){ if(-not (& $ok1 $x.t)){ [void]$nulli.Add($x.t + ' (' + $nulMap[$x.t] + ')') } else { [void]$nonNulli.Add($x.t) } }; $ri=New-Object System.Collections.ArrayList; [void]$ri.Add('RIEPILOGO R253 -- RICERCA: CONTINUAZIONE DEL GAP-DOWN ALL APERTURA XETRA, SOLO SHORT (2 file, 4 celle, 8 passate di cui 4 monconi, ABTG_GapContinuation D30EUR, grafico M5 inerte, TICK REALI, deposito 10000, rischio di banco 1,0)'); [void]$ri.Add('data: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss')); [void]$ri.Add('fine: ' + (Get-Date).ToString('yyyy-MM-dd HH:mm:ss') + '   durata minuti: ' + [int](((Get-Date)-$T0).TotalMinutes) + '   (dichiarato 5-12 [STIMA], file di testa par. 7; tetto 40, oltre ci si ferma e si guarda; questa e la PRIMA misura di tempo di questo EA su questa macchina: va nel referto)'); [void]$ri.Add('pc  : ' + $env:COMPUTERNAME + '   (PC di backtest; VPS VMI3047753 e TUTTE le sue cartelle dati NON toccati)'); [void]$ri.Add('pin : ' + $PIN); [void]$ri.Add('deposito 10000 passato; modello 4; spread, ritardo e FrazioneIS NON passati; @DAQUANDO 2024.09.26 @FINOA 2026.06.30 @FRAZIONEIS 0.001 -> gamba IS = MONCONE 2024.09.26, gamba OOS = CONTINUA 2024.09.27 -> 2026.06.30 (641 giorni in una passata sola); etichette R253a, R253b in quest ordine; modello della riga RIGA_R252 (a sua volta RIGA_R246_OROLOGIO finestre A), classe 766'); [void]$ri.Add(''); foreach($z in $esiti){ [void]$ri.Add($z) }; [void]$ri.Add(''); [void]$ri.Add('rc 0 = ROUND GIRATO (anche il moncone ha operato) | 2 = NON MISURATO (moncone vuoto: ATTESO, assolto SOLO da E0 = _OOS a 2 righe con Trades>0) | 3 = GIRATO CON RILIEVI (leggere il REFERTO_ROUND) | 1 = non e partito. Guasto vero = rc 1, oppure _OOS mancante, vecchio o senza 2 righe con Trades>0, oppure per-trade non della gamba continua.'); [void]$ri.Add('ROUND PARTITI (rc diverso da 1): ' + $nPart + ' su ' + @($J).Count); [void]$ri.Add('CLASSE 166 (EA e include arrivano dal RAMO lavoro, non dal pin; SHA256 dopo ogni job: ABTG_GapContinuation.mq5 + ABTG_PausaGuardian.mqh, unico include nostro dell EA; piu il file prova scaricato dal pin): ' + $(if($nPart -eq 0){'NESSUN ROUND PARTITO: niente di verificato'}elseif($nDiv -eq 0){'MOTORE = PIN in tutti i ' + $nPart + ' round partiti su ' + @($J).Count}else{'MOTORE DIVERSO DAL PIN in ' + $nDiv + ' round: quei file NON si leggono, si rifa il pin sul ramo e si rilancia'})); [void]$ri.Add('TETTO BARRE (riga del REFERTO_ROUND, job per job; atteso MaxBars=100000000): ' + ((@($J | ForEach-Object { $_.t + ' [' + $tettoMap[$_.t] + ']' })) -join ' | ')); [void]$ri.Add('CARTELLE ATTESE: 2   TROVATE: ' + $nCart + $(if($nCart -eq 2){'   -> CATENA COMPLETA'}else{'   -> CATENA ROTTA, MANCANO: ' + ($manc -join ', ')})); [void]$ri.Add('PERTRADE (C0: Common Files, abtg_trades_ABTG_GapContinuation_D30EUR_<magic>.csv, magic 792701/792751 e 792702/792752, scritti DOPO l avvio del proprio job; UNO per magic = la gamba CONTINUA 2024.09.27 -> 2026.06.30, che gira per seconda e sovrascrive il moncone, classe 455): ' + $npt + ' su ' + $ptAtt + $(if($ptManc.Count -gt 0){'   MANCANO: ' + ($ptManc -join ', ')}else{''})); [void]$ri.Add('LOG_TESTER: ' + $nl + ' file .log scritti DOPO l avvio (servono se un round esce NON MISURATO per un guasto vero; in ottimizzazione Print e spento, classe 526: il funnel di PrintFunnel NON e qui, serve una corsa singola).'); [void]$ri.Add(''); [void]$ri.Add('PRE-LETTURA DELLA RIGA (NON e il verdetto, NON e la curva in fase: quella la ricompone il referto dai per-trade = righe d estate di R253a + righe d inverno di R253b, testa par. 4; K1, R1-R3, O1, O2 e il confronto di segno con CE9 sono del referto, testa par. 6):'); [void]$ri.Add('FILE NULLI (rc 1, motore diverso dal pin, prova diversa dal pin, E0 fallito, asse o P0 pin dal CSV diversi, C0 per-trade non buoni, G1, L0 o S2 falliti; escono da OGNI conteggio, classi 772/775/781): ' + $(if($nulli.Count -gt 0){$nulli -join ' | '}else{'nessuno'})); [void]$ri.Add('FILE NON NULLI, per nome: ' + $(if($nonNulli.Count -gt 0){$nonNulli -join ', '}else{'NESSUNO'})); [void]$ri.Add('P0 PIN DAL CSV (classe 780): per ogni riga del _OOS i 33 pin fissi del file prova (32 numerici come numeri, bool 0/1, InpSessionTimeMode = 1; InpComment come stringa esatta) e InpMagicNumber = gemella: colonna assente o valore diverso = NULLO; confronti diversi da 68 = NON LEGGIBILE. Vedi colonna _OOS negli ESITI'); [void]$ri.Add('G1 DETERMINISMO (per file: gemelle identiche nel CSV _OOS su Trades, Profit, PF, Equity DD, Peggior Giornata e per-trade identici riga per riga tranne il magic; KO = quel file NULLO): vedi colonna G1 negli ESITI'); [void]$ri.Add('L0 LATO (tutti i deal di uscita del per-trade con deal_type 0 = BUY che chiude uno short; un deal_type 1 = un LONG = pin InpEnableBuyGaps non arrivato; KO = quel file NULLO): vedi colonna L0 negli ESITI'); [void]$ri.Add('S2 OROLOGIO (R253a: tutte le uscite con 08:15:00 <= close_time <= 16:26:59 server; R253b: 09:15:00 <= close_time <= 17:26:59; contro-esempio: InpSessionTimeMode non arrivato = seduta JST convertita, uscite fra 01:15 e 10:25 o 02:15 e 11:25; KO = quel file NULLO. La CHIUSURA non arrivata S2 non la vede: la prende P0): vedi colonna S2 negli ESITI'); [void]$ri.Add('PRE-LETTURA GREZZA per file (deal, posizioni = position_id distinti, Profit, PF, Equity DD %, Peggior Giornata % dell INTERA gamba continua; le 4 sedute di confine della classe 805 segnalate se hanno una posizione): ' + ((@($J | ForEach-Object { $_.t + ' -> ' + $preMap[$_.t] })) -join ' || ')); [void]$ri.Add(''); [void]$ri.Add('DA QUI IN POI E LETTURA, NON RIGA: curva in fase = righe d estate di R253a + righe d inverno di R253b (inverno = 2024.10.27 <= d < 2025.03.30 e 2025.10.26 <= d < 2026.03.29 per data di chiusura, sedute di confine escluse); K1 stop mediano >= 68 pti; R1 DD <= 6,5% / R2 peggior giornata >= -1,10% / R3 serie perdente <= 8 con verdetto ASIMMETRICO (classe 804: ROSSO boccia a qualunque n, VERDE si scrive NON VIOLATO su n = X); MERITO SOSPESO per aritmetica; O1 contro 770411 (R246i/R246k), O2 contro R252a/R252b SOLO se G1 e S1 loro sono verdi, altrimenti NON MISURABILE; CERTIFICATO: comunque vada NON ANCORA MISURATO (testa par. 6).'); ($ri -join "`r`n") | Set-Content -LiteralPath (Join-Path $out 'RIEPILOGO_R253.txt') -Encoding ASCII; $zip=Join-Path $dsk ('ROUND_R253_GAPCONT_DAX_SHORT_' + $T0.ToString('yyyy-MM-dd') + '.zip'); if(Test-Path -LiteralPath $zip){ Remove-Item -LiteralPath $zip -Force -ErrorAction SilentlyContinue }; Compress-Archive -Path (Join-Path $out '*') -DestinationPath $zip -Force; $fAtt=New-Object System.Collections.ArrayList; foreach($x in $J){ [void]$fAtt.Add('ROUND_' + $x.t + '\REFERTO_ROUND_' + $x.t + '.txt'); [void]$fAtt.Add('ROUND_' + $x.t + '\' + $x.e + '_' + $x.s + '_IS_' + $x.t + '.csv'); [void]$fAtt.Add('ROUND_' + $x.t + '\' + $x.e + '_' + $x.s + '_OOS_' + $x.t + '.csv'); [void]$fAtt.Add('ROUND_' + $x.t + '\' + $x.p) }; foreach($x in $J){ foreach($mg in @($x.g1,$x.g2)){ [void]$fAtt.Add('PERTRADE\abtg_trades_' + $x.e + '_' + $x.s + '_' + $mg + '.csv') } }; [void]$fAtt.Add('RIEPILOGO_R253.txt'); $nfOk=0; Write-Host ''; Write-Host ('RACCOLTA: ' + $out); Write-Host ('FILE ATTESI NELLA RACCOLTA (' + $fAtt.Count + '), controllati uno per uno:') -ForegroundColor Cyan; foreach($fa in $fAtt){ $fp=Join-Path $out $fa; if(Test-Path -LiteralPath $fp){ $nfOk=$nfOk+1; $fb=(Get-Item -LiteralPath $fp).Length; if($fa -like '*_IS_*.csv' -and $fb -eq 0){ Write-Host ('   OK    ' + $fa + '   (0 byte: moncone di UN giorno, ATTESO)') -ForegroundColor Green } else { Write-Host ('   OK    ' + $fa + '   (' + $fb + ' byte)') -ForegroundColor Green } } else { if($fa -like '*_IS_*.csv'){ Write-Host ('   MANCA ' + $fa + '   (moncone di UN giorno: il driver copia solo i CSV freschi, un _IS mai scritto NON e un guasto; conta il _OOS)') -ForegroundColor Yellow } else { Write-Host ('   MANCA ' + $fa) -ForegroundColor Red } } }; Write-Host ('   piu LOG_TESTER\ con ' + $nl + ' file .log') -ForegroundColor Gray; Write-Host ('FILE ATTESI TROVATI: ' + $nfOk + ' su ' + $fAtt.Count + ' (i CSV _IS mancanti o da 0 byte sono ATTESI: 4 monconi)') -ForegroundColor $(if($nfOk -eq $fAtt.Count){'Green'}else{'Yellow'}); if(Test-Path -LiteralPath $zip){ Write-Host ('ZIP PRONTO DA MANDARE: ' + $zip) -ForegroundColor Green } else { Write-Host ('ZIP NON CREATO: ' + $zip + ' -- manda la cartella ' + $out) -ForegroundColor Red }; if($nCart -eq 2){ Write-Host 'CATENA COMPLETA: 2 cartelle su 2.' -ForegroundColor Green } else { Write-Host ('CATENA ROTTA: attese 2, TROVATE ' + $nCart + '. MANCANO: ' + ($manc -join ', ')) -ForegroundColor Red }; if($npt -eq $ptAtt){ Write-Host ('PERTRADE: ' + $npt + ' su ' + $ptAtt + ' freschi.') -ForegroundColor Green } else { Write-Host ('PERTRADE: ' + $npt + ' su ' + $ptAtt + ' freschi. MANCANO: ' + ($ptManc -join ', ')) -ForegroundColor Red }; Write-Host ('ROUND PARTITI (rc diverso da 1): ' + $nPart + ' su ' + @($J).Count) -ForegroundColor $(if($nPart -eq @($J).Count){'Green'}else{'Red'}); if($nPart -eq 0){ Write-Host 'CLASSE 166: NESSUN ROUND PARTITO, niente di verificato.' -ForegroundColor Red } elseif($nDiv -eq 0){ Write-Host ('CLASSE 166: il motore compilato e quello del pin in tutti i ' + $nPart + ' round partiti su ' + @($J).Count + ' (SHA256).') -ForegroundColor Green } else { Write-Host ('CLASSE 166: MOTORE DIVERSO DAL PIN in ' + $nDiv + ' round. Quei file NON si leggono: si rifa il pin sul ramo e si rilancia.') -ForegroundColor Red }; Write-Host ('TETTO BARRE: ' + ((@($J | ForEach-Object { $_.t + ' [' + $tettoMap[$_.t] + ']' })) -join ' | ')) -ForegroundColor $(if(@($J | Where-Object { $tettoMap[$_.t] -notlike '*MaxBars=100000000*' }).Count -eq 0){'Green'}else{'Yellow'}); Write-Host ('FILE NULLI: ' + $(if($nulli.Count -gt 0){$nulli -join ' | '}else{'nessuno'})) -ForegroundColor $(if($nulli.Count -eq 0){'Green'}else{'Red'}); Write-Host ('FILE NON NULLI, per nome: ' + $(if($nonNulli.Count -gt 0){$nonNulli -join ', '}else{'NESSUNO'})) -ForegroundColor $(if($nonNulli.Count -eq @($J).Count){'Green'}else{'Yellow'}); foreach($x in $J){ Write-Host ('PRE-LETTURA GREZZA ' + $x.t + ': ' + $preMap[$x.t]) -ForegroundColor Gray }; Get-ChildItem -LiteralPath $out | Select-Object Name,LastWriteTime | Format-Table -AutoSize | Out-Host; Write-Host 'ESITI:' -ForegroundColor Cyan; foreach($z in $esiti){ Write-Host ('   ' + $z) }; Write-Host 'PRE-LETTURA, NON VERDETTO: la curva in fase NON e qui, la ricompone il referto dai 4 per-trade (testa par. 4); K1, R1-R3, O1, O2 e il confronto di segno con CE9 li scrive il referto coi criteri congelati nel file di testa (par. 6). E RICERCA: comunque vada il certificato resta NON ANCORA MISURATO. Manda lo ZIP comunque, anche se qualcosa qui sopra e rosso.' -ForegroundColor Cyan; Write-Host ('durata totale minuti: ' + [int](((Get-Date)-$T0).TotalMinutes)) -ForegroundColor Cyan }