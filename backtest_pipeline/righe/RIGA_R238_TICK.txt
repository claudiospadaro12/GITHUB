& { $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; $PIN='fb910b03885ef61975b8e9fd4a0d4a758e42d85f'; $T0=Get-Date; if($env:COMPUTERNAME -ne 'DESKTOP-H4D7CAJ'){ throw ('QUESTA RIGA GIRA SOLO SUL PC DI BACKTEST DESKTOP-H4D7CAJ. Qui la macchina si chiama: ' + $env:COMPUTERNAME + '. Sul VPS VMI3047753 non si lancia: la challenge FTMO 541452707 sta operando.') }; if((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0){ throw 'MT5 risulta APERTO su questo PC. Chiudilo A MANO (terminale del PC di backtest, conto 50503392) e rilancia: col terminale aperto il tester non parte e escono ZERO CSV.' }; $W=Join-Path $env:USERPROFILE 'abtg_round'; New-Item -ItemType Directory -Force -Path $W | Out-Null; $S=Join-Path $W 'RIGA_ROUND_VPS.ps1'; Remove-Item -LiteralPath $S -Force -ErrorAction SilentlyContinue; irm ('https://raw.githubusercontent.com/claudiospadaro12/GITHUB/' + $PIN + '/backtest_pipeline/righe/RIGA_ROUND_VPS.ps1?cb=' + [Guid]::NewGuid().ToString('N')) -OutFile $S; if(-not (Select-String -LiteralPath $S -SimpleMatch -Pattern 'MARCATORE_RIGA_ROUND_VPS_v2' -Quiet)){ throw 'RIGA_ROUND_VPS.ps1 scaricata SENZA il marcatore v2: copia vecchia o cache di GitHub. Non si parte.' }; Write-Host ('pc  : ' + $env:COMPUTERNAME) -ForegroundColor Green; Write-Host ('pin : ' + $PIN) -ForegroundColor Green; Write-Host ('data: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss')) -ForegroundColor Green; Write-Host 'R238 -- la finestra oraria a TICK REALI, due lati. 8 passate, attese ~43 minuti.' -ForegroundColor Cyan; Write-Host 'Deposito 100000 PASSATO ESPLICITAMENTE: il default del driver e 10000 e cambierebbe n.' -ForegroundColor Yellow; $ErrorActionPreference='Continue'; $J=@(@{e='ABTG_SupertrendReversal';p='R238a_finestra_TICK_SUPREV_NASUSD_short.txt';t='R238a';m=4},@{e='ABTG_SupertrendReversal';p='R238b_finestra_TICK_SUPREV_NASUSD_long.txt';t='R238b';m=4}); $esiti=New-Object System.Collections.ArrayList; $dsk=[Environment]::GetFolderPath('Desktop'); foreach($x in $J){ $vec=Join-Path $dsk ('ROUND_' + $x.t); if(Test-Path -LiteralPath $vec){ Remove-Item -LiteralPath $vec -Recurse -Force -ErrorAction SilentlyContinue; Write-Host ('  tolta la cartella di una corsa PRECEDENTE: ROUND_' + $x.t) -ForegroundColor DarkYellow }; $vez=Join-Path $dsk ('ROUND_' + $x.t + '.zip'); if(Test-Path -LiteralPath $vez){ Remove-Item -LiteralPath $vez -Force -ErrorAction SilentlyContinue } }; foreach($x in $J){ $E=$x.e; $P=$x.p; $L=$x.t; $M=$x.m; $att=0; while((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0 -and $att -lt 36){ Start-Sleep -Seconds 5; $att=$att+1 }; if((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0){ Write-Host ('ATTENZIONE: dopo 180 secondi MT5 e ancora VIVO. Il round ' + $L + ' parte lo stesso, ma il pre-volo del driver lo fermera con rc 1: NON sara misurato.') -ForegroundColor Red }; Write-Host ''; Write-Host ('=== ' + $L + '   ' + $E + '   ' + $P + '   modello ' + $M + '   deposito 100000 ===') -ForegroundColor Cyan; & powershell.exe -NoProfile -ExecutionPolicy Bypass -File $S -Expert $E -Prova $P -Etichetta $L -Pin $PIN -Modello $M -Deposito 100000; [void]$esiti.Add(($L.PadRight(6) + ' modello ' + $M + '   rc ' + $LASTEXITCODE)) }; $out=Join-Path $dsk 'ROUND_R238_2026-09-23'; if(Test-Path -LiteralPath $out){ Remove-Item -LiteralPath $out -Recurse -Force -ErrorAction SilentlyContinue }; New-Item -ItemType Directory -Force -Path $out | Out-Null; $nCart=0; $manc=New-Object System.Collections.ArrayList; foreach($x in $J){ $src=Join-Path $dsk ('ROUND_' + $x.t); if(Test-Path -LiteralPath $src){ Copy-Item -LiteralPath $src -Destination $out -Recurse -Force -ErrorAction SilentlyContinue; $nCart=$nCart+1 } else { [void]$manc.Add($x.t) } }; foreach($et in @('R238a','R238b')){ $cs=Join-Path $out ('ROUND_' + $et); if(Test-Path -LiteralPath $cs){ (@('NON PROMUOVIBILE -- E IL MOTIVO NON E IL COSTO. LEGGERE PRIMA DI OGNI VERDETTO.','','1) IL DIVIETO DI PROMOZIONE POGGIA SULL n, ed e definitivo:','     corto  : 16 IS / 24 OOS sulla cella ACCESA','     lungo  : 45 IS / 76 OOS spenta, 37 IS / 52 OOS accesa','   Sotto 150 (Emendamento A del 16/08). Nessuna promozione, nessuno','   spegnimento, qualunque PF esca.','','2) IL CANCELLO DEL COSTO E SOSPESO, NON PRONUNCIATO. Una prima stesura di','   questo round lo dava per FALLITO citando lo stop 27,10 idx della sedia','   970913. Il cancello ha bocciato quel trasferimento, ed era giusto:','     - ABTG_SupertrendReversal.mq5 r.386-389 fa','         sl = isLong ? MathMin(stLine,ext)-buf : MathMax(stLine,ext)+buf','       cioe lo stop e il PIU PROTETTIVO fra la linea Supertrend e l estremo','       di swing, e il buffer e inerte (3 pip = 0,03 idx). Quindi lo stop','       dipende da InpStMult e InpStAtrPeriod, che la descrizione d archivio','       (swing 5 barre + 3 pip) NON contiene;','     - e quei parametri sono DIVERSI: 970913 gira Supertrend (10 x 3,0),','       R238a (7 x 3,5), R238b (12 x 2,5). Tre geometrie, non una;','     - il 27,10 e uno stop REALIZZATO con breakeven e trailing accesi:','       e un LIMITE INFERIORE, non una distanza d ingresso;','     - e l n=5 e un pool dei DUE lati.','   >>> La distanza vera ingresso->SL di queste due geometrie resta','       NON MISURATA. Serve la FASE 1 (corsa singola, Optimization=0,','       Giornale acceso), che oggi non esiste come riga di lancio.','','3) LA FALSIFICAZIONE, congelata prima dei numeri. NON e una soglia sull n:','   questo EA tiene UNA POSIZIONE ALLA VOLTA (r.313-320, if(HasPosition())','   ... return), quindi a tick reali n si muove LEGITTIMAMENTE.','   Si guardano i contatori dell imbuto nel GIORNALE (InpLogImbuto e gia','   pinnato, l EA li stampa a r.200-218):','     A BARRA CHIUSA, DEVONO ESSERE IDENTICI fra OHLC e tick:','        valutate . supertrend n/d . fuori orario','        se si muovono QUESTI, ho misurato il BANCO e non il motore;','     INTRABARRA, POSSONO muoversi ED E IL RISULTATO:','        occupata . pendente in attesa','   >>> I contatori stanno nel Giornale, NON nel CSV. Se questa corsa non li','       consegna, il controllo NON E STATO FATTO e va scritto cosi.','','4) A COSA SERVE ALLORA QUESTO ROUND: a dire se il vantaggio della finestra','   (OOS PF da 1,671 a 3,729 sul corto, letto in OHLC) SOPRAVVIVE al tick','   reale, oppure era un artefatto delle quattro meccaniche intrabarra che','   l OHLC indovina (pendente, trailing, breakeven, mezza uscita a TP1).','   E a misurare il RISCHIO a tick reali, che si legge a qualunque n.') -join "`r`n") | Set-Content -LiteralPath (Join-Path $cs 'NON_PROMUOVIBILE.txt') -Encoding ASCII } }; $lg=Join-Path $out 'LOG_TESTER'; New-Item -ItemType Directory -Force -Path $lg | Out-Null; $nl=0; foreach($f in @(Get-ChildItem -Path (Join-Path $env:APPDATA 'MetaQuotes') -Recurse -Filter '*.log' -ErrorAction SilentlyContinue | Where-Object { $_.LastWriteTime -ge $T0 } | Sort-Object LastWriteTime -Descending | Select-Object -First 200)){ Copy-Item -LiteralPath $f.FullName -Destination (Join-Path $lg (([string]$nl).PadLeft(4,'0') + '_' + $f.Directory.Name + '_' + $f.Name)) -Force -ErrorAction SilentlyContinue; $nl=$nl+1 }; $ri=New-Object System.Collections.ArrayList; [void]$ri.Add('RIEPILOGO R238 -- la finestra oraria a TICK REALI, due lati (NASUSD H1, SupertrendReversal)'); [void]$ri.Add('data: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss') + '   <-- SE QUESTA DATA NON E DI OGGI, IL FILE E VECCHIO'); [void]$ri.Add('fine: ' + (Get-Date).ToString('yyyy-MM-dd HH:mm:ss') + '   durata minuti: ' + [int](((Get-Date)-$T0).TotalMinutes) + '   (attese ~43)'); [void]$ri.Add('pc  : ' + $env:COMPUTERNAME + '   (PC di backtest; VPS VMI3047753 NON toccato)'); [void]$ri.Add('pin : ' + $PIN); [void]$ri.Add('deposito passato: 100000 (il default del driver e 10000)'); [void]$ri.Add('-Spread: NON passato, come nella corsa di R236e/f. Confronto pulito.'); [void]$ri.Add(''); foreach($z in $esiti){ [void]$ri.Add($z) }; [void]$ri.Add(''); [void]$ri.Add('rc 0 = ROUND GIRATO | 2 = NON MISURATO | 3 = GIRATO CON RILIEVI | 1 = non e partito'); [void]$ri.Add(''); [void]$ri.Add('CARTELLE ATTESE: 2   TROVATE: ' + $nCart + $(if($nCart -eq 2){'   -> CATENA COMPLETA'}else{'   -> CATENA ROTTA, MANCANO: ' + ($manc -join ', ')})); [void]$ri.Add(''); [void]$ri.Add('NON PROMUOVIBILE, e il motivo NON e il costo: il divieto poggia sull n'); [void]$ri.Add('(16/24 sul corto, max 76 sul lungo, sotto 150). Il cancello del costo e'); [void]$ri.Add('SOSPESO: serve la FASE 1, che non esiste ancora. Leggere il file'); [void]$ri.Add('NON_PROMUOVIBILE.txt dentro ogni cartella PRIMA di scrivere qualunque verdetto.'); ($ri -join "`r`n") | Set-Content -LiteralPath (Join-Path $out 'RIEPILOGO_R238.txt') -Encoding ASCII; $zip=Join-Path $dsk 'ROUND_R238_2026-09-23.zip'; if(Test-Path -LiteralPath $zip){ Remove-Item -LiteralPath $zip -Force -ErrorAction SilentlyContinue }; Compress-Archive -Path (Join-Path $out '*') -DestinationPath $zip -Force; Write-Host ''; Write-Host ('RACCOLTA: ' + $out); Write-Host ('ZIP PRONTO DA MANDARE: ' + $zip) -ForegroundColor Green; Write-Host 'FILE ATTESI NELLO ZIP:' -ForegroundColor Gray; foreach($x in $J){ Write-Host ('   ROUND_' + $x.t + ' = REFERTO_ROUND_' + $x.t + '.txt + CSV IS + CSV OOS + ' + $x.p + ' + NON_PROMUOVIBILE.txt') -ForegroundColor Gray }; Write-Host '   RIEPILOGO_R238.txt' -ForegroundColor Gray; if($nCart -eq 2){ Write-Host 'CATENA COMPLETA: 2 cartelle su 2.' -ForegroundColor Green } else { Write-Host ('CATENA ROTTA: cartelle attese 2, TROVATE ' + $nCart + '. MANCANO: ' + ($manc -join ', ')) -ForegroundColor Red }; Write-Host ('   LOG_TESTER = ' + $nl + ' file .log del tester (servono se un round esce NON MISURATO, e contengono i contatori dell imbuto)') -ForegroundColor Gray; Get-ChildItem -LiteralPath $out | Select-Object Name,LastWriteTime | Format-Table -AutoSize | Out-Host; Write-Host 'ESITI:' -ForegroundColor Cyan; foreach($z in $esiti){ Write-Host ('   ' + $z) }; Write-Host ('durata totale minuti: ' + [int](((Get-Date)-$T0).TotalMinutes)) -ForegroundColor Cyan }