& { $ErrorActionPreference='Continue'; $INV=[Globalization.CultureInfo]::InvariantCulture; $BT='C:\Program Files\BCM Markets MT5 Terminal'; Write-Host ''; Write-Host '=== PRE-VOLO PC DI BACKTEST -- SOLA LETTURA, NON TOCCA NIENTE ===' -ForegroundColor Cyan; Write-Host ('macchina: ' + $env:COMPUTERNAME) -ForegroundColor Yellow; if($env:COMPUTERNAME -ne 'DESKTOP-H4D7CAJ'){ Write-Host ('  QUESTA NON E LA MACCHINA ATTESA: la riga dei round pretende DESKTOP-H4D7CAJ.') -ForegroundColor Red; Write-Host ('  MANDAMI IL NOME QUI SOPRA e non lanciare niente. Mi fermo adesso: su una macchina') -ForegroundColor Red; Write-Host ('  diversa quel percorso di programma e un terminale VIETATO con sedie vive, e non lo tocco.') -ForegroundColor Red; throw 'PRE-VOLO FERMATO: macchina diversa da DESKTOP-H4D7CAJ. Niente e stato letto ne toccato.' }; Write-Host '  OK: e il nome che la riga dei round si aspetta.' -ForegroundColor Green; Write-Host ''; Write-Host '--- 1) MT5 APERTI su questa macchina (la riga dei round NON parte se ce ne sono) ---' -ForegroundColor Cyan; $pr=@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue); if($pr.Count -eq 0){ Write-Host '  nessun terminal64 vivo: la riga dei round puo partire.' -ForegroundColor Green } else { Write-Host ('  ' + $pr.Count + ' terminale/i APERTO/I -- vanno chiusi A MANO prima di lanciare i round:') -ForegroundColor Red; foreach($p in $pr){ $pt='(percorso non leggibile)'; try{ $pt=$p.Path }catch{ $pt='(percorso non leggibile)' }; Write-Host ('    PID ' + $p.Id + '  |  ' + $p.MainWindowTitle + '  |  ' + $pt) -ForegroundColor Red } }; Write-Host ''; Write-Host '--- 2) IL COMPILATORE (senza, il round muore in due secondi con rc 1) ---' -ForegroundColor Cyan; $med=Join-Path $BT 'metaeditor64.exe'; if(Test-Path -LiteralPath $med -PathType Leaf){ Write-Host ('  metaeditor64.exe OK   ' + $med) -ForegroundColor Green } else { Write-Host ('  metaeditor64.exe MANCA in ' + $BT + '  -> il round si ferma al primo file.') -ForegroundColor Red }; Write-Host ''; Write-Host '--- 3) QUALE CARTELLA DATI USERA IL ROUND (stessa regola del driver: origin.txt) ---' -ForegroundColor Cyan; $DF=''; $altre=@(); $root=Join-Path $env:APPDATA 'MetaQuotes\Terminal'; if(Test-Path -LiteralPath $root){ foreach($d in @(Get-ChildItem -LiteralPath $root -Directory -ErrorAction SilentlyContinue)){ if($d.Name -ieq 'Common'){ continue }; $o=Join-Path $d.FullName 'origin.txt'; $inst='(nessun origin.txt)'; if(Test-Path -LiteralPath $o){ try{ $inst=(Get-Content -LiteralPath $o -Raw -ErrorAction Stop).Trim() }catch{ $inst='(origin.txt illeggibile)' } }; if($inst -ieq $BT -and $DF -eq ''){ $DF=$d.FullName; Write-Host ('  E QUESTA: ' + $d.Name) -ForegroundColor Green; Write-Host ('            origin.txt = ' + $inst) -ForegroundColor Green } else { $altre=$altre+($d.Name + '   origin = ' + $inst) } } } else { Write-Host ('  NON TROVATA: ' + $root) -ForegroundColor Yellow }; if($DF -eq '' -and (Test-Path -LiteralPath (Join-Path $BT 'config\common.ini'))){ $DF=$BT; Write-Host '  E QUESTA: installazione PORTABLE -- i dati stanno DENTRO la cartella programma.' -ForegroundColor Green }; if($DF -eq ''){ Write-Host '  NESSUNA cartella dati riconducibile a quel terminale: il round andrebbe a tentoni.' -ForegroundColor Red; Write-Host '  MANDAMI QUESTO OUTPUT PRIMA DI LANCIARE.' -ForegroundColor Red }; foreach($a in $altre){ Write-Host ('  (altra cartella dati -- il round NON la usa): ' + $a) -ForegroundColor DarkGray }; Write-Host ''; Write-Host '--- 4) TETTO BARRE NEL GRAFICO -- classe 160, e il controllo che vale di piu ---' -ForegroundColor Cyan; $mb=$null; if($DF -ne ''){ $ci=Join-Path $DF 'config\common.ini'; if(Test-Path -LiteralPath $ci){ foreach($l in @(Get-Content -LiteralPath $ci -ErrorAction SilentlyContinue)){ if($l -match '^\s*MaxBars(InChart)?\s*=\s*([0-9]+)\s*$'){ $mb=$Matches[2] } } } }; if($null -eq $mb){ Write-Host '  MaxBars NON LEGGIBILE da qui: il driver lo dira e chiedera di metterlo a mano.' -ForegroundColor Yellow } elseif([int64]$mb -ge 1000 -and [int64]$mb -lt 200000){ Write-Host ('  MaxBars=' + $mb + '  -> IL ROUND MUORE AL PRIMO FILE (rc 1).') -ForegroundColor Red; Write-Host '  Va messo Illimitato PRIMA (Strumenti > Opzioni > Grafici), poi MT5 CHIUSO, poi si lancia.' -ForegroundColor Red } else { Write-Host ('  MaxBars=' + $mb + ' -- sufficiente.') -ForegroundColor Green }; Write-Host ''; Write-Host '--- 5) EX5 GIA PRESENTI -- SOLO INFORMATIVO, NON E UNA GARANZIA ---' -ForegroundColor Cyan; Write-Host '  Il round CANCELLA l ex5 e RICOMPILA da un mq5 appena scaricato' -ForegroundColor Yellow; Write-Host '  (walkforward generico r.1927 Remove-Item + r.1929 compile, classe 270).' -ForegroundColor Yellow; Write-Host '  Quindi un ex5 presente NON promette niente e uno assente NON e un problema.' -ForegroundColor Yellow; if($DF -ne ''){ $ex=Join-Path $DF 'MQL5\Experts'; foreach($e in @('ABTG_IntradayMomentum','ABTG_EMA200','ABTG_SupertrendReversal')){ $f=Join-Path $ex ($e + '.ex5'); if(Test-Path -LiteralPath $f){ Write-Host ('    presente ' + $e + '   del ' + (Get-Item -LiteralPath $f).LastWriteTime.ToString('yyyy-MM-dd HH:mm',$INV) + '   (verra comunque rifatto)') -ForegroundColor Gray } else { Write-Host ('    assente  ' + $e + '   (lo compila la corsa)') -ForegroundColor Gray } } }; Write-Host ''; Write-Host '--- 6) CACHE LOCALE dei tre simboli -- NON e la profondita del BROKER ---' -ForegroundColor Cyan; Write-Host '  Cosa dice davvero: se qui manca roba, MT5 la SCARICA durante la corsa.' -ForegroundColor Yellow; Write-Host '  Il round non si ACCORCIA: si ALLUNGA. Serve a prevedere la DURATA.' -ForegroundColor Yellow; if($DF -ne ''){ $tk=Join-Path $DF 'bases'; if(-not (Test-Path -LiteralPath $tk)){ Write-Host ('    nessuna cartella bases sotto ' + $DF) -ForegroundColor DarkGray } else { foreach($s in @('NASUSD','U30USD','D30EUR')){ $dirs=@(Get-ChildItem -LiteralPath $tk -Directory -Recurse -Depth 3 -Filter $s -ErrorAction SilentlyContinue); if($dirs.Count -eq 0){ Write-Host ('    ' + $s + ': nessuna cartella sotto bases (cache mai costruita)') -ForegroundColor DarkGray } else { foreach($dd in $dirs){ $foglie=@($dd); $diretti=@(Get-ChildItem -LiteralPath $dd.FullName -File -ErrorAction SilentlyContinue); if($diretti.Count -eq 0){ $foglie=@(Get-ChildItem -LiteralPath $dd.FullName -Directory -ErrorAction SilentlyContinue) }; if($foglie.Count -eq 0){ Write-Host ('    ' + $s + ': niente file ne sottocartelle -> ' + $dd.FullName) -ForegroundColor DarkYellow } else { foreach($fo in $foglie){ $ff=@(Get-ChildItem -LiteralPath $fo.FullName -File -ErrorAction SilentlyContinue | Sort-Object Name); $et=$dd.Parent.Name + '/' + $fo.Name; if($ff.Count -eq 0){ Write-Host ('    ' + $s + ' [' + $et + ']: vuota') -ForegroundColor DarkGray } else { $nomi=@($ff | ForEach-Object { $_.Name }); if($nomi.Count -gt 40){ $nomi=@($nomi[0..39]) + ('...(' + $ff.Count + ' in tutto)') }; Write-Host ('    ' + $s + ' [' + $et + ']: ' + $ff.Count + ' file -> ' + ($nomi -join ' ')) -ForegroundColor Green } } } } } } } }; Write-Host ''; Write-Host '--- 7) CHE COSA MANDARMI ---' -ForegroundColor Cyan; Write-Host '  Copia TUTTO quello che sta qui sopra e mandamelo. I due punti che FERMANO' -ForegroundColor Gray; Write-Host '  il round sono il 2) e il 4): se sono rossi, non lanciare e scrivimi.' -ForegroundColor Gray; Write-Host '  Il 3) mi dice su quale cartella dati sto guardando: senza, il 5) e il 6)' -ForegroundColor Gray; Write-Host '  potrebbero parlare di un altra installazione.' -ForegroundColor Gray; Write-Host ''; Write-Host 'PRE-VOLO FINITO. Niente e stato scritto, aperto o chiuso.' -ForegroundColor Cyan }