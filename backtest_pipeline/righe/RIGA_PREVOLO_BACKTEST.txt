& { $ErrorActionPreference='Continue'; $INV=[Globalization.CultureInfo]::InvariantCulture; Write-Host ''; Write-Host '=== PRE-VOLO PC DI BACKTEST -- SOLA LETTURA, NON TOCCA NIENTE ===' -ForegroundColor Cyan; Write-Host ('macchina: ' + $env:COMPUTERNAME) -ForegroundColor Yellow; if($env:COMPUTERNAME -eq 'DESKTOP-H4D7CAJ'){ Write-Host '  OK: e il nome che la riga dei round si aspetta.' -ForegroundColor Green } else { Write-Host ('  ATTENZIONE: la riga dei round pretende DESKTOP-H4D7CAJ e qui siamo su ' + $env:COMPUTERNAME + '. Si fermerebbe subito senza fare danni. Mandami questo nome.') -ForegroundColor Red }; Write-Host ''; Write-Host '--- 1) MT5 APERTI su questa macchina (la riga dei round NON parte se ce ne sono) ---' -ForegroundColor Cyan; $pr=@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue); if($pr.Count -eq 0){ Write-Host '  nessun terminal64 vivo: la riga dei round puo partire.' -ForegroundColor Green } else { Write-Host ('  ' + $pr.Count + ' terminale/i APERTO/I -- vanno chiusi A MANO prima di lanciare i round:') -ForegroundColor Red; foreach($p in $pr){ Write-Host ('    PID ' + $p.Id + '  |  ' + $p.MainWindowTitle + '  |  ' + $p.Path) -ForegroundColor Red } }; Write-Host ''; Write-Host '--- 2) CARTELLE DATI e PROFONDITA DEI TICK (servono dal 2024.09.26) ---' -ForegroundColor Cyan; $base=Join-Path $env:APPDATA 'MetaQuotes\Terminal'; if(-not (Test-Path -LiteralPath $base)){ Write-Host ('  NON TROVATA: ' + $base) -ForegroundColor Red } else { foreach($d in @(Get-ChildItem -LiteralPath $base -Directory -ErrorAction SilentlyContinue | Where-Object { $_.Name -match '^[0-9A-F]{32}$' })){ Write-Host ('  cartella dati: ' + $d.Name) -ForegroundColor Yellow; $ex=Join-Path $d.FullName 'MQL5\Experts'; foreach($e in @('ABTG_IntradayMomentum','ABTG_EMA200','ABTG_SupertrendReversal')){ $f=Join-Path $ex ($e + '.ex5'); if(Test-Path -LiteralPath $f){ Write-Host ('      ex5 OK   ' + $e + '   compilato il ' + (Get-Item -LiteralPath $f).LastWriteTime.ToString('yyyy-MM-dd HH:mm',$INV)) -ForegroundColor Green } else { Write-Host ('      ex5 MANCA ' + $e) -ForegroundColor Red } }; $tk=Join-Path $d.FullName 'bases'; foreach($s in @('NASUSD','U30USD','D30EUR')){ $dirs=@(Get-ChildItem -LiteralPath $tk -Directory -Recurse -Filter $s -ErrorAction SilentlyContinue); if($dirs.Count -eq 0){ Write-Host ('      tick ' + $s + ': nessuna cartella (storico assente in questa cartella dati)') -ForegroundColor DarkGray } else { foreach($dd in $dirs){ $ff=@(Get-ChildItem -LiteralPath $dd.FullName -File -ErrorAction SilentlyContinue | Sort-Object Name); if($ff.Count -eq 0){ Write-Host ('      tick ' + $s + ': cartella VUOTA -> ' + $dd.FullName) -ForegroundColor Red } else { Write-Host ('      tick ' + $s + ': ' + $ff.Count + ' file, dal ' + $ff[0].Name + ' al ' + $ff[$ff.Count-1].Name + '   (' + $dd.Parent.Name + ')') -ForegroundColor Green } } } } } }; Write-Host ''; Write-Host '--- 3) CHE COSA MANDARMI ---' -ForegroundColor Cyan; Write-Host '  Copia TUTTO quello che sta qui sopra e mandamelo. Mi serve per sapere:' -ForegroundColor Gray; Write-Host '    (a) il nome vero della macchina; (b) se gli ex5 sono compilati;' -ForegroundColor Gray; Write-Host '    (c) se i tick partono da PRIMA del 2024.09: se partono dopo, i round' -ForegroundColor Gray; Write-Host '        escono con finestre accorciate SENZA DIRLO, e l ora e buttata.' -ForegroundColor Gray; Write-Host ''; Write-Host 'PRE-VOLO FINITO. Niente e stato scritto, aperto o chiuso.' -ForegroundColor Cyan }
