& { $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; $PIN='876ec042416a7d864ba399638e18a224a09a5403'; $T0=Get-Date; if($env:COMPUTERNAME -ne 'DESKTOP-H4D7CAJ'){ throw ('QUESTA RIGA GIRA SOLO SUL PC DI BACKTEST DESKTOP-H4D7CAJ. Qui la macchina si chiama: ' + $env:COMPUTERNAME + '. Sul VPS VMI3047753 non si lancia: la challenge FTMO 541452707 sta operando.') }; if((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0){ throw 'MT5 risulta APERTO su questo PC. PRIMA di chiuderlo GUARDA I GRAFICI: questo terminale e C:\Program Files\BCM Markets MT5 Terminal ed e loggato sul DEMO 50503392, lo stesso conto del piccolo sul VPS. Il 14/08/2026 da QUESTA macchina sono partiti ordini VERI (#3160534/#3160535, -104,60). Se ci sono SEDIE attaccate ai grafici, staccale. Poi chiudi il terminale A MANO e rilancia.' }; $W=Join-Path $env:USERPROFILE 'abtg_round'; New-Item -ItemType Directory -Force -Path $W | Out-Null; $S=Join-Path $W 'RIGA_ROUND_VPS.ps1'; Remove-Item -LiteralPath $S -Force -ErrorAction SilentlyContinue; irm ('https://raw.githubusercontent.com/claudiospadaro12/GITHUB/' + $PIN + '/backtest_pipeline/righe/RIGA_ROUND_VPS.ps1?cb=' + [Guid]::NewGuid().ToString('N')) -OutFile $S; if(-not (Select-String -LiteralPath $S -SimpleMatch -Pattern 'MARCATORE_RIGA_ROUND_VPS_v2' -Quiet)){ throw 'RIGA_ROUND_VPS.ps1 scaricata SENZA il marcatore v2: copia vecchia o cache di GitHub. Non si parte.' }; Write-Host ('pc  : ' + $env:COMPUTERNAME) -ForegroundColor Green; Write-Host ('pin : ' + $PIN) -ForegroundColor Green; Write-Host ('data: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss')) -ForegroundColor Green; Write-Host 'R239 -- il LATO delle due Aperture in campo. 4 file, 8 celle, 16 passate, M5 TICK REALI.' -ForegroundColor Cyan; Write-Host 'TEMPO: [NON MISURATO] su M5. Limite INFERIORE derivato da R238 (8 passate tick H1 in 44 min = 5,5 min/passata): almeno ~1h40 piu 4 avvii del driver. M5 puo costare MOLTO di piu: NON fermarla prima di 4 ore.' -ForegroundColor Yellow; Write-Host 'TETTO BARRE: il driver lo legge da config\common.ini e muore se e fra 1.000 e 200.000. M5 su 21 mesi vuole ~132.000 barre: se parte, il tetto e alto abbastanza.' -ForegroundColor Yellow; Write-Host 'BERSAGLIO: SOLO il terminale del PC di backtest (C:\Program Files\BCM Markets MT5 Terminal, demo 50503392). NON TOCCATI, per nome: VPS VMI3047753 e i suoi sei terminali -- FTMO 541452707 (C:\FTMO, sei sedie vive), REALE 10105439 (C:\BCM_Reale), 100k 50504263, piccolo 50503392, manuale 50503635, banco 50504400 (C:\MT5_Backtest). Questa riga NON chiude nessun processo: se trova MT5 aperto si FERMA.' -ForegroundColor Yellow; Write-Host 'Deposito 100000 PASSATO ESPLICITAMENTE: il default del driver e 10000 e a lotto nullo l EA salta l operazione, quindi il deposito muove n.' -ForegroundColor Yellow; $ErrorActionPreference='Continue'; $J=@(@{e='ABTG_DAX_Apertura_EU';p='R239a_lato_APERTURA_dax_short.txt';t='R239a';m=4},@{e='ABTG_DAX_Apertura_EU';p='R239b_lato_APERTURA_dax_long.txt';t='R239b';m=4},@{e='ABTG_Dow_Apertura_US';p='R239c_lato_APERTURA_dow_short.txt';t='R239c';m=4},@{e='ABTG_Dow_Apertura_US';p='R239d_lato_APERTURA_dow_long.txt';t='R239d';m=4}); $esiti=New-Object System.Collections.ArrayList; $dsk=[Environment]::GetFolderPath('Desktop'); foreach($x in $J){ $vec=Join-Path $dsk ('ROUND_' + $x.t); if(Test-Path -LiteralPath $vec){ Remove-Item -LiteralPath $vec -Recurse -Force -ErrorAction SilentlyContinue; Write-Host ('  tolta la cartella di una corsa PRECEDENTE: ROUND_' + $x.t) -ForegroundColor DarkYellow }; $vez=Join-Path $dsk ('ROUND_' + $x.t + '.zip'); if(Test-Path -LiteralPath $vez){ Remove-Item -LiteralPath $vez -Force -ErrorAction SilentlyContinue } }; foreach($x in $J){ $E=$x.e; $P=$x.p; $L=$x.t; $M=$x.m; $att=0; while((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0 -and $att -lt 36){ Start-Sleep -Seconds 5; $att=$att+1 }; if((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0){ Write-Host ('ATTENZIONE: dopo 180 secondi MT5 e ancora VIVO. Il round ' + $L + ' parte lo stesso, ma il pre-volo del driver lo fermera con rc 1: NON sara misurato.') -ForegroundColor Red }; Write-Host ''; Write-Host ('=== ' + $L + '   ' + $E + '   ' + $P + '   modello ' + $M + '   deposito 100000 ===') -ForegroundColor Cyan; & powershell.exe -NoProfile -ExecutionPolicy Bypass -File $S -Expert $E -Prova $P -Etichetta $L -Pin $PIN -Modello $M -Deposito 100000; [void]$esiti.Add(($L.PadRight(6) + ' modello ' + $M + '   rc ' + $LASTEXITCODE)) }; $out=Join-Path $dsk 'ROUND_R239_2026-09-24'; if(Test-Path -LiteralPath $out){ Remove-Item -LiteralPath $out -Recurse -Force -ErrorAction SilentlyContinue }; New-Item -ItemType Directory -Force -Path $out | Out-Null; $nCart=0; $manc=New-Object System.Collections.ArrayList; foreach($x in $J){ $src=Join-Path $dsk ('ROUND_' + $x.t); if(Test-Path -LiteralPath $src){ Copy-Item -LiteralPath $src -Destination $out -Recurse -Force -ErrorAction SilentlyContinue; $nCart=$nCart+1 } else { [void]$manc.Add($x.t) } }; foreach($et in @('R239a','R239b','R239c','R239d')){ $cs=Join-Path $out ('ROUND_' + $et); if(Test-Path -LiteralPath $cs){ (@('NON PROMUOVIBILE -- LEGGERE PRIMA DI SCRIVERE QUALUNQUE VERDETTO','','1) IL MERITO E IMPOSSIBILE PER ARITMETICA, non per prudenza.','   InpOneTradePerDay=true mette un tetto di UNA operazione al giorno.','   L IS e il 40 per cento di ~455 giornate feriali = ~182 giornate, e il','   LIMIT del retest scade spesso inevaso. NESSUNA cella arrivera a n>=150','   in IS (Emendamento A del 16/08). Nessuna promozione, nessuno spegnimento.','','2) IL COSTO: DIVERSO SUI DUE SIMBOLI, e sul DAX NON PASSA.','   DAX 770101 -> la geometria VIVA (range 35, buffer 500, RETEST offset 200)','     e la riga FIGLIA di CANCELLO_COSTO_FLOTTA r.396: 56,1 idx [MIS] n=2','     -> 33,0x -> NO, 82 per cento della frontiera. (La riga PADRE dice 42,3x','     ma e su 7 gambe di cui CINQUE di un altra geometria.)','   DOW 770202 -> rifatto sulla geometria vera (range 35, buffer 1000):','     range 149,5 idx + 2 x buffer = 169,5 idx -> ~84,8x. PASSA.','   >>> Quindi R239a/b (DAX) nascono col cancello del costo NON PASSATO, e','       si marca PRIMA di guardare il PF.','','3) LA FALSIFICAZIONE NON E L ADDITIVITA. Una prima stesura aveva congelato','   n(corto)+n(lungo)=n(long+short), copiata da R235. E FALSA qui:','   InpEntryMode=2 fa gPhase=PH_PLACED al primo LIMIT piazzato, e il codice','   dice da se un ciclo, giornata finita (DAX r.893).','   DAX: i cinque filtri sono spenti -> TrendBias()=0 -> tutti e due i lati','     validi -> nelle giornate a DOPPIA ROTTURA le celle pure fanno 1','     operazione a testa e la LONG+SHORT ne fa UNA.','     >>> INVARIANTE VERO: max(nC,nL) <= n(L+S) <= nC+nL.','     >>> E il residuo nC+nL-n(L+S) NON e uno scarto: E IL NUMERO DI','         GIORNATE A DOPPIA ROTTURA, cioe un RISULTATO del round.','   DOW: InpUseEmaFilter=true su H4 congela gBias a +1 o -1 -> un solo lato','     per giornata -> l additivita REGGE, ma PER IL FILTRO, non per il','     motore. Va scritto cosi, se no si legge come una conferma che non e.','','4) SUL DAX ACCENDERE LO SHORT NON SOMMA OPERAZIONI: LE SOSTITUISCE.','   La cella LONG+SHORT non e la sedia piu il corto: e la sedia a cui il','   corto RUBA GIORNATE. Un PF che peggiora puo voler dire che il corto ha','   preso il posto di long che vincevano -- conclusione OPPOSTA a quella','   comoda. Si guarda il residuo del punto 3 prima di concludere.','','5) UN CANCELLO DI DETERMINISMO GRATIS, gia pagato e mai letto.','   La cella LONG+SHORT di R239a e quella di R239b sono la STESSA','   configurazione: stessi 80 pin, cambia solo il magic. Due passate','   INDIPENDENTI che DEVONO coincidere al centesimo. Idem R239c e R239d.','   >>> Se non coincidono, il banco non e deterministico e NESSUNA lettura','       di questo round vale. Si guarda QUESTO per primo.','','6) L ANCORA: la cella LUNGO PURO e esattamente la sedia com e schierata','   oggi (long si, short no). Deve somigliare a cio che la sedia fa in','   campo; se non ci somiglia, il banco non descrive la sedia.','','7) LE SEDIE IN CAMPO NON SI TOCCANO. 770101 e 770202 stanno operando sulla','   challenge FTMO 541452707. Questo e un round di MISURA sul banco: ogni','   cambio in campo e di CLAUDIO.') -join "`r`n") | Set-Content -LiteralPath (Join-Path $cs 'NON_PROMUOVIBILE.txt') -Encoding ASCII } }; $lg=Join-Path $out 'LOG_TESTER'; New-Item -ItemType Directory -Force -Path $lg | Out-Null; $nl=0; foreach($f in @(Get-ChildItem -Path (Join-Path $env:APPDATA 'MetaQuotes') -Recurse -Filter '*.log' -ErrorAction SilentlyContinue | Where-Object { $_.LastWriteTime -ge $T0 } | Sort-Object LastWriteTime -Descending | Select-Object -First 300)){ Copy-Item -LiteralPath $f.FullName -Destination (Join-Path $lg (([string]$nl).PadLeft(4,'0') + '_' + $(if($f.Directory.Parent){$f.Directory.Parent.Name}else{'ignota'}) + '_' + $f.Directory.Name + '_' + $f.Name)) -Force -ErrorAction SilentlyContinue; $nl=$nl+1 }; $ri=New-Object System.Collections.ArrayList; [void]$ri.Add('RIEPILOGO R239 -- il LATO delle due Aperture in campo (770101 DAX, 770202 Dow)'); [void]$ri.Add('data: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss')); [void]$ri.Add('fine: ' + (Get-Date).ToString('yyyy-MM-dd HH:mm:ss') + '   durata minuti: ' + [int](((Get-Date)-$T0).TotalMinutes)); [void]$ri.Add('pc  : ' + $env:COMPUTERNAME + '   (PC di backtest; VPS VMI3047753 NON toccato)'); [void]$ri.Add('pin : ' + $PIN); [void]$ri.Add('deposito 100000 passato; -Spread NON passato = INCOGNITA DICHIARATA'); [void]$ri.Add(''); foreach($z in $esiti){ [void]$ri.Add($z) }; [void]$ri.Add(''); [void]$ri.Add('rc 0 = ROUND GIRATO | 2 = NON MISURATO | 3 = GIRATO CON RILIEVI | 1 = non e partito'); [void]$ri.Add(''); [void]$ri.Add('CARTELLE ATTESE: 4   TROVATE: ' + $nCart + $(if($nCart -eq 4){'   -> CATENA COMPLETA'}else{'   -> CATENA ROTTA, MANCANO: ' + ($manc -join ', ')})); [void]$ri.Add(''); [void]$ri.Add('PRIMA DI QUALUNQUE NUMERO: leggere NON_PROMUOVIBILE.txt in ogni cartella.'); [void]$ri.Add('In sintesi: merito impossibile per aritmetica (1 trade/giorno, IS ~182'); [void]$ri.Add('giornate); costo NON PASSA sul DAX (33,0x, geometria viva) e PASSA sul Dow'); [void]$ri.Add('(~84,8x); la falsificazione NON e l additivita ma la banda'); [void]$ri.Add('max(nC,nL) <= n(L+S) <= nC+nL, e il residuo E il numero di giornate a'); [void]$ri.Add('doppia rottura; sul DAX lo short SOSTITUISCE operazioni, non le somma;'); [void]$ri.Add('e la cella LONG+SHORT di a e b (e di c e d) DEVE coincidere al centesimo:'); [void]$ri.Add('e un cancello di determinismo gratis, si guarda QUELLO per primo.'); ($ri -join "`r`n") | Set-Content -LiteralPath (Join-Path $out 'RIEPILOGO_R239.txt') -Encoding ASCII; $zip=Join-Path $dsk 'ROUND_R239_2026-09-24.zip'; if(Test-Path -LiteralPath $zip){ Remove-Item -LiteralPath $zip -Force -ErrorAction SilentlyContinue }; Compress-Archive -Path (Join-Path $out '*') -DestinationPath $zip -Force; Write-Host ''; Write-Host ('RACCOLTA: ' + $out); Write-Host ('ZIP PRONTO DA MANDARE: ' + $zip) -ForegroundColor Green; if($nCart -eq 4){ Write-Host 'CATENA COMPLETA: 4 cartelle su 4.' -ForegroundColor Green } else { Write-Host ('CATENA ROTTA: attese 4, TROVATE ' + $nCart + '. MANCANO: ' + ($manc -join ', ')) -ForegroundColor Red }; Write-Host ('   LOG_TESTER = ' + $nl + ' file .log (servono se un round esce NON MISURATO). NON contengono i contatori dell imbuto: le passate girano in Optimization=1, cioe sugli AGENTI, e li le Print non si leggono.') -ForegroundColor Gray; Get-ChildItem -LiteralPath $out | Select-Object Name,LastWriteTime | Format-Table -AutoSize | Out-Host; Write-Host 'ESITI:' -ForegroundColor Cyan; foreach($z in $esiti){ Write-Host ('   ' + $z) }; Write-Host ('durata totale minuti: ' + [int](((Get-Date)-$T0).TotalMinutes)) -ForegroundColor Cyan }