$t = @(Get-Process terminal64,metaeditor64 -ErrorAction SilentlyContinue); Write-Output ('CENSIMENTO MT5 -- ' + (Get-Date -Format 'yyyy-MM-dd HH:mm:ss') + ' ora locale VPS (ora server BCM = -1h) -- processi vivi: ' + $t.Count); if ($t.Count -eq 0) { Write-Output 'NESSUN terminale MT5 e nessun MetaEditor in esecuzione: censimento RIUSCITO, elenco vuoto (non e un errore)' } else { $t | Sort-Object ProcessName, Id | Select-Object ProcessName, Id, @{n='CartellaProgramma';e={ if ($_.Path) { $_.Path } else { 'NON LEGGIBILE (processo di altro utente o elevato): rilanciare PowerShell come amministratore' } }}, @{n='TitoloFinestra';e={ if ($_.MainWindowTitle) { $_.MainWindowTitle } else { '(nessuna finestra in questa sessione)' } }} | Format-List | Out-String -Width 400 }
