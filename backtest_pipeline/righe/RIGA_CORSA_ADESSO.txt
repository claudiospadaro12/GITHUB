& { $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; $pin='8027068f97c0e35dce6970fe15d958c410678725'; $atteso='21AC667285B49D631510AC9E84AF290299EEAA53270D7FE4CE389E7088006631'; $t0=Get-Date; $ps=@(Get-CimInstance Win32_Process -Filter "Name='powershell.exe'" -EA SilentlyContinue); $gia=@($ps | Where-Object { $_.CommandLine -like '*runner_abtg.ps1*' }); $ciechi=@($ps | Where-Object { -not $_.CommandLine }); $mt=@(Get-Process -Name 'metatester64' -EA SilentlyContinue); if($gia.Count -gt 0){ throw ('IL RUNNER STA GIA GIRANDO (PID ' + ($gia.ProcessId -join ',') + '): NON ne lancio un secondo, si pesterebbero i piedi sullo stesso banco. Aspetta che finisca, oppure chiudi quella finestra.') }; if($mt.Count -gt 0){ throw ('CE GIA UN TESTER IN CORSA (metatester64 PID ' + ($mt.Id -join ',') + '): una seconda corsa sullo stesso banco darebbe numeri NON ATTRIBUIBILI. Non lancio niente.') }; if($ciechi.Count -gt 0){ Write-Host ('ATTENZIONE: ' + $ciechi.Count + ' processi powershell con riga di comando NON LEGGIBILE (PID ' + ($ciechi.ProcessId -join ',') + '). Su quelli la guardia e CIECA: se uno di loro fosse il runner, questa corsa sarebbe la seconda.') -ForegroundColor Yellow }; $os=Get-CimInstance Win32_OperatingSystem; $ramPri=[math]::Round($os.FreePhysicalMemory/1MB,2); $termPri=@(Get-Process -Name 'terminal64' -EA SilentlyContinue); Write-Host ''; Write-Host '--- FOTO DELLA MACCHINA PRIMA DI PARTIRE -----------------------------' -ForegroundColor Cyan; Write-Host ('  processori logici: ' + $env:NUMBER_OF_PROCESSORS + '    RAM libera: ' + $ramPri + ' GB'); Write-Host ('  terminali MT5 vivi adesso: ' + $termPri.Count); $termPri | ForEach-Object { Write-Host ('    PID ' + $_.Id + '   ' + $_.Path) }; Write-Host '----------------------------------------------------------------------' -ForegroundColor Cyan; Write-Host ''; Write-Host 'LEGGI PRIMA DI RISPONDERE -- QUESTA CORSA NON E UGUALE A QUELLA DELLE 03:30' -ForegroundColor Yellow; Write-Host '  Il codice e identico e il bersaglio e sempre e solo il banco 50504400.' -ForegroundColor Yellow; Write-Host '  Cambia UNA cosa: lORARIO. Alle 03:30 i mercati sono chiusi; adesso no.' -ForegroundColor Yellow; Write-Host '  Su questa macchina ci sono 6 processori logici e i terminali elencati qui' -ForegroundColor Yellow; Write-Host '  sopra, fra cui quelli con sedie VIVE. Il tester apre i suoi agenti locali e' -ForegroundColor Yellow; Write-Host '  li porta al 100%: il numero di agenti lo decide il terminale, il runner NON' -ForegroundColor Yellow; Write-Host '  puo fissarlo. Il carico di un round con le sedie accese NON E MAI STATO' -ForegroundColor Yellow; Write-Host '  MISURATO: nessun round e mai girato dal runner (stanotte tutti e 4 rifiutati' -ForegroundColor Yellow; Write-Host '  dal v2), e il collaudo del carico era previsto DOPO la prima notte.' -ForegroundColor Yellow; Write-Host '  Questa riga NON tocca nessuna sedia e non chiude nessun terminale, ma non' -ForegroundColor Yellow; Write-Host '  puo garantire che gli EA vivi non rallentino mentre il tester macina.' -ForegroundColor Yellow; Write-Host '  Se preferisci il rischio zero: non rispondere SI e aspetta le 03:30.' -ForegroundColor Yellow; Write-Host ''; $ok=Read-Host 'Scrivi SI e premi Invio per lanciare adesso (qualunque altra cosa annulla)'; if($ok -ne 'SI'){ Write-Host 'ANNULLATO: non ho scaricato ne lanciato niente. Stanotte alle 03:30 la coda gira lo stesso.' -ForegroundColor Cyan; return }; $cart=Join-Path ([Environment]::GetFolderPath('Desktop')) ('CORSA_ADESSO_' + $t0.ToString('yyyyMMdd_HHmm')); New-Item -ItemType Directory -Force -Path $cart | Out-Null; $r=Join-Path $cart 'runner_abtg.ps1'; $p=$null; try { irm "https://raw.githubusercontent.com/claudiospadaro12/GITHUB/$pin/backtest_pipeline/runner_abtg.ps1" -OutFile $r -EA Stop; $h=(Get-FileHash -LiteralPath $r -Algorithm SHA256).Hash; if($h -ne $atteso){ throw ('IMPRONTA DIVERSA dal runner v3 verificato: ' + $h + ' -- NON LANCIO NIENTE') }; if(-not (Select-String -LiteralPath $r -SimpleMatch -Pattern 'MARCATORE_RUNNER_ABTG_v3' -Quiet)){ throw 'MANCA IL MARCATORE v3 -- NON LANCIO NIENTE' }; Write-Host 'runner v3 scaricato dal pin: impronta uguale, marcatore presente, nessuna corsa in atto' -ForegroundColor Green; Write-Host ('PARTITA alle ' + $t0.ToString('HH:mm:ss') + ' -- la coda ha 18 righe: 11 di lettura e 7 di round (78 passate). Metti in conto 45-90 minuti, non 30. NON chiudere questa finestra.') -ForegroundColor Cyan; $log=Join-Path $cart 'corsa.txt'; $p=Start-Process -FilePath 'powershell.exe' -ArgumentList @('-NoProfile','-ExecutionPolicy','Bypass','-File',$r) -NoNewWindow -PassThru -Wait -RedirectStandardOutput $log -RedirectStandardError (Join-Path $cart 'corsa_errori.txt') } finally { $log=Join-Path $cart 'corsa.txt'; foreach($t in @(Get-Content -LiteralPath $log -EA SilentlyContinue)){ Write-Host $t }; if($p){ Write-Host ('CODICE DI USCITA DEL RUNNER: ' + $p.ExitCode + '   (0 = fatto . 4 = fatto ma token non trovato, i referti restano sul VPS)') } else { Write-Host 'IL RUNNER NON E NEMMENO PARTITO: leggi lerrore qui sopra.' -ForegroundColor Red }; Write-Host ('DURATA: ' + [math]::Round(((Get-Date)-$t0).TotalMinutes,1) + ' minuti') -ForegroundColor Cyan; $termDopo=@(Get-Process -Name 'terminal64' -EA SilentlyContinue); $persi=@($termPri | Where-Object { @($termDopo | ForEach-Object { $_.Id }) -notcontains $_.Id }); $ramDopo=[math]::Round((Get-CimInstance Win32_OperatingSystem).FreePhysicalMemory/1MB,2); Write-Host ''; Write-Host ('TERMINALI MT5: ' + $termPri.Count + ' prima, ' + $termDopo.Count + ' dopo.   RAM libera: ' + $ramPri + ' GB prima, ' + $ramDopo + ' GB dopo.'); if($persi.Count -eq 0){ Write-Host '  STESSI PID: nessun terminale in forward e sparito durante la corsa.' -ForegroundColor Green } else { Write-Host ('  SPARITI I PID ' + (@($persi | ForEach-Object { $_.Id }) -join ',') + ' -- CONTROLLA SUBITO I TERMINALI IN FORWARD.') -ForegroundColor Red }; $trovati=0; foreach($d in @('abtg_round\risultati_prove','abtg_sottile','abtg_runner')){ $s=Join-Path $env:USERPROFILE $d; if(Test-Path -LiteralPath $s){ foreach($f in @(Get-ChildItem -LiteralPath $s -Recurse -File -EA SilentlyContinue | Where-Object { $_.LastWriteTime -ge $t0 })){ $trovati++; Copy-Item -LiteralPath $f.FullName -Destination $cart -Force -EA SilentlyContinue } } }; foreach($d in @(Get-ChildItem -LiteralPath ([Environment]::GetFolderPath('Desktop')) -Directory -Filter 'ROUND_*' -EA SilentlyContinue | Where-Object { $_.LastWriteTime -ge $t0 })){ foreach($f in @(Get-ChildItem -LiteralPath $d.FullName -File -EA SilentlyContinue)){ $trovati++; Copy-Item -LiteralPath $f.FullName -Destination $cart -Force -EA SilentlyContinue } }; $rif=@(Select-String -LiteralPath $log -SimpleMatch -Pattern 'RIFIUTATO' -EA SilentlyContinue).Count; $inCart=@(Get-ChildItem -LiteralPath $cart -File -EA SilentlyContinue); $nc=@($inCart | Where-Object { $_.Extension -eq '.csv' }).Count; Write-Host ''; Write-Host ('righe RIFIUTATE: ' + $rif + '   file trovati: ' + $trovati + '   file nella cartella: ' + $inCart.Count + '   CSV: ' + $nc + ' (attesi 14 = IS e OOS per r132c r133b r133c r136a r136b r136c r136d; meno di 14 vuol dire che qualche round non e girato, e il motivo sta qui sopra)') -ForegroundColor Cyan; if($trovati -gt $inCart.Count){ Write-Host ('  NB: ' + ($trovati-$inCart.Count) + ' omonimi appiattiti dalla raccolta (attesi: i CSV compaiono sia in risultati_prove sia in Desktop\ROUND_*).') -ForegroundColor DarkGray }; $inCart | Select-Object Name, Length | Format-Table -AutoSize; if($inCart.Count -gt 0){ Compress-Archive -Path (Join-Path $cart '*') -DestinationPath ($cart + '.zip') -Force; Write-Host ('MANDA IN CHAT QUESTO FILE: ' + $cart + '.zip') -ForegroundColor Green } else { Write-Host 'NESSUN FILE RACCOLTO: niente zip, non ce niente da mandare.' -ForegroundColor Red }; Write-Host 'NB: stanotte alle 03:30 il runner rigira la stessa coda e RISCRIVE gli stessi nomi in risultati_prove e Desktop\ROUND_*. Lo zip di adesso e la copia congelata di questa corsa, e il confronto fra le due e una prova di RIPRODUCIBILITA gratis (r132c e r136a sono proprio cancelli di determinismo).' -ForegroundColor Yellow } }
