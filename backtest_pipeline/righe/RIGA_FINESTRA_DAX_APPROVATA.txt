& { $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; $PIN='08b70b51c31a3d6641a58bcd86ce156b1aa01dd3'; $p="$env:USERPROFILE\finestra_dax.py"; Remove-Item $p -EA SilentlyContinue; irm "https://raw.githubusercontent.com/claudiospadaro12/GITHUB/$PIN/backtest_pipeline/finestra_dax.py" -OutFile $p -EA Stop; if(-not (Test-Path $p)){ throw 'DOWNLOAD FALLITO' }; if(-not (Select-String -Path $p -SimpleMatch -Pattern 'MARCATORE_FINESTRA_DAX_v5' -Quiet)){ throw 'SCRIPT VECCHIO: il pin non contiene la v5' }; $py=(Get-Command python.exe -EA SilentlyContinue | Where-Object { $_.Source -notlike '*\WindowsApps\*' } | Select-Object -First 1).Source; if(-not $py){ $py='C:\python313\python.exe' }; if(-not (Test-Path $py)){ throw ('PYTHON ASSENTE: ' + $py) }; $cache=Join-Path $env:USERPROFILE 'histdata_m1'; if(-not (Test-Path $cache)){ throw ('CACHE ASSENTE: ' + $cache + ' -- MACCHINA SBAGLIATA: gli zip 2019-2026 del DAX stanno sul PC DI BACKTEST') }; $dsk=[Environment]::GetFolderPath('Desktop'); if([string]::IsNullOrWhiteSpace($dsk)){ $dsk=Join-Path $env:USERPROFILE 'Desktop' }; $out=Join-Path $dsk 'FINESTRA_DAX.txt'; Remove-Item $out -EA SilentlyContinue; $ErrorActionPreference='Continue'; $cap=('macchina ' + $env:COMPUTERNAME + '   utente ' + $env:USERNAME + '   python ' + $py + '   cache ' + $cache); Write-Host $cap -ForegroundColor Cyan; $cap | Out-File -LiteralPath $out -Encoding ascii -Width 500; Write-Host '--- 1) AUTOTEST (deve finire con n/n OK e nessun [KO) ---' -ForegroundColor Cyan; $global:LASTEXITCODE=0; & $py $p --autotest 2>&1 | Tee-Object -Variable oa | Out-Host; $rcA=$LASTEXITCODE; $oa | Out-File -LiteralPath $out -Encoding ascii -Width 500 -Append; $testoA=($oa | Out-String -Width 500); if($rcA -ne 0 -or ($testoA -notmatch 'AUTOTEST: \d+/\d+ OK') -or ($testoA -match '\[KO')){ Write-Host ('AUTOTEST FALLITO (rc=' + $rcA + '): mi fermo, non misuro niente con uno strumento rotto.') -ForegroundColor Red; ('AUTOTEST FALLITO rc=' + $rcA) | Out-File -LiteralPath $out -Encoding ascii -Width 500 -Append } else { Write-Host '--- 2) LA MISURA VERA sulla cache ---' -ForegroundColor Cyan; $global:LASTEXITCODE=0; & $py $p --cache $cache --simbolo grxeur 2>&1 | Tee-Object -Variable ob | Out-Host; $rcB=$LASTEXITCODE; $ob | Out-File -LiteralPath $out -Encoding ascii -Width 500 -Append; $msg=('=== ESITO FINESTRA DAX: ' + $rcB + '   (0 = CONFERMATA col taglio, 1 = SMENTITA o parziale, 2 = NON CONCLUSIVO / cache incompleta / difetto)'); Write-Host $msg -ForegroundColor Cyan; $msg | Out-File -LiteralPath $out -Encoding ascii -Width 500 -Append }; if(Test-Path $out){ Write-Host ('referto sul Desktop: ' + $out) -ForegroundColor Green; Get-Item $out | Format-List Name, Length, LastWriteTime } else { Write-Host 'REFERTO NON CREATO: la corsa non ha prodotto niente.' -ForegroundColor Red } }
