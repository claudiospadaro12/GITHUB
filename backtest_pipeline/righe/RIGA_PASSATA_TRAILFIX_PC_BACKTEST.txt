& { if($env:COMPUTERNAME -ne 'DESKTOP-H4D7CAJ'){ throw ('VIETATO: questa riga si incolla SOLO nella finestra PowerShell del PC di backtest DESKTOP-H4D7CAJ. Macchina attuale: ' + $env:COMPUTERNAME + '. Qui non si esegue niente.') }; $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.ServicePointManager]::SecurityProtocol -bor [Net.SecurityProtocolType]::Tls12; Write-Host 'BERSAGLIO: finestra PowerShell sul PC di backtest DESKTOP-H4D7CAJ. Pilota da sola il terminale C:\Program Files\BCM Markets MT5 Terminal (demo 50503392) in 8 corse SINGOLE dello Strategy Tester (R254, prova di neutralita della TrailFix: EA al pin in campo contro pin + Parte A, D30EUR/U30USD/NASUSD M5 tick reali 2026.05.31-2026.09.25, deposito 10000, rischio di banco 1%). Prima di partire controlla che su questo PC NON ci sia nessun terminal64 vivo: se c e, la riga si RIFIUTA e non chiude niente. Gli EA eventualmente rimasti sui grafici del profilo NON vengono controllati uno per uno: li disarma AllowLiveTrading=false, scritto in ogni .ini. Verso fine corsa puo comparire la finestra dei parametri dello script N3: premi OK senza toccare niente. Scrive solo in USERPROFILE\abtg_passata_trailfix_riga (lo script scaricato), nella cartella dati di quel terminale, in Common\Files di MT5 (solo RINOMINA di un per-trade vecchio con lo stesso nome) e sul Desktop (cartella PASSATA_TRAILFIX + zip). Tetto 60 minuti.' -ForegroundColor Yellow; Write-Host 'NON TOCCATI, per nome: il VPS VMI3047753 e tutte le sue cartelle dati, cioe FTMO 541452707 (C:\FTMO), REALE 10105439 (C:\BCM_Reale), 100k 50504263 (BCM Markets MT5 Terminal -V3), piccolo 50503392 sul VPS, manuale 50503635 (C:\MT5_MANUALE), banco 50504400 (C:\MT5_Backtest), Pepperstone, Tickmill. Nessun preset, nessuna sedia, nessun conto: la ricompilazione CLAU12 su FTMO NON e in questa riga.' -ForegroundColor Yellow; $PIN='7769a5d68d879b7cfb92a79d772397285d3d8eee'; $SHA='F619863FF0F25CF98C153C17048746236BBEEB9DDCC99333F35DFDEC7E55C232'; $PROVA='51f345502b140a9a160c37385a6d727d02b99750'; $W=Join-Path $env:USERPROFILE 'abtg_passata_trailfix_riga'; New-Item -ItemType Directory -Force -Path $W | Out-Null; $S=Join-Path $W 'PASSATA_TRAILFIX.ps1'; Remove-Item -LiteralPath $S -Force -ErrorAction SilentlyContinue; irm ('https://raw.githubusercontent.com/claudiospadaro12/GITHUB/' + $PIN + '/backtest_pipeline/righe/PASSATA_TRAILFIX.ps1?cb=' + [Guid]::NewGuid().ToString('N')) -OutFile $S; if(-not (Select-String -LiteralPath $S -SimpleMatch -Pattern 'MARCATORE_PASSATA_TRAILFIX_v1' -Quiet)){ throw 'PASSATA_TRAILFIX.ps1 scaricato SENZA il marcatore v1: copia vecchia o cache di GitHub. Non si parte.' }; if((Get-FileHash -LiteralPath $S -Algorithm SHA256).Hash -ne $SHA){ throw 'PASSATA_TRAILFIX.ps1 scaricato NON identico al pin (SHA256 diverso). Non si parte.' }; $ErrorActionPreference='Continue'; & powershell -NoProfile -ExecutionPolicy Bypass -File $S -Pin $PROVA -TettoMin 60; $RC=$LASTEXITCODE; if($RC -eq 0){ Write-Host 'FINITO senza errori di script. NON vuol dire NEUTRA e NON vuol dire che ci sono tutti i file: guarda qui sopra la riga attesi 25, mancanti N e le righe ESITO per sedia. Sul Desktop: cartella PASSATA_TRAILFIX e PASSATA_TRAILFIX.zip: mandami lo ZIP anche se qualcosa MANCA.' -ForegroundColor Green } else { Write-Host ('Uscita con codice ' + $RC + ': leggi le righe rosse qui sopra e mandami una foto di questa finestra E lo zip se esiste.') -ForegroundColor Red } }
