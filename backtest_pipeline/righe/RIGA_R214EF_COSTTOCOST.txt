& { $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; $PIN='d58ecb6661ce991235a7205becfbac09b98051ef'; $T0=Get-Date; if($env:COMPUTERNAME -ne 'DESKTOP-H4D7CAJ'){ throw ('QUESTA RIGA GIRA SOLO SUL PC DI BACKTEST DESKTOP-H4D7CAJ. Qui la macchina si chiama: ' + $env:COMPUTERNAME + '. Sul VPS VMI3047753 non si lancia: la challenge FTMO 541452707 sta operando.') }; if((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0){ throw 'MT5 risulta APERTO su questo PC. PRIMA di chiuderlo GUARDA I GRAFICI: questo terminale e C:\Program Files\BCM Markets MT5 Terminal ed e loggato sul DEMO 50503392, lo stesso conto del piccolo sul VPS. Il 14/08/2026 da QUESTA macchina sono partiti ordini VERI (#3160534/#3160535, -104,60). Se ci sono SEDIE attaccate ai grafici, staccale. Poi chiudi il terminale A MANO e rilancia.' }; $W=Join-Path $env:USERPROFILE 'abtg_round'; New-Item -ItemType Directory -Force -Path $W | Out-Null; $S=Join-Path $W 'RIGA_ROUND_VPS.ps1'; Remove-Item -LiteralPath $S -Force -ErrorAction SilentlyContinue; irm ('https://raw.githubusercontent.com/claudiospadaro12/GITHUB/' + $PIN + '/backtest_pipeline/righe/RIGA_ROUND_VPS.ps1?cb=' + [Guid]::NewGuid().ToString('N')) -OutFile $S; if(-not (Select-String -LiteralPath $S -SimpleMatch -Pattern 'MARCATORE_RIGA_ROUND_VPS_v2' -Quiet)){ throw 'RIGA_ROUND_VPS.ps1 scaricata SENZA il marcatore v2: copia vecchia o cache di GitHub. Non si parte.' }; Write-Host ('pc  : ' + $env:COMPUTERNAME) -ForegroundColor Green; Write-Host ('pin : ' + $PIN) -ForegroundColor Green; Write-Host ('data: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss')) -ForegroundColor Green; Write-Host 'R214f + R214e -- le TRE uscite del COST-TO-COST (ABTG_CostToCost, EURJPY H4, solo long, asse InpExitMode 0/1/2). Prima R214f: OHLC M1 (modello 1) sulla finestra lunga 2020.01.01-2026.06.30, con l ANCORA r127c. Poi R214e: tick reali (modello 4) dal 2024.07.05. 2 file, 6 celle, 12 passate.' -ForegroundColor Cyan; Write-Host 'TEMPO ATTESO: [NON MISURATO]. Nessuna corsa di ABTG_CostToCost su questa macchina, e qui gira UN agente solo: le 12 passate vanno in serie. R214f (modello 1, 6 passate): tetto 14 minuti = 6 x 135 s, il ritmo per passata piu lento misurato qui (R240 del 24/09, altro EA, modello 4, costi fissi compresi): per un modello 1 e largo. R214e (modello 4, 6 passate): fra 7 e 76 minuti, perche i tick EURJPY di BCM non sono mai stati contati; il tetto suppone il DOPPIO dei tick al mese di NASUSD al ritmo piu lento misurato qui. Se i tick EURJPY non sono ancora su questo PC, il primo scarico si aggiunge e non e misurato. TETTO TOTALE 90 minuti. NON fermarla prima di 120 minuti: oltre i 120 non e lenta, e bloccata.' -ForegroundColor Yellow; Write-Host 'BERSAGLIO: SOLO il terminale del PC di backtest (C:\Program Files\BCM Markets MT5 Terminal, demo 50503392). NON TOCCATI, per nome: VPS VMI3047753 e i suoi sei terminali -- FTMO 541452707 (C:\FTMO, sei sedie vive), REALE 10105439 (C:\BCM_Reale), 100k 50504263, piccolo 50503392, manuale 50503635, banco 50504400 (C:\MT5_Backtest). Questa riga NON chiude nessun processo: se trova MT5 aperto si FERMA.' -ForegroundColor Yellow; Write-Host 'PROMEMORIA CHE VALE ANCHE A TERMINALE GIA CHIUSO (li la guardia qui sopra non parla): il driver APRE quel terminale per far girare il tester e poi lo spegne da solo, e quel terminale e loggato sul DEMO 50503392. Se ai grafici sono rimaste SEDIE attaccate con Algo Trading acceso, quelle OPERANO DAVVERO mentre il tester gira -- il 14/08/2026 da questa macchina e successo. Se non sei sicuro, fermati ADESSO con Ctrl+C, apri il terminale, guarda i grafici, stacca le sedie, chiudilo e rilancia.' -ForegroundColor Yellow; Write-Host 'Deposito 100000 PASSATO ESPLICITAMENTE su tutti e due i job, come dichiarato nei file prova: e il deposito di r127c, l ancora di R214f. Modello 1 per R214f (quello di r127c), modello 4 per R214e.' -ForegroundColor Yellow; $ErrorActionPreference='Continue'; $J=@(@{e='ABTG_CostToCost';p='R214f_uscite_COSTTOCOST_EURJPY_ohlc_lungo.txt';t='R214F';m=1},@{e='ABTG_CostToCost';p='R214e_uscite_COSTTOCOST_EURJPY_tick.txt';t='R214E';m=4}); $sha=@{}; $sha[(Join-Path $W 'src_prove\ABTG_CostToCost.mq5')]='0C9CEFE11B1EF03CA490FE3A9B7B702E11839D645025CFE6ADE324D0FA143C57'; $sha[(Join-Path $W 'src_include\ABTG_PausaGuardian.mqh')]='3EC971152E85E0082488CC4243FF45AE09948C191D52AB96050B48F94641A737'; $nDiv=0; $esiti=New-Object System.Collections.ArrayList; $dsk=[Environment]::GetFolderPath('Desktop'); foreach($x in $J){ $vec=Join-Path $dsk ('ROUND_' + $x.t); if(Test-Path -LiteralPath $vec){ Remove-Item -LiteralPath $vec -Recurse -Force -ErrorAction SilentlyContinue; Write-Host ('  tolta la cartella di una corsa PRECEDENTE: ROUND_' + $x.t) -ForegroundColor DarkYellow }; $vez=Join-Path $dsk ('ROUND_' + $x.t + '.zip'); if(Test-Path -LiteralPath $vez){ Remove-Item -LiteralPath $vez -Force -ErrorAction SilentlyContinue } }; foreach($x in $J){ $E=$x.e; $P=$x.p; $L=$x.t; $M=$x.m; $tent=0; while((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0 -and $tent -lt 36){ Start-Sleep -Seconds 5; $tent=$tent+1 }; if((@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue)).Count -gt 0){ Write-Host ('ATTENZIONE: dopo 180 secondi MT5 e ancora VIVO. Il round ' + $L + ' parte lo stesso, ma il pre-volo del driver lo fermera con rc 1: NON sara misurato.') -ForegroundColor Red }; Write-Host ''; Write-Host ('=== ' + $L + '   ' + $E + '   ' + $P + '   modello ' + $M + '   deposito 100000 ===') -ForegroundColor Cyan; $tj=Get-Date; & powershell.exe -NoProfile -ExecutionPolicy Bypass -File $S -Expert $E -Prova $P -Etichetta $L -Pin $PIN -Modello $M -Deposito 100000; $rc=$LASTEXITCODE; $mj=[int](((Get-Date)-$tj).TotalMinutes); $div=@(); if($rc -ne 1){ foreach($k in @($sha.Keys)){ if(-not (Test-Path -LiteralPath $k)){ $div += ((Split-Path -Leaf $k) + ' ASSENTE') } elseif((Get-FileHash -LiteralPath $k -Algorithm SHA256).Hash -ne $sha[$k]){ $div += ((Split-Path -Leaf $k) + ' SHA256 DIVERSO DAL PIN') } } }; if($rc -eq 1){ $m166='motore NON VERIFICATO (non e partito)' } elseif($div.Count -eq 0){ $m166='motore = pin (SHA256 di EA e include, ' + $sha.Count + ' file)' } else { $m166=('MOTORE DIVERSO DAL PIN: ' + ($div -join ', ')); $nDiv=$nDiv+1 }; $nCsv=0; $nPG=0; $cartJ=Join-Path $dsk ('ROUND_' + $L); if(Test-Path -LiteralPath $cartJ){ foreach($c in @(Get-ChildItem -LiteralPath $cartJ -Filter '*.csv' -File -ErrorAction SilentlyContinue)){ $nCsv=$nCsv+1; $h1=''; try{ $h1=[string](Get-Content -LiteralPath $c.FullName -TotalCount 1 -ErrorAction Stop) }catch{}; if($h1.Contains('Peggior Giornata %')){ $nPG=$nPG+1 } } }; [void]$esiti.Add(($L.PadRight(6) + ' modello ' + $M + '   rc ' + $rc + '   minuti ' + $mj + '   ' + $m166 + '   colonna Peggior Giornata % in ' + $nPG + ' CSV su ' + $nCsv + ' (attesi 2 su 2)')) }; $out=Join-Path $dsk ('ROUND_R214EF_' + $T0.ToString('yyyy-MM-dd')); if(Test-Path -LiteralPath $out){ Remove-Item -LiteralPath $out -Recurse -Force -ErrorAction SilentlyContinue }; New-Item -ItemType Directory -Force -Path $out | Out-Null; $nCart=0; $manc=New-Object System.Collections.ArrayList; foreach($x in $J){ $src=Join-Path $dsk ('ROUND_' + $x.t); if(Test-Path -LiteralPath $src){ Copy-Item -LiteralPath $src -Destination $out -Recurse -Force -ErrorAction SilentlyContinue; $nCart=$nCart+1 } else { [void]$manc.Add($x.t) } }; $lg=Join-Path $out 'LOG_TESTER'; New-Item -ItemType Directory -Force -Path $lg | Out-Null; $nl=0; foreach($f in @(Get-ChildItem -Path (Join-Path $env:APPDATA 'MetaQuotes') -Recurse -Filter '*.log' -ErrorAction SilentlyContinue | Where-Object { $_.LastWriteTime -ge $T0 } | Sort-Object LastWriteTime -Descending | Select-Object -First 200)){ Copy-Item -LiteralPath $f.FullName -Destination (Join-Path $lg (([string]$nl).PadLeft(4,'0') + '_' + $(if($f.Directory.Parent){$f.Directory.Parent.Name}else{'ignota'}) + '_' + $f.Directory.Name + '_' + $f.Name)) -Force -ErrorAction SilentlyContinue; $nl=$nl+1 }; $tf=New-Object System.Collections.ArrayList; foreach($f in @(Get-ChildItem -LiteralPath $lg -File -ErrorAction SilentlyContinue)){ $tx=''; try{ $b=[IO.File]::ReadAllBytes($f.FullName); if($b.Length -ge 2 -and (($b[0] -eq 255 -and $b[1] -eq 254) -or $b[1] -eq 0)){ $tx=[Text.Encoding]::Unicode.GetString($b) } else { $tx=[Text.Encoding]::UTF8.GetString($b) } }catch{}; foreach($r in ($tx -split "`r?`n")){ if($r.Contains('ticks data begins') -and $r.Contains('EURJPY') -and $tf.Count -lt 10){ [void]$tf.Add($r.Replace([string][char]0xFEFF,'').Trim()) } } }; $ri=New-Object System.Collections.ArrayList; [void]$ri.Add('RIEPILOGO R214EF -- le tre uscite del Cost-to-Cost, EURJPY H4 solo long (ABTG_CostToCost). R214F = OHLC M1 finestra lunga con ancora r127c. R214E = tick reali dal 2024.07.05.'); [void]$ri.Add('data: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss')); [void]$ri.Add('fine: ' + (Get-Date).ToString('yyyy-MM-dd HH:mm:ss') + '   durata minuti: ' + [int](((Get-Date)-$T0).TotalMinutes) + '   (tempo NON MISURATO prima di questa corsa; tetto 90; oltre 120 e BLOCCATA)'); [void]$ri.Add('pc  : ' + $env:COMPUTERNAME + '   (PC di backtest; VPS VMI3047753 NON toccato)'); [void]$ri.Add('pin : ' + $PIN); [void]$ri.Add('deposito 100000 passato su tutti e due i job; R214F modello 1, R214E modello 4; -Spread NON passato'); [void]$ri.Add(''); foreach($z in $esiti){ [void]$ri.Add($z) }; [void]$ri.Add(''); [void]$ri.Add('rc 0 = ROUND GIRATO | 2 = NON MISURATO | 3 = GIRATO CON RILIEVI (R214F: atteso il rilievo del modello 1) | 1 = non e partito'); [void]$ri.Add('minuti per job = la PRIMA misura del tempo di ABTG_CostToCost su questa macchina: va scritta nel referto.'); [void]$ri.Add('CLASSE 166 (EA e include arrivano dal RAMO lavoro, non dal pin): ' + $(if($nDiv -eq 0){'MOTORE = PIN in ogni round partito'}else{'MOTORE DIVERSO DAL PIN in ' + $nDiv + ' round: l ancora NON si legge, si rifa il pin sul ramo e si rilancia'})); [void]$ri.Add(''); [void]$ri.Add('CARTELLE ATTESE: 2   TROVATE: ' + $nCart + $(if($nCart -eq 2){'   -> CATENA COMPLETA'}else{'   -> CATENA ROTTA, MANCANO: ' + ($manc -join ', ')})); [void]$ri.Add(''); [void]$ri.Add('PAVIMENTO TICK EURJPY dal Diario del tester (righe ticks data begins con EURJPY): ' + $tf.Count + $(if($tf.Count -eq 0){'   -> NON TROVATA: il pavimento EURJPY resta NON MISURATO'}else{''})); foreach($z in $tf){ [void]$ri.Add('   ' + $z) }; [void]$ri.Add(''); [void]$ri.Add('PRIMA DI QUALUNQUE NUMERO: (1) R214F, cella InpExitMode=2 = ancora r127c Pass 3: IS Profit 13368.74 PF 1.17686 DD 10.9946 n 153 PeggGiorn -4.2160; OOS Profit 71284.16 PF 1.52341 DD 12.2627 n 242 PeggGiorn -8.0159. Se non torna, R214F e MUTO. (2) Asse inerte: meno di 2 Profit distinti fra le 3 celle = asse non arrivato, nessun numero si legge.'); [void]$ri.Add('La colonna Peggior Giornata % e dall EQUITY al primo tick del giorno server BCM: e il numero del Guardian in campo, NON la perdita giornaliera FTMO, che parte dal BALANCE delle 00:00 CE(S)T (R214e punto C7-bis). Taglia: variabile R_sedia, soglia sulla colonna a 1% = -5,00 x (1 / R_sedia), nessuna taglia proposta.'); ($ri -join "`r`n") | Set-Content -LiteralPath (Join-Path $out 'RIEPILOGO_R214EF.txt') -Encoding ASCII; $zip=Join-Path $dsk ('ROUND_R214EF_' + $T0.ToString('yyyy-MM-dd') + '.zip'); if(Test-Path -LiteralPath $zip){ Remove-Item -LiteralPath $zip -Force -ErrorAction SilentlyContinue }; Compress-Archive -Path (Join-Path $out '*') -DestinationPath $zip -Force; Write-Host ''; Write-Host ('RACCOLTA: ' + $out); Write-Host ('ZIP PRONTO DA MANDARE: ' + $zip) -ForegroundColor Green; if($nCart -eq 2){ Write-Host 'CATENA COMPLETA: 2 cartelle su 2 (ROUND_R214F, ROUND_R214E).' -ForegroundColor Green } else { Write-Host ('CATENA ROTTA: attese 2, TROVATE ' + $nCart + '. MANCANO: ' + ($manc -join ', ')) -ForegroundColor Red }; if($nDiv -eq 0){ Write-Host 'CLASSE 166: il motore compilato e quello del pin in ogni round partito (SHA256 di EA e include).' -ForegroundColor Green } else { Write-Host ('CLASSE 166: MOTORE DIVERSO DAL PIN in ' + $nDiv + ' round. L ancora NON si legge: si rifa il pin sul ramo e si rilancia.') -ForegroundColor Red }; Write-Host ('PAVIMENTO TICK EURJPY: ' + $(if($tf.Count -gt 0){$tf[0]}else{'riga NON TROVATA nei log: resta NON MISURATO'})) -ForegroundColor Gray; Write-Host ('   LOG_TESTER = ' + $nl + ' file .log. NON contengono i contatori dell imbuto: le passate girano in Optimization=1, cioe sugli AGENTI, e li le Print non si leggono.') -ForegroundColor Gray; Write-Host 'FILE ATTESI NELLO ZIP: RIEPILOGO_R214EF.txt, cartella ROUND_R214F (REFERTO_ROUND_R214F.txt + 2 CSV _ohlc_R214F + file prova), cartella ROUND_R214E (REFERTO_ROUND_R214E.txt + 2 CSV _R214E + file prova), cartella LOG_TESTER.' -ForegroundColor Gray; Get-ChildItem -LiteralPath $out | Select-Object Name,LastWriteTime | Format-Table -AutoSize | Out-Host; Write-Host 'ESITI:' -ForegroundColor Cyan; foreach($z in $esiti){ Write-Host ('   ' + $z) }; Write-Host ('durata totale minuti: ' + [int](((Get-Date)-$T0).TotalMinutes)) -ForegroundColor Cyan }