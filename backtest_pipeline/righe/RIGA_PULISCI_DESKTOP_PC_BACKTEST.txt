& { if($env:COMPUTERNAME -ne 'DESKTOP-H4D7CAJ'){ throw ('VIETATO: questa riga si incolla SOLO nella finestra PowerShell del PC di backtest DESKTOP-H4D7CAJ. Macchina attuale: ' + $env:COMPUTERNAME + '. Qui non si esegue niente.') }; $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; Write-Host 'BERSAGLIO: finestra PowerShell sul PC di backtest DESKTOP-H4D7CAJ. Tocca SOLO il Desktop di questo utente: NON cancella niente, SPOSTA gli elementi in una cartella ARCHIVIO_DESKTOP_<data> sul Desktop stesso. RESTANO dove sono: collegamenti .lnk/.url, strumenti (.ps1 .py .exe .bat .cmd .mq5 .ex5 ...), desktop.ini, nascosti, giunzioni, pagelle, le cartelle ARCHIVIO/ARCHIVIO_TEST/ABTG_* dei riordini precedenti, le tue cartelle tematiche (EASYTREND, INDICATORI, PIANO DI TRADING, ...) e le copie GITHUB*, tutto cio che e stato scritto nelle ultime 6 ore e tutto cio che usa un attivita pianificata. Se il tester MT5 sta lavorando (round in corso) si RIFIUTA.' -ForegroundColor Yellow; Write-Host 'NON TOCCATI, per nome: MT5 e il terminale C:\Program Files\BCM Markets MT5 Terminal (demo 50503392), la cartella abtg_round, il VPS VMI3047753 con FTMO 541452707 (C:\FTMO), REALE 10105439 (C:\BCM_Reale), 100k 50504263 (-V3), piccolo 50503392, manuale 50503635 (C:\MT5_MANUALE), banco 50504400 (C:\MT5_Backtest), Pepperstone, Tickmill. Nessun processo chiuso o aperto.' -ForegroundColor Yellow; $PIN='cc94cb6ecf55745a3889e1e0a77f9cea575798a5'; $SHA='3F90F596E4F01B203406A2B0F5A38CCA0C5E45EC8031DB56386D81655C589BA2'; $W=Join-Path $env:USERPROFILE 'abtg_pulisci_desktop'; New-Item -ItemType Directory -Force -Path $W | Out-Null; $S=Join-Path $W 'PULISCI_DESKTOP_PC.ps1'; Remove-Item -LiteralPath $S -Force -ErrorAction SilentlyContinue; irm ('https://raw.githubusercontent.com/claudiospadaro12/GITHUB/' + $PIN + '/backtest_pipeline/righe/PULISCI_DESKTOP_PC.ps1?cb=' + [Guid]::NewGuid().ToString('N')) -OutFile $S; if(-not (Select-String -LiteralPath $S -SimpleMatch -Pattern 'MARCATORE_PULISCI_DESKTOP_PC_v2' -Quiet)){ throw 'PULISCI_DESKTOP_PC.ps1 scaricato SENZA il marcatore v2: copia vecchia o cache di GitHub. Non si parte.' }; if((Get-FileHash -LiteralPath $S -Algorithm SHA256).Hash -ne $SHA){ throw 'PULISCI_DESKTOP_PC.ps1 scaricato NON identico al pin (SHA256 diverso). Non si parte.' }; $ErrorActionPreference='Continue'; Write-Host '=== PASSO 1: PROVA A SECCO (elenca soltanto, non sposta niente) ===' -ForegroundColor Cyan; & powershell -NoProfile -ExecutionPolicy Bypass -File $S; $RC1=$LASTEXITCODE; if($RC1 -ne 0){ Write-Host ('La prova a secco e uscita con codice ' + $RC1 + ': NON si prosegue.') -ForegroundColor Red; return }; $r=Read-Host 'Scrivi SI (maiuscolo) per SPOSTARE questi elementi nell archivio; qualunque altra cosa = annulla'; if($r -cne 'SI'){ Write-Host 'ANNULLATO: niente spostato.' -ForegroundColor Yellow; return }; Write-Host '=== PASSO 2: SPOSTAMENTO ===' -ForegroundColor Cyan; & powershell -NoProfile -ExecutionPolicy Bypass -File $S -Esegui; $RC2=$LASTEXITCODE; if($RC2 -eq 0){ Write-Host 'FATTO. Controlla la riga ESITO_PULIZIA qui sopra e la cartella ARCHIVIO_DESKTOP_<data> sul Desktop (dentro: ESITO_PULIZIA.txt ed ELENCO_SPOSTATI.csv). Per rimettere tutto com era: la riga grigia Per tornare indietro qui sopra. Niente da mandarmi.' -ForegroundColor Green } else { Write-Host ('Uscita con codice ' + $RC2 + ': qualche elemento NON e stato spostato (vedi le righe rosse). Mandami una foto di questa finestra.') -ForegroundColor Red } }
