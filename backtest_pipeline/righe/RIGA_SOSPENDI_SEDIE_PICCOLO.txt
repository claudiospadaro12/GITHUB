& { if($env:COMPUTERNAME -ne 'VMI3047753'){ throw ('VIETATO: questa riga si incolla SOLO nella finestra PowerShell del VPS VMI3047753. Macchina attuale: ' + $env:COMPUTERNAME + '. Qui non si esegue niente.') }; $ErrorActionPreference='Stop'; [Net.ServicePointManager]::SecurityProtocol=[Net.SecurityProtocolType]::Tls12; $INV=[Globalization.CultureInfo]::InvariantCulture; Write-Host 'BERSAGLIO: finestra PowerShell sul VPS VMI3047753. Tocca SOLO i file .chr del profilo ORO del piccolo 50503392 (cartella programma C:\Program Files\BCM Markets MT5 Terminal, SENZA -V3; cartella dati 215D85D767A1C39E22D242C8114BF9F5), e SOLO se quel terminale e CHIUSO: se lo trova aperto si FERMA. Il terminale del piccolo NON va aperto prima, e questa riga non lo apre.' -ForegroundColor Yellow; Write-Host 'NON TOCCATI, per nome: FTMO 541452707 (C:\FTMO, challenge viva), REALE 10105439 (C:\BCM_Reale), 100k 50504263 (BCM Markets MT5 Terminal -V3: il suo 225JPY si toglie A MANO, non da qui), manuale 50503635 (C:\MT5_MANUALE), banco 50504400 (C:\MT5_Backtest), Pepperstone, Tickmill. Nessun processo chiuso o aperto.' -ForegroundColor Yellow; Write-Host 'ATTENZIONE: questa riga cambia SOLO i file dei grafici. Ordini pendenti e posizioni gia sul server del piccolo 50503392 NON vengono toccati: si guardano nella scheda Trade quando ti diro di riaprirlo.' -ForegroundColor Yellow; $PIN='b392f20df78b88b1fd06f4407a698785c704b9a1'; if($PIN -notmatch '^[0-9a-f]{40}$'){ throw 'PIN non fissato: questa riga non e ancora pronta.' }; $T0=Get-Date; $TSR=$T0.ToString('yyyyMMdd_HHmmss',$INV); $DSK=[Environment]::GetFolderPath('Desktop'); $W=Join-Path $env:USERPROFILE 'abtg_sospendi_piccolo'; New-Item -ItemType Directory -Force -Path $W | Out-Null; $S=Join-Path $W 'SOSPENDI_SEDIE_PICCOLO_CHR.ps1'; Remove-Item -LiteralPath $S -Force -ErrorAction SilentlyContinue; irm ('https://raw.githubusercontent.com/claudiospadaro12/GITHUB/' + $PIN + '/backtest_pipeline/righe/SOSPENDI_SEDIE_PICCOLO_CHR.ps1?cb=' + [Guid]::NewGuid().ToString('N')) -OutFile $S; if(-not (Select-String -LiteralPath $S -SimpleMatch -Pattern 'MARCATORE_SOSPENDI_PICCOLO_CHR_v2' -Quiet)){ throw 'SOSPENDI_SEDIE_PICCOLO_CHR.ps1 scaricato SENZA il marcatore v2: copia vecchia o cache di GitHub. Non si parte.' }; $ErrorActionPreference='Continue'; $C1=Join-Path $W ('console_secco_' + $TSR + '.txt'); $C2=Join-Path $W ('console_esegui_' + $TSR + '.txt'); Write-Host '=== PASSO 1: PROVA A SECCO (non scrive niente) ===' -ForegroundColor Cyan; & powershell -NoProfile -ExecutionPolicy Bypass -File $S 2>&1 | Tee-Object -FilePath $C1; $RC1=$LASTEXITCODE; Write-Host ('codice di uscita della prova a secco, INFORMATIVO (classe 154, non decide): ' + $RC1); $E1=@(Get-ChildItem -LiteralPath $DSK -Filter 'esito_sospendi_piccolo_*.txt' -File -ErrorAction SilentlyContinue | Where-Object { $_.LastWriteTime -ge $T0 } | Sort-Object LastWriteTime -Descending); $ST1=''; if($E1.Count -gt 0){ $ST1=[string](@(Select-String -LiteralPath $E1[0].FullName -Pattern '^STATO: ' | Select-Object -Last 1) | ForEach-Object { $_.Line }) }; Write-Host ('stato della prova a secco, letto dal file (non dal codice di uscita): ' + $ST1) -ForegroundColor Cyan; $FAT=$false; if($ST1 -ceq 'STATO: SECCO_OK'){ Write-Host 'Leggi la tabella qui sopra: le righe SOSPENDI sono le sedie della tabella del report (attese 15), le righe SPAZZATA sono grafici indice con un EA FUORI tabella.' -ForegroundColor Yellow; $R1=Read-Host 'Scrivi SOSPENDI (maiuscolo) per togliere gli EA dai grafici SOSPENDI; qualunque altra cosa = mi fermo e non scrivo niente'; if($R1 -ceq 'SOSPENDI'){ $NSP=0; $mS=@(Select-String -LiteralPath $E1[0].FullName -Pattern 'SPAZZATA \(indice con EA, fuori tabella\): (\d+)' | Select-Object -First 1); if($mS.Count -gt 0){ $NSP=[int]::Parse($mS[0].Matches[0].Groups[1].Value,$INV) }; $AR=@('-NoProfile','-ExecutionPolicy','Bypass','-File',$S,'-Esegui'); if($NSP -gt 0){ $R2=Read-Host ('La SPAZZATA ha trovato ' + $NSP + ' grafici indice con un EA fuori tabella (elencati sopra). Scrivi ANCHE (maiuscolo) per togliere anche quelli; INVIO = restano come sono'); if($R2 -ceq 'ANCHE'){ $AR+='-AncheSpazzata' } }; Write-Host ('=== PASSO 2: SCRITTURA (' + ($AR -join ' ') + ') ===') -ForegroundColor Cyan; & powershell @AR 2>&1 | Tee-Object -FilePath $C2; $RC2=$LASTEXITCODE; Write-Host ('codice di uscita della scrittura, INFORMATIVO (classe 154, non decide): ' + $RC2); $E2=@(Get-ChildItem -LiteralPath $DSK -Filter 'esito_sospendi_piccolo_*.txt' -File -ErrorAction SilentlyContinue | Where-Object { $_.LastWriteTime -ge $T0 } | Sort-Object LastWriteTime -Descending); if($E2.Count -gt 0){ Write-Host ('stato della scrittura, letto dal file: ' + [string](@(Select-String -LiteralPath $E2[0].FullName -Pattern '^STATO: ' | Select-Object -Last 1) | ForEach-Object { $_.Line })) -ForegroundColor Cyan }; $FAT=$true } else { Write-Host 'FERMATO su tua scelta: NIENTE e stato scritto.' -ForegroundColor Yellow } } else { Write-Host 'La prova a secco NON ha dato SECCO_OK (non trovate, doppie o anomale, oppure FERMO): NIENTE viene scritto. Con il piccolo chiuso le sue sedie non girano, quindi fermarsi qui non costa niente: mandami lo zip.' -ForegroundColor Red }; $D=Join-Path $DSK ('SOSPENDI_PICCOLO_' + $TSR); New-Item -ItemType Directory -Force -Path $D | Out-Null; $EE=@(Get-ChildItem -LiteralPath $DSK -Filter 'esito_sospendi_piccolo_*.txt' -File -ErrorAction SilentlyContinue | Where-Object { $_.LastWriteTime -ge $T0 }); foreach($f in $EE){ Copy-Item -LiteralPath $f.FullName -Destination $D -Force }; foreach($c in @($C1,$C2)){ if(Test-Path -LiteralPath $c){ Copy-Item -LiteralPath $c -Destination $D -Force } }; $Z=Join-Path $DSK ('SOSPENDI_PICCOLO_' + $TSR + '.zip'); Compress-Archive -Path (Join-Path $D '*') -DestinationPath $Z -Force; Write-Host ('RACCOLTA: ' + $Z) -ForegroundColor Green; Write-Host ('file attesi nello zip: esito_sospendi_piccolo_*.txt (1 se ti sei fermato, 2 se hai scritto), console_secco_' + $TSR + '.txt' + $(if($FAT){', console_esegui_' + $TSR + '.txt'}else{''})) -ForegroundColor Green; foreach($f in @(Get-ChildItem -LiteralPath $D -File)){ Write-Host ('   nello zip: ' + $f.Name + '   byte=' + $f.Length) }; $BKS=@(Get-ChildItem -LiteralPath $DSK -Directory -Filter 'BACKUP_PROFILO_ORO_*' -ErrorAction SilentlyContinue | Where-Object { $_.CreationTime -ge $T0 }); foreach($b in $BKS){ Write-Host ('BACKUP del profilo ORO (resta sul Desktop, NON nello zip): ' + $b.FullName) -ForegroundColor Cyan }; Write-Host 'FINE. Mandami lo zip. Il terminale del piccolo 50503392 NON riaprirlo finche non ti confermo la lettura. Alla riapertura guarderemo insieme: Algo Trading verde, faccina sulle sedie forex e oro, i 15 grafici indice SENZA nome di EA in alto a destra, e la scheda Trade (pendenti e posizioni su U30USD, D30EUR, NASUSD, 225JPY).' -ForegroundColor Yellow }
