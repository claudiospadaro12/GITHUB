& { $ErrorActionPreference='Stop'; $T0=Get-Date; Write-Host '=== INVENTARIO DEI DESKTOP -- SOLA LETTURA, NON CANCELLA NIENTE ===' -ForegroundColor Cyan; Write-Host ('macchina: ' + $env:COMPUTERNAME + '   ora: ' + $T0.ToString('yyyy-MM-dd HH:mm:ss')) -ForegroundColor Green; $dsk=[Environment]::GetFolderPath('Desktop'); Write-Host ('desktop : ' + $dsk) -ForegroundColor Green; $dr=(Get-PSDrive -Name ($dsk.Substring(0,1)) -ErrorAction SilentlyContinue); if($dr){ Write-Host ('DISCO   : ' + [math]::Round($dr.Free/1GB,1) + ' GB liberi su ' + [math]::Round(($dr.Free+$dr.Used)/1GB,1) + ' GB  (' + [math]::Round(100*$dr.Free/($dr.Free+$dr.Used),1) + ' per cento)') -ForegroundColor Yellow }; Write-Host ''; Write-Host 'ELEMENTI DI PRIMO LIVELLO SUL DESKTOP, dal piu grande:' -ForegroundColor Cyan; $righe=@(); foreach($e in @(Get-ChildItem -LiteralPath $dsk -Force -ErrorAction SilentlyContinue)){   $b=0; $n=0;   if($e.PSIsContainer){ foreach($f in @(Get-ChildItem -LiteralPath $e.FullName -Recurse -File -Force -ErrorAction SilentlyContinue)){ $b=$b+$f.Length; $n=$n+1 } } else { $b=$e.Length; $n=1 };   $righe=$righe+@([pscustomobject]@{ Nome=$e.Name; Tipo=$(if($e.PSIsContainer){'cartella'}else{'file'}); MB=[math]::Round($b/1MB,1); File=$n; Ultima=$e.LastWriteTime.ToString('yyyy-MM-dd HH:mm'); Giorni=[int]((Get-Date)-$e.LastWriteTime).TotalDays }) }; $righe | Sort-Object MB -Descending | Format-Table Nome,Tipo,MB,File,Ultima,Giorni -AutoSize | Out-Host; Write-Host ('TOTALE DESKTOP: ' + [math]::Round(($righe | Measure-Object MB -Sum).Sum,1) + ' MB in ' + ($righe | Measure-Object File -Sum).Sum + ' file, ' + $righe.Count + ' elementi di primo livello') -ForegroundColor Yellow; Write-Host ''; Write-Host 'RAGGRUPPATO PER FAMIGLIA, con quanto e RIPRODUCIBILE dal repo:' -ForegroundColor Cyan; $fam=@{}; foreach($r in $righe){ $k='altro -- DA GUARDARE A MANO, potrebbe essere l unica copia';   if($r.Nome -match '^ROUND_'){ $k='ROUND_*  -- risultati di round. Riproducibili RILANCIANDO, se lo zip e gia stato mandato' }   elseif($r.Nome -match '^pagella'){ $k='pagella_*  -- il contenuto sta anche su GitHub' }   elseif($r.Nome -match 'GITHUB'){ $k='copie del repo  -- RIPRODUCIBILI: stanno su GitHub' }   elseif($r.Nome -match '\.zip$'){ $k='*.zip  -- pacchetti gia spediti' }   elseif($r.Nome -match '^abtg_|^risultati'){ $k='cartelle di lavoro degli script' };   if(-not $fam.ContainsKey($k)){ $fam[$k]=@(0.0,0) }; $fam[$k][0]=$fam[$k][0]+$r.MB; $fam[$k][1]=$fam[$k][1]+1 }; foreach($k in ($fam.Keys | Sort-Object { -$fam[$_][0] })){ Write-Host ('   ' + ([math]::Round($fam[$k][0],1)).ToString().PadLeft(9) + ' MB  ' + ([string]$fam[$k][1]).PadLeft(4) + ' elem.  ' + $k) }; Write-Host ''; Write-Host 'ROUND_* PIU VECCHIE DI 7 GIORNI -- le candidate numero uno:' -ForegroundColor Cyan; $vecchi=@($righe | Where-Object { $_.Nome -match '^ROUND_' -and $_.Giorni -ge 7 }); if($vecchi.Count -eq 0){ Write-Host '   nessuna.' -ForegroundColor Gray } else { $vecchi | Sort-Object Giorni -Descending | Format-Table Nome,MB,Giorni,Ultima -AutoSize | Out-Host; Write-Host ('   -> ' + [math]::Round(($vecchi | Measure-Object MB -Sum).Sum,1) + ' MB recuperabili') -ForegroundColor Green }; Write-Host ''; Write-Host 'STA GIRANDO UN ROUND ADESSO?' -ForegroundColor Cyan; $t=@(Get-Process -Name terminal64 -ErrorAction SilentlyContinue); $m=@(Get-Process -Name metatester64 -ErrorAction SilentlyContinue); if($m.Count -gt 0){ Write-Host ('   SI. terminal64: ' + $t.Count + '   metatester64: ' + $m.Count + '   >>> UN ROUND STA SCRIVENDO SUL DESKTOP: NON PULIRE ADESSO.') -ForegroundColor Red } else { Write-Host ('   No. terminal64: ' + $t.Count + '   metatester64: 0') -ForegroundColor Green }; Write-Host ''; Write-Host 'QUESTA RIGA HA SOLO LETTO E STAMPATO: non ha cancellato, spostato ne modificato NIENTE.' -ForegroundColor Green }